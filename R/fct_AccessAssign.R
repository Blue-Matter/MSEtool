


## ---- MPs ----

# Clashes with Slick method

# #' @rdname Access
# #' @export
# MPs <- function(x) {
#   if (isS4(x)) {
#     slots <- slotNames(x)
#     if (!'MPs' %in% slots)
#       cli::cli_abort('No slot `MPs` in object of class {.var {class(x)}}')
#     return(x@MPs |> names())
#   }
#
#   if (is.list(x))
#     purrr::map(x, MPs)
#
#}

# Rec Devs 
