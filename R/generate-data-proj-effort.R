
GenProjData_Effort <- function(x, Proj, DataYear, YearsAll, y, stocks) {
  
  EffortData <- Proj@Data[[x]][[i]]@Effort
  
  if (EmptyObject(EffortData))
    return(Proj)
  
  
  
  
  
  
  EffortData
}