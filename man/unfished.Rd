% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-unfished.R
\name{unfished}
\alias{unfished}
\alias{B0}
\alias{SB0}
\alias{SP0}
\alias{N0}
\title{Unfished Reference Point Quantities}
\usage{
B0(Hist, type = c("Equilibrium", "Dynamic"), Reduce = TRUE)

SB0(Hist, type = c("Equilibrium", "Dynamic"), Reduce = TRUE)

SP0(Hist, type = c("Equilibrium", "Dynamic"), Reduce = TRUE)

N0(Hist, type = c("Equilibrium", "Dynamic"), Reduce = TRUE)
}
\arguments{
\item{Hist}{A \link[=hist-class]{hist} object; the output of \code{\link[=Simulate]{Simulate()}}, containing
the simulated historical fishery dynamics.}

\item{type}{Character. One of \code{'Equilibrium'} or \code{'Dynamic'}; partial
matching is supported.
\itemize{
\item \code{'Equilibrium'}: unfished values assuming equilibrium conditions — no
recruitment deviations and all life-history parameters constant over
time.
\item \code{'Dynamic'}: unfished values calculated by running the full population
dynamics model, including recruitment deviations and any time-varying
parameters.
}}

\item{Reduce}{Logical. If \code{TRUE} (default), calls \code{\link[=ReduceDims]{ReduceDims()}} to
collapse any \code{Sim}, \code{Year}, or \code{Age} dimensions where values are
identical across that dimension, reducing array size. If \code{FALSE}, the
slot contents are returned as-is with all dimensions intact.}
}
\value{
\itemize{
\item \code{N0()}: a named list with one element per Stock. Each element is an array
with dimensions \verb{[Sim, Age, Year]}. The \code{Sim} and \code{Year} dimensions are
collapsed by \code{\link[=ReduceDims]{ReduceDims()}} when \code{Reduce = TRUE} and values do not vary
across those dimensions.
\item \code{B0()}, \code{SB0()}, \code{SP0()}: an array with dimensions \verb{[Sim, Stock, Year]}.
The \code{Sim} and \code{Year} dimensions are collapsed by \code{\link[=ReduceDims]{ReduceDims()}} when
\code{Reduce = TRUE} and values do not vary across those dimensions.
}
}
\description{
Extract unfished reference point quantities from a \link[=hist-class]{hist} object.
\code{B0()} returns total biomass, \code{SB0()} spawning biomass, \code{SP0()} spawning
production, and \code{N0()} numbers-at-age. All functions support both
equilibrium and dynamic unfished calculations (see \code{type} argument).
}
\seealso{
\code{\link[=ReduceDims]{ReduceDims()}} for details on dimension collapsing.
}
