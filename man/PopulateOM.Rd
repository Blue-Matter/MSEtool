% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_PopulateFleet.R, R/fct_PopulateOM.R,
%   R/fct_PopulateStock.R
\name{PopulateFleet}
\alias{PopulateFleet}
\alias{PopulateOM}
\alias{PopulateStock}
\title{Populate operating model components}
\usage{
PopulateFleet(Fleet, Stock, seed = 103, silent = FALSE, force = FALSE)

PopulateOM(OM, silent = FALSE, force = FALSE)

PopulateStock(
  Stock,
  nYear,
  pYear,
  CurrentYear = NULL,
  nSim = NULL,
  Seasons = 1,
  ALK = TRUE,
  AWK = TRUE,
  seed = 102,
  silent = FALSE,
  force = FALSE
)
}
\arguments{
\item{Fleet}{A \link{Fleet} object.}

\item{Stock}{A \link{Stock} object.}

\item{seed}{Integer random seed used for generating stochastic values.}

\item{silent}{Logical; suppress messages.}

\item{force}{Logical; force re-population even if the object digest is unchanged.}

\item{OM}{An \link{OM} object.}

\item{nYear}{Number of historical years.}

\item{pYear}{Number of projection years.}

\item{CurrentYear}{Character; current calendar year.}

\item{nSim}{Number of simulation replicates.}

\item{Seasons}{Number of seasons per year.}

\item{ALK}{Logical; whether to populate age–length keys.}

\item{AWK}{Logical; whether to populate age–weight keys.}
}
\value{
An object of the same class as the input, with populated and updated slots.
}
\description{
Populate and update \code{\link[=OM]{OM()}} objects by generating stochastic values,
filling derived slots, and checking object structure and contents.
}
\details{
These functions are internal population engines used to initialise and
update \link{OM}, \link{Stock}, and \link{Fleet} objects used by \code{\link[=Simulate]{Simulate()}}.

Population is skipped if the object digest is unchanged, unless
\code{force = TRUE}.
}
\section{Functions}{

\describe{
\item{\code{PopulateOM()}}{
Populate a complete operating model.
}
\item{\code{PopulateStock()}}{
Populate biological stock components including growth, mortality,
maturity, fecundity, recruitment, spatial structure, and depletion.
}
\item{\code{PopulateFleet()}}{
Populate fleet components including effort, catchability, selectivity,
retention, discard mortality, and spatial closures.
}
}
}

\examples{
\dontrun{
# Populate a full operating model
OM <- PopulateOM(OM)

# Populate a stock
Stock <- PopulateStock(
  Stock,
  nYear = 40,
  pYear = 20,
  nSim = 100
)

# Populate a fleet
Fleet <- PopulateFleet(
  Fleet,
  Stock = Stock,
  nSim = 100
)
}
}
\seealso{
\link{OM},
\link{Stock},
\link{Fleet}
}
