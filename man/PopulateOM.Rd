% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-om.R
\name{PopulateOM}
\alias{PopulateOM}
\title{Populate an Operating Model}
\usage{
PopulateOM(
  OM,
  silent = FALSE,
  force = FALSE,
  standardize_effort = TRUE,
  adjust_fecundity = TRUE
)
}
\arguments{
\item{OM}{An \code{\link[=OM]{OM()}} object to populate.}

\item{silent}{Logical. If \code{TRUE}, suppress informational messages and
warnings during population.}

\item{force}{Logical. If \code{TRUE}, force re-population even if the internal
object digest indicates no changes since the last call.}

\item{standardize_effort}{Logical. Used internally. Apply \code{.StandardizeEffort()}?}

\item{adjust_fecundity}{Logical. Used internally. Apply \code{\link[=AdjustSeasonalFecundity]{AdjustSeasonalFecundity()}}?}
}
\value{
An \code{\link[=OM]{OM()}} object populated with simulation- and time-specific values.
}
\description{
Populate a complete operating model (\code{om} object) by populating all
component objects (stocks, fleets, observation models, and derived
structures), performing internal consistency checks, and initializing
simulation-specific values.
}
\details{
This function is typically called internally, but may also be called directly.

\code{PopulateOM()} is the top-level population routine for operating models.
It orchestrates population of all model components, including:
\itemize{
\item Stocks via \code{\link[=PopulateStock]{PopulateStock()}}
\item Fleets via \code{\link[=PopulateFleet]{PopulateFleet()}}
\item Implementation models (\code{imp})
\item Observation models (\code{obs})
}

To avoid unnecessary recomputation, a digest of the operating model is
checked before population. If the digest is unchanged and \code{force = FALSE},
the input object is returned unchanged.
}
\examples{
\dontrun{
OM <- PopulateOM(OM)

}

}
\seealso{
\code{\link[=Populate]{Populate()}}, \code{\link[=PopulateStock]{PopulateStock()}}, \code{\link[=PopulateFleet]{PopulateFleet()}}
}
