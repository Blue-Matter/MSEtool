% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-simcontrol.R
\name{SimControl}
\alias{SimControl}
\title{Simulation Control Options}
\usage{
SimControl(
  DynamicUnfished = TRUE,
  RefLandings = FALSE,
  RefRemovals = FALSE,
  ConditionObs = TRUE,
  EstimateBeta = FALSE,
  GenerateData = TRUE,
  MSYRefs = TRUE,
  MSYRefsCpp = TRUE,
  RefPoints = TRUE,
  MGT = TRUE,
  BLow = FALSE,
  CalcCatchAtSize = NA,
  CalcCatchAtSizeCpp = TRUE,
  ...
)
}
\arguments{
\item{DynamicUnfished}{Logical. Calculate the dynamic unfished population
dynamics? Default \code{TRUE}.}

\item{RefLandings}{Logical. Calculate reference yield based on landings?
Computationally expensive. Default \code{FALSE}.}

\item{RefRemovals}{Logical. Calculate reference yield based on total
removals (landings + discards)? Default \code{FALSE}.}

\item{ConditionObs}{Logical. Condition the observation model on historical
fishery data? Default \code{TRUE}.}

\item{EstimateBeta}{Logical. Estimate the index hyperstability/
hyperdepletion parameter \code{Beta} by regression when conditioning on real
data (see \code{\link[=IndicesObs]{IndicesObs()}})? Default \code{FALSE}. If \code{FALSE}, \code{Beta} is fixed at \code{1}
unless the user already supplied a value for that index. If \code{TRUE},
\code{Beta} is estimated per simulation; see \code{\link[=EstimateBeta]{EstimateBeta()}} and
\code{Index_Obs@Misc$BetaFit} for diagnostics.}

\item{GenerateData}{Logical. Generate historical fishery data from the
observation model? Default \code{TRUE}.}

\item{MSYRefs}{Logical. Calculate MSY-based reference points (see
\code{\link[=CalcMSY]{CalcMSY()}})? Numerically optimizes apical F per simulation, so cost
scales with \code{nSim}; use \code{parallel = TRUE} in \code{\link[=Simulate]{Simulate()}} for large
\code{nSim}. Default \code{TRUE}.}

\item{MSYRefsCpp}{Logical. Use the C++
implementation of the MSY reference point calculation instead of the R
implementation (\code{\link[=CalcMSY]{CalcMSY()}})? Default \code{TRUE}.}

\item{RefPoints}{Logical. Calculate F0.1, Fmax, Fx\%SPR, Fmed, Fcrash and
SPRcrash reference points (see \code{\link[=CalcRefPoints]{CalcRefPoints()}})? Default \code{TRUE}.}

\item{MGT}{Logical. Calculate mean generation time (see \code{\link[=CalcMGT]{CalcMGT()}})?
Default  \code{TRUE}.}

\item{BLow}{Logical. Calculate the \code{BLow} rebuilding reference point (see
\code{\link[=CalcBLow]{CalcBLow()}})? Substantially more expensive than the other reference
points. Default \code{FALSE}.}

\item{CalcCatchAtSize}{Logical, or \code{NA}. Calculate historical and
projected landings/discards-at-size?
Computationally expensive for models with many fleets and/or fine
seasonal time steps. \code{NA} (default) auto-detects, per stock: skipped
unless either a fleet's size-composition observation model is
configured (relevant when \code{GenerateData = TRUE}) or real
size-composition data is supplied for conditioning (relevant when
\code{ConditionObs = TRUE}). Set \code{TRUE} to always calculate it regardless or \code{FALSE} to always skip it.}

\item{CalcCatchAtSizeCpp}{Logical. Use the C++ implementation of the
landings/discards-at-size projection
instead of its pure-R equivalent? Default
\code{TRUE}. Set \code{FALSE} as a fallback if the C++ version has problems}

\item{...}{Additional named arguments. Any unrecognised names trigger a
warning.}
}
\value{
A named list of simulation control settings.
}
\description{
Constructs a named list of optional-calculation switches for \code{\link[=Simulate]{Simulate()}}.
Pass the result to the \code{control} argument of \code{\link[=Simulate]{Simulate()}} to toggle which
computations are performed.
}
\examples{
# Default settings
SimControl()

# Skip MSY reference points and reference yield calculations
SimControl(MSYRefs = FALSE)

# Calculate reference yield based on landings
SimControl(RefLandings = TRUE)

# Also calculate the (expensive) BLow rebuilding reference point
SimControl(BLow = TRUE)



}
\seealso{
\code{\link[=Simulate]{Simulate()}}, \code{\link[=CalcRefPoints]{CalcRefPoints()}}, \code{\link[=CalcMSY]{CalcMSY()}}, \code{\link[=CalcMGT]{CalcMGT()}},
\code{\link[=CalcBLow]{CalcBLow()}}
}
