% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-stock.R
\name{PopulateStock}
\alias{PopulateStock}
\title{Populate a Stock Object}
\usage{
PopulateStock(
  Stock,
  nYear,
  pYear,
  CurrentYear = NULL,
  nSim = NULL,
  Seasons = 1,
  ALK = TRUE,
  AWK = TRUE,
  seed = 102,
  silent = FALSE,
  force = FALSE,
  CalcAtLength = FALSE
)
}
\arguments{
\item{Stock}{A \code{\link[=Stock]{Stock()}} object to populate.}

\item{nYear}{Integer. Number of historical years.}

\item{pYear}{Integer. Number of projection years.}

\item{CurrentYear}{Numeric or character. Calendar year of the final
historical year. Defaults to the current system year if \code{NULL}.}

\item{nSim}{Integer. Number of simulation replicates.}

\item{Seasons}{Integer. Number of seasons per year.}

\item{ALK}{Logical. If \code{TRUE}, populate age–length keys.}

\item{AWK}{Logical. If \code{TRUE}, populate age–weight keys.}

\item{seed}{Integer. Random seed used for stochastic generation.}

\item{silent}{Logical. If \code{TRUE}, suppress informational messages.}

\item{force}{Logical. If \code{TRUE}, force re-population even if the object
digest is unchanged.}

\item{CalcAtLength}{Logical. If \code{TRUE}, calculate at-length quantities
from at-age arrays where applicable}
}
\value{
A populated \code{\link[=Stock]{Stock()}} object.
}
\description{
Populate a \code{Stock} object by generating stochastic biological quantities,
expanding values across simulation replicates and years, and filling
derived age- and length-based components.
}
\details{
\code{PopulateStock()} coordinates population of all biological components
associated with a stock, including:
\itemize{
\item Length-at-age via \code{\link[=PopulateLength]{PopulateLength()}}
\item Weight-at-age via \code{\link[=PopulateWeight]{PopulateWeight()}}
\item Natural mortality via \code{\link[=PopulateNaturalMortality]{PopulateNaturalMortality()}}
\item Maturity via \code{\link[=PopulateMaturity]{PopulateMaturity()}}
\item Fecundity via \code{\link[=PopulateFecundity]{PopulateFecundity()}}
\item Stock–recruitment via \code{\link[=PopulateSRR]{PopulateSRR()}}
\item Spatial structure via \code{\link[=PopulateSpatial]{PopulateSpatial()}}
\item Initial depletion via \code{\link[=PopulateDepletion]{PopulateDepletion()}}
}

To avoid unnecessary recomputation, a digest of the stock object and key
arguments is checked before population. If the digest is unchanged and
\code{force = FALSE}, the input object is returned unchanged.
}
\examples{
\dontrun{
Stock <- PopulateStock(
  Stock,
  nYear = 40,
  pYear = 20,
  nSim = 100,
  seed = 123
)

}

}
\seealso{
\code{\link[=Populate]{Populate()}}, \code{\link[=PopulateOM]{PopulateOM()}}
}
