% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-closure.R
\name{PlotClosure}
\alias{PlotClosure}
\title{Plot Spatial Closures}
\usage{
PlotClosure(object, Stock = NULL, byStock = NULL, Stocks = NULL, Years = NULL)
}
\arguments{
\item{object}{A \link[=fleet-class]{fleet} object, or an \link[=om-class]{om}, \link[=hist-class]{hist}, or
\link[=mse-class]{mse} object.}

\item{Stock}{A \link[=stock-class]{stock} object supplying the biology needed to
populate a bare \link[=fleet-class]{fleet} \code{object}. Ignored otherwise. Default
\code{NULL} uses an example stock, with a message noting this.}

\item{byStock}{Logical. Facet by stock? Default \code{NULL} facets
automatically when \code{object} has more than one stock.}

\item{Stocks}{Character or numeric vector. Restrict the plot to specific
stocks, either by name (matching \code{\link[=StockNames]{StockNames()}}) or by index. Default
\code{NULL} (all stocks).}

\item{Years}{Optional numeric vector, or \code{"all"}. Default \code{NULL} plots
every available year.}
}
\value{
A \code{ggplot} object, or \code{NULL} invisibly if no selected stock/fleet
has any closures specified.
}
\description{
Heatmap of each fleet's spatio-temporal closure schedule
(\code{Fleet@Closure});the proportion of simulations in which an area is
closed to fishing, by year. Areas that are open in every year for every
simulation contribute nothing to look at, so a stock/fleet with no
closures specified is silently omitted; if none has any, an
informational message is printed and \code{NULL} is returned invisibly.
}
\seealso{
\code{\link[=Fleet]{Fleet()}}
}
