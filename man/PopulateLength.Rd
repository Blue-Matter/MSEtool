% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-length.R
\name{PopulateLength}
\alias{PopulateLength}
\title{Populate a Length Object}
\usage{
PopulateLength(
  Length,
  Ages = NULL,
  Years = NULL,
  nSim = 5,
  ALK = TRUE,
  seed = NULL,
  silent = FALSE,
  force = FALSE
)
}
\arguments{
\item{Length}{A \code{\link[=Length]{Length()}} object to populate.}

\item{Ages}{An \code{\link[=Ages]{Ages()}} object defining age classes. If \code{NULL}, defaults
ages 0:10 are used.}

\item{Years}{Numeric vector of model years. If \code{NULL}, defaults years
\code{seq(1950, CurrentYear + 5)} are used.}

\item{nSim}{Integer. Number of simulation replicates.}

\item{ALK}{Logical. If \code{TRUE}, populate age–length keys.}

\item{seed}{Integer. Random seed used for stochastic generation.}

\item{silent}{Logical. If \code{TRUE}, suppress informational messages.}

\item{force}{Logical. If \code{TRUE}, force re-population even if the object
digest is unchanged.}
}
\value{
A populated \code{\link[=Length]{Length()}} object.
}
\description{
Populate a \code{Length} object by generating stochastic length-at-age values,
expanding parameters across simulation replicates and years, and optionally
constructing age–length keys.
}
\details{
\code{PopulateLength()} expands length-at-age parameters and generates stochastic
realizations across simulations and years. Population involves:
\itemize{
\item Structuring model parameters across \code{nSim} and \code{Years}
\item Resolving the length-at-age model
\item Generating mean and random length-at-age values
\item Expanding coefficients of variation
\item Optionally constructing age–length keys
}

To avoid unnecessary recomputation, a digest of the length object and key
arguments is checked before population. If the digest is unchanged and
\code{force = FALSE}, the input object is returned unchanged.
}
\examples{
\dontrun{
Length <- PopulateLength(
  Length,
  Ages = Ages,
  Years = Years,
  nSim = 100,
  ALK = TRUE,
  seed = 123
)
}

}
\seealso{
\code{\link[=Populate]{Populate()}}, \code{\link[=PopulateStock]{PopulateStock()}}
}
