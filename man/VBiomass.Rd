% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vbiomass.R
\name{VBiomass}
\alias{VBiomass}
\title{Vulnerable Biomass}
\usage{
VBiomass(
  object,
  df = TRUE,
  byFleet = TRUE,
  type = c("Removals", "Landings"),
  Reduce = TRUE,
  IncYear = FALSE
)
}
\arguments{
\item{object}{A \link[=hist-class]{hist} or \link[=mse-class]{mse} object.}

\item{df}{Logical. If \code{FALSE} the raw array is returned. If \code{TRUE}
(default) a tidy \code{data.frame} is returned.}

\item{byFleet}{Logical. If \code{TRUE} (default) the data frame retains the
\code{Fleet} dimension. Otherwise values are summed over fleets.}

\item{type}{Character. \code{"Removals"} (default) weights biomass by
\verb{Selectivity x (Retention + (1 - Retention) x DiscardMortality)} (i.e.
fish that die from the encounter, whether landed or discarded).
\code{"Landings"} weights by \verb{Selectivity x Retention} only.}

\item{Reduce}{Logical. If \code{TRUE} (default) simulation dimensions are
reduced using \code{\link[=ReduceDims]{ReduceDims()}} before conversion to a data frame.}

\item{IncYear}{Logical. Passed to \code{\link[=ReduceDims]{ReduceDims()}}; controls whether the
year dimension is retained during reduction. Default \code{FALSE}.}
}
\value{
\itemize{
\item \code{df = FALSE}: for \link[=hist-class]{hist}, a numeric array \verb{Sim x Stock x Fleet x Year}; for \link[=mse-class]{mse}, a named list (\code{Historical} plus one element per
MP) of such arrays.
\item \code{df = TRUE}: a tidy \code{data.frame} with columns \code{Sim}, \code{Stock}, \code{Year},
\code{Period}, \code{MP} (MSE only), \code{Fleet}, \code{Value}, \code{Variable}.
}
}
\description{
\code{VBiomass()} returns vulnerable biomass -- biomass weighted by the
fraction of it actually exposed to a fleet's gear -- as a tidy
\code{data.frame} or raw array, for a \link[=hist-class]{hist} or \link[=mse-class]{mse} object.
}
\details{
Vulnerable biomass is computed at the stock level from
\verb{Number() x Stock@Weight (raw, unselected) x eff_sel}, then summed within each
\code{OM@Complexes} group -- weighted by \code{OM@Allocation} -- since it depends
on the gear allocation across stocks within a complex, not on any one
stock in isolation. \code{Stock} in the returned \code{data.frame} therefore names
the complex, matching the convention used by \code{\link[=Biomass]{Biomass()}} etc.

For \link[=hist-class]{hist} objects, and the historical period of \link[=mse-class]{mse}
objects, gear curves (\code{Selectivity}, \code{Retention}, \code{DiscardMortality}) are
always read from \code{object@OM@Fleet}. For the projection period of an
\link[=mse-class]{mse} object, a management procedure can change a fleet's
selectivity, retention, or discard mortality via \code{Advice()} (see
\code{\link[=Advice]{Advice()}}); when it does, the actual curve used by that MP is recorded
in \code{MSE@Misc[["Selectivity"/"Retention"/"DiscardMortality"]]} (see
\code{.RecordSelRetDisc()} in \code{mse-update.R}) and is used here instead of the
OM baseline. MPs that never change a regulation fall back to the OM
baseline for that regulation.
}
\seealso{
\code{\link[=Biomass]{Biomass()}}, \code{\link[=Advice]{Advice()}}
}
