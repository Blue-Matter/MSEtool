% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert-legacy-mp.R
\name{.WrapLegacyMP}
\alias{.WrapLegacyMP}
\title{Wrap a Legacy \code{"MP"}-Class Function for the New MP Framework}
\usage{
.WrapLegacyMP(MP)
}
\arguments{
\item{MP}{A function of class \code{"MP"}.}
}
\value{
A function of class \code{"mp"} wrapping \code{MP}.
}
\description{
Wraps a legacy (DLMtool/SAMtool) management procedure - \code{class(fn) <- "MP"}, signature \verb{function(x, Data, reps, plot)}, consuming a legacy
\link[=Data-legacy-class]{Data-legacy} object and returning a \link[=Rec-class]{Rec} object - so it can
be called by the new \code{om}/\code{hist} projection pipeline, which calls MPs as
\verb{function(Data)} on a \link[=data-class]{data} object and expects an \link[=advice-class]{advice}
object back.
}
\details{
The wrapped function converts \code{Data} (new \code{data}-class) to a legacy \code{Data}
object with \code{nsim = 1} via \code{\link[=Convert]{Convert()}}, calls \code{MP} with \code{x = 1} and
\code{reps = 1} (a single draw, since \code{advice@TAC}/\code{advice@Effort} expect a
single value rather than the distribution over \code{reps} legacy MPs use to
express implementation uncertainty), then maps the returned \code{Rec} onto an
\code{advice} object via \code{.Rec2Advice()}.

If \code{MP} needs a \code{Data} slot that \code{\link[=Convert]{Convert()}} could not populate from the
source \code{data} object (see \code{Data@Misc$UnmappedSlots}), the call to \code{MP}
will typically error; this is caught by the caller
(\code{.CalcAdviceSimMP()}) and logged rather than aborting the run.
}
\keyword{internal}
