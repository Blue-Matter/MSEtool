% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mp-surplus-production.R
\name{SurplusProduction}
\alias{SurplusProduction}
\title{Surplus Production Model Management Procedure}
\usage{
SurplusProduction(
  Data,
  Indices = NULL,
  IndexSource = "Survey",
  IndexFreq = NULL,
  IndexWeight = NULL,
  IndexSeasons = NULL,
  IndexUnits = c("Biomass", "Number"),
  CatchType = c("Removals", "Landings"),
  FitYears = NULL,
  MinIndexYears = 5,
  Model = c("internal", "spict"),
  Shape = 2,
  EstShape = FALSE,
  ShapeCV = 0.5,
  Depletion = 1,
  EstDepletion = FALSE,
  IndexSD = c("estimate", "data"),
  Priors = list(),
  Control = NULL,
  FTarget = 1,
  HCRControlPointsBiomass = c(0, 0.5),
  HCRControlPointsRate = c(0, 1),
  RampType = c("linear", "smooth"),
  FractileB = NULL,
  FractileF = NULL,
  tunepar = 1,
  AdviceYear = NULL,
  Interval = NULL,
  GapCatch = c("TAC", "LastCatch"),
  TACYears = c("mean", "first"),
  AdviceType = c("TAC", "Effort"),
  TACType = c("Removals", "Landings"),
  LandedFracYears = 3,
  Responsiveness = 1,
  DeltaDown = c(0.01, 0.5),
  DeltaUp = c(0.01, 0.5),
  TACRange = NULL,
  Allocation = NULL,
  OnFail = c("hold", "trend"),
  FallbackYears = c(2, 3),
  Diagnostics = c("min", "none", "full")
)
}
\arguments{
\item{Data}{A \link[=data-class]{data} object.}

\item{Indices, IndexSource, IndexFreq, IndexWeight, IndexSeasons}{Index
selection, as in \code{\link[=IndexRate]{IndexRate()}}. \code{IndexWeight} is normalised to a mean of
\code{1}.}

\item{IndexUnits}{Character vector of index \code{Units} to use: one or both of
\code{'Biomass'} and \code{'Number'}. Indices with other units (including
\code{'Recruitment'}) are excluded. Indices with no units are assumed to be
in biomass.}

\item{CatchType}{Character. The catch the model is fitted to:
\code{'Removals'} (landings plus discards, default) or \code{'Landings'}.}

\item{FitYears}{\code{NULL} (default) fits from the first year with catch data
to the last data year. Otherwise a single number (fit to that many most
recent years) or a vector of consecutive years. Missing catch after the
first fitted year is set to \code{0}.}

\item{MinIndexYears}{Positive integer. Minimum number of years with index
observations required to fit the model. Default \code{5}.}

\item{Model}{Character. The surplus production model: \code{'internal'}
(default) or \code{'spict'}. See \code{\link[=FitSP]{FitSP()}}.}

\item{Shape}{Positive number. Shape parameter \code{n} (fixed value, or the
median of its prior when \code{EstShape = TRUE}). Default \code{2} (Schaefer).}

\item{EstShape}{Logical. Estimate \code{Shape}? Default \code{FALSE}.}

\item{ShapeCV}{Positive number. CV of the lognormal prior on \code{Shape} when
\code{EstShape = TRUE} and \code{Priors$Shape} is not given. Default \code{0.5}.}

\item{Depletion}{Positive number. Biomass in the first fitted year
relative to \code{K} (fixed value, or starting value when
\code{EstDepletion = TRUE}). Default \code{1}.}

\item{EstDepletion}{Logical. Estimate \code{Depletion}? Default \code{FALSE}.}

\item{IndexSD}{Character. \code{'estimate'} (default) estimates the observation
standard deviation of each index; \code{'data'} calculates it from the index
\code{CV} in \code{Data} (with \code{Model = 'spict'}, the \code{CV}s scale the estimated
standard deviation; see \code{\link[=SpictControl]{SpictControl()}}). See \code{\link[=FitSP]{FitSP()}}.}

\item{Priors}{Named list of lognormal priors, each \code{c(median, CV)}, on any
of \code{FMSY}, \code{MSY} (in catch units), \code{Depletion}, and \code{Shape}. A prior on a
fixed parameter is ignored.}

\item{Control}{An \code{\link[=SPControl]{SPControl()}} (\code{Model = 'internal'}) or
\code{\link[=SpictControl]{SpictControl()}} (\code{Model = 'spict'}) list. \code{NULL} (default) uses the
defaults of the selected model.}

\item{FTarget}{Positive number. Target fishing mortality as a fraction of
the estimated \code{FMSY}. Default \code{1}.}

\item{HCRControlPointsBiomass}{Numeric vector (length \verb{>= 2},
non-decreasing). Harvest control rule control points in units of
\code{B/BMSY}. Default \code{c(0, 0.5)}: fishing mortality declines linearly from
the target at \verb{0.5 BMSY} to zero at \code{B = 0}.}

\item{HCRControlPointsRate}{Numeric vector, the same length as
\code{HCRControlPointsBiomass}. Multipliers of the target fishing mortality at
each control point. Default \code{c(0, 1)}.}

\item{RampType}{Character. \code{'linear'} (default) or \code{'smooth'}; see
\code{\link[=HockeyStickHCR]{HockeyStickHCR()}}.}

\item{FractileB, FractileF}{\code{NULL} (default) uses the point estimates of
\code{B/BMSY} and \code{FMSY}. Otherwise a probability in \verb{(0, 1)}; e.g. \code{0.35}
uses the 35th percentile. See Details, step 3.}

\item{tunepar}{Positive number. Multiplier of the target fishing mortality
used to tune the MP. Default \code{1}.}

\item{AdviceYear}{\code{NULL} (default) or the calendar year the advice first
applies in. \code{NULL} uses \code{Data@Misc$AdviceYear} (set during a
projection), else the year after the last data year.}

\item{Interval}{\code{NULL} (default) or the number of years the advice applies
for. \code{NULL} uses \code{Data@Misc$Interval} (set during a projection), else
\code{1}.}

\item{GapCatch}{Character. Catch assumed in the years between the last
data year and \code{AdviceYear}: \code{'TAC'} (default; the TAC in force, see
\code{\link[=LastTAC]{LastTAC()}}, converted to \code{CatchType}) or \code{'LastCatch'} (the catch in
the last fitted year).}

\item{TACYears}{Character. \code{'mean'} (default; mean catch over the
\code{Interval} years) or \code{'first'} (catch in the first year).}

\item{AdviceType}{Character. \code{'TAC'} (default) or \code{'Effort'}.}

\item{TACType}{Character. \code{'Removals'} (default) or \code{'Landings'}. See
\code{\link[=Advice]{Advice()}}.}

\item{LandedFracYears}{Positive integer. Number of most recent data years
used to calculate the landed fraction of the removals, used when
\code{TACType} differs from \code{CatchType}. Default \code{3}.}

\item{Responsiveness}{Positive number. The change in advice is
\code{exp(log(new / previous) * Responsiveness)}. Default \code{1}.}

\item{DeltaDown, DeltaUp}{Numeric vectors, length 2 (\code{c(min, max)}).
Minimum and maximum fractional decrease (\code{DeltaDown}) and increase
(\code{DeltaUp}) in the advice between management cycles. Changes smaller than
the minimum are set to zero; see \code{\link[=ConstrainTAC]{ConstrainTAC()}}.}

\item{TACRange}{Numeric vector, length 2. Absolute bounds on the TAC.
\code{NULL} (default) uses \code{c(0, 100 * max(catch))}. Ignored for effort.}

\item{Allocation}{\code{NULL} (default), or a non-negative numeric vector of
length \code{nFleet} giving the fraction of the TAC allocated to each fleet
(normalised to sum to \code{1}). See \code{\link[=IndexRate]{IndexRate()}}. Ignored for effort.}

\item{OnFail}{Character. \code{'hold'} (default) or \code{'trend'}; see Details.}

\item{FallbackYears}{Positive integer vector, length 2. Recent and
preceding window lengths (years) for \code{OnFail = 'trend'}. Default
\code{c(2, 3)}.}

\item{Diagnostics}{Character. Information stored in
\code{Advice@Misc$SurplusProduction}: \code{'min'} (default; the parameter
estimates and a one-row summary, used by \code{\link[=SPEstimates]{SPEstimates()}}), \code{'none'} (the
parameter estimates only, used to start the next fit), or \code{'full'} (also
the \code{spfit} object, without \code{Prep} and \code{Spict}; see \code{\link[=FitSP]{FitSP()}}). When the
advice is set by \code{OnFail}, \code{'min'} is used.}
}
\value{
An \link[=advice-class]{advice} object.
}
\description{
A model-based management procedure that fits a surplus production model to
the catch and one or more relative abundance indices with \code{\link[=FitSP]{FitSP()}}, sets a
fishing mortality from the estimated stock status with a harvest control
rule, and projects the fitted model to calculate the TAC (or effort) for
the next management interval.
}
\details{
\subsection{Model parameters}{

The leading parameters of the surplus production model are:
\itemize{
\item \code{FMSY}: the fishing mortality that produces MSY. Always estimated.
\item \code{MSY}: the maximum sustainable yield, in catch units. Always estimated.
\item \code{Depletion}: biomass at the start of the first fitted year relative to
\code{K}. Fixed at \code{Depletion} (default \code{1}, an unfished stock at the start
of the fitted years), or estimated with \code{EstDepletion = TRUE}.
\item \code{Shape}: the shape parameter \code{n} of the production function, which sets
\code{BMSY/K = n^(1/(1-n))}. \code{n = 2} (the default) is the Schaefer model
(\code{BMSY/K = 0.5}), and \code{n = 1} the Fox model (\code{BMSY/K = exp(-1)}, the
limit as \code{n} approaches \code{1}). Fixed at \code{Shape}, or estimated with
\code{EstShape = TRUE} with a lognormal prior (median \code{Shape}, CV \code{ShapeCV},
or \code{Priors$Shape}).
}

The derived quantities are \code{BMSY = MSY / FMSY} and \code{K = BMSY / (BMSY/K)}.
Lognormal priors can be set on any estimated leading parameter with
\code{Priors}. With \code{Model = 'internal'}, the catchability of each index is
calculated at its maximum likelihood value given the leading parameters,
and the observation standard deviation of each index is either calculated
the same way (\code{IndexSD = 'estimate'}) or set from the index \code{CV}
(\code{IndexSD = 'data'}). See \code{\link[=FitSP]{FitSP()}} for the model equations, and
\code{\link[=SpictControl]{SpictControl()}} for how the leading parameters map to the \code{spict}
parameterisation with \code{Model = 'spict'}, which also estimates process
error and catch observation error.
}

\subsection{Procedure}{
\enumerate{
\item The model is fitted to the calendar-year data (see \code{\link[=FitSP]{FitSP()}} for the
models and the index selection arguments, and \code{\link[=AnnualData]{AnnualData()}} for
seasonal data). Each fit starts from the estimates of the previous
management cycle, if these were made with the same \code{Model} (\code{WarmStart}
in \code{\link[=SPControl]{SPControl()}}/\code{\link[=SpictControl]{SpictControl()}}).
\item Biomass at the start of \code{AdviceYear} is calculated by projecting the
fitted model through any years between the last fitted year and
\code{AdviceYear} (when there is a data lag, or \code{FitYears} ends before the
last data year), with the catch set by \code{GapCatch}.
\item The fishing mortality is
\code{HockeyStickHCR(tunepar * FTarget * FMSY, Est = B/BMSY, Ref = 1)}, where
\code{B/BMSY} is the projected status at the start of \code{AdviceYear} and the
control points are \code{HCRControlPointsBiomass}/\code{HCRControlPointsRate} (see
\code{\link[=HockeyStickHCR]{HockeyStickHCR()}}). With \code{FractileB} (\code{FractileF}), the \code{FractileB}
(\code{FractileF}) quantile of \code{B/BMSY} (\code{FMSY}) is used instead of the point
estimate, assuming a lognormal distribution with the standard error of
the estimate (for \code{B/BMSY}, the standard error at the start of the year
after the last fitted year).
\item The model is projected at that fishing mortality for the \code{Interval}
years of the management cycle. The TAC is the mean (\code{TACYears = 'mean'})
or first-year (\code{'first'}) catch. With \code{AdviceType = 'Effort'}, the advice
is the effort relative to the last historical year, \code{F / F(YearLH)}.
\item The TAC is converted to \code{TACType} using the landed fraction of the
removals over the last \code{LandedFracYears} data years (when \code{TACType}
differs from \code{CatchType}). The change from the previous TAC (or effort)
is scaled by \code{Responsiveness} and constrained by \code{DeltaDown} and
\code{DeltaUp}, and the TAC is bounded by \code{TACRange} (see \code{\link[=ConstrainTAC]{ConstrainTAC()}}).
If the previous TAC is zero, the TAC is only bounded by \code{TACRange}.
}

If the data cannot be prepared, fewer than \code{MinIndexYears} years have
index observations, the model fit fails or does not converge, the
standard errors needed by \code{FractileB}/\code{FractileF} are not available, or
(with \code{AdviceType = 'Effort'}) the estimated fishing mortality in the last
historical year is zero, the advice is set by \code{OnFail}: \code{'hold'} keeps
the previous TAC (or effort), and \code{'trend'} changes it by the ratio of the
mean index over the last \code{FallbackYears[1]} fitted years to the mean over
the \code{FallbackYears[2]} years before them (the geometric mean over indices,
weighted by \code{IndexWeight}, and raised to \code{Responsiveness}). If the data
cannot be prepared, \code{'trend'} keeps the previous advice. The fallback
advice is constrained as in
step 5. Each such management cycle is recorded as a warning in the \code{Log}
of the returned \link[=advice-class]{advice} object.

The estimates of each management cycle are stored in
\code{Advice@Misc$SurplusProduction} (see \code{Diagnostics}); \code{\link[=SPEstimates]{SPEstimates()}}
extracts them from an \link[=mse-class]{mse} object.

\code{tunepar = 1} applies the MP as specified; larger values increase the
target fishing mortality proportionally. See \code{\link[=TuneMP]{TuneMP()}}.
}
}
\examples{
\dontrun{
Hist <- Simulate(SingleStockOM)
SurplusProduction(Hist@Data[[1]][[1]])

SP_Fox <- SetMPArgs(SurplusProduction, Shape = 1, FTarget = 0.8)
MSE <- Project(Hist, MPs = list(SP = SurplusProduction, SP_Fox = SP_Fox))
SPEstimates(MSE)
}

}
\seealso{
\code{\link[=FitSP]{FitSP()}}, \code{\link[=SPEstimates]{SPEstimates()}}, \code{\link[=HockeyStickHCR]{HockeyStickHCR()}}, \code{\link[=ConstrainTAC]{ConstrainTAC()}},
\code{\link[=IndexRate]{IndexRate()}}, \code{\link[=TuneMP]{TuneMP()}}
}
