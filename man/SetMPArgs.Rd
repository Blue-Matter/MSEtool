% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mp-args.R
\name{SetMPArgs}
\alias{SetMPArgs}
\title{Set Argument Values of a Management Procedure}
\usage{
SetMPArgs(MP, ...)
}
\arguments{
\item{MP}{An MP function (class \code{mp} or \code{mmp}), or the name of one.}

\item{...}{Named argument values. Each name must be an argument of \code{MP}
(unless \code{MP} has a \code{...} argument). A value of \code{NULL} sets that
argument's default to \code{NULL}.}
}
\value{
A function of the same class as \code{MP}.
}
\description{
Returns a copy of a management procedure (MP) with new default values for
some of its arguments, e.g. to define a variant of a built-in MP with
different settings.
}
\examples{
IR5 <- SetMPArgs(IndexRate, CalibYears = 5, Smooth = FALSE)
formals(IR5)$CalibYears

\dontrun{
MSE <- Project(Hist, MPs = list(IR5 = IR5, IR2 = IndexRate))
}

}
\seealso{
\code{\link[=Project]{Project()}}, \code{\link[=TuneMP]{TuneMP()}}
}
