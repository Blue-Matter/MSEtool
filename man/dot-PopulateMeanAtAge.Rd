% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-mean-at.R
\name{.PopulateMeanAtAge}
\alias{.PopulateMeanAtAge}
\title{Populate the \code{MeanAtAge} Slot of a Selectivity, Retention, or Similar Object}
\usage{
.PopulateMeanAtAge(object, Ages = NULL, Years = NULL, Length = NULL)
}
\arguments{
\item{object}{An S4 object with slots \code{MeanAtAge}, \code{Model}, and \code{Pars}, and
optionally \code{Timing} and \code{Classes}.}

\item{Ages}{An \link[=ages-class]{ages} object supplying age class labels and count.
Required when the model uses age-based inputs or when promoting a numeric
vector.}

\item{Years}{Numeric vector of year labels. Used to assign the \code{Year}
dimension when \code{MeanAtAge} lacks dimnames.}

\item{Length}{A \code{\link[=Length]{Length()}} object. Required when \code{object@Model} expects a
\code{Length} argument.}
}
\value{
\code{object} with \code{object@MeanAtAge} populated as a named
\verb{Sim × Age × Year} array.
}
\description{
Ensures \code{object@MeanAtAge} is a correctly structured and named
\verb{Sim × Age × Year} array. Handles three input cases:
}
\details{
\itemize{
\item If \code{object@MeanAtAge} is already a named array, it is returned with
dimension names assigned if missing.
\item If \code{object@MeanAtAge} is a numeric vector of length \code{nAge}, it is
converted to a \verb{1 × nAge × 1} array.
\item If \code{object@Model} and \code{object@Pars} are populated, \code{MeanAtAge} is
generated by calling the model function via \code{\link[=GenMeanAtAge]{GenMeanAtAge()}} or
\code{\link[=GenMeanAtLength]{GenMeanAtLength()}}, depending on whether the model expects \code{Ages} or
\code{Length} arguments.
}

Returns \code{object} unchanged if \code{object@Model} is \code{NULL} and \code{MeanAtAge} is
already populated.
}
\keyword{internal}
