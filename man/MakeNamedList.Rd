% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-make-named-list.R
\name{MakeNamedList}
\alias{MakeNamedList}
\title{Create a Named List}
\usage{
MakeNamedList(nms, value = NULL)
}
\arguments{
\item{nms}{\code{character} vector of names for the list elements.}

\item{value}{Optional. A single value assigned to every element. When
\code{NULL} (default), all elements are initialised to \code{NULL}.}
}
\value{
A named list of length \code{length(nms)}. If \code{value} is supplied,
every element is set to \code{value}; otherwise every element is \code{NULL}.
}
\description{
Creates a named list of a given length, optionally filling every element
with the same value. A thin convenience wrapper around \code{\link[stats:setNames]{stats::setNames()}}
and \code{\link[base:vector]{base::vector()}}.
}
\examples{
# Empty named list
MakeNamedList(c("a", "b", "c"))

# Named list with a common value
MakeNamedList(c("x", "y"), value = 0)

}
