% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-data-comp.R
\name{CompData}
\alias{CompData}
\title{CompData Constructor}
\usage{
CompData(
  Name = NULL,
  Value = NULL,
  Classes = NULL,
  Units = NULL,
  Log = list(),
  Misc = list()
)
}
\arguments{
\item{Name}{\code{character} or \code{NULL}. Fleet names, length \code{nFleet}.
Default \code{NULL}.}

\item{Value}{\code{array} or \code{NULL}. Composition counts with dimensions
\verb{[nYear x nFleet x nClass]}. Values represent counts scaled to sum to
\code{SampleSize} within each year and fleet — see \code{\link[=CompObs]{CompObs()}} for the
simulation model. Default \code{NULL}.}

\item{Classes}{\code{numeric} or \code{NULL}. Class midpoints — ages in years for
age compositions, or bin midpoints in the appropriate length unit for
size compositions. Length \code{nClass}. Default \code{NULL}.}

\item{Units}{\code{character} or \code{NULL}. Units of the class variable
(e.g., \code{"years"}, \code{"cm"}, \code{"mm"}). Default \code{NULL}.}

\item{Log}{\code{list}. Named list for diagnostic and audit logging.
Default \code{list()}.}

\item{Misc}{\code{list}. Named list for additional metadata. Default \code{list()}.}
}
\value{
A \link[=compdata-class]{compdata} object.
}
\description{
Construct a \link[=compdata-class]{compdata} object storing age or size composition
observations of catch samples.
}
\details{
\subsection{Usage}{

All arguments are optional; an empty object is valid and slots are
populated later (e.g., by \code{\link[=GenHistData_AgeComp]{GenHistData_AgeComp()}} or
\code{\link[=GenHistData_SizeComp]{GenHistData_SizeComp()}}.
}

\subsection{Value Dimensions and Units}{

\code{Value} stores counts, compositions are normalised
to sum to \code{SampleSize} within each year-fleet combination. The
simulation model that generates \code{Value} is defined in \code{\link[=CompObs]{CompObs()}}.
}

\subsection{Attaching to a Data Object}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{LandingsAtAge(data)  <- CompData(Value = myArray, Classes = ages)
DiscardsAtAge(data)  <- CompData(Value = myArray, Classes = ages)
LandingsAtSize(data) <- CompData(Value = myArray, Classes = bins)
DiscardsAtSize(data) <- CompData(Value = myArray, Classes = bins)
}\if{html}{\out{</div>}}
}
}
\examples{
# Empty object
cd <- CompData()

# With values
yrs    <- 2000:2020
ages   <- 1:10
fleets <- c("Trawl", "Longline")
val <- array(0L,
             dim = c(length(yrs), length(fleets), length(ages)),
             dimnames = list(Year = yrs, Fleet = fleets, Age = ages))
cd <- CompData(Name    = fleets,
               Value   = val,
               Classes = ages,
               Units   = "years")
}
\seealso{
\itemize{
\item \link[=compdata-class]{compdata} for the class definition.
\item \link[=data-class]{data} and \code{\link[=Data]{Data()}} for the enclosing data object.
\item \code{\link[=CompObs]{CompObs()}} for the observation error structure that generates
\link[=compdata-class]{compdata} objects during simulation.
}

Other data:
\code{\link[=CatchData]{CatchData()}},
\code{\link[=EffortData]{EffortData()}},
\code{\link{catchdata-class}},
\code{\link{compdata-class}},
\code{\link{effortdata-class}}
}
\concept{data}
