% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_Class_length.R, R/01_Generic_length.R,
%   R/02_Methods_length.R
\name{Length}
\alias{Length}
\alias{Length<-}
\alias{Length,missing-method}
\alias{Length,list-method}
\alias{Length,stock-method}
\alias{Length<-,stock,length-method}
\title{Length Class and Constructor}
\usage{
Length(Pars, ...)

Length(x) <- value

\S4method{Length}{missing}(Pars, ...)

\S4method{Length}{list}(
  Pars,
  Model = NULL,
  Units = "mm",
  MeanAtAge = NULL,
  CVatAge = 0.1,
  Dist = "normal",
  TruncSD = 2,
  Timing = 0,
  Random = NULL,
  ASK = NULL,
  Classes = NULL,
  Misc = list(),
  ...
)

\S4method{Length}{stock}(Pars, ...)

\S4method{Length}{stock,length}(x) <- value
}
\arguments{
\item{Pars}{A named list of parameters defining a valid length-at-age model
(see \code{\link[=LengthModels]{LengthModels()}}). Not required if \code{MeanAtAge} is supplied directly.}

\item{x}{A \code{\link[=Stock]{Stock()}} objectect.}

\item{value}{A \code{\link[=Length]{Length()}} objectect to assign.}

\item{Model}{A character string or function identifying the growth model.
If \code{NULL}, the model is inferred from \code{Pars}. Not required if \code{MeanAtAge} is supplied directly.}

\item{Units}{Character string giving the length units (e.g. \code{"mm"}).}

\item{MeanAtAge}{Numeric array giving mean length-at-age directly.}

\item{CVatAge}{Numeric value or array giving the coefficient of variation
of length-at-age.}

\item{Dist}{Character string specifying the error distribution.}

\item{TruncSD}{Numeric value or array specifying truncation in SD units.}

\item{Timing}{Numeric value or array specifying timing within the time step.}

\item{Random}{Optional random effects structure. Not currently used.}

\item{Classes}{Optional numeric vector of length classes used to for \code{at-length}
schedueles (e.g., \code{\link[=Maturity]{Maturity()}}, \code{\link[=Selectivity]{Selectivity()}}.}

\item{Misc}{A list for additional miscellaneous objectects.}
}
\value{
\itemize{
\item \code{Length}: a \link{Length} class object
\item \code{Length(x)}: a \code{Length} object from object \code{x}
\item \verb{Length<-}: the modified \code{\link[=Stock]{Stock()}} object
}
}
\description{
The \code{Length} class defines the length-at-age structure associated with a
\code{\link[=Stock]{Stock()}} objectect. It stores the parameters and model used to generate
mean length-at-age, along with variability and assumptions regarding the
distribution of length-at-age.
}
\details{
Note: \code{Pars} will overwrite \code{MeanAtAge}

Named dimensions

#' @details

The \code{Length} generic is used to:
\itemize{
\item construct new \code{Length} objectects;
\item access \code{Length} when supplied with a \code{\link[=Stock]{Stock()}} objectect;
\item assign a \code{Length} objectect to a \code{\link[=Stock]{Stock()}} objectect.
}
\subsection{Parameters (\code{Pars})}{

The \code{Pars} slot stores parameters for the growth model used to generate
mean length-at-age. Supported structures include:
\itemize{
\item \strong{Constant}: numeric scalar
\item \strong{Uniform across simulations}: numeric length 2
\item \strong{Lognormal inter-annual variation}: \code{SD}-suffixed parameters
\item \strong{Simulation-specific}: numeric vector of length \code{nSim}
\item \strong{Time-varying}: numeric matrix with \code{nTS} columns
}
}

\subsection{Slots}{

Objects of class \code{"length"} contain the following slots:
\itemize{
\item \code{Pars}: Named list of growth parameters
\item \code{Model}: Growth model identifier
\item \code{Units}: Length units
\item \code{MeanAtAge}: Mean length-at-age array
\item \code{CVatAge}: Coefficient of variation at age
\item \code{Dist}: Distribution name
\item \code{TruncSD}: Truncation in SD units
\item \code{Timing}: Timing within time step
\item \code{Random}: Random effects
\item \code{ASK}: Age–length key
\item \code{Classes}: Length classes
\item \code{Misc}: Additional metadata
}
}
}
\examples{

# Use a built-in growth model
LengthModels()

## Constant over simulations and time
Length <- Length(Pars=list(Linf=100, K=0.2, t0=-0.1))


FindModel()

# Populate the object (usually done internally; see `?Populate`)
Length <- Populate(Length, Ages(20))
MeanAtAge(Length)

plot(Length)

## Uniform distributed over simulations, constant over time
Length <- Length(Pars=list(Linf=c(90,100),
                             K=c(0.15,0.2),
                             t0=-0.1))

# Log-normally distributed over time with 5\% CV on Linf
Length <- Length(Pars=list(Linf=c(90,100),
                             LinfSD=0.05,
                             K=c(0.15,0.2),
                             t0=-0.1))

# Time-varying parameters
# Linf - uniformly distributed over simulations, normally distributed over time
nTS <- 30
nsim <- 10

Linf_mu <- runif(nsim, 50, 100)
Linf_sd <- 0.1 * Linf_mu

Length <- Length(Pars=list(Linf=matrix(rnorm(nTS,Linf_mu,Linf_sd),
                                       nsim, nTS),
                             K=c(0.15,0.2),
                             t0=-0.1))

# Change points in time-varying parameters
Length <- Length(Pars=list(
  Linf=matrix(rnorm(3, 100, 10),
              nrow=1, ncol=3), # single simulation, 3 time steps
  K=c(0.15,0.2), # uniform distribution
  t0=-0.1) # single value
  )

attributes(Length)$TimeSteps <- c(1980, 2000, 2020)


# Manually specify `MeanAtAge`, constant over sims and time

MeanAtAge <- vonBert(0:20, 100, 0.1, 0)

Length <- Length(MeanAtAge=MeanAtAge)

# Classes & Age-Size Key

Length <- Length(Pars=list(Linf=100, K=0.2, t0=-0.1),
                   Classes=seq(from=2.5, by=2.5, to=130))

plot(Length, Ages=Ages(20), type='ASK')

}
\seealso{
\code{\link[=LengthModels]{LengthModels()}}, \code{\link[=Populate]{Populate()}}, \code{\link[=Stock]{Stock()}}
}
