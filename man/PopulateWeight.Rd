% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-weight.R
\name{PopulateWeight}
\alias{PopulateWeight}
\title{Populate a Weight-at-Age Object}
\usage{
PopulateWeight(
  Weight,
  Ages = NULL,
  Length = NULL,
  Years = NULL,
  nSim = 5,
  AWK = FALSE,
  CalcAtLength = FALSE,
  seed = NULL,
  silent = FALSE,
  force = FALSE
)
}
\arguments{
\item{Weight}{A \code{\link[=Weight]{Weight()}} object to populate.}

\item{Ages}{An \code{\link[=Ages]{Ages()}} object defining age classes. If \code{NULL}, defaults
are inferred from \code{Weight}.}

\item{Length}{A \code{\link[=Length]{Length()}} object. Required if the weight model depends on
length-at-age.}

\item{Years}{Numeric vector of model years. If \code{NULL}, defaults are inferred
from \code{Weight}.}

\item{nSim}{Integer. Number of simulation replicates.}

\item{AWK}{Logical. If \code{TRUE}, populate age–weight keys.}

\item{CalcAtLength}{Logical. If \code{TRUE}, calculate weight-at-length values
from weight-at-age arrays.}

\item{seed}{Integer. Random seed used for stochastic generation.}

\item{silent}{Logical. If \code{TRUE}, suppress informational messages.}

\item{force}{Logical. If \code{TRUE}, force re-population even if the object
digest is unchanged.}
}
\value{
A populated \code{\link[=Weight]{Weight()}} object.
}
\description{
Populate a \code{Weight} object by generating stochastic weight-at-age values,
expanding parameters across simulation replicates and years, and optionally
constructing age–weight keys.
}
\details{
\code{PopulateWeight()} handles weight-at-age population for a stock,
including stochastic generation and propagation across simulations and years.
Steps include:
\itemize{
\item Structuring model parameters across \code{nSim} and \code{Years}
\item Resolving the weight model class
\item Generating mean weight-at-age or weight-at-length values
\item Converting between mean-at-length and mean-at-age as needed
\item Optionally constructing age–weight keys if \code{AWK = TRUE}
\item Generating random variation and coefficients of variation
}
}
\examples{
\dontrun{
Weight <- PopulateWeight(
  Weight,
  Ages = Ages,
  Length = Length,
  Years = Years,
  nSim = 100,
  AWK = TRUE,
  CalcAtLength = TRUE,
  seed = 123
)
}

}
\seealso{
\code{\link[=Populate]{Populate()}}, \code{\link[=PopulateStock]{PopulateStock()}}, \code{\link[=PopulateLength]{PopulateLength()}}
}
