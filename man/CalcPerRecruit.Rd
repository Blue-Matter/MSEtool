% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-per-recruit.R
\name{CalcPerRecruit}
\alias{CalcPerRecruit}
\title{Calculate Per-Recruit Quantities}
\usage{
CalcPerRecruit(OM, apicalF = 0.1, Years = NULL, Complex = NULL)
}
\arguments{
\item{OM}{A \link[=om-class]{om} or \link[=hist-class]{hist} object. If an \code{om} is supplied
it is first converted to a \code{hist} object.}

\item{apicalF}{Numeric vector of apical fishing mortality values at which
per-recruit quantities are evaluated. Default \code{0.1}.}

\item{Years}{Integer vector of years for which per-recruit quantities are
evaluated. Biological and fishery parameters are subset to these years
If \code{NULL} (default), the final historical year is used.}

\item{Complex}{Character vector of complex names to evaluate. If \code{NULL}
(default), all complexes are evaluated.}
}
\value{
A \link[=perrecruit-class]{perrecruit} object containing numbers-per-recruit,
spawning-per-recruit, biomass-per-recruit, removals, and landings
evaluated at each value of \code{apicalF}, with results reported per stock
and apical F defined as the maximum F across all stocks within each
complex.
}
\description{
Extracts biological and fishery parameters from a \link[=hist-class]{hist} or \link[=om-class]{om}
object and evaluates per-recruit quantities at a given apical fishing
mortality.
}
\details{
\subsection{Complexes}{

Per-recruit quantities are calculated separately for each complex, with
\code{apicalF} defined as the maximum fishing mortality across all ages and
stocks within the complex. Results are reported per stock and reassembled
into a single \link[=perrecruit-class]{perrecruit} object spanning all stocks. The \code{Complex}
argument can be used to restrict calculations to a subset of complexes.
}

\subsection{Fleet allocation}{

The relative contribution of each fleet to total fishing mortality is
computed as the effort-weighted catchability
(i.e. \verb{Effort × Efficiency}) normalised across fleets. These proportions
are used to distribute \code{apicalF} across fleets before selectivity is applied.
}

\subsection{Years}{

When \code{Years} contains multiple values, biological and fishery parameters
are subset to those years and the per-recruit calculations are evaluated
for each year independently. When \code{Years = NULL}, only the final historical
year is used, giving a single set of per-recruit quantities representative
of current conditions.
}
}
\seealso{
\code{\link[=CalcSPR0]{CalcSPR0()}}, \link[=perrecruit-class]{perrecruit}
}
