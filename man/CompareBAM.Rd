% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare-bam.R
\name{CompareBAM}
\alias{CompareBAM}
\title{Compare BAM and OM Output}
\usage{
CompareBAM(Stock, OM = NULL, plot = FALSE, thresh = 0.5)
}
\arguments{
\item{Stock}{Character string matching a stock in \code{bamExtras}, a list of BAM
output objects containing elements \code{rdat} and \code{dat}, or an object of class
\code{BAMdata} as returned by \code{\link[=GetBAMOutput]{GetBAMOutput()}}.}

\item{OM}{Optional OM or \code{hist} object. If \code{NULL} (default), one is
constructed internally via \code{\link[=ImportBAM]{ImportBAM()}}.}

\item{plot}{Logical. Plot the timeseries for the \code{OM} and \code{BAM} output?
If \code{plot=FALSE} (default) the plots will only be printed if \code{MARE>thresh}
in some years.}

\item{thresh}{Numeric. MARE threshold (as a percentage) above which a
comparison is flagged, printed, and plotted. Default \verb{0.5\%}.}
}
\value{
Invisibly returns a named list with elements \code{Stock}, \code{Recruits},
\code{Number}, and \code{Biomass}. Each of \code{Recruits}, \code{Number}, and \code{Biomass} is a
list with elements \code{df} (long-format data.frame of OM and BAM values by
year) and \code{MARE} (absolute relative error by year).
}
\description{
Compares key population time series between BAM output and a simulated
operating model (OM), reporting the mean absolute relative error (MARE) for
recruits, total numbers, and total biomass. Series with MARE exceeding
\code{thresh} are printed and plotted automatically.
}
\seealso{
\code{\link[=ImportBAM]{ImportBAM()}}, \code{\link[=GetBAMOutput]{GetBAMOutput()}}
}
