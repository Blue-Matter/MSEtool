% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models-srr.R
\name{SRRModels}
\alias{SRRModels}
\alias{BevertonHolt}
\alias{BevertonHolt_RelRec}
\alias{Ricker}
\alias{Ricker_RelRec}
\alias{HockeyStick}
\alias{HockeyStick_RelRec}
\title{Stock–Recruitment Relationships (SRR)}
\usage{
BevertonHolt(S, S0, R0, h)

BevertonHolt_RelRec(Pars, SPR)

Ricker(S, S0, R0, hR)

Ricker_RelRec(Pars, SPR)

HockeyStick(S, S0, R0, Shinge)

HockeyStick_RelRec(Pars, SPR)

SRRModels(full = TRUE, print = TRUE)
}
\arguments{
\item{S}{Spawning production (biomass or eggs). Numeric length >= 1}

\item{S0}{Unfished equilibrium spawning production. Numeric scalar.}

\item{R0}{Unfished equilibrium recruitment. Numeric scalar.}

\item{h}{Beverton–Holt steepness. A named argument in \code{Pars}. 0.2 < h < 1}

\item{Pars}{Named list of parameters required by a specific SRR model.}

\item{SPR}{Spawning-per-recruit multiplier. Numeric scalar.}

\item{hR}{Ricker steepness. A named argument in \code{Pars}.}

\item{Shinge}{Hockey-stick hinge point \strong{relative to \code{S0}}. A named argument in \code{Pars}.0 < Shinge <= 1}

\item{full}{Logical. Provide a complete table (TRUE) or just the model names (FALSE)?}

\item{print}{Logical. Print out the results (TRUE) or just return the data.frame (FALSE)?}
}
\value{
For stock–recruitment functions (\code{BevertonHolt()}, \code{Ricker()}, \code{HockeyStick()}):
A numeric scalar giving expected recruitment for the specified spawning production.

For relative-recruitment functions (\verb{*_RelRec()}):
A numeric scalar giving equilibrium recruitment relative to unfished recruitment
(\code{R / R0}) as a function of spawning-per-recruit (SPR).

For \code{SRRModels()}:
If \code{print = TRUE}, prints the available stock–recruitment models to the console
and invisibly returns the result. If \code{print = FALSE}, returns a character vector
or data frame describing the available models.
}
\description{
Deterministic stock–recruitment and relative-recruitment models evaluated for a
single simulation and a single year.
}
\details{
This family of functions provides:
\itemize{
\item \strong{Stock–recruitment functions} that return expected recruitment given
spawning production.
\item \strong{Relative-recruitment functions} that return equilibrium recruitment
relative to \code{R0} as a function of spawning-per-recruit (SPR).
}
\subsection{Model listing}{

\code{SRRModels()} prints or returns the set of stock–recruitment models
available in the package.
}

\subsection{Beverton–Holt}{
\itemize{
\item \code{BevertonHolt()} evaluates expected recruitment:
\deqn{R = \frac{\alpha S}{1 + \beta S}}
\item \code{BevertonHolt_RelRec()} evaluates equilibrium recruitment relative to unfished
recruitment as a function of SPR.
}
}

\subsection{Ricker}{
\itemize{
\item \code{Ricker()} evaluates expected recruitment:
\deqn{R = \alpha S e^{-\beta S}}
\item \code{Ricker_RelRec()} evaluates equilibrium relative recruitment as a function
of SPR.
}
}

\subsection{Hockey Stick}{
\itemize{
\item \code{HockeyStick()} evaluates recruitment assuming linear increase up to a hinge
point followed by saturation at \code{R0}:
\deqn{R = \begin{cases}
\frac{R0}{Shinge} S, & S < Shinge \\
R0, & S \ge Shinge
\end{cases}}
\item \code{HockeyStick_RelRec()} evaluates relative equilibrium recruitment as a
function of SPR.
}
}
}
\examples{

## Compare stock–recruitment relationships

R0 <- 1000
S0 <- 1000
h <- 0.8
Shinge <- 0.2
S <- seq(0, S0, length.out = 200)

R_bh <- BevertonHolt(S, S0, R0, h)
R_rk <- Ricker(S, S0, R0, hR = h)
R_hs <- HockeyStick(S, S0, R0, Shinge)

plot(S, R_bh, type = "l", lwd = 2,
     ylim = c(0, 1200), xlab = "Spawning biomass (S)",
     ylab = "Recruitment")
lines(S, R_rk, lwd = 2, lty = 2)
lines(S, R_hs, lwd = 2, lty = 3)
abline(h=h*R0, lty=3)
abline(v=0.2*S0, lty=3)
legend("bottomright",
       legend = c("Beverton–Holt", "Ricker", "Hockey Stick"),
       lty = c(1, 2, 3), lwd = 2, bty = "n")


## Compare relative equilibrium recruitment vs SPR
SPR <- seq(0, 1, length.out = 200)

Pars_bh <- list(h = h)
Pars_rk <- list(hR = h)
Pars_hs <- list(Shinge = Shinge)

RR_bh <- sapply(SPR, BevertonHolt_RelRec, Pars = Pars_bh)
RR_rk <- sapply(SPR, Ricker_RelRec, Pars = Pars_rk)
RR_hs <- sapply(SPR, HockeyStick_RelRec, Pars = Pars_hs)

plot(SPR, RR_bh, type = "l", lwd = 2,
     ylim = c(0, 1.05), xlab = "SPR",
     ylab = "Relative recruitment (R / R0)")
lines(SPR, RR_rk, lwd = 2, lty = 2)
lines(SPR, RR_hs, lwd = 2, lty = 3)
legend("bottomright",
       legend = c("Beverton–Holt", "Ricker", "Hockey Stick"),
       lty = c(1, 2, 3), lwd = 2, bty = "n")


}
\seealso{
\code{\link[=SRR]{SRR()}}
}
