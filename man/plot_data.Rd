% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-data.R
\name{plot_data}
\alias{plot_data}
\alias{PlotEffort}
\alias{PlotCPUE}
\alias{PlotSurvey}
\alias{PlotData}
\title{Plot Observed Fishery Data}
\usage{
PlotEffort(object)

PlotCPUE(object)

PlotSurvey(object)

PlotData(object)
}
\arguments{
\item{object}{A \link[=data-class]{data} object.}
}
\value{
A \code{ggplot} object (a \code{patchwork} object for \code{PlotData()}), or
\code{NULL} invisibly if the relevant slot isn't populated.
}
\description{
Plotting functions for \link[=data-class]{data} objects: the observed (or simulated,
with observation error) fishery timeseries, as opposed to the underlying
simulated dynamics plotted by \link{plot_hist}. A \link[=data-class]{data} object holds a
single simulation/stock realization (no \code{Sim} dimension), so these
functions draw a single line per fleet or index rather than a
median/ribbon summary.
}
\details{
\code{PlotEffort()}, \code{PlotCPUE()}, and \code{PlotSurvey()} plot the \code{Effort}, \code{CPUE},
and \code{Survey} slots respectively. \code{PlotLandings()} and \code{PlotDiscards()}
(see \link{plot_hist}) also accept a \link[=data-class]{data} object and are documented
here for that case.

If the relevant slot is not populated (\code{NULL}, e.g. no survey configured
for this OM), the function prints an informational message and returns
\code{NULL} invisibly rather than plotting an empty panel.

\code{PlotData()} arranges whichever of \code{Landings}, \code{Discards}, \code{Effort},
\code{CPUE}, and \code{Survey} are populated into a single figure with
\code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}, silently omitting panels for slots that
aren't; unlike the individual \verb{Plot*()} functions it does not print a
message for missing slots. If none of the five are populated, it prints
a message and returns \code{NULL} invisibly instead of an empty figure.
}
\examples{
\dontrun{
Hist <- Simulate(SingleStockOM)
dat  <- Hist@Data[[1]][[1]]

PlotLandings(dat)
PlotDiscards(dat)
PlotEffort(dat)
PlotCPUE(dat)
PlotSurvey(dat)
PlotData(dat)
}

}
\seealso{
\link{plot_hist}, \code{\link[=Landings]{Landings()}}, \code{\link[=Discards]{Discards()}}, \code{\link[=CPUE]{CPUE()}}, \code{\link[=Survey]{Survey()}}
}
