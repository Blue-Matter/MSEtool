% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate.R
\name{Populate}
\alias{Populate}
\title{Populate Operating Model Components}
\usage{
Populate(object, ...)
}
\arguments{
\item{object}{An object to populate. The class of \code{object} determines which
class-specific \verb{Populate*()} function is called.}

\item{...}{Named arguments passed to the appropriate \verb{Populate*()} function.
Only arguments relevant to the class of \code{object} are used.}
}
\value{
The populated object, of the same class as \code{object}.
}
\description{
Generic dispatcher function to populate operating model objects and their components.
}
\details{
Generally, users will not need to use the \verb{Populate*} functions as
they are called internally.

Depending on the class of \code{object}, the function calls a class-specific
\verb{Populate*()} function that generates stochastic values, fills derived slots,
and checks object structure and contents.

\code{Populate()} is a wrapper around class-specific population functions.
The following methods may be called depending on the class of \code{object}:
\itemize{
\item \code{\link[=PopulateOM]{PopulateOM()}}
\item \code{\link[=PopulateStock]{PopulateStock()}}
\item \code{\link[=PopulateLength]{PopulateLength()}}
\item \code{\link[=PopulateWeight]{PopulateWeight()}}
\item \code{\link[=PopulateNaturalMortality]{PopulateNaturalMortality()}}
\item \code{\link[=PopulateMaturity]{PopulateMaturity()}}
\item \code{\link[=PopulateFecundity]{PopulateFecundity()}}
\item \code{\link[=PopulateSRR]{PopulateSRR()}}
\item \code{\link[=PopulateSpatial]{PopulateSpatial()}}
\item \code{\link[=PopulateDepletion]{PopulateDepletion()}}
\item \code{\link[=PopulateFleet]{PopulateFleet()}}
\item \code{\link[=PopulateEffort]{PopulateEffort()}}
\item \code{\link[=PopulateCatchability]{PopulateCatchability()}}
\item \code{\link[=PopulateSelectivity]{PopulateSelectivity()}}
\item \code{\link[=PopulateRetention]{PopulateRetention()}}
\item \code{\link[=PopulateDiscardMortality]{PopulateDiscardMortality()}}
\item \code{\link[=PopulateObs]{PopulateObs()}}
\item \code{\link[=PopulateEffortObs]{PopulateEffortObs()}}
\item \code{\link[=PopulateCatchObs]{PopulateCatchObs()}}
\item \code{\link[=PopulateIndexObs]{PopulateIndexObs()}}
\item \code{\link[=PopulateCompObs]{PopulateCompObs()}}
}

Each \verb{Populate*()} function has its own documentation detailing which named
arguments it accepts. Users should consult the specific \verb{Populate*()} help
page for information about required slots, default behavior, and argument
descriptions.
}
\examples{
\dontrun{
# Populate a full operating model
OM <- Populate(OM)

# Populate a stock with specific arguments
Stock <- Populate(
  Stock,
  nYear = 40,
  pYear = 20,
  nSim = 100
)

# Populate a fleet using an already-populated stock
Fleet <- Populate(
  Fleet,
  Stock = Stock,
  nSim = 100
)
}

}
