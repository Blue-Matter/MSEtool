% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-index-fit.R
\name{PlotIndexFit}
\alias{PlotIndexFit}
\title{Plot Observed/Simulated Index Against the Underlying Population Trend}
\usage{
PlotIndexFit(
  object,
  type = c("CPUE", "Survey"),
  Fleets = NULL,
  Stocks = NULL,
  probs = c(0.05, 0.95),
  nsim = 0,
  Years = NULL,
  IncHist = TRUE,
  free_y = TRUE
)
}
\arguments{
\item{object}{A \link[=hist-class]{hist} or \link[=mse-class]{mse} object.}

\item{type}{Character. One of \code{"CPUE"} or \code{"Survey"}.}

\item{Fleets}{Character vector. Restrict to these fleet names. Default
\code{NULL} (all fleets with a populated \code{type} index).}

\item{Stocks}{Character vector. Restrict to these stock/complex names
(matching \code{names(object@OM@Complexes)}, or \code{StockNames()} when
\code{Complexes} is unset). Default \code{NULL} (all).}

\item{probs}{Numeric vector of length 2. Lower and upper quantiles of the
shaded ribbon across simulations. Default \code{c(0.05, 0.95)}.}

\item{nsim}{Integer. Number of individual simulation replicates to overlay
as thin lines, in addition to the median/ribbon. Default \code{0} (none).}

\item{Years}{Optional numeric vector. Subset the time series to these
years before plotting.}

\item{IncHist}{Logical. For \link[=mse-class]{mse} objects, include the historical
period? Default \code{TRUE}. No effect for \link[=hist-class]{hist} objects.}

\item{free_y}{Logical. Let each facet panel's y-axis scale independently.
Default \code{TRUE}.}
}
\value{
A \code{ggplot} object, or \code{NULL} invisibly if no populated \code{type}
index is found.
}
\description{
Overlays the \code{CPUE}/\code{Survey} index, on its own natural scale, against the
underlying population trend scaled to match it. Diagnostic for how well
an index tracks the simulated dynamics (hyperstability, error,
autocorrelation). See \code{\link[=IndexFitTable]{IndexFitTable()}} for the matching parameter
summary.
}
\details{
The \code{Index} series is plotted on its natural scale, The \code{True} (OM)
series is rescaled, per simulation, so
its mean over the years the real index spans (\code{Period == "Historical"}
and non-\code{NA}) equals the \code{Index} series' own mean over those same years.

Panels are faceted by \code{Fleet} (and \code{Stock} when more than one
stock/complex is plotted). For \link[=mse-class]{mse} objects with more than one MP,
an \code{MP} facet column is added; the historical period is repeated in every
MP's panel so each line is unbroken across the historical/projection
boundary.
}
\seealso{
\code{\link[=IndexFitTable]{IndexFitTable()}}, \code{\link[=PlotCPUE]{PlotCPUE()}}, \code{\link[=PlotSurvey]{PlotSurvey()}}, \code{\link[=Biomass]{Biomass()}},
\code{\link[=Number]{Number()}}
}
