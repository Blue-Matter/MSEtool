% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-mp.R
\name{.ExtractAdviceLogs}
\alias{.ExtractAdviceLogs}
\title{Extract Advice Logs and Detect Total MP Failure}
\usage{
.ExtractAdviceLogs(AdviceSimList, Proj, Year, MPName = NULL)
}
\arguments{
\item{AdviceSimList}{Nested list of \code{Advice} objects (or \code{try-error}/
character failures), one per sim, then per stock/complex.}

\item{Proj}{\code{Hist} object to record log entries onto.}

\item{Year}{Integer. Current projection year.}

\item{MPName}{Character. Name of the management procedure.}
}
\value{
A list with \code{Proj} (updated) and \code{AllFailed} (logical - \code{TRUE} if
every sim/stock combination in \code{AdviceSimList} was a failure).
}
\description{
Flattens per-sim, per-stock \code{Advice@Log} entries (and any \code{try-error}/
non-\code{advice} results) into \code{Proj@Log}, tagging each entry with \code{sim},
\code{year}, and \code{mp} for later filtering. Also determines, directly from
\code{AdviceSimList} rather than from the Log content, whether every sim/stock
failed this year - keeping that control-flow decision independent of
what an MP chooses to log as a warning or assumption on its own \code{Advice}.
}
\keyword{internal}
