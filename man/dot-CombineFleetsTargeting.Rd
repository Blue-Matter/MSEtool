% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine-fleet.R
\name{.CombineFleetsTargeting}
\alias{.CombineFleetsTargeting}
\title{Combine stocktargeting slots after fleet aggregation}
\usage{
.CombineFleetsTargeting(OM, FleetList, FleetIndList, silent = FALSE)
}
\arguments{
\item{OM}{An \code{\link[=OM]{OM()}} object (already populated and with fleet slots updated).}

\item{FleetList}{A named list of character vectors as passed to \code{\link[=CombineFleets]{CombineFleets()}}.}

\item{FleetIndList}{A named list of integer vectors; the resolved fleet
indices corresponding to \code{FleetList}.}

\item{silent}{\code{logical(1)}. Suppresses messages when \code{TRUE}.}
}
\value{
Updated \code{OM} object with \verb{@StockTargeting} reflecting the combined
fleet structure.
}
\description{
Updates \code{OM@StockTargeting} so that the combined fleet's targeting
parameters reflect an apical-F-weighted average of the source fleets'
parameters.
}
\details{
The combined targeting deviation for stock \code{s} at time \code{t} is:

\deqn{\tau_{s,\text{comb},t} =
  \frac{\sum_f F^{apical}_{s,f,t} \cdot \tau_{s,f,t}}{\sum_f F^{apical}_{s,f,t}}}

and analogously for \code{Mean} and \code{Covariance}.

If \code{OM@StockTargeting} is uninitialised (all \code{NA}), the function returns
\code{OM} unchanged.
}
\keyword{internal}
