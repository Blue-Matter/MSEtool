% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-om-herm.R
\docType{data}
\name{HermOM}
\alias{HermOM}
\title{Hermaphroditic Wrasse Example Operating Model}
\format{
An \link[=om-class]{om} object with the following slots populated:
\itemize{
\item \strong{\code{Name}}: \code{"Hermaphroditic Wrasse"}.
\item \strong{\code{nSim}}: \code{8}. Number of stochastic simulations.
\item \strong{\code{nYear}}: \code{20}. Number of historical years.
\item \strong{\code{pYear}}: \code{30}. Number of projection years.
\item \strong{\code{Stock}}: a length-2 list of
\verb{[[WrasseFemaleExStock, WrasseMaleExStock]]}.
\item \strong{\code{Fleet}}: a length-2 list, each containing a length-2 list of
\verb{[[AsympExFleet, DomeExFleet]]}. Structure is \verb{[[stock]][[fleet]]}.
Both stocks are fished by the same two fleet types.
\item \strong{\code{Obs}}: a length-2 list, each containing a length-2 list of
\verb{[[AgeStructuredObs, CommercialFleetObs]]}. Structure is
\verb{[[stock]][[fleet]]}.
\item \strong{\code{Imp}}: a length-1 \link{FullComplianceImp}, broadcast to both stocks and
fleets. Perfect TAC/effort implementation and full immediate
size-regulation compliance.
\item \strong{\code{Herm}}: a length-1 list containing one \link[=stocktransition-class]{stocktransition}
object (built via \code{\link[=Herm]{Herm()}}) transitioning individuals from the female
stock into the male stock as they age. See \verb{## Herm Transition} below.
}
}
\usage{
HermOM
}
\description{
An example \link[=om-class]{om} object representing a protogynous hermaphrodite
wrasse (\emph{Thalassoma} spp.): a female stock and a male stock, each
exploited by two fleets.
}
\details{
Linked by two mechanisms that operate together
but are otherwise independent:  \code{SRR@SPFrom} (the male sources its
spawning production from the female) and a \code{\link[=Herm]{Herm()}} transition (a
fraction of females change sex into males as they age).

Intended to illustrate both mechanisms together on a realistic case study. Pairings
of stocks, fleets, and observation models are for illustration only and
should not be taken as ecologically meaningful. See also \link{MultiStockOM}
for a non-hermaphroditic two-stock example.
\subsection{Stocks}{

\link{WrasseFemaleExStock} and \link{WrasseMaleExStock} share an identical age
structure (\code{Ages@Classes = 0:15}) - required for the \code{\link[=Herm]{Herm()}} transition,
which reclassifies individuals age-for-age between the two stocks - but
differ in growth, maturity, natural mortality, and \code{SRR}. See
\link{WrasseFemaleExStock}/\link{WrasseMaleExStock} for full parameter details.
}

\subsection{Fleets}{

Both stocks are fished by the same two fleet types, as in \link{MultiStockOM}:
\itemize{
\item \link{AsympExFleet}: asymptotic length-based selectivity; effort stabilises
early in the historical period.
\item \link{DomeExFleet}: dome-shaped selectivity; effort increases gradually
across the historical period.
}
}

\subsection{Observation Model}{

Both stocks use the same pair of observation models by fleet:
\link{AgeStructuredObs} (landings, a spawning-biomass survey, and
age-composition samples) on the \link{AsympExFleet} fleet, and
\link{CommercialFleetObs} (landings, discards, effort, and a commercial CPUE
index) on the \link{DomeExFleet} fleet.
}

\subsection{Implementation Error}{

\link{FullComplianceImp} models perfect implementation - realised TAC/effort
always equal advice exactly, and size-regulation advice is adopted
immediately. See \code{\link[=Imp]{Imp()}} for details.
}

\subsection{SPFrom}{

\code{SRR@SPFrom} lets one stock's recruitment be driven by another stock's
spawning production, rather than its own.

In this example only the
female stock spawns: \link{WrasseMaleExStock}'s \code{SRR} sets
\code{SPFrom = "Example Wrasse Female Stock"}, so the male stock's spawning
biomass/production (\code{SP}/\code{SP0}) is read from the female stock.
}

\subsection{Herm Transition}{

\code{\link[=Herm]{Herm()}} physically moves individuals from one stock's numbers-at-age
into another's as they age. \code{HermOM@Herm} is a \link[=stocktransition-class]{stocktransition} object,
\code{Herm(From = "Example Wrasse Female Stock", To = "Example Wrasse Male Stock", Frac = ...)}, where \code{Frac} is the cumulative fraction of
individuals remaining female by age: close to 1 at young ages,
decreasing smoothly (a logistic curve centred on age 8 here) toward 0 by
the oldest ages. Once transitioned, an individual is an
age-matched male from that point on: weight, maturity, natural
mortality, and selectivity are all determined by the male stock's own
biology.
}
}
\examples{
HermOM
nStock(HermOM)
HermOM@Herm

\dontrun{
Hist <- runMSE(HermOM, MPs = ExampleMPs())
}

}
\seealso{
\link{MultiStockOM} for a non-hermaphroditic two-stock structural variant.
\link{WrasseFemaleExStock}, \link{WrasseMaleExStock}, \link{AsympExFleet}, \link{DomeExFleet},
\link{AgeStructuredObs}, \link{CommercialFleetObs}, \link{FullComplianceImp} for the
component objects.
\code{\link[=SRR]{SRR()}} for \code{SPFrom}, \code{\link[=Herm]{Herm()}} and \link[=stocktransition-class]{stocktransition} for the
transition mechanism.
\code{\link[=OM]{OM()}}, \link[=om-class]{om}, \code{\link[=runMSE]{runMSE()}}, \code{\link[=PopulateOM]{PopulateOM()}}, \code{\link[=ExampleMPs]{ExampleMPs()}}

Other om:
\code{\link{ComplexOM}},
\code{\link[=Herm]{Herm()}},
\code{\link{MultiStockOM}},
\code{\link[=OM]{OM()}},
\code{\link{OM-accessors}},
\code{\link{SeasonalSpatialOM}},
\code{\link{SingleStockOM}},
\code{\link{TwoFleetOM}},
\code{\link{om-class}},
\code{\link{stocktransition-class}}
}
\concept{om}
\keyword{datasets}
