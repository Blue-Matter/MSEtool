% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-array-to-df.R
\name{array_df_conversion}
\alias{array_df_conversion}
\alias{Array2DF}
\alias{DF2Array}
\title{Convert Between Arrays and Data Frames}
\usage{
Array2DF(array)

DF2Array(DF)
}
\arguments{
\item{array}{A \code{matrix} or \code{array} object.}

\item{DF}{A \code{data.frame} containing a \code{Value} column and at least one
dimension column (\code{Sim}, \code{Stock}, \code{Age}, \code{Year}, \code{Fleet}, \code{Area}).}
}
\value{
\itemize{
\item \code{Array2DF()} returns a \code{data.frame} with one row per array
element and one column per dimension plus a \code{Value} column.
\item \code{DF2Array()} returns a named \code{array} whose dimensions and dimnames are
derived from the unique values of the dimension columns in \code{DF}.
}
}
\description{
\code{Array2DF()} converts a matrix or array to a tidy data frame, with dimension
names mapped to columns (\code{Sim}, \code{Stock}, \code{Age}, \code{Year}, \code{Fleet}, \code{Area}) and
values in a \code{Value} column. Numeric columns are coerced with \code{\link[=as.numeric]{as.numeric()}};
\code{Stock} and \code{Fleet} are returned as ordered factors.
}
\details{
\code{DF2Array()} is the inverse: it reshapes a tidy data frame back into a named
array, using any columns whose names match \code{Sim}, \code{Stock}, \code{Age}, \code{Year},
\code{Fleet}, or \code{Area} as array dimensions.
}
\examples{
# Round-trip: array -> data frame -> array
a <- array(1:24, dim = c(2, 3, 4),
           dimnames = list(Sim = 1:2, Year = 1:3, Age = 1:4))
df <- Array2DF(a)
df
DF2Array(df)

}
