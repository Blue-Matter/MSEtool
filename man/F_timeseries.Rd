% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-F-timeseries.R
\name{FInteract}
\alias{FInteract}
\alias{F_timeseries}
\alias{FDead}
\alias{FRetain}
\title{Extract Fishing Mortality Time Series}
\usage{
FInteract(
  object,
  df = TRUE,
  byAge = TRUE,
  byArea = FALSE,
  byFleet = FALSE,
  Reduce = TRUE,
  IncYear = FALSE
)

FDead(
  object,
  df = TRUE,
  byAge = TRUE,
  byArea = FALSE,
  byFleet = FALSE,
  Reduce = TRUE,
  IncYear = FALSE
)

FRetain(
  object,
  df = TRUE,
  byAge = TRUE,
  byArea = FALSE,
  byFleet = FALSE,
  Reduce = TRUE,
  IncYear = FALSE
)
}
\arguments{
\item{object}{A \link[=hist-class]{hist} or \link[=mse-class]{mse} object.}

\item{df}{Logical. If \code{FALSE} the raw array slot is returned.
If \code{TRUE} (default)  a tidy \code{data.frame} is returned.}

\item{byAge}{Logical. If \code{TRUE} the data frame retains the \code{Age}
dimension (sourced from the \verb{*Area} slots, which always include
\code{Area}). If \code{FALSE} the apical fishing mortality — the maximum over
ages — is returned. Ignored when \code{df = FALSE}.}

\item{byArea}{Logical. If \code{TRUE} the \code{Area} dimension is retained.
When \code{byAge = TRUE} area is always included regardless of this
argument. Ignored when \code{df = FALSE}.}

\item{byFleet}{Logical. If \code{TRUE} the \code{Fleet} dimension is retained.
If \code{FALSE} values are summed over fleets before the apical or
age-structured extraction. Ignored when \code{df = FALSE}.}

\item{Reduce}{Logical. If \code{TRUE} (default) simulation dimensions are
reduced using  \code{\link[=ReduceDims]{ReduceDims()}} before conversion to a data frame.}

\item{IncYear}{Logical. Passed to \code{\link[=ReduceDims]{ReduceDims()}}; controls whether the
year dimension is retained during reduction. Default \code{FALSE}.}
}
\value{
\itemize{
\item \code{df = FALSE} — the raw array slot (\code{FInteract}, \code{FDead}, or
\code{FRetain}).
\item \code{df = TRUE} — a tidy \code{data.frame} with columns \code{Sim}, \code{Stock},
\code{Year}, \code{Period}, \code{MP} (MSE only), and optionally \code{Age}, \code{Area},
and/or \code{Fleet}, plus \code{Value} and \code{Variable}.
}
}
\description{
\code{FInteract()}, \code{FDead()}, and \code{FRetain()} extract fishing mortality for
interactions (encounters), killed, and retained fish
respectively, from a \link[=hist-class]{hist} or \link[=mse-class]{mse} object.
}
\details{
When \code{byAge = FALSE} the apical (maximum over ages) fishing mortality
rate is returned. When \code{byAge = TRUE} the full age-structured array is
retained; \code{byArea} is always included when \code{byAge = TRUE}.

When applied to an \link[=mse-class]{mse} object the historical and projection
periods are row-bound and labelled via the \code{Period} column; historical
rows carry \code{MP = "Historical"}.
}
\examples{
Hist <- Simulate(ExampleOM)
MSE <- Project(Hist, 'CurrentEffort')

# Raw array slots
FInteract(Hist)
FDead(Hist)
FRetain(MSE)

# Age-structured F (area always included)
FInteract(Hist, df = TRUE)
FDead(MSE, df = TRUE)
FRetain(MSE, df = TRUE)

# Retain fleet dimension
FDead(MSE, df = TRUE, byFleet = TRUE)

# Apical F (max over ages) as a tidy data frame
FInteract(Hist, df = TRUE, byAge = FALSE)
FDead(MSE,      df = TRUE, byAge = FALSE)
FRetain(MSE,    df = TRUE, byAge = FALSE)

# Age + Fleet
FDead(MSE, df = TRUE, byFleet = TRUE)

# Area only (apical F per area)
FDead(MSE, df = TRUE, byAge = FALSE, byArea = TRUE)
FDead(MSE, df = TRUE, byAge = FALSE, byArea = TRUE, byFleet = TRUE)

}
