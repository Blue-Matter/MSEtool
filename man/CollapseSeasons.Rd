% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse-seasons.R
\name{CollapseSeasons}
\alias{CollapseSeasons}
\title{Collapse a Seasonal Operating Model to an Annual Operating Model}
\usage{
CollapseSeasons(OM, silent = FALSE)
}
\arguments{
\item{OM}{A seasonal \code{\link[=OM]{OM()}} object (\code{OM@Seasons > 1}).
Multi-area (\code{nArea > 1}) OMs are not yet supported.}

\item{silent}{\code{logical(1)}. If \code{TRUE}, suppresses informational console
messages. Defaults to \code{FALSE}.}
}
\value{
An updated \code{\link[=OM]{OM()}} object with \code{Seasons = 1} and re-derived
biology/fishery arrays at annual resolution.
\subsection{Method}{

Seasons are included in the \code{Year} dimension (as fractional-year timesteps) and the
\code{Age} dimension (as fractional-age classes) of every populated array. So
"collapsing seasons" means re-deriving both dimensions at annual
resolution, following a cohort's actual path through the \code{Seasons}
sub-annual steps of a calendar year (age \code{a}, \code{a + 1/Seasons}, ...,
\code{a + (Seasons-1)/Seasons}):
\itemize{
\item \strong{Natural mortality} and \strong{fecundity} are summed
\item \strong{Length-} and \strong{weight-at-age}, and \strong{maturity-at-age} are read
directly off the populated seasonal array at the exact integer-age, first-season-of-year cell
\item \strong{Seasonal \code{R0}} is summed to annual \code{R0}
\item \strong{Fleet dynamics} (effort, catchability, selectivity, retention,
discard mortality, weight-at-age-in-the-catch) follow the same
F-weighted-average reconstruction used by \code{\link[=CombineFleets]{CombineFleets()}}, but
weighting across a cohort's season-steps within a year instead of
across fleets.
}

This is an approximation, not an exact transformation: the population
dynamics apply mortality and fishing simultaneously within each season
(\code{exp(-(M+F))} per timestep), so an annual model cannot exactly reproduce
\code{Seasons} sequential simultaneous-mortality events with a single annual
one. The approximation is expected to be close whenever seasonal
heterogeneity in \code{F} and \code{M} is mild.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Converts an Operating Model with sub-annual seasonal resolution
(\code{OM@Seasons > 1}) into an annual Operating Model (\code{Seasons = 1}) that
approximates the same annual-scale fishery dynamics.
}
\seealso{
\code{\link[=CombineFleets]{CombineFleets()}} for the analogous fleet-aggregation function.
}
