% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-sp-spict.R
\name{SpictControl}
\alias{SpictControl}
\title{Settings for the SPiCT Surplus Production Model}
\usage{
SpictControl(
  dteuler = 1,
  SeasonalCatch = FALSE,
  Robust = FALSE,
  Priors = list(),
  FixedDepletionCV = 0.01,
  stabilise = TRUE,
  optimiser = c("nlminb", "optim"),
  optimiser.control = list(iter.max = 500, eval.max = 1000),
  SDReport = c("auto", "always", "never"),
  getReportCovariance = FALSE,
  WarmStart = TRUE,
  Quiet = TRUE,
  Inp = list()
)
}
\arguments{
\item{dteuler}{Positive number. Euler time step (years) of the model.
Default \code{1}. With sub-annual catch (\code{SeasonalCatch = TRUE}), the time
step is at most the catch interval.}

\item{SeasonalCatch}{Logical. Fit to the seasonal catch of seasonal
\code{Data} (\code{Data@Seasons > 1}) rather than the annual total? Default
\code{FALSE}.}

\item{Robust}{Logical. Use robust (mixture) observation error for the
catch and indices (\code{robflagc}, \code{robflagi})? Default \code{FALSE}.}

\item{Priors}{Named list of \code{spict} priors (e.g. \code{logalpha}, \code{logbeta}),
each \code{c(mean, sd, use)} on the log scale. These are applied after, and
replace, any priors set from the \code{\link[=FitSP]{FitSP()}} arguments.}

\item{FixedDepletionCV}{Positive number. CV of the prior on the initial
depletion when \code{EstDepletion = FALSE}. Default \code{0.01}.}

\item{stabilise}{Logical. Use the \code{spict} vague stabilising priors?
Default \code{TRUE}.}

\item{optimiser}{Character. \code{'nlminb'} (default) or \code{'optim'}.}

\item{optimiser.control}{List of optimiser controls. Default
\code{list(iter.max = 500, eval.max = 1000)}.}

\item{SDReport}{Character. When to run \code{TMB::sdreport()}, which is needed
for the standard errors: \code{'auto'} (default; when \code{Uncertainty = TRUE} in
\code{\link[=FitSP]{FitSP()}}, or when \code{\link[=SurplusProduction]{SurplusProduction()}} uses \code{FractileB}, \code{FractileF},
or \code{Diagnostics = 'full'}), \code{'always'}, or \code{'never'}.}

\item{getReportCovariance}{Logical. Passed to \code{TMB::sdreport()}. Default
\code{FALSE}.}

\item{WarmStart}{Logical. In \code{\link[=SurplusProduction]{SurplusProduction()}}, start each fit from
the estimates of the previous management cycle? Default \code{TRUE}.}

\item{Quiet}{Logical. Suppress \code{spict} messages and warnings, recording
warnings in the fit's \code{Log}? Default \code{TRUE}.}

\item{Inp}{Named list of any other \code{spict} \code{inp} elements. Applied last,
so these replace any values set by \code{FitSP()} or the other arguments.}
}
\value{
A list of class \code{spictcontrol}.
}
\description{
Controls for \code{\link[=FitSP]{FitSP()}} and \code{\link[=SurplusProduction]{SurplusProduction()}} with \code{Model = 'spict'},
which fit the stochastic surplus production model in continuous time
(SPiCT) with the \code{spict} package. \code{spict} is not on CRAN; install it with
\code{pak::pkg_install('DTUAqua/spict/spict')}.
}
\details{
The model shape, initial depletion, index weighting, and priors are set
with the \code{\link[=FitSP]{FitSP()}} arguments (\code{Shape}, \code{EstShape}, \code{Depletion},
\code{EstDepletion}, \code{IndexSD}, \code{IndexWeight}, \code{Priors}), which are converted to
the equivalent \code{spict} inputs:
\itemize{
\item \code{EstShape = FALSE} fixes \code{n} at \code{Shape}; \code{EstShape = TRUE} estimates
\code{n} with a lognormal prior, \code{Priors$Shape} if given, otherwise with
median \code{Shape} and CV \code{ShapeCV}.
\item \code{Depletion} is the starting value of the initial depletion
(\code{logbkfrac}). With \code{EstDepletion = FALSE} it is held close to
\code{Depletion} by a lognormal prior with CV \code{FixedDepletionCV}; with
\code{EstDepletion = TRUE} it has the prior \code{Priors$Depletion}, if given.
\item \code{IndexWeight} and, with \code{IndexSD = 'data'}, the relative index \code{CV}s set
the \code{stdevfacI} scaling of each index observation's standard deviation.
The standard deviation of each index is still estimated.
\item \code{Priors$FMSY} sets a prior on the intrinsic growth rate, \code{FMSY * n}
(with \code{n = Shape}), and \code{Priors$MSY} a prior on \code{m} (MSY).
\item \code{Start} (or the warm start in \code{\link[=SurplusProduction]{SurplusProduction()}}) sets the starting
values of \code{m} and \code{K}.
}
}
\references{
Pedersen, M. W. and Berg, C. W. 2017. A stochastic surplus
production model in continuous time. Fish and Fisheries 18: 226-243.
}
\seealso{
\code{\link[=FitSP]{FitSP()}}, \code{\link[=SurplusProduction]{SurplusProduction()}}, \code{\link[=SPControl]{SPControl()}}
}
