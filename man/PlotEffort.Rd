% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-hist.R
\name{PlotEffort}
\alias{PlotEffort}
\alias{PlotEffortCurve}
\title{Plot Fishing Effort}
\usage{
PlotEffort(
  object,
  Stock = NULL,
  units = NULL,
  byStock = NULL,
  byFleet = NULL,
  Stocks = NULL,
  probs = c(0.05, 0.95),
  nsim = 0,
  Years = NULL,
  free_y = NULL,
  IncHist = TRUE,
  byMP = FALSE,
  AggregateYear = FALSE
)

PlotEffortCurve(
  object,
  Stock = NULL,
  units = NULL,
  byStock = NULL,
  byFleet = NULL,
  Stocks = NULL,
  probs = c(0.05, 0.95),
  nsim = 0,
  Years = NULL,
  free_y = NULL
)
}
\arguments{
\item{object}{A \link[=fleet-class]{fleet} object, an \link[=om-class]{om}, \link[=hist-class]{hist}, or
\link[=mse-class]{mse} object, or a \link[=data-class]{data} object (plots \code{Effort(object)}
directly).}

\item{Stock}{A \link[=stock-class]{stock} object supplying the biology needed to
populate a bare \link[=fleet-class]{fleet} \code{object}. Ignored otherwise. Default
\code{NULL} uses an example stock, with a message noting this.}

\item{units}{Character or \code{NULL}. \code{NULL} (default) uses the coarsest
unit common to every plotted fleet (\code{"Effort"}, \code{"Trips"}, or
\code{"Anglers"}, depending on what each fleet's \code{\link[=Effort]{Effort()}} object
supplies). Set explicitly to force one; requesting a unit some fleet
lacks the data for is an error.}

\item{byStock, byFleet}{One of \code{TRUE}, \code{FALSE}, or \code{NULL} (default,
facets automatically when \code{object} has more than one stock/fleet).
\code{FALSE} sums effort across fleets instead of faceting/coloring by
fleet (only when every summed fleet shares the same \code{units}).}

\item{Stocks}{Character or numeric vector. Restrict the plot to specific
stocks, either by name (matching \code{\link[=StockNames]{StockNames()}}) or by index. Default
\code{NULL} (all stocks).}

\item{probs}{Numeric vector of length 2. Lower and upper quantiles of
the across-simulation ribbon. Default \code{c(0.05, 0.95)}.}

\item{nsim}{Integer. Number of individual simulation trajectories to
overlay as thin lines, in addition to the median/ribbon. Default \code{0}.}

\item{Years}{Optional numeric vector, or \code{"all"}. Default \code{NULL} plots
every available year.}

\item{free_y}{Logical. Free the y-axis scale across facets? Default
\code{NULL} resolves to \code{TRUE}.}

\item{IncHist, byMP, AggregateYear}{\code{.PlotEffortRealized()} only (via
\code{PlotEffort()} on a \link[=hist-class]{hist}/\link[=mse-class]{mse} \code{object}); see
\link{plot_hist}.}
}
\value{
\code{PlotEffortCurve()} returns a \code{ggplot} object; \code{PlotEffort()}
returns a \code{ggplot} object too (\code{.PlotEffortRealized()}'s, for
\link[=hist-class]{hist}/\link[=mse-class]{mse} \code{object}, otherwise \code{PlotEffortCurve()}'s).
}
\description{
For a bare \link[=fleet-class]{fleet} or \link[=om-class]{om} \code{object} (nothing simulated
yet), \code{PlotEffort()} plots the specified input effort trend
(\code{PlotEffortCurve()}, \code{Fleet@Effort@Effort}, historical years only).
For a \link[=hist-class]{hist}/\link[=mse-class]{mse} \code{object}, it plots the realized
historical/projection effort trajectory.
}
\seealso{
\code{\link[=Effort]{Effort()}}, \code{\link[=PlotCatchability]{PlotCatchability()}}, \code{\link[=Fleet]{Fleet()}}
}
