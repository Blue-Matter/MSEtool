% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helpers.R
\name{SumOverAge}
\alias{SumOverAge}
\alias{SumOverArea}
\alias{SumOverFleet}
\alias{SumOverStock}
\alias{SumOverYear}
\title{Sum over named array dimensions}
\usage{
SumOverAge(x)

SumOverArea(x)

SumOverFleet(x)

SumOverStock(x)

SumOverYear(x)
}
\arguments{
\item{x}{An array. If the requested dimension is named, it will be summed
out; otherwise the last dimension is used as a fallback.}
}
\value{
An array with the specified dimension removed. Dimension names are preserved
where possible.
}
\description{
Convenience wrappers for summing an array over a specific named dimension.
These functions provide fast, explicit alternatives to \code{apply()} when
working with multi-dimensional arrays that use named dimensions.
}
\details{
If the requested dimension name is present in \code{names(dimnames(x))}, that
dimension is summed out. If the dimension is not named (or \code{x} has no
dimension names), the \strong{last dimension} of the array is summed by default.
\subsection{Exported functions}{
\itemize{
\item \code{SumOverAge()} – sum over the \code{"Age"} dimension
\item \code{SumOverArea()} – sum over the \code{"Area"} dimension
\item \code{SumOverFleet()} – sum over the \code{"Fleet"} dimension
\item \code{SumOverStock()} – sum over the \code{"Stock"} dimension
\item \code{SumOverYear()} – sum over the \code{"Year"} dimension
}
}
}
