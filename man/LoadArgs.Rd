% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-load-args.R
\name{LoadArgs}
\alias{LoadArgs}
\title{Load Function Arguments into an Environment}
\usage{
LoadArgs(fun = "Simulate", envir = .GlobalEnv, debug = FALSE)
}
\arguments{
\item{fun}{Character string naming a function, or a function object.
Default \code{"Simulate"}.}

\item{envir}{Environment to assign default values into. Default
\code{.GlobalEnv}.}

\item{debug}{Logical. If \code{TRUE}, emits a message for each argument
indicating whether it already exists in \code{envir} and whether it is being
assigned. Default \code{FALSE}.}
}
\value{
\code{NULL} invisibly. Called for its side effect of populating \code{envir}.
}
\description{
Assigns the default argument values of a function to \code{envir}, skipping any
argument that already exists in that environment as a non-function object.
Useful during interactive development for populating the workspace with a
function's expected inputs without overwriting variables already set.
}
\examples{
# Populate the global workspace with the default arguments of [CalcSurvival()],
# without overwriting any variables that are already defined:
\dontrun{
NaturalMortality <- my_array   # already set; will not be overwritten
LoadArgs("CalcSurvival")
# FishingMortality, PlusGroup, SpawnTimeFrac, and Semelparous are now
# assigned their defaults in .GlobalEnv; NaturalMortality is unchanged.
}

}
