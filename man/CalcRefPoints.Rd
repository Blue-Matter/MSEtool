% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-ref-points.R
\name{CalcRefPoints}
\alias{CalcRefPoints}
\title{Calculate Per-Recruit and Replacement-Line Reference Points}
\usage{
CalcRefPoints(
  Hist,
  Years = NULL,
  SPRTarget = c(0.3, 0.4, 0.5),
  type = c("Removals", "Landings"),
  nF = 100,
  BLow = FALSE,
  HZN = 2,
  Bfrac = 0.5,
  silent = FALSE
)
}
\arguments{
\item{Hist}{A \link[=hist-class]{hist} or \link[=om-class]{om} object. If an \code{om} is supplied
it is first converted to a \code{hist} object.}

\item{Years}{Integer vector of years for which reference points are
evaluated. If \code{NULL} (default), the final historical year is used.}

\item{SPRTarget}{Numeric vector of SPR targets (e.g. \code{0.4} for F40\%) at
which to evaluate \verb{Fx\%SPR}. Default \code{c(0.3, 0.4, 0.5)}.}

\item{type}{Character. Whether yield-per-recruit is defined in terms of
total removals or landings only. One of \code{'Removals'} (default) or
\code{'Landings'}.}

\item{nF}{Integer. Number of apical F values in the log-spaced search
grid spanning \code{0} to \code{Hist@OM@maxF}. Default \code{100}.}

\item{BLow}{Logical. Also calculate the \code{BLow} reference point (see
\code{\link[=CalcBLow]{CalcBLow()}})? Default \code{FALSE}. Substantially more expensive than the
other reference points here -- a full population projection plus a
numerical search, repeated per simulation and stock -- so it is kept
as a separate opt-in rather than bundled into the default set.}

\item{HZN, Bfrac}{Passed to \code{\link[=CalcBLow]{CalcBLow()}} when \code{BLow = TRUE}.}

\item{silent}{Logical. If \code{TRUE}, suppresses progress messages. Default
\code{FALSE}.}
}
\value{
The input \link[=hist-class]{hist} object with \code{Hist@Reference@F01},
\verb{@FMax}, \verb{@FSPR}, \verb{@FMed}, \verb{@FCrash}, \verb{@SPRcrash}, and (if
\code{BLow = TRUE}) \verb{@BLow} populated.
}
\description{
Computes \code{F0.1}, \code{Fmax}, \verb{Fx\%SPR}, \code{Fmed}, \code{Fcrash}, and \code{SPRcrash}
biological reference points and stores them in \code{Hist@Reference}.
}
\details{
All reference points except \code{BLow} are evaluated on the same per-recruit
apical-F grid via \code{\link[=CalcPerRecruit]{CalcPerRecruit()}}, reusing a single evaluation rather
than re-optimizing per reference point.
\itemize{
\item \code{F0.1}: apical F at which the slope of the yield-per-recruit curve is
10\% of the slope at the origin.
\item \code{Fmax}: apical F maximising yield-per-recruit. Undefined (returned as
the upper grid bound) for asymptotic selectivity, where
yield-per-recruit has no interior maximum.
\item \verb{Fx\%SPR}: apical F at which spawning-per-recruit relative to unfished
equals each value in \code{SPRTarget}.
\item \code{Fcrash}/\code{SPRcrash}: the apical F (and corresponding relative SPR) at
which the per-recruit replacement line crosses the compensation ratio
implied by steepness, evaluated against a fixed reference
spawning-per-recruit at the first historical year -- matching the
fixed-alpha/beta stock-recruitment curve.
Only defined for \code{BevertonHolt} and \code{Ricker} stock-recruitment models.
\item \code{Fmed}: apical F at which the per-recruit replacement line equals the
median historical recruits-per-spawner ratio.
\item \code{BLow} (optional): see \code{\link[=CalcBLow]{CalcBLow()}}.
}
}
\seealso{
\code{\link[=CalcPerRecruit]{CalcPerRecruit()}}, \code{\link[=CalcMSY]{CalcMSY()}}, \code{\link[=CalcBLow]{CalcBLow()}}, \link[=reference-class]{reference},
\code{\link[=F01]{F01()}}, \code{\link[=FMax]{FMax()}}, \code{\link[=FSPR]{FSPR()}}, \code{\link[=FMed]{FMed()}}, \code{\link[=FCrash]{FCrash()}}, \code{\link[=SPRcrash]{SPRcrash()}}
}
