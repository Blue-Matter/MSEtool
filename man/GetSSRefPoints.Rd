% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-ss3.R
\name{GetSSRefPoints}
\alias{GetSSRefPoints}
\title{Get Reference Points from Stock Synthesis Output}
\usage{
GetSSRefPoints(
  SSDir,
  Labels = c("annF_F01", "Dead_Catch_F01", "SSB_SPR", "annF_SPR", "Dead_Catch_SPR",
    "SSB_MSY", "SPR_MSY", "annF_MSY", "Dead_Catch_MSY", "Ret_Catch_MSY")
)
}
\arguments{
\item{SSDir}{One of:
\itemize{
\item A character string giving the path to an SS3 output directory,
passed to \code{\link[=ImportSSReport]{ImportSSReport()}}.
\item A \code{RepList} object returned by \code{\link[=ImportSSReport]{ImportSSReport()}}.
\item A plain list in \code{RepList} format, i.e. \code{RepList[[1]]}.
}}

\item{Labels}{Character vector of \code{derived_quants} labels to extract.
Defaults to a standard set of F, catch, and biomass reference points:
\itemize{
\item \code{"annF_F01"}, \code{"Dead_Catch_F01"} — F0.1 fishing mortality and dead catch.
\item \code{"SSB_SPR"}, \code{"annF_SPR"}, \code{"Dead_Catch_SPR"} — SSB, F, and dead catch
at the SPR target.
\item \code{"SSB_MSY"}, \code{"SPR_MSY"}, \code{"annF_MSY"}, \code{"Dead_Catch_MSY"},
\code{"Ret_Catch_MSY"} — SSB, SPR, F, dead catch, and retained catch at MSY.
}}
}
\value{
If \code{SSDir} contains a single model, a data frame with columns:
\itemize{
\item \code{Label} — the \code{derived_quants} label.
\item \code{Value} — the point estimate.
\item \code{StdDev} — the standard deviation.
}

If \code{SSDir} contains multiple models, a list of such data frames, one
per model.
}
\description{
Reads biological reference points from one or more Stock Synthesis (SS3)
model outputs and returns the requested quantities from the
\code{derived_quants} table.
}
\seealso{
\code{\link[=ImportSSReport]{ImportSSReport()}}
}
