% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-spatial-structure.R
\name{.CheckSpatial}
\alias{.CheckSpatial}
\title{Check and simplify spatial structure of a named array}
\usage{
.CheckSpatial(array, name = NULL)
}
\arguments{
\item{array}{A named array, optionally with an \code{Area} dimension.}

\item{name}{\code{character()} or \code{NULL}. Name of the array used in the error
message. Defaults to \code{NULL}.}
}
\value{
\code{array} with the \code{Area} dimension dropped, or aborts if the array
varies across areas.
}
\description{
If \code{array} lacks an \code{Area} dimension, has only one area, or is identical
across all areas, the \code{Area} dimension is dropped via \code{\link[=DropDimension]{DropDimension()}}.
Aborts if the array varies across areas, as spatial structure is not
currently supported.
}
\keyword{internal}
