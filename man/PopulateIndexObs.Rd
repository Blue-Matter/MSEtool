% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-obs-index.R
\name{PopulateIndexObs}
\alias{PopulateIndexObs}
\title{Populate Index Observation Error}
\usage{
PopulateIndexObs(Index, nSim, HistYears, ProjYears)
}
\arguments{
\item{Index}{An \link[=indicesobs-class]{indicesobs} object. Used for both CPUE indices
(\code{Obs@CPUE}) and fishery-independent survey indices (\code{Obs@Survey}).}

\item{nSim}{Integer. Number of simulation replicates.}

\item{HistYears}{Integer vector. Calendar years of the historical period.}

\item{ProjYears}{Integer vector. Calendar years of the projection period.}
}
\value{
A populated \link[=indicesobs-class]{indicesobs} object.
}
\description{
Populate an \link[=indicesobs-class]{indicesobs} object by expanding CV, generating
stochastic error arrays, applying lag-1 autocorrelation, applying
multiplicative bias, and populating index years, across simulation
replicates and years.
}
\details{
If \code{\link[=EmptyObject]{EmptyObject()}} returns \code{TRUE} for \code{Index}, the object is returned
unchanged and no population is performed.

Otherwise the following slots are populated in order:
\itemize{
\item \code{CV} — expanded to a named \verb{[nSim]} array via \code{\link[=PopulateObsCV]{PopulateObsCV()}}.
\item \code{Error} — generated as a lognormal draw from \code{CV} across all historical
and projection years, producing a named \verb{[nSim x nYear]} array via
\code{\link[=PopulateObsError]{PopulateObsError()}}. If \code{Error} is already a fully-formed array it is
validated and dimension names are applied. If \code{AC} is non-zero, the
error series is subsequently transformed to an AR(1) process (see below).
\item \code{AC} — expanded to a named \verb{[nSim]} array via \code{\link[=PopulateObsAC]{PopulateObsAC()}}.
Accepted inputs: scalar applied to all simulations, length-2 vector
\code{c(lower, upper)} for a per-simulation Uniform draw, or a vector of
length \code{nSim} used directly. Defaults to 0 (no autocorrelation).
\item \code{Ref} — generated as a lognormal draw from a CV, producing a named
\verb{[nSim]} reference level error array via \code{\link[=PopulateObsRef]{PopulateObsRef()}}.
\item \code{Years} — if empty, defaults to all historical years (\code{HistYears}).
}

The following slots are used internally during the simulation and are not
populated here: \code{Beta}, \code{Selectivity}, \code{Type}, \code{Efficiency}.
\subsection{Autocorrelated index error}{

When \code{AC} is non-zero, the independently-drawn lognormal error series is
transformed into a variance-preserving AR(1) process on the log scale.
Let \eqn{\varepsilon_t = \log(\mathrm{Error}_t)} be the raw log-errors
drawn from \eqn{N(\mu, \sigma^2)} where \eqn{\mu} and \eqn{\sigma} are
derived from \code{CV}. The AR(1) series is constructed as:

\deqn{\varepsilon_t^* = \rho \, \varepsilon_{t-1}^* +
  \sqrt{1 - \rho^2} \, \varepsilon_t}

where \eqn{\rho} is the per-simulation lag-1 autocorrelation coefficient
(\code{AC}). This formulation preserves the marginal variance
\eqn{\mathrm{Var}(\varepsilon_t^*) = \sigma^2} for all values of
\eqn{\rho}. The AR(1) series is then exponentiated to return to the
natural scale: \eqn{\mathrm{Error}_t^* = \exp(\varepsilon_t^*)}.
}
}
\seealso{
\code{\link[=PopulateObs]{PopulateObs()}}, \link[=indicesobs-class]{indicesobs}, \code{\link[=PopulateObsCV]{PopulateObsCV()}},
\code{\link[=PopulateObsError]{PopulateObsError()}}, \code{\link[=PopulateObsAC]{PopulateObsAC()}},
\code{\link[=PopulateObsRef]{PopulateObsRef()}}
}
