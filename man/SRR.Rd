% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_Class_srr.R, R/01_Generic_srr.R,
%   R/02_Methods_srr.R
\docType{class}
\name{SRR}
\alias{SRR}
\alias{SRR<-}
\alias{SRR,missing-method}
\alias{SRR,list-method}
\alias{SRR,stock-method}
\alias{SRR<-,stock,srr-method}
\title{SRR Class and Constructor}
\usage{
SRR(Pars, ...)

SRR(x) <- value

\S4method{SRR}{missing}(Pars, ...)

\S4method{SRR}{list}(
  Pars,
  Model = "BevertonHolt",
  R0 = array(),
  SD = array(),
  AC = array(),
  SPFrom = NULL,
  TruncSD = 2,
  RecDevInit = array(),
  RecDevHist = array(),
  RecDevProj = array(),
  SpawnTimeFrac = 0,
  RelRecFun = NULL,
  Units = 1,
  Misc = list(),
  ...
)

\S4method{SRR}{stock}(Pars, ...)

\S4method{SRR}{stock,srr}(x) <- value
}
\arguments{
\item{Model}{A named list of parameters for a model to generate
the expected stock-recruit curve. See \code{Parameters} section in \code{Details}}

\item{SD}{Numeric vector. The standard deviation of the recruitment deviations in log-space.
Either length 1 (constant across simulations), length 2
(uniform distribution across simulations), or length \code{nSim}.
See \code{Parameters} section in \code{Details}.}

\item{AC}{Numeric vector. The lag-1 autocorrelation factor of the recruitment deviations in log-space.
Same structure as \code{SD}.}

\item{TruncSD}{The number of standard deviations to truncated the log-normal distribution
used to generate recruitment deviations. Defaults to 2.}

\item{RecDevInit}{Optional. Numeric matrix with dimensions: \code{c(nSim, MaxAge)}.
The recruitment deviations for the age classes in the initial time step. Populated
internally from \code{SD} and \code{AC} if not specified.}

\item{RecDevHist}{Optional. Numeric matrix with dimensions: \code{c(nSim, nHistTS)}.
The recruitment deviations for the historical time steps. Populated
internally from \code{SD} and \code{AC} if not specified.}

\item{RecDevProj}{Optional. Numeric matrix with dimensions: \code{c(nSim, nProjectionTS)}.
The recruitment deviations for the projecion time steps. Populated
internally from \code{SD} and \code{AC} if not specified.}

\item{SpawnTimeFrac}{Numeric value between 0 (default) and 1. The relative time in between
the time steps when spawning occurs, with 0 indicating the beginning of the time step.}
}
\description{
SRR Class and Constructor
}
\section{Slots}{

\describe{
\item{\code{Model}}{A named list of parameters for a model to generate
the expected stock-recruit curve. See \code{Parameters} section in \code{Details}}

\item{\code{SD}}{Numeric vector. The standard deviation of the recruitment deviations in log-space.
Either length 1 (constant across simulations), length 2
(uniform distribution across simulations), or length \code{nSim}.
See \code{Parameters} section in \code{Details}.}

\item{\code{AC}}{Numeric vector. The lag-1 autocorrelation factor of the recruitment deviations in log-space.
Same structure as \code{SD}.}

\item{\code{TruncSD}}{The number of standard deviations to truncated the log-normal distribution
used to generate recruitment deviations. Defaults to 2.}

\item{\code{RecDevInit}}{Optional. Numeric matrix with dimensions: \code{c(nSim, MaxAge)}.
The recruitment deviations for the age classes in the initial time step. Populated
internally from \code{SD} and \code{AC} if not specified.}

\item{\code{RecDevHist}}{Optional. Numeric matrix with dimensions: \code{c(nSim, nHistTS)}.
The recruitment deviations for the historical time steps. Populated
internally from \code{SD} and \code{AC} if not specified.}

\item{\code{RecDevProj}}{Optional. Numeric matrix with dimensions: \code{c(nSim, nProjectionTS)}.
The recruitment deviations for the projecion time steps. Populated
internally from \code{SD} and \code{AC} if not specified.}

\item{\code{SpawnTimeFrac}}{Numeric value between 0 (default) and 1. The relative time in between
the time steps when spawning occurs, with 0 indicating the beginning of the time step.}
}}

\examples{
SRRModels()


# Beverton-Holt Model
SRR <- SRR(Pars=list(R0=1e5, h=0.6), SD=0.6, AC=0.1)

# Populate the object (usually done internally; see `?Populate`)
SRR <- Populate(SRR, MaxAge=10, nHistTS=20, nProjTS=5, nsim=2)

plot(SRR)

# see Examples in `?Length` for other `Pars` configurations
}
