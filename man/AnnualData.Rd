% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-data.R
\name{AnnualData}
\alias{AnnualData}
\title{Aggregate Seasonal Data to Annual Time Steps}
\usage{
AnnualData(Data, IndexSeasons = NULL, CompleteYears = TRUE)
}
\arguments{
\item{Data}{A \link[=data-class]{data} object.}

\item{IndexSeasons}{Seasons (integers in \code{1:Data@Seasons}) averaged to
form each annual index value. \code{NULL} (default) uses every season. Either
an integer vector applied to every index, or a list with elements
\code{Survey} and/or \code{CPUE}, each either an integer vector applied to every
index in that slot or a list with one element (integer vector, or \code{NULL}
for every season) per index. Ignored when \code{Seasons = 1}.}

\item{CompleteYears}{Logical. If \code{TRUE} (default), calendar years with
fewer than \code{Seasons} time steps in \code{Data@Years} (e.g. a partially
observed final year) are dropped, so a partial year's catch is not read
as a low annual catch.}
}
\value{
A \link[=data-class]{data} object with \code{Seasons = 1} and whole-number \code{Years}.
Every aggregated slot has one row per element of the returned \code{Years}.
}
\description{
Converts a seasonal \link[=data-class]{data} object (\code{Seasons > 1}, one row per time
step) to one row per calendar year. Data with \code{Seasons = 1} is returned
unchanged.
}
\details{
\itemize{
\item \code{Landings}, \code{Discards}, \code{Effort}, and the composition slots
(\code{LandingsAtAge}, \code{DiscardsAtAge}, \code{LandingsAtSize}, \code{DiscardsAtSize})
are summed over the seasons of each calendar year. A cell with no
non-\code{NA} value in a year stays \code{NA}. The \code{CV} of summed catch and effort
assumes independent errors among seasons.
\item \code{CPUE} and \code{Survey} values and \code{CV}s are averaged (simple mean, ignoring
\code{NA} and negative values) over the seasons in \code{IndexSeasons}. Index \code{Ref}
values are not modified, and so are assumed to be in the units of the
aggregated annual index.
}

The \code{Advice}, \code{LifeHistory}, \code{Exploitation}, and \code{Reference} slots are not
modified.
}
\seealso{
\link{DataHelpers}, \link{IndexMPs}
}
