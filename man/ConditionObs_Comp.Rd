% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition-obs-comp.R
\name{ConditionObs_Comp}
\alias{ConditionObs_Comp}
\title{Condition Composition Observation Error from Observed Data}
\usage{
ConditionObs_Comp(
  Hist,
  FisheryData,
  HistYears,
  ProjYears,
  stocks,
  i,
  type = c("LandingsAtAge", "DiscardsAtAge", "LandingsAtSize", "DiscardsAtSize")
)
}
\arguments{
\item{Hist}{A \code{\link[=Hist]{Hist()}} object populated with historical fishery dynamics.}

\item{FisheryData}{A \code{\link[=Data]{Data()}} object containing real fishery composition
data.}

\item{HistYears}{Numeric vector of historical years.}

\item{ProjYears}{Numeric vector of projection years.}

\item{stocks}{Integer vector of stock indices in the complex.}

\item{i}{Integer index of the observed data set.}

\item{type}{Character; one of \code{"LandingsAtAge"}, \code{"DiscardsAtAge"},
\code{"LandingsAtSize"}, or \code{"DiscardsAtSize"}.}
}
\value{
\code{Hist} with \code{Shift} and \code{ESS} populated in the relevant
\link[=compobs-class]{compobs} slots of \code{Hist@OM@Obs[[i]]} for each fleet where
observed composition data are available.
}
\description{
Internal function to condition \link[=compobs-class]{compobs} observation error parameters
from observed age or size composition data. Populates \code{Shift} (per-bin
log-concentration residual) and \code{ESS} (effective sample size) for each
fleet where observed composition data are available.
}
\details{
\subsection{Early Exit Conditions}{

The function returns \code{Hist} unchanged if:
\itemize{
\item The relevant \link[=compdata-class]{compdata} slot of \code{FisheryData} is empty or has no
\code{Value} array.
\item A fleet has no matching column in the observed data, or its \link[=compobs-class]{compobs}
object is a default unconditioned object (as determined by \code{isNewObject()}),
or \code{SampleSize} is \code{NULL}.
}
}

\subsection{OM-Predicted Compositions}{

True catch-at-age or catch-at-size is aggregated over stocks within the
complex and over areas:
\itemize{
\item \strong{\verb{*AtAge}}: extracted from \code{Hist@LandingsAtAge} or \code{Hist@DiscardsAtAge}
(list over stocks, each \verb{[nSim x nAge x nYear x nFleet x nArea]}), summed
over areas and stocks.
\item \strong{\verb{*AtSize}}: extracted from \code{Hist@LandingsAtSize} or
\code{Hist@DiscardsAtSize} (nested list \verb{[stock][fleet]}, each
\verb{[nSim x nClass x nYear x nArea]}), summed over areas and stocks,
assembled over fleets.
}

Predicted proportions \eqn{\hat{p}_{s,t,b}} are computed by normalising
within each simulation and year.
}

\subsection{Conditioned Parameters}{
\subsection{\code{Shift} per-bin log-concentration residual}{

For each simulation \code{s}, fleet, and conditioning year \code{t}, the
log-concentration residual for bin \eqn{b} is:

\deqn{r_{s,t,b} = \log(p^{\mathrm{obs}}_{t,b}) - \log(\hat{p}_{s,t,b})}

where \eqn{p^{\mathrm{obs}}_{t,b}} is the observed proportion (identical
across simulations) and \eqn{\hat{p}_{s,t,b}} is the OM-predicted
proportion for simulation \code{s}. \code{Shift} is set to \eqn{r_{s,t,b}} directly
for each conditioning year, giving a \verb{[nSim x nYear x nBin]} array.

For projection years, the shift from the last conditioning year is held
constant. \strong{This assumes that the compositional bias structure observed in
the final conditioning year persists unchanged into projection years.} If
the bias is driven by a time-varying process (e.g. gear change, spatial
shift), this assumption may not hold. The \code{Shift} slot can be overwritten
directly after conditioning if a different projection assumption is
preferred.
}

\subsection{\code{ESS} effective sample size}{

\code{ESS} is estimated per simulation and year using the chi-square effective N:

\deqn{\mathrm{ESS}_{s,t} = \frac{1}{\displaystyle\sum_b
  \frac{(p^{\mathrm{obs}}_{t,b} - \hat{p}_{s,t,b})^2}{\hat{p}_{s,t,b}}}}

The result is stored as a \verb{[nSim x nYear]} array. Years with no valid
observed or predicted compositions are set to \code{NA}. Non-finite estimates
(e.g. where all predicted catch is zero) fall back to \code{1}.

For projection years, the ESS from the last conditioning year is held
constant. \strong{This carries the same assumption as \code{Shift}: that the
observation error structure of the final conditioning year is representative
of future years.} The \code{ESS} slot can be overwritten directly after
conditioning if a different assumption is preferred.
}

\subsection{\code{Theta} not conditioned}{

\code{Theta} is not estimated because it is not separately identifiable from
\code{ESS} given only observed composition proportions — both parameters control
overdispersion relative to the multinomial. \strong{Conditioning implicitly
assumes \code{Theta = 1}, i.e. that all overdispersion is absorbed into \code{ESS}.}
If a different value is preferred, it can be set directly after conditioning:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{LandingsAtAge(Hist@OM@Obs[[i]][[fl]])@Theta <- 0.5
}\if{html}{\out{</div>}}
}

}
}
\seealso{
\code{\link[=CompObs]{CompObs()}}, \link[=compobs-class]{compobs}, \link[=compdata-class]{compdata},
\code{\link[=ConditionObs_Catch]{ConditionObs_Catch()}}, \code{\link[=GenHistData_AgeComp]{GenHistData_AgeComp()}}, \code{\link[=GenHistData_SizeComp]{GenHistData_SizeComp()}}
}
\keyword{internal}
