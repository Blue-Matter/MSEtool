% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-om.R
\name{EffortAllocation}
\alias{EffortAllocation}
\alias{EffortAllocation<-}
\title{Fleet Allocation of Absolute Effort for OM Objects}
\usage{
EffortAllocation(x)

EffortAllocation(x) <- value
}
\arguments{
\item{x}{An \link[=om-class]{om}, \link[=hist-class]{hist}, or \link[=mse-class]{mse} object.}

\item{value}{Replacement value; a named list of length 0 or the number of
stock complexes, each element an \code{nSim} (or 1, recycled) by \code{nFleet}
matrix with rows summing to 1. \code{NULL} clears it.}
}
\value{
\itemize{
\item \code{EffortAllocation()} returns the value of the \code{EffortAllocation} slot.
\item \verb{EffortAllocation<-()} returns \code{x} with the \code{EffortAllocation} slot
updated.
}
}
\description{
Access or replace the \code{EffortAllocation} slot of an \link[=om-class]{om} object:
the fleet-level split applied when an MP returns a single, scalar,
absolute Effort recommendation (\code{EffType = "Abs"}) for a complex with
more than one fleet. Also accepts \link[=hist-class]{hist} and \link[=mse-class]{mse} objects,
extracting the embedded \code{OM} slot transparently.
}
\details{
A scalar \code{Advice@Effort} value is interpreted differently depending on
\code{EffType}:
\itemize{
\item \code{EffType = "Rel"} (the default): the scalar is a fraction of the last
historical year's effort, applied per fleet to that fleet's own
historical level. Each fleet's share of total effort is preserved
automatically, with no allocation step needed.
\item \code{EffType = "Abs"}: the scalar is a single total effort value to be
split across fleets. \code{EffortAllocation} supplies that split: for a
complex with \code{nFleet > 1}, the total is multiplied by
\code{EffortAllocation[[complex]][sim, ]} to get each fleet's absolute
effort.
}

\code{EffortAllocation} is only consulted for this scalar-plus-\code{"Abs"} case.
It is ignored for scalar \code{"Rel"} effort, a length-\code{nFleet} effort vector
(already fleet-specific), a fleet x area effort matrix (already
fleet-specific), and any complex with only one fleet.

If unspecified for a complex, \code{EffortAllocation} falls back to the mean relative effort
by fleet over the last five historical years, i.e. absolute effort is
split in the same proportions the fleets have historically fished in.
}
\examples{
om <- OM(nSim = 10)
EffortAllocation(om) <- list(Stock1 = matrix(c(0.3, 0.7), 10, 2, byrow = TRUE))
EffortAllocation(om)

}
\seealso{
\itemize{
\item \code{\link[=OM]{OM()}} for the constructor, \link[=om-class]{om} for the class definition.
\item \code{\link[=Advice]{Advice()}} for the interpretation of scalar Effort advice.
\item \code{\link[=FleetAllocation]{FleetAllocation()}} for the equivalent split applied to TAC.
\item \link{OM-accessors} for the remaining \code{OM} slot accessors.
}

Other om:
\code{\link[=CatchFrac]{CatchFrac()}},
\code{\link{ComplexOM}},
\code{\link[=DataLag]{DataLag()}},
\code{\link[=Herm]{Herm()}},
\code{\link{HermOM}},
\code{\link[=HistoricalWeight]{HistoricalWeight()}},
\code{\link[=InterimAdvice]{InterimAdvice()}},
\code{\link{MultiStockOM}},
\code{\link[=OM]{OM()}},
\code{\link{OM-accessors}},
\code{\link[=RefSeason]{RefSeason()}},
\code{\link{SeasonalSpatialOM}},
\code{\link{SingleStockOM}},
\code{\link{TwoFleetOM}},
\code{\link{om-class}},
\code{\link{stocktransition-class}}
}
\concept{om}
