% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-om.R
\name{HistoricalWeight}
\alias{HistoricalWeight}
\alias{HistoricalWeight<-}
\alias{SeasonalAllocation}
\alias{SeasonalAllocation<-}
\title{Seasonal Allocation of TAC/Effort for OM Objects}
\usage{
HistoricalWeight(x)

HistoricalWeight(x) <- value

SeasonalAllocation(x)

SeasonalAllocation(x) <- value
}
\arguments{
\item{x}{An \link[=om-class]{om}, \link[=hist-class]{hist}, or \link[=mse-class]{mse} object.}

\item{value}{Replacement value; a named list of length 0 or the number of
stock complexes. For \code{HistoricalWeight}, each element is a named numeric
vector over fleet names with values in \verb{[0,1]} (or a single unnamed
value applied to every fleet). For \code{SeasonalAllocation}, each element is
an \code{nSim} (or 1, recycled) by \code{Seasons} by \code{nFleet} array, each
\verb{[sim, , fleet]} slice summing to 1. \code{NULL} clears it.}
}
\value{
\itemize{
\item \code{HistoricalWeight()}/\code{SeasonalAllocation()} return the value of the
corresponding slot.
\item \verb{HistoricalWeight<-()}/\verb{SeasonalAllocation<-()} return \code{x} with the
corresponding slot updated.
}
}
\description{
Access or replace the \code{HistoricalWeight} and \code{SeasonalAllocation} slots of
an \link[=om-class]{om} object. Only relevant for seasonal OMs (\code{Seasons > 1}):
together they control how a periodically-set TAC or Effort (an MP is not
necessarily called every season - see \code{\link[=Interval]{Interval()}}) is split across the
seasons of the interval it covers. Also accepts \link[=hist-class]{hist} and
\link[=mse-class]{mse} objects, extracting the embedded \code{OM} slot transparently.
}
\details{
For each stock complex, \code{SeasonalAllocation} is resolved in this order:
\enumerate{
\item Use \code{SeasonalAllocation} if specified.
\item Derive it from \code{HistoricalWeight}, blending, per fleet, the historical
seasonal share of removals with the seasonal share of population
biomass:
\code{HistoricalWeight * historical_share + (1 - HistoricalWeight) * biomass_share},
pooled over the last \code{OM@Control$SeasonalAllocationYears} years
(default \code{5}).
}

\code{HistoricalWeight = 1} (the default when unspecified) uses the historical
removals pattern only; \code{HistoricalWeight = 0} uses the population biomass
pattern only.
}
\examples{
om <- OM(Seasons = 4)
HistoricalWeight(om) <- list(Stock1 = c(F1 = 0.8, F2 = 1))
HistoricalWeight(om)

}
\seealso{
\itemize{
\item \code{\link[=OM]{OM()}} for the constructor, \link[=om-class]{om} for the class definition.
\item \code{\link[=FleetAllocation]{FleetAllocation()}} for how a stock/complex TAC is split among fleets.
\item \code{\link[=Interval]{Interval()}} for the management update frequency that determines how
many seasons a single TAC/Effort value covers.
\item \link{OM-accessors} for the remaining \code{OM} slot accessors.
}

Other om:
\code{\link[=CatchFrac]{CatchFrac()}},
\code{\link{ComplexOM}},
\code{\link[=DataLag]{DataLag()}},
\code{\link[=EffortAllocation]{EffortAllocation()}},
\code{\link[=Herm]{Herm()}},
\code{\link{HermOM}},
\code{\link[=InterimAdvice]{InterimAdvice()}},
\code{\link{MultiStockOM}},
\code{\link[=OM]{OM()}},
\code{\link{OM-accessors}},
\code{\link[=RefSeason]{RefSeason()}},
\code{\link{SeasonalSpatialOM}},
\code{\link{SingleStockOM}},
\code{\link{TwoFleetOM}},
\code{\link{om-class}},
\code{\link{stocktransition-class}}
}
\concept{om}
