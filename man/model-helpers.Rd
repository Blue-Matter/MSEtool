% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{model-helpers}
\alias{model-helpers}
\alias{allometric}
\alias{dnormal}
\alias{double_normal}
\alias{logistic_50_95}
\title{Selectivity, Retention, Weight, Fecundity, and Maturity Model Helpers}
\usage{
allometric(x, scale, exponent)

dnormal(lens, lfs, sl, sr)

double_normal(x, x5, xF, xMax)

logistic_50_95(x, x50, x50_95, asymp = 1)
}
\arguments{
\item{x}{Numeric vector of values (ages, lengths, or weights) at which to
evaluate the curve.}

\item{scale}{Multiplicative scale parameter (\code{allometric}).}

\item{exponent}{Power exponent (\code{allometric}).}

\item{lens}{Numeric vector of lengths (\code{dnormal}).}

\item{lfs}{Length at full selection, the peak of the \code{dnormal} curve.}

\item{sl}{Standard deviation of the ascending limb of \code{dnormal}.}

\item{sr}{Standard deviation of the descending limb of \code{dnormal}. Use
\code{Inf} for a flat-topped (one-sided) curve.}

\item{x5}{Value of \code{x} at 5\% of the maximum (ascending limb of
\code{double_normal}).}

\item{xF}{Value of \code{x} at the peak of the \code{double_normal} curve.}

\item{xMax}{Maximum value on the descending limb of \code{double_normal},
expressed as a proportion between 0 and 1.}

\item{x50}{Value of \code{x} at 50\% of the asymptote (\code{logistic_50_95}).}

\item{x50_95}{Interval between the 50\% and 95\% points (\code{logistic_50_95}).}

\item{asymp}{Asymptote of the logistic curve. Defaults to \code{1}.}
}
\value{
Each function returns a numeric vector of the same length as the primary
input (\code{x} or \code{lens}).
}
\description{
Low-level curve functions used internally by
\code{\link[=FecundityModels]{FecundityModels()}}, \code{\link[=MaturityModels]{MaturityModels()}}, \code{\link[=RetentionModels]{RetentionModels()}},
\code{\link[=SelectivityModels]{SelectivityModels()}}, and \code{\link[=WeightModels]{WeightModels()}}.
}
\details{
\strong{\code{allometric(x, scale, exponent)}}

Returns \code{scale * x ^ exponent}. Suitable for length-weight and
length-fecundity relationships.

\strong{\code{dnormal(lens, lfs, sl, sr)}}

Double-normal (dome-shaped) selectivity curve evaluated at \code{lens}.
The ascending limb uses standard deviation \code{sl} and the descending limb
uses \code{sr}. Set \code{sr = Inf} for a flat-topped curve.

\deqn{S(l) = 2^{-\left(\frac{l - l_{fs}}{s}\right)^2}}

where \eqn{s = sl} if \eqn{l \le l_{fs}} and \eqn{s = sr} otherwise.

\strong{\code{double_normal(x, x5, xF, xMax)}}

Parameterises and evaluates \code{dnormal} from three inputs: the
5\% ascending point \code{x5}, the peak \code{xF}, and the descending plateau
\code{xMax}. Returns a flat vector of ones if both \code{x5} and \code{xF} are zero.

\strong{\code{logistic_50_95(x, x50, x50_95, asymp)}}

Standard logistic curve parameterised by the 50\% point and the
50-to-95\% interval:

\deqn{S(x) = \frac{a}{1 + \exp\!\left(-\frac{\ln 19}{x_{50,95}}(x - x_{50})\right)}}

where \eqn{a} is \code{asymp}.
}
\seealso{
\code{\link[=FecundityModels]{FecundityModels()}}, \code{\link[=MaturityModels]{MaturityModels()}}, \code{\link[=RetentionModels]{RetentionModels()}},
\code{\link[=SelectivityModels]{SelectivityModels()}}, \code{\link[=WeightModels]{WeightModels()}}
}
