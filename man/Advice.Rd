% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-advice.R
\name{Advice}
\alias{Advice}
\title{Management Advice Constructor and Accessors}
\usage{
Advice(
  TAC = NULL,
  TACType = "Removals",
  TACUnit = "Biomass",
  Effort = NULL,
  EffType = "Rel",
  Closure = NULL,
  Selectivity = NULL,
  Retention = NULL,
  DiscardMortality = NULL,
  ApicalF = NULL,
  Limit = NULL,
  LimitType = "angler",
  ClosureMode = "discard",
  Misc = list()
)
}
\arguments{
\item{TAC}{Numeric vector or matrix specifying total allowable catch.
See \code{Details}}

\item{TACType}{Character. Does the TAC refer to \code{"Removals"} (default) or
\code{"Landings"}. Either length 1 (applied to all fleets) or a character
vector of length \code{nFleet}.}

\item{TACUnit}{Character. Units in which the TAC is expressed: \code{"Biomass"}
(default) or \code{"Number"}. Either length 1 (applied to all fleets) or a
character vector of length \code{nFleet}.}

\item{Effort}{Numeric vector or matrix specifying relative or absolute
fishing effort. See \code{Details}}

\item{EffType}{Character. Effort interpretation: \code{"Rel"} for relative to
the last historical year, or \code{"Abs"} for absolute effort units (in units
of \code{\link[=Effort]{Effort()}}). Either length 1 (applied to all fleets) or a character
vector of length \code{nFleet}. Default is \code{"Rel"}.}

\item{Closure}{Numeric vector or array specifying spatial closures. See \code{Details}}

\item{Selectivity}{A \code{\link[=Selectivity]{Selectivity()}} object or an \code{nFleet}-length list of such objects. See \code{Details}}

\item{Retention}{A \code{\link[=Retention]{Retention()}} object or an \code{nFleet}-length list of such objects. See \code{Details}}

\item{DiscardMortality}{A \code{\link[=DiscardMortality]{DiscardMortality()}} object or an \code{nFleet}-length list
of such objects. See \code{Details}}

\item{ApicalF}{Numeric array specifying target apical fishing mortality. Not currently used}

\item{LimitType}{Character or \code{NULL}. Whether \code{BagLimit} is a per-angler or
per-vessel regulation. Either length 1 (applied to all fleets) or a
character vector of length \code{nFleet}. Valid values:
\itemize{
\item \code{"angler"} (default): limit is per angler per trip; the fleet-level
retention cap is \eqn{B_f \cdot A_f(t) \cdot T_f(t)}, where
\eqn{A_f(t)} is mean anglers per trip (\code{\link[=AnglerPerTrip]{AnglerPerTrip()}}) and
\eqn{T_f(t)} is the number of trips (\code{\link[=TripsScalar]{TripsScalar()}}).
\item \code{"boat"}: limit is per vessel per trip; the fleet-level retention cap
is \eqn{B_f \cdot T_f(t)}, and \code{\link[=AnglerPerTrip]{AnglerPerTrip()}} is not used.
}}

\item{ClosureMode}{Character or \code{NULL}. Determines how the OM handles catch
that exceeds the bag limit. Either length 1 (applied to all fleets) or a
character vector of length \code{nFleet}. Valid values:
\itemize{
\item \code{"discard"} (default): catch exceeding the bag limit is converted to
discards; discard mortality is applied via the fleet's
\code{\link[=DiscardMortality]{DiscardMortality()}} object.
\item \code{"stop"}: effort is reduced so that projected catch does not exceed the
bag limit retention cap; no additional discards are generated beyond the
baseline discard rate from non-retention of undersized fish.
}}

\item{Misc}{Miscellaneous list. Will be passed to \code{Data@Misc} in following
time steps.}

\item{BagLimit}{Numeric vector or \code{NULL}. Aggregate bag limit in fish per
angler per trip (when \code{LimitType = "angler"}) or fish per vessel per
trip (when \code{LimitType = "boat"}). Accepted forms:
\itemize{
\item \code{NULL} (default): no bag limit regulation is active for any fleet.
\item Single numeric value: the same limit applied to all fleets.
\item Numeric vector of length \code{nFleet}: fleet-specific bag limits.
}

The bag limit regulation persists across time steps until a new
\code{\link[=Advice]{Advice()}} object is returned by the MP. See Details.}
}
\value{
An \link[=advice-class]{advice} object.
}
\description{
Construct an \link[=advice-class]{advice} object defining management advice returned by a
management procedure.
}
\details{
\code{Advice()} constructs a container for management regulations returned by a
management procedure. All slots are optional; any regulation not supplied
will remain unchanged from the previous time step.

The management regulations in an \code{Advice} object will be applied in the
year/time-step that the \code{Advice} object is produced (i.e., when the MP is run)
and will apply to all future time-steps until a new \code{Advice} object is
returned by an MP.

The \code{Closure}, \code{Selectivity}, \code{Retention}, and \code{DiscardMortality} regulations
(if any) are applied first before calculating the fishing mortality corresponding
with \code{TAC}.

If both \code{TAC} and \code{Effort} regulations are returned, the expected
fishing effort will be calculated to achieve a catch
equal to the \code{TAC}. If the expected effort is greater than that set in
\code{Effort}, the catch will be constrained to that corresponding with \code{Effort}.
Otherwise, the actual fishing effort will be lower than \code{Effort}.
\subsection{Valid Configurations for Management Regulations}{

There are several options for populating the slots in an \code{Advice} object:
}

\subsection{TAC}{

Total allowable catch in units of \code{"Biomass"} (default) or \code{"Number"}
(see \code{TACUnit}). \code{TACType} and \code{TACUnit} are each either length 1
(applied uniformly across all fleets) or a character vector of length
\code{nFleet} specifying per-fleet values.
\itemize{
\item single numeric value: global TAC distributed across
the stocks and fleets in the \code{\link[=Data]{Data()}} object according to \code{Allocation(OM)}.
\item numeric vector length \code{nFleet}: fleet-specific TAC.
}
}

\subsection{Effort}{

Either in absolute units corresponding with fleet-specific effort
(\code{EffType = "Abs"}), or relative to the effort in the last historical year
(\code{EffType = "Rel"}; default). \code{EffType} is either length 1 (applied
uniformly across all fleets) or a character vector of length \code{nFleet}
specifying per-fleet values.

Note that when \code{Effort} is supplied as a matrix (fleet × area), it is
always treated as absolute regardless of \code{EffType}.
\itemize{
\item single numeric value:
\itemize{
\item If \code{EffType}=\code{"Rel"}: Effort relative to last historical year, applied to all
fleets. E.g., if \code{Effort=0.5}, effort for all fleets will be set to half
the effort in the last historical year.
\item If \code{EffType}=\code{"Abs"}: the total effort (summed over fleets unless provide
as a length \code{nFleet} vector); Note that in this case all fleets in the OM must
have the same units for \code{Effort}).
}
\item numeric vector length \code{nFleet}: Fleet-specific relative or absolute Effort.
\item numeric matrix: Fleet- and Area-specific relative or absolute Effort.
Must have \code{nFleet} rows and \code{nArea} columns.
}
}

\subsection{Closure}{

Can only be \code{0} (closed) or \code{1} (open). Otherwise will be converted to
\code{0} (\code{Closure}<0.5) or \code{1}
\itemize{
\item vector length \code{nArea}
\item matrix with \code{nFleet} rows and \code{nArea} columns: fleet-specific closures.
}
}

\subsection{Selectivity}{

Either a \code{\link[=Selectivity]{Selectivity()}} object, or a list length \code{nFleet} of
\code{Selectivity} objects.

If a single \code{Selectivity} object, the prescribed selectivity schedule
is applied to all fleets.

See section below for more details.
}

\subsection{Retention}{

Same as described for \code{Selectivity}, but for \code{\link[=Retention]{Retention()}} objects.
}

\subsection{Discard Mortality}{

Same as described for \code{Selectivity}, but for \code{\link[=DiscardMortality]{DiscardMortality()}} objects.
}

\subsection{Bag Limit}{

\code{BagLimit}, \code{LimitType}, and \code{ClosureMode} together define a bag-limit
regulation. The bag limit caps the number of fish an angler (or vessel)
may retain per trip for either a single species or across all species
within a complex.

When \code{ClosureMode = "discard"}, catch exceeding the retention cap is
converted to discards and discard mortality is applied via the fleet's
\code{\link[=DiscardMortality]{DiscardMortality()}} object. When \code{ClosureMode = "stop"}, effort is
reduced to prevent exceedance and no additional discards are generated.
}

\subsection{ApicalF}{

Not currently used.
}

\subsection{Misc}{

A list of miscellaneous information that needs to be stored and accessed by
the MP in future timesteps. Contents of \code{Advice@Misc} will be available in
the \code{Data@Misc} slot in subsequent time steps.
}

\subsection{Populating Selectivity, Retention, and Discard Mortality Objects}{

The \code{Selectivity}, \code{Retention}, and \code{DiscardMortality} objects are used to
set the selectivity-, retention-, and discard mortality- at-age or -at-size
schedules.

If values are provided for these slots in the \code{Advice} object,
the new regulations/specifications will apply to all future time steps
until modified again by the \code{MP}.

Values can be set for these objects in the following ways:
\itemize{
\item \code{MeanAtAge}:
\itemize{
\item A numeric vector length \code{nAge}, where \code{nAge} is the number of
age classes for the stock (or stocks) that are being managed by the \code{MP}.
\item For area-specific schedules, a numeric matrix with \code{nAge} rows and
\code{nArea} columns.
}
\item \code{MeanAtLength}:
\itemize{
\item A numeric vector length \code{nClass}, where \code{nClass} is the number of
length classes for the stock (or stocks) that are being managed by the \code{MP}.
The \code{Classes} slot can be used to set the size classes corresponding with
\code{MeanAtLength}. Otherwise, the classes will be taken
from the corresponding \code{Length} object in the \code{OM}(if they exist, otherwise
an error).
\item For area-specific schedules, a numeric matrix with \code{nClass} rows and
\code{nArea} columns.
}
\item \code{MeanAtWeight}:
\itemize{
\item A numeric vector length \code{nClass}, where \code{nClass} is the number of
weight classes for the stock (or stocks) that are being managed by the \code{MP}.
The \code{Classes} slot can be used to set the size classes corresponding with
\code{MeanAtWeight}. Otherwise, the classes will be taken
from the corresponding \code{Weight} object in the \code{OM} (if they exist, otherwise
an error).
\item For area-specific schedules, a numeric matrix with \code{nClass} rows and
\code{nArea} columns.
}
\item \code{Pars} and \code{Model}: for \code{Selectivity} and \code{Retention} only.
}

These slots can be used to set the age- or size-schedules using parameters and
a model. See \code{\link[=SelectivityModels]{SelectivityModels()}} and \code{\link[=RetentionModels]{RetentionModels()}} for a details
of built-in models.

\code{Pars} should be a named list of parameters, where each parameter can be either
a single numeric value, or a numeric vector length \code{nArea} for area-specific schedules.
}
}
\examples{
Advice(TAC = 1000)

Advice(
  Effort = c(1, 0.8),
  EffType = "Rel"
)
# Bag limit: 5 fish per angler per trip, discard excess
Advice(
  Limit       = 5,
  LimitType   = "angler",
  ClosureMode = "discard"
)

# Fleet-specific bag limits
Advice(
  Limit       = c(5, 10),
  LimitType   = "angler",
  ClosureMode = "discard"
)

}
\seealso{
\itemize{
\item \link[=advice-class]{advice} for the class definition and slot-level documentation.
\item \code{\link[=Selectivity]{Selectivity()}}, \code{\link[=Retention]{Retention()}}, \code{\link[=DiscardMortality]{DiscardMortality()}} for gear regulation
sub-objects.
\item \code{\link[=TripsScalar]{TripsScalar()}}, \code{\link[=AnglerPerTrip]{AnglerPerTrip()}} for the effort-to-trips and
angler-scaling parameters consumed by the bag limit model.
\item \code{\link[=Theta]{Theta()}} for the within-trip catch overdispersion parameter.
}
}
