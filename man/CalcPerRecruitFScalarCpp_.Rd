% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{CalcPerRecruitFScalarCpp_}
\alias{CalcPerRecruitFScalarCpp_}
\title{Internal C++ per-recruit calculation (annual/non-seasonal, parallel implementation)}
\usage{
CalcPerRecruitFScalarCpp_(
  apicalF,
  StockFleetAllocation,
  NaturalMortalityList,
  PlusGroupVec,
  MaturityList,
  SemelparousList,
  WeightList,
  SpawnTimeFracVec,
  FecundityList,
  WeightFleetRetainedList,
  WeightFleetSelectedList,
  SelectivityFleetList,
  RetentionFleetList,
  DiscardMortalityFleetList,
  NPR0List,
  NPR0_SPList,
  IsSpawnTimeFrac
)
}
\arguments{
\item{apicalF}{Scalar apical fishing mortality.}

\item{StockFleetAllocation}{nStock x nFleet matrix -- fraction of apicalF
allocated to each fleet, per stock (constant across age).}

\item{NaturalMortalityList, MaturityList, SemelparousList, WeightList, FecundityList, NPR0List, NPR0_SPList}{Named lists (one element per stock) of per-age \code{NumericVector}s.}

\item{PlusGroupVec, SpawnTimeFracVec}{Per-stock scalars (length nStock).}

\item{WeightFleetRetainedList, WeightFleetSelectedList, SelectivityFleetList, RetentionFleetList, DiscardMortalityFleetList}{Named lists (one element per stock) of nAge x nFleet \code{NumericMatrix}.}

\item{IsSpawnTimeFrac}{Logical. Whether any stock has a non-zero spawn-time fraction.}
}
\value{
A named \code{List} of per-stock (length nStock) \code{NumericVector}s:
NPR0, NPRF, NPR0_SP, NPRF_SP (all summed-over-age, matching what
\code{.CalcPerRecruitFScalar()} stores in the \code{perrecruit} slots of the same
name), SPRF, Biomass, SBiomass, SProduction, Landings, Discards, Removals.
}
\description{
Computes the apicalF-dependent per-recruit quantities for one sim/year,
mirroring \code{.CalcPerRecruitFScalar()}
(\code{R/calc-per-recruit.R}).
}
