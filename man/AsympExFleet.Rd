% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-fleet.R
\docType{data}
\name{AsympExFleet}
\alias{AsympExFleet}
\title{Asymptotic Selectivity Example Fleet}
\format{
A \link[=fleet-class]{fleet} object with the following slots populated:
\itemize{
\item \code{Name}: \code{"AsympExFleet"}. Unique fleet identifier.
\item \code{Effort}: An \code{\link[=Effort]{Effort()}} object with a piecewise-linear historical effort
trajectory defined at four relative time points (0, 0.3, 0.6, 1.0),
with lower and upper bounds of (0, 0.4, 1, 1) and (0, 0.6, 1, 1)
respectively, and \code{CV = 0.1} controlling stochastic variation around
the trend.
\item \code{Selectivity}: A \code{\link[=Selectivity]{Selectivity()}} object with asymptotic length-based
selectivity parameters — \code{L5} (0.4–0.5), \code{LFS} (0.7–0.8), and
\code{Vmaxlen = 1} (fixed) — with \code{isRel = TRUE} indicating that \code{L5} and
\code{LFS} are expressed relative to maturity \code{L50}.
\item \code{Catchability}: An empty \code{\link[=Catchability]{Catchability()}} object; efficiency defaults to
1 across all years during population (see Details).
\item \code{Retention}: An empty \code{\link[=Retention]{Retention()}} object; full retention is assumed
during population (see Details).
\item \code{DiscardMortality}: An empty \code{\link[=DiscardMortality]{DiscardMortality()}} object.
\item \code{Closure}: \code{NULL}. No spatial or temporal closures defined.
\item \code{WeightFleetRetained}, \code{WeightFleetSelected}: \code{NA}. Computed from
selectivity, retention, and the stock's weight-at-length during
population (see Details).
\item \code{Bioeconomic}: An empty \code{\link[=Bioeconomic]{Bioeconomic()}} object.
\item \code{nYear}, \code{pYear}, \code{nSim}, \code{CurrentYear}, \code{Years}, \code{Seasons}: Inherited
from the paired \code{\link[=Stock]{Stock()}} object by \code{\link[=PopulateFleet]{PopulateFleet()}}.
\item \code{Misc}: Empty list for user-defined information.
}
}
\usage{
AsympExFleet
}
\description{
An example \link[=fleet-class]{fleet} object demonstrating a fleet with effort that
stabilises early in the historical period and asymptotic length-based
selectivity. Intended for use with \link{AlbacoreExStock} or \link{ButterfishExStock}
to illustrate fleet parameterization in \code{MSEtool}. See also \link{DomeExFleet}
for a contrasting dome-shaped selectivity example.
}
\details{
\code{\link[=PopulateFleet]{PopulateFleet()}} expands the input parameters into \verb{[nSim x nAge x nYear]}
arrays using the age, length, and spatial structure of the paired populated
stock. The sections below describe what each component-level \verb{Populate*()}
function produces.
\subsection{Effort}{

\code{\link[=PopulateEffort]{PopulateEffort()}} interpolates the piecewise-linear effort trajectory
across all historical years for each simulation, then applies log-normal
stochastic deviates scaled by \code{CV = 0.1}. The trajectory rises from 0 to
40–60\% of maximum effort by 30\% of the historical period, then reaches
full effort by 60\% of the period and remains stable thereafter. This
contrasts with \link{DomeExFleet}, where effort continues to rise through to
the final historical year.
}

\subsection{Catchability}{

Because the \code{Catchability} slot is empty, \code{\link[=PopulateCatchability]{PopulateCatchability()}} sets
catchability efficiency to 1 across all simulations and years. No trend
(\code{qInc}) or inter-annual variability (\code{qCV}) is applied.
}

\subsection{Selectivity}{

\code{\link[=PopulateSelectivity]{PopulateSelectivity()}} samples \code{L5} and \code{LFS} for each simulation from
uniform distributions defined by the prior bounds. Because \code{isRel = TRUE},
both are scaled by the maturity \code{L50} of the paired stock before computing
the selectivity curve. \code{Vmaxlen = 1} is fixed, so selectivity reaches 1.0
at maximum length in all simulations, producing a strictly asymptotic
curve. The resulting selectivity-at-length is converted to
selectivity-at-age using the stock age–length key, and an area dimension
is added.
}

\subsection{Retention}{

Because the \code{Retention} slot is empty, \code{\link[=PopulateRetention]{PopulateRetention()}} sets full
retention (1.0) across all ages, lengths, simulations, and years.
}

\subsection{Discard Mortality}{

Because the \code{DiscardMortality} slot is empty, \code{\link[=PopulateDiscardMortality]{PopulateDiscardMortality()}}
applies default discard mortality values. All discarded fish are assumed
to survive (discard mortality = 0) unless overridden.
}

\subsection{Spatial Closures}{

Because \code{Closure} is \code{NULL}, \code{\link[=PopulateClosure]{PopulateClosure()}} applies no spatial or
temporal closures; all areas are open to fishing in all years.
}

\subsection{Fleet Weight-at-Age}{

Because \code{WeightFleetRetained}/\code{WeightFleetSelected} are \code{NA},
\code{\link[=PopulateFleet]{PopulateFleet()}} computes them from selectivity, retention, and the
stock's weight-at-length, so fleet-level landings/discards biomass
calculations reflect the fleet's own selectivity and retention pattern
rather than the stock's unselected mean weight-at-age.
}
}
\examples{
AsympExFleet

\dontrun{
PopulatedStock <- PopulateStock(AlbacoreExStock, nYear = 50, pYear = 30, nSim = 48)
PopulatedFleet <- PopulateFleet(AsympExFleet, Stock = PopulatedStock, seed = 42)
}

}
\seealso{
\link{DomeExFleet}, \link{AlbacoreExStock}, \link{ButterfishExStock}, \code{\link[=Fleet]{Fleet()}},
\code{\link[=PopulateFleet]{PopulateFleet()}}, \code{\link[=Populate]{Populate()}}, \link[=fleet-class]{fleet}
}
\keyword{datasets}
