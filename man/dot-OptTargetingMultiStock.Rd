% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt-targeting-multi-stock.R
\name{.OptTargetingMultiStock}
\alias{.OptTargetingMultiStock}
\title{Estimate stock-specific targeting weights for a fixed fleet effort}
\usage{
.OptTargetingMultiStock(
  Proj,
  Year,
  TSIndex,
  sim,
  StockNames,
  FleetNames,
  Effort,
  lambda = NULL,
  n_recent = 5,
  minEffort = 1e-08,
  tol = 1e-06,
  maxEval = 500
)
}
\arguments{
\item{Proj}{A projection object containing effort, targeting
history, operating model settings, and stock/fleet dimensions.}

\item{Year}{Integer. The simulation year index passed to
\code{.CalcFisheryDynamics()}.}

\item{TSIndex}{Integer. Time-step index into \code{Proj} arrays for the
current year. \code{TSIndex - 1} is used to initialise the warm start.}

\item{sim}{Integer. Simulation replicate index.}

\item{StockNames}{Character vector of stock names.}

\item{FleetNames}{Character vector of fleet names.}

\item{Effort}{Numeric vector of length \code{nFleet}. Fixed fleet effort
for this year (from the MP's \code{Effort} advice) - not optimised.}

\item{lambda}{Numeric vector of length \code{nFleet} (or scalar, recycled),
or \code{NULL} (default \code{1} for all fleets). Ridge penalty weight controlling
resistance to year-to-year targeting changes - see
\code{.OptEffortMultiStock()}.}

\item{n_recent}{Integer (default \code{5}). Number of most-recent historical
years examined to determine which stocks each fleet actively targets.}

\item{minEffort}{Numeric (default \code{1e-8}). Floor applied to fleet effort
to avoid degenerate zero-effort fleets.}

\item{tol}{Numeric (default \code{1e-6}). Convergence tolerance passed
to the optimiser.}

\item{maxEval}{Integer (default \code{500}). Maximum function evaluations
allowed across the BFGS and Nelder-Mead phases combined.}
}
\value{
A named list with elements:
\describe{
\item{\code{Delta}}{Numeric matrix \verb{[nFleet x nStock]}. Final targeting
weights. Fleets/stocks not actively re-optimised retain their
previous value.}
\item{\code{converged}}{Logical. \code{TRUE} if the BFGS or Nelder-Mead optimiser
reported convergence.}
\item{\code{ActiveStock}}{Logical matrix \verb{[nFleet x nStock]}. Which stocks
are considered active for each fleet based on recent targeting
history.}
}
}
\description{
\code{.OptTargetingMultiStock()} is the effort-control counterpart to
\code{.OptEffortMultiStock()}. Under TAC management, fleet effort \emph{and}
stock-specific targeting weights are solved jointly so each fleet catches
its TAC. Under effort management there is no TAC to solve for - fleet
effort is instead fixed by the MP's \code{Effort} advice - but a fleet fishing
several stocks still has to decide how to point that fixed effort across
them. This function solves for the targeting weights \verb{delta_\{s,f\}} that
maximise a concave transform of total catch (an interim stand-in for
catch \emph{value}, pending stock-specific bio-economic prices - see Details),
subject to the same soft ridge penalty on year-to-year targeting change
used by \code{.OptEffortMultiStock()}.
}
\details{
The catch objective for each active fleet is \code{sum(log1p(Catch_s))} over
the fleet's active stocks, where \code{Catch_s} is landings + discards
(biomass) for stock \code{s} at the trial targeting weights. \code{log1p} is a
concave transform: it gives diminishing weight to additional catch of an
already-abundant stock, which discourages the optimiser from collapsing
onto whichever single stock is most catchable.
}
\seealso{
\code{.OptEffortMultiStock()} for the TAC-based counterpart this
function mirrors.
}
\keyword{internal}
