% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-pm.R
\name{NewPM}
\alias{NewPM}
\title{Create a Custom Performance Metric}
\usage{
NewPM(df, Name, Caption = Name, Ref = NULL, Op = NULL, Years = NULL, OM = NULL)
}
\arguments{
\item{df}{A data frame with columns \code{Sim}, \code{Stock}, \code{Year}, \code{MP}, and
\code{Value} (and optionally \code{Period}; only \code{'Projection'} rows are used).}

\item{Name}{Character. Short name of the metric.}

\item{Caption}{Character. Description of the metric. Default \code{Name}.}

\item{Ref}{\code{NULL} (default) or the reference value compared with \code{Value}
by \code{Op}.}

\item{Op}{\code{NULL} (default) or a comparison function, e.g. \code{`>`}. When
given, \code{Prob} is, for each simulation, the proportion of years in which
\code{Op(Value, Ref)} is \code{TRUE}, and \code{Mean} is \code{Prob} averaged over
simulations; otherwise \code{Prob} is \code{NA} and \code{Mean} is \code{Stat} (the
per-simulation mean \code{Value}) averaged over simulations.}

\item{Years}{\code{NULL} (default, all projection years) or the years to include.}

\item{OM}{\code{NULL}, or the \link[=om-class]{om} object, to exclude interim years
before \code{OM@MPStartYear}.}
}
\value{
A \link[=pm-class]{pm} object.
}
\description{
Builds a \link[=pm-class]{pm} object from a data frame of per-simulation, per-year
values, so any metric can be used as a compatible performance metric (PM), e.g.
as the objective or a constraint in \code{\link[=TuneMP]{TuneMP()}}.
}
\details{
A custom PM function takes an
\link[=mse-class]{mse} object, calculates the metric (e.g. from \code{\link[=Landings]{Landings()}} or
\code{\link[=SB_SBMSY]{SB_SBMSY()}} with \code{df = TRUE}), and returns \code{NewPM(...)}.
}
\examples{
\dontrun{
PM_MinSB <- function(object, Years = NULL) {
  df <- SB_SBMSY(object, df = TRUE, Reduce = FALSE)
  df <- df[df$Period == 'Projection', ]
  df <- dplyr::summarise(dplyr::group_by(df, Sim, Stock, MP),
                         Value = min(Value), Year = max(Year), .groups = 'drop')
  NewPM(df, Name = 'MinSB', Caption = 'P(min SB/SBMSY > 0.5)', Ref = 0.5, Op = `>`)
}
class(PM_MinSB) <- 'pm'
}

}
\seealso{
\link{PM}, \code{\link[=TuneMP]{TuneMP()}}
}
