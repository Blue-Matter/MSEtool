% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util-dimension.R
\name{dimension-utils}
\alias{dimension-utils}
\alias{AddDimension}
\alias{DropDimension}
\title{Add or Drop Named Dimensions of an Array}
\usage{
AddDimension(array, name, val = 1, pos = NULL)

DropDimension(array, name, warn = TRUE)
}
\arguments{
\item{array}{An array with named dimensions}

\item{name}{Character vector of dimension names to add or drop.
For \code{AddDimension}, a single name.
For \code{DropDimension}, one or more existing dimension names.}

\item{val}{Value to assign to the new dimension name in \code{AddDimension}.}

\item{pos}{Integer giving the position at which to insert the new dimension
in \code{AddDimension}. Defaults to 1.}

\item{warn}{Logical; if \code{TRUE}, warn when dropping a dimension of length
greater than one in \code{DropDimension}.}
}
\value{
An array with modified dimensions and updated \code{dimnames}.
}
\description{
Utilities to add or drop named dimensions from arrays
}
\details{
\strong{AddDimension}

Adds a new dimension of length \code{val} to an array. The new dimension is inserted
at position \code{pos} and optionally given a name and value. If the dimension
name already exists, the array is returned unchanged.

\strong{DropDimension}

Drops one or more named dimensions from an array.
\itemize{
\item All requested dimensions must exist
\item At least one dimension must remain
\item Dimensions of length one are removed by reshaping only
\item Dimensions of length greater than one are sliced at their first index,
with an optional warning
}

Other dimensions are preserved.
}
\examples{

array <- array(1:6, dim=c(1,2,3),
               dimnames = list(
                 Sim=1,
                 Age=0:1,
                 Year=2023:2025
               ))

AddDimension(array, c('Test', 'best'),pos=4)


DropDimension(array, 'Sim')
DropDimension(array, c('Sim', 'Age'))
DropDimension(array, c('Age', 'Year'), warn=FALSE)
}
