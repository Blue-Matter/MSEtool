% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-data.R
\name{IndicesData}
\alias{IndicesData}
\title{IndicesData Constructor}
\usage{
IndicesData(
  Name = NULL,
  Value = NULL,
  CV = NULL,
  Units = NULL,
  Ref = NULL,
  RefCV = NULL,
  Timing = 0,
  Selectivity = NULL,
  Misc = list()
)
}
\arguments{
\item{Name}{\code{character} or \code{NULL}. Name of the index. Default \code{NULL}.}

\item{Value}{\code{array} or \code{NULL}. Observed index values with dimensions
\verb{[nYear x nIndex]}. Default \code{NULL}.}

\item{CV}{\code{array} or \code{NULL}. Coefficients of variation for the index
observations, matching the dimensions of \code{Value}. Default \code{NULL}.}

\item{Units}{\code{character} or \code{NULL}. Units of the index (e.g., \code{"kg/trip"},
\code{"numbers/tow"}). Default \code{NULL}.}

\item{Ref}{\code{numeric} or \code{NULL}. Reference value for each index (e.g., a
historical mean or target level), length \code{nIndex}. Default \code{NULL}.}

\item{RefCV}{\code{array} or \code{NULL}. Coefficients of variation for the reference
values, matching the dimensions of \code{Ref}. Default \code{NULL}.}

\item{Timing}{\code{numeric}. Within-year timing of each observation as a
fraction of the year (0-1), length \code{nIndex}. Default \code{0}.}

\item{Selectivity}{An array or character specification mapping each index
to a fleet selectivity or defining an independent selectivity curve, or
\code{NULL}. Default \code{NULL}.}

\item{Misc}{\code{list}. Named list for additional metadata. Default \code{list()}.}
}
\value{
An \link[=indicesdata-class]{indicesdata} object.
}
\description{
Construct an \link[=indicesdata-class]{indicesdata} object storing an abundance or biomass
index and associated uncertainty.
}
\details{
\subsection{Usage}{

All arguments are optional; an empty object is valid and slots are
populated later (e.g., by \code{.GenHistDataIndices()}).
}

\subsection{Attaching to a Data Object}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{CPUE(data)   <- IndicesData(Value = myArray, CV = myCVArray)
Survey(data) <- IndicesData(Value = myArray, CV = myCVArray)
}\if{html}{\out{</div>}}
}
}
\examples{
# Empty object
id <- IndicesData()

# With values
yrs <- 2000:2020
val <- array(runif(length(yrs)),
             dim = c(length(yrs), 1),
             dimnames = list(Year = yrs, Index = "Survey1"))
id <- IndicesData(Name  = "Survey1",
                  Value = val,
                  CV    = array(0.2, dim = dim(val), dimnames = dimnames(val)),
                  Units = "kg/tow")

}
\seealso{
\itemize{
\item \link[=indicesdata-class]{indicesdata} for the class definition.
\item \link[=data-class]{data} and \code{\link[=Data]{Data()}} for the enclosing data object.
\item \code{\link[=IndicesObs]{IndicesObs()}} for the observation error structure that generates
\link[=indicesdata-class]{indicesdata} objects during simulation.
}

Other data:
\code{\link[=CatchData]{CatchData()}},
\code{\link[=CompData]{CompData()}},
\code{\link[=EffortData]{EffortData()}},
\code{\link{catchdata-class}},
\code{\link{compdata-class}},
\code{\link{effortdata-class}},
\code{\link{indicesdata-class}}
}
\concept{data}
