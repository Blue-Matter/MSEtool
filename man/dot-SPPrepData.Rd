% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-sp.R
\name{.SPPrepData}
\alias{.SPPrepData}
\title{Prepare Annual Catch and Index Data for a Surplus Production Model}
\usage{
.SPPrepData(
  Data,
  Indices,
  IndexSource,
  IndexFreq,
  IndexWeight,
  IndexSeasons,
  IndexUnits,
  CatchType,
  FitYears
)
}
\arguments{
\item{Data}{A \link[=data-class]{data} object.}

\item{Indices, IndexSource, IndexFreq, IndexWeight, IndexSeasons}{Index
selection, as in \code{\link[=IndexRate]{IndexRate()}}. \code{IndexWeight} is normalised to a mean of
\code{1}.}

\item{IndexUnits}{Character vector of index \code{Units} to use: one or both of
\code{'Biomass'} and \code{'Number'}. Indices with other units (including
\code{'Recruitment'}) are excluded. Indices with no units are assumed to be
in biomass.}

\item{CatchType}{Character. The catch the model is fitted to:
\code{'Removals'} (landings plus discards, default) or \code{'Landings'}.}

\item{FitYears}{\code{NULL} (default) fits from the first year with catch data
to the last data year. Otherwise a single number (fit to that many most
recent years) or a vector of consecutive years. Missing catch after the
first fitted year is set to \code{0}.}
}
\value{
A list with \code{Years} (fitted years), \code{Catch} (fitted catch),
\code{Landings}, \code{Discards} (annual totals over all years), \code{AllYears},
\code{Index}, \code{CV} (\verb{[nFitYear x nIndex]}), \code{Timing}, \code{Weight}, \code{Name},
\code{Units}, \code{YearLH}, \code{LastDataYear}, and \code{Log} (character vector of
assumptions).
}
\description{
Prepare Annual Catch and Index Data for a Surplus Production Model
}
\keyword{internal}
