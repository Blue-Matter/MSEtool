% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-sp.R
\name{SPControl}
\alias{SPControl}
\title{Numerical Settings for the Internal Surplus Production Model}
\usage{
SPControl(
  nSubStep = 4,
  nItF = 5,
  Fmax = 3,
  FPenalty = 1000,
  FoxTol = 0.001,
  CatchScale = c("mean", "none"),
  WarmStart = TRUE,
  nGrid = 12,
  GridFMSY = c(0.02, 1),
  GridMSY = c(0.2, 5),
  nRestart = 2,
  MinSD = 0.05,
  GradTol = 0.001,
  BoundTol = 0.01,
  Bounds = list(FMSY = c(0.005, 3), MSY = c(0.01, 100), Depletion = c(0.05, 1.5), Shape =
    c(0.2, 10)),
  nlminb = list(iter.max = 500, eval.max = 1000),
  HessianStep = 1e-05,
  Hessian = c("auto", "always", "never")
)
}
\arguments{
\item{nSubStep}{Positive integer. Sub-steps per year used to integrate the
biomass dynamics. Default \code{4}.}

\item{nItF}{Positive integer. Maximum Newton iterations used to solve the
annual fishing mortality that produces the observed catch. Default \code{5}.}

\item{Fmax}{Positive number. Maximum annual fishing mortality. Default \code{3}.}

\item{FPenalty}{Non-negative number. Weight of the penalty on the squared
log difference between observed and predicted catch, added to the
negative log-likelihood. The difference is non-zero when the catch cannot
be taken with \code{F <= Fmax}. Default \code{1000}.}

\item{FoxTol}{Positive number. \code{Shape} within \code{FoxTol} of \code{1} uses the Fox
model. Default \code{1e-3}.}

\item{CatchScale}{Character. \code{'mean'} (default) rescales catch to a mean of
\code{1} during fitting; \code{'none'} fits on the original scale.}

\item{WarmStart}{Logical. In \code{\link[=SurplusProduction]{SurplusProduction()}}, start each fit from
the estimates of the previous management cycle? Default \code{TRUE}.}

\item{nGrid}{Positive integer. Number of \code{FMSY} and \code{MSY} values in the
grid of starting values (\verb{nGrid x nGrid} cells), used when there is no
\code{Start} (or warm start) or the fit from it does not converge. The first
fit starts from the cell with the lowest negative log-likelihood.
Default \code{12}.}

\item{GridFMSY}{Numeric vector, length 2. Range of \code{FMSY} in the grid.
Default \code{c(0.02, 1)}.}

\item{GridMSY}{Numeric vector, length 2. Range of \code{MSY} in the grid, as
multiples of the mean catch. Default \code{c(0.2, 5)}.}

\item{nRestart}{Non-negative integer. Maximum number of additional fits,
started from the next-best grid cells, when the fit from the best cell
does not converge. Default \code{2}.}

\item{MinSD}{Positive number. Lower bound on an estimated index
observation standard deviation (\code{IndexSD = 'estimate'}). Default \code{0.05}.}

\item{GradTol}{Positive number. A fit is converged when, for each
estimated (log-scale) parameter, the gradient divided by the curvature of
the negative log-likelihood (the estimated distance to the optimum) is
below \code{GradTol}, and the curvature is positive. Default \code{1e-3}.}

\item{BoundTol}{Non-negative number. A fit with a log-scale parameter
within \code{BoundTol} of a bound is not converged. Default \code{0.01}.}

\item{Bounds}{Named list of \code{c(lower, upper)} bounds on the estimated
\code{FMSY}, \code{MSY} (as multiples of the mean catch), \code{Depletion}, and \code{Shape}.
Elements not given keep their defaults: \code{FMSY = c(0.005, 3)},
\code{MSY = c(0.01, 100)}, \code{Depletion = c(0.05, 1.5)}, \code{Shape = c(0.2, 10)}.}

\item{nlminb}{List of \code{control} settings passed to \code{\link[stats:nlminb]{stats::nlminb()}}.}

\item{HessianStep}{Positive number. Finite-difference step (on the log
scale of the parameters) used to calculate the Hessian; steps 10 and 100
times smaller are tried if the Hessian is not positive definite. Default
\code{1e-5}.}

\item{Hessian}{Character. When to calculate the Hessian used for the
standard errors: \code{'auto'} (default; when \code{Uncertainty = TRUE} in
\code{\link[=FitSP]{FitSP()}}, or when \code{\link[=SurplusProduction]{SurplusProduction()}} uses \code{FractileB}, \code{FractileF},
or \code{Diagnostics = 'full'}), \code{'always'}, or \code{'never'}.}
}
\value{
A list of class \code{spcontrol}.
}
\description{
Controls for \code{\link[=FitSP]{FitSP()}} and \code{\link[=SurplusProduction]{SurplusProduction()}} with
\code{Model = 'internal'}.
}
\seealso{
\code{\link[=FitSP]{FitSP()}}, \code{\link[=SurplusProduction]{SurplusProduction()}}, \code{\link[=SpictControl]{SpictControl()}}
}
