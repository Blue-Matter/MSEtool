% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-awk.R
\name{PlotAWK}
\alias{PlotAWK}
\title{Plot the Age-Weight Key}
\usage{
PlotAWK(
  object,
  Sim = NULL,
  byStock = NULL,
  Stocks = NULL,
  Years = NULL,
  units = TRUE
)
}
\arguments{
\item{object}{A \link[=stock-class]{stock}/\link[=om-class]{om}/\link[=hist-class]{hist}/\link[=mse-class]{mse} object.}

\item{Sim}{Integer or \code{NULL} (default). Which simulation replicate to
plot. \code{NULL} averages across all simulations.}

\item{byStock}{Unused (present for signature consistency with the other
\verb{Plot*()} functions).}

\item{Stocks}{Character or numeric vector. Restrict the plot to specific
stocks, either by name (matching \code{\link[=StockNames]{StockNames()}}) or by index. Default
\code{NULL} (all stocks).}

\item{Years}{Optional numeric vector, or \code{"all"}. Default \code{NULL}: the
last available calendar year only. \code{"all"} facets by every available
year.}

\item{units}{Logical. \code{TRUE} (default) labels the age/weight axes with
the stock's \code{Ages@Units}/\code{Weight@Units} when set and agrees across
every plotted stock. \code{FALSE} suppresses unit labeling.}
}
\value{
A \code{ggplot} object, or \code{NULL} invisibly if no stock has a
populated \code{AWK}.
}
\description{
Heatmap of a stock's age-weight key (\code{Weight@AWK}; see \code{\link[=AWK]{AWK()}}); the
probability of belonging to each weight class given age.
}
\details{
Only one calendar year is shown by default (the last available); pass
\code{Years} for more. When \code{object} has more than one simulation, the
probability shown at each age/weight cell is the mean across simulations
before that age's probabilities are rescaled (see below).

Color is rescaled within each age class (its own max -> darkest color,
\code{0} -> white); no color legend is shown.

When \code{object} has more than one stock, each gets its own panel
(\code{patchwork}-combined).

Returns \code{NULL} invisibly when a stock's \code{Weight@CVatAge} isn't set --
\code{AWK} is only populated when it is (see \code{\link[=Weight]{Weight()}}).
}
\seealso{
\code{\link[=PlotWeight]{PlotWeight()}}, \code{\link[=AWK]{AWK()}}, \code{\link[=CalcAgeSizeKey]{CalcAgeSizeKey()}}, \code{\link[=Weight]{Weight()}}
}
