% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-array-operations.R
\name{ArrayOperations}
\alias{ArrayOperations}
\alias{ArrayAdd}
\alias{ArrayDivide}
\alias{ArrayMultiply}
\alias{ArraySubtract}
\alias{ArrayExtend}
\alias{ArrayFill<-}
\title{Perform Operations on Two Arrays}
\usage{
ArrayAdd(array1, array2)

ArrayDivide(array1, array2)

ArrayMultiply(array1, array2)

ArraySubtract(array1, array2)

ArrayExtend(array1, array2)

ArrayFill(object) <- value
}
\arguments{
\item{array1}{An array with named dimensions.}

\item{array2}{An array with the same named dimensions as \code{array1}.}

\item{object}{An array with named dimensions}

\item{value}{An array with the same dimension names as \code{object}}
}
\value{
The resulting array
}
\description{
Multiply, divide, subtract, or add two arrays together.
}
\details{
The arrays must have the same named dimensions, but do not need to have
the same length for each dimension.
\itemize{
\item \code{ArrayAdd}: Sum \code{array1} and \code{array2}
\item \code{ArrayDivide}: Divide \code{array1} by \code{array2}
\item \code{ArrayMultiply}: Multiply \code{array1} and \code{array2}
\item \code{ArraySubtract}: Subtract \code{array2} from \code{array1}
\item \code{ArrayExtend}: extends the arrays to have matching dimensions (see \code{\link[=Extend]{Extend()}})
\item \code{ArrayFill}: Update array \code{object} with the values in \code{value}
}
}
\examples{

# Array with Sim, Year 
array1 <- array(1:20, 
                dim=c(2,5),
                dimnames = list(
                  Sim=1:2,
                  Year=2021:2025
                ))

array2 <- array(1:20, 
                dim=c(1,3),
                dimnames = list(
                  Sim=1,
                  Year=2024:2026
                ))

ArrayExtend(array1, array2)

ArrayAdd(array1, array2)
ArrayDivide(array1, array2)
ArrayMultiply(array1, array2)
ArraySubtract(array1, array2)


# Array with Age, Year 
array1 <- array(1:20, 
                dim=c(5,2),
                dimnames = list(
                  Age=1:5,
                  Year=2021:2022
                ))

array2 <- array(1:20, 
                dim=c(1,3),
                dimnames = list(
                  Age=1,
                  Year=2024:2026
                ))

ArrayExtend(array1, array2)

ArrayAdd(array1, array2)
ArrayDivide(array1, array2)
ArrayMultiply(array1, array2)
ArraySubtract(array1, array2)


# ArrayFill

## Same dimensions
object <- array(NA, 
               dim=c(3,5),
               dimnames=list(Sim=1:3,
                             Year=2021:2025)
)

value <- array(1:2, 
               dim=c(1,2),
               dimnames=list(Sim=2,
                             Year=2022:2023)
)

ArrayFill(object) <- value
object


## Extend Dimensions
object <- array(NA, 
                dim=c(3,5),
                dimnames=list(Sim=1:3,
                              Year=2021:2025)
)

value <- array(1:12, 
               dim=c(3,4),
               dimnames=list(Sim=1:3,
                             Year=c(2022, 2025:2027))
)

ArrayFill(object) <- value
object
}
