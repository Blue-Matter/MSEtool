% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_ArrayOperations.R
\name{ArrayOperations}
\alias{ArrayOperations}
\alias{ArrayAdd}
\alias{ArrayDivide}
\alias{ArrayMultiply}
\alias{ArraySubtract}
\alias{ArrayExtend}
\alias{ArrayFill<-}
\title{Perform Operations on Two Arrays}
\usage{
ArrayAdd(array1, array2)

ArrayDivide(array1, array2)

ArrayMultiply(array1, array2)

ArraySubtract(array1, array2)

ArrayExtend(array1, array2)

ArrayFill(object) <- value
}
\arguments{
\item{array1}{An array with named dimensions.}

\item{array2}{An array with the same named dimensions as \code{array1}.}
}
\value{
The resulting array
}
\description{
Multiply, divide, subtract, or add two arrays together.
}
\details{
The arrays must have the same named dimensions, but do not need to have
the same length for each dimension.
\itemize{
\item \code{ArrayExtend} extends the arrays to have matching dimensions (see \code{\link[=Extend]{Extend()}})
}
}
\examples{

# Array with Sim, Year 
array1 <- array(1:20, 
                dim=c(2,5),
                dimnames = list(
                  Sim=1:2,
                  Year=2021:2025
                ))

array2 <- array(1:20, 
                dim=c(1,3),
                dimnames = list(
                  Sim=1,
                  Year=2024:2026
                ))

ArrayExtend(array1, array2)

ArrayAdd(array1, array2)
ArrayDivide(array1, array2)
ArrayMultiply(array1, array2)
ArraySubtract(array1, array2)


# Array with Age, Year 
array1 <- array(1:20, 
                dim=c(5,2),
                dimnames = list(
                  Age=1:5,
                  Year=2021:2022
                ))

array2 <- array(1:20, 
                dim=c(1,3),
                dimnames = list(
                  Age=1,
                  Year=2024:2026
                ))

ArrayExtend(array1, array2)

ArrayAdd(array1, array2)
ArrayDivide(array1, array2)
ArrayMultiply(array1, array2)
ArraySubtract(array1, array2)
}
