% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-herm.R
\name{.HermHazardRate}
\alias{.HermHazardRate}
\title{Convert a Herm \code{Frac} Curve to a Per-Age Hazard Rate}
\usage{
.HermHazardRate(Frac)
}
\arguments{
\item{Frac}{\code{array} or \code{NULL}. \verb{[sim, age]} or \verb{[sim, age, year]}, values
in \verb{[0, 1]}. \code{NULL} returns \code{NULL}.}
}
\value{
An array the same shape as \code{Frac}: \code{hazard[,1,] = 0} (no younger
age to compare against); for age \code{a >= 2},
\code{hazard[,a,] = clamp(1 - Frac[,a,]/Frac[,a-1,], 0, 1)}, with the ratio
forced to \code{1} (full hazard) wherever \code{Frac[,a-1,] <= 0} (nobody left in
\code{From} at the previous age, avoiding \code{0/0}/\code{x/0}).
}
\description{
Convert a Herm \code{Frac} Curve to a Per-Age Hazard Rate
}
\keyword{internal}
