% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-comp.R
\name{plot_comp}
\alias{plot_comp}
\alias{PlotLandingsAtAge}
\alias{PlotDiscardsAtAge}
\alias{PlotLandingsAtSize}
\alias{PlotDiscardsAtSize}
\title{Plot Age and Size Composition Data}
\usage{
PlotLandingsAtAge(
  object,
  byStock = NULL,
  byFleet = NULL,
  Sim = NULL,
  Years = NULL,
  IncHist = TRUE,
  Stocks = NULL
)

PlotDiscardsAtAge(
  object,
  byStock = NULL,
  byFleet = NULL,
  Sim = NULL,
  Years = NULL,
  IncHist = TRUE,
  Stocks = NULL
)

PlotLandingsAtSize(
  object,
  byStock = NULL,
  byFleet = NULL,
  Sim = NULL,
  Years = NULL,
  IncHist = TRUE,
  Stocks = NULL
)

PlotDiscardsAtSize(
  object,
  byStock = NULL,
  byFleet = NULL,
  Sim = NULL,
  Years = NULL,
  IncHist = TRUE,
  Stocks = NULL
)
}
\arguments{
\item{object}{A \link[=hist-class]{hist}, \link[=mse-class]{mse}, or \link[=data-class]{data} object.
\code{Sim}, \code{Years}, \code{IncHist}, and \code{Stocks} have no effect for \link[=data-class]{data}
objects, which hold a single (stock-complex, simulation) realization.}

\item{byStock}{One of \code{TRUE}, \code{FALSE}, or \code{NULL} (default). \code{TRUE} facets
by stock. \code{FALSE} sums counts across stocks before computing
proportions; for \verb{*AtSize()}, this raises an error if stocks don't all
share the exact same size-class grid (see \link[=compdata-class]{compdata}). \code{NULL}
facets automatically when \code{object} has more than one (selected) stock.
No effect for \link[=data-class]{data} objects (single stock complex).}

\item{byFleet}{One of \code{TRUE}, \code{FALSE}, or \code{NULL} (default). \code{TRUE} facets
by fleet. \code{FALSE} sums counts across fleets before computing
proportions, via \code{\link[=Landings]{Landings()}}/\code{\link[=Discards]{Discards()}}; for \verb{*AtSize()}, this
raises an error if fleets don't all share the exact same size-class
grid, since fleets are not required to (see \link[=compdata-class]{compdata}). \code{NULL}
facets automatically when \code{object} has more than one fleet.}

\item{Sim}{Integer or \code{NULL} (default). For \link[=hist-class]{hist}/\link[=mse-class]{mse}
objects, which simulation replicate to plot. \code{NULL} takes the median
proportion across all simulations (computed per simulation, then
summarised), rather than the median of raw counts.}

\item{Years}{Optional numeric vector. Subset the time series to these
years before plotting.}

\item{IncHist}{Logical. For \link[=mse-class]{mse} objects, include the historical
period? Default \code{TRUE}. No effect for \link[=hist-class]{hist} objects.}

\item{Stocks}{Character or numeric vector. Restrict the plot to specific
stocks, either by name (matching \code{\link[=StockNames]{StockNames()}}) or by index. Default
\code{NULL} (all stocks).}
}
\value{
A \code{ggplot} object, or \code{NULL} invisibly if the relevant
\link[=data-class]{data} slot isn't populated.
}
\description{
Bubble plots of catch-at-age or catch-at-size composition: \code{Year} on the
x-axis, \code{Age}/size \code{Class} on the y-axis, and bubble area proportional to
the proportion of that year's catch falling in that age/class. \code{PlotLandingsAtAge()},
\code{PlotDiscardsAtAge()}, \code{PlotLandingsAtSize()}, and \code{PlotDiscardsAtSize()}
plot the \code{\link[=Landings]{Landings()}}/\code{\link[=Discards]{Discards()}} \code{byAge}/\code{bySize} composition data for a
\link[=hist-class]{hist}, \link[=mse-class]{mse}, or \link[=data-class]{data} object.
}
\details{
Proportions are computed within each \code{Year} (and \code{Stock}/\code{Fleet}/\code{MP}
panel), i.e. each year's bubbles sum to 1 within a panel, making years
with very different total catch comparable and cohorts easier to track
visually. For \link[=hist-class]{hist}/\link[=mse-class]{mse} objects with \code{Sim = NULL},
proportions are computed per simulation and then summarised with the
median across simulations, cell by cell -- not the proportion of median
counts.

Panels are faceted by whichever of \code{Stock}, \code{Fleet}, and \code{MP} have more
than one level after filtering (\code{MP} always facets when \link[=mse-class]{mse}
objects have more than one MP -- there is no color channel free for it,
since bubble size already encodes proportion).

If the relevant \verb{*AtAge}/\verb{*AtSize} data is not populated for a
\link[=data-class]{data} object, the function prints an informational message and
returns \code{NULL} invisibly rather than plotting an empty panel.
}
\examples{
\dontrun{
Hist <- Simulate(SingleStockOM)
PlotLandingsAtAge(Hist)
PlotLandingsAtSize(Hist)
PlotLandingsAtSize(Hist, Sim = 3)

MSE <- Project(Hist, ExampleMPs())
PlotDiscardsAtSize(MSE, byMP = TRUE)

dat <- Hist@Data[[1]][[1]]
PlotLandingsAtSize(dat)
}

}
\seealso{
\code{\link[=Landings]{Landings()}}, \code{\link[=Discards]{Discards()}}, \link[=compdata-class]{compdata}
}
