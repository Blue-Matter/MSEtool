% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-parallel.R
\name{future_plan_helpers}
\alias{future_plan_helpers}
\alias{use_multisession}
\alias{use_sequential}
\title{Manage parallel processing plans with \code{future}}
\usage{
use_multisession(workers = future::availableCores())

use_sequential()
}
\arguments{
\item{workers}{Integer; the number of worker processes to launch.
Defaults to \code{future::availableCores()}.}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
These functions help configure the \code{future} framework for parallel
processing. \code{use_multisession()} sets up a multisession backend,
creating background R sessions for concurrent evaluation.
\code{use_sequential()} restores sequential execution.
}
\details{
\strong{Important:} These functions modify the \strong{global future plan} for the
R session. Users should be aware that this affects all future-based
operations (e.g., \code{furrr::future_map()}) until the plan is changed again.
\subsection{OS-specific notes}{
\itemize{
\item On \strong{Windows}, \code{future::multisession} uses PSOCK clusters. Worker
startup can be slower and may take several seconds per worker.
\item On \strong{Unix-like systems} (Linux, macOS), PSOCK or multicore backends
may be used; worker creation is generally faster.
}
}
}
\examples{
\dontrun{
# Enable parallel processing with 4 workers
use_multisession(workers = 4)

# Example parallel map
res <- furrr::future_map(x, f)

# Restore sequential processing
use_sequential()
}

}
\seealso{
\code{\link[future:plan]{future::plan()}}, \code{\link[furrr:future_map]{furrr::future_map()}}
}
