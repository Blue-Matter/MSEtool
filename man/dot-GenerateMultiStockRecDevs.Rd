% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-om.R
\name{.GenerateMultiStockRecDevs}
\alias{.GenerateMultiStockRecDevs}
\title{Generate correlated multi-stock projection recruitment deviations}
\usage{
.GenerateMultiStockRecDevs(OM, overwrite, silent = FALSE)
}
\description{
Internal \code{PopulateOM()} pipeline step. Controlled by
\code{OM@Control$CorrelatedRecDevs} (\code{logical(1)}, default \code{TRUE}): whether
multi-stock operating models get projection recruitment deviations
correlated with historical cross-stock covariance (via
\code{\link[=GenMultiStockRecDevs]{GenMultiStockRecDevs()}}), rather than independent per-stock deviations.
A property of the OM itself -- not a \code{\link[=SimControl]{SimControl()}} toggle -- since it
changes simulated dynamics, not just what gets computed/reported.
}
\details{
Runs after \code{.PopulateStockList()} has generated each stock's (independent)
\code{RecDevProj}, then overwrites it where applicable; stocks whose
\code{RecDevProj} was user-supplied before population (\code{overwrite = FALSE})
are left untouched.
}
\keyword{internal}
