% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition-obs-index.R
\name{.EstimateBeta}
\alias{.EstimateBeta}
\title{Estimate (or Fix) the Hyperstability Parameter Beta}
\usage{
.EstimateBeta(
  logObs,
  logSim,
  beta = NULL,
  MinPoints = 8,
  MinLogSD = 0.1,
  alpha = 0.05,
  Bounds = c(0.1, 3)
)
}
\arguments{
\item{logObs}{Numeric vector, length n. \code{log(Observed)} for the years
being fit (already subset to valid/fitted years).}

\item{logSim}{Numeric matrix, nSim x n. \code{log(Simulated)} for the same
years, one row per simulation.}

\item{beta}{\code{NULL}, numeric scalar, or length-nSim vector. When \code{NULL}
(default), \code{Beta} is estimated as described above. Otherwise the
supplied value(s) are used directly (recycled to nSim) and no fitting
or testing is performed (\code{Status = "fixed_user"}).}

\item{MinPoints}{Integer. Minimum usable years required before attempting
a free fit. Default \code{8}.}

\item{MinLogSD}{Numeric. Minimum standard deviation of \code{log(Simulated)}
(per simulation) required before attempting a free fit -- guards against
an unstable slope estimate from a near-flat nominal index. Default \code{0.1}.}

\item{alpha}{Numeric. Two-sided significance level for testing whether
the fitted slope differs from \code{1}. Default \code{0.05}.}

\item{Bounds}{Numeric length 2. Lower/upper clamp applied to a significant,
freely estimated \code{Beta}. Default \code{c(0.1, 3)}.}
}
\value{
A list with elements, each numeric length nSim unless noted:
\itemize{
\item \code{Beta}: the fitted or supplied \code{Beta}.
\item \code{SE_Beta}: standard error of the OLS slope. \code{NA} unless \code{Status == "estimated"}.
\item \code{CI_Lower}, \code{CI_Upper}: two-sided \code{1 - alpha} confidence interval on the
OLS slope (pre-clamping). \code{NA} unless \code{Status == "estimated"} or \code{"fixed_bounds"}.
\item \code{R2}: fit R-squared. \code{NA} unless \code{Status == "estimated"} or \code{"fixed_bounds"}.
\item \code{PValue}: two-sided p-value for \code{Beta == 1}. \code{NA} unless \code{Status == "estimated"} or \code{"fixed_bounds"}.
\item \code{nPoints}: scalar integer, the number of years used in the fit (\code{n}).
\item \code{Status}: character. One of \code{"estimated"}, \code{"fixed_user"} (\code{Beta} was
supplied), \code{"fixed_insufficient_data"} (\code{n < MinPoints}),
\code{"fixed_low_variance"} (nominal index too flat to fit reliably),
\code{"fixed_not_significant"} (slope not distinguishable from \code{1} at
\code{alpha}), or \code{"fixed_bounds"} (significant slope clamped to \code{Bounds}).
}
}
\description{
Fits \code{Beta} per simulation by OLS regression of \code{log(Observed)} on
\code{log(Simulated)}, or returns a supplied fixed value.
}
\details{
When estimating, \code{Beta} is fixed at \code{1} when there isn't enough evidence to
distinguish it from \code{1}, or clamped to \verb{[0.1, 3]} when the fit is
significant but extreme. The \code{Status} element records which of these
happened for each simulation, so callers can surface it rather than have
\code{Beta} silently fall back to \code{1}.
}
\keyword{internal}
