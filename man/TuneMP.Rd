% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune-mp.R
\name{TuneMP}
\alias{TuneMP}
\title{Tune a Management Procedure to Meet Management Objectives}
\usage{
TuneMP(
  Hist,
  MP,
  Objective = TuneObjective(PM_Landings),
  Constraints = list(),
  Configs = NULL,
  HistWeights = NULL,
  TuneArg = "tunepar",
  Control = TuneControl(),
  ValidationHist = NULL,
  parallel = FALSE,
  silent = FALSE
)
}
\arguments{
\item{Hist}{A \link[=hist-class]{hist} object, or a (named) list of them (e.g. a
reference set of operating models).}

\item{MP}{An MP function (or its name) with a \code{TuneArg} argument.}

\item{Objective}{A \code{\link[=TuneObjective]{TuneObjective()}} object, or \code{NULL} to maximise the
tuning argument subject to the constraints. Default
\code{TuneObjective(PM_Landings)}.}

\item{Constraints}{A \code{\link[=TuneConstraint]{TuneConstraint()}} object or a list of them.}

\item{Configs}{\code{NULL} (default), or candidate configurations of other MP
arguments; see Details.}

\item{HistWeights}{\code{NULL} (default, equal weights) or a positive numeric
vector, one per \code{Hist}, used by metrics with \code{HistSummary = 'weighted'}.}

\item{TuneArg}{Character. Name of the numeric MP argument to tune. Default
\code{'tunepar'}.}

\item{Control}{A \code{\link[=TuneControl]{TuneControl()}} list.}

\item{ValidationHist}{\code{NULL} (default), or a \link[=hist-class]{hist} object or list
of them, used only to report the final MP's performance.}

\item{parallel}{Logical. Project candidates in parallel (see
\code{\link[=SetupParallel]{SetupParallel()}})? Default \code{FALSE}.}

\item{silent}{Logical. Suppress progress messages? Default \code{FALSE}.}
}
\value{
A \link[=tunemp-class]{tunemp} object. The tuned MP is \verb{@MP}.
}
\description{
Finds the value of a management procedure's tuning argument (\code{tunepar} by
default) that maximises an objective performance metric (e.g. mean
landings) while meeting minimum or maximum thresholds on other performance
metrics (e.g. \code{P(SB > SBMSY) >= 0.6}), evaluated over one or more
\link[=hist-class]{hist} objects. Optionally, first chooses the best values of other
MP arguments from a set of candidate configurations.
}
\details{
\subsection{Objective and constraints}{

The objective and constraints are performance metric functions (see \link{PM},
\code{\link[=NewPM]{NewPM()}}), specified with \code{\link[=TuneObjective]{TuneObjective()}} and \code{\link[=TuneConstraint]{TuneConstraint()}}. When
the objective is still increasing at the boundary set by the constraints
(e.g. yield increases with \code{tunepar} until the biomass constraint is
breached), the result is the feasible value closest to that boundary. When
the objective peaks inside the feasible range, the result is that peak.

With \code{Objective = NULL}, the tuning argument itself is maximised (or, with
\code{Direction = 'decreasing'} in \code{\link[=TuneControl]{TuneControl()}}, minimised) subject to the
constraints: the classic tuning of an MP to a target.
}

\subsection{Search}{

The tuning argument is first evaluated on a grid of \code{nGrid} log-spaced
values over \code{Interval} (see \code{\link[=TuneControl]{TuneControl()}}). All grid values are projected
in one \code{\link[=Project]{Project()}} call per \code{Hist} object, so they run in parallel when
\code{parallel = TRUE}. The range is extended by \code{ExpandFactor} (up to
\code{MaxExpand} times) when no grid value meets the constraints, or when the
objective is still increasing at the end of the range. The best grid value
is then refined towards the constraint boundary, evaluating \code{nPerRound} values
per round, until the bracket is narrower than \code{TolTune} (relative), the slack
is within \code{TolPM}, the objective changes by less than \code{TolObj} (relative),
or after \code{MaxIter} rounds.

The slack of a constraint is \verb{(Value - Min) / |Min|} and/or
\verb{(Max - Value) / |Max|}. A value is feasible when every slack is at
least \code{-TolPM}.

Every candidate is evaluated with the same process and observation errors
(stored in each \code{Hist}), and any random numbers drawn by the MP are seeded
from \code{OM@Seed} (see \code{\link[=Project]{Project()}}), so tuned performance is reproduced
exactly by \code{Project()}.
}

\subsection{Configurations (optional first stage)}{

\code{Configs} gives candidate values of other MP arguments. It can be a named
list of candidate values (all combinations are evaluated; wrap
vector-valued candidates in \code{list()}, e.g. \code{IndexWeight = list(c(1, 1), c(2, 1))}), a list of named lists (each a configuration), or a data frame
(one configuration per row; list-columns allowed). Configurations are
compared at their own approximately tuned value: each is evaluated on a
coarse grid of \code{nGridConfig} values of the tuning argument (optionally on
the first \code{nSimConfig} simulations), its constrained optimum is
interpolated, and the configurations are ranked by the objective there.
The \code{nRefine} best are then tuned fully, and the one with the highest
objective is returned. Selecting configurations on the same operating
models used to tune them is optimistic; \code{ValidationHist} reports the
performance of the final MP on other operating models.
}
}
\examples{
\dontrun{
Hist <- Simulate(SingleStockOM)
Tuned <- TuneMP(Hist, IndexTarget,
                Objective   = TuneObjective(PM_Landings),
                Constraints = list(TuneConstraint(PM_SBSBMSY, Min = 0.6),
                                   TuneConstraint(PM_Safety, Lim = 0.4, Min = 0.9)),
                Configs     = list(Smooth = c(TRUE, FALSE), RecentYears = 1:2))
Tuned
MSE <- Project(Hist, MPs = list(IT_Tuned = Tuned@MP))
}

}
\seealso{
\code{\link[=TuneObjective]{TuneObjective()}}, \code{\link[=TuneConstraint]{TuneConstraint()}}, \code{\link[=TuneControl]{TuneControl()}},
\code{\link[=TuneTable]{TuneTable()}}, \code{\link[=SetMPArgs]{SetMPArgs()}}, \link{PM}
}
