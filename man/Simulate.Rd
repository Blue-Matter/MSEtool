% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{Simulate}
\alias{Simulate}
\title{Simulate Operating Model Dynamics}
\usage{
Simulate(
  OM = NULL,
  parallel = FALSE,
  silent = FALSE,
  nSim = NULL,
  nsim = NULL,
  control = SimControl(),
  Reduce = TRUE,
  refpointsMSY = NULL,
  ...
)
}
\arguments{
\item{OM}{An \link[=om-class]{om} or \link[=OM-legacy-class]{OM-legacy} object. If \code{NULL} (default),
\code{MSEtool::SingleStockOM} is used.}

\item{parallel}{Logical. Use parallel processing (across simulations) for
MSY reference point calculation (see \code{\link[=CalcMSY]{CalcMSY()}}), final-depletion
catchability calibration, reference yield calculation, and historical
data generation? Requires a \code{future} plan established by
\code{\link[=SetupParallel]{SetupParallel()}} -- errors if \code{TRUE} and no plan is active. Only used
for \link[=om-class]{om} objects. Default \code{FALSE}.}

\item{silent}{Logical. Suppress progress messages if \code{TRUE}. Default
\code{FALSE}.}

\item{nSim}{Integer. Number of simulation replicates. If \code{NULL} (default),
the value in \code{OM} is used. Only used for \link[=om-class]{om} objects; use \code{nsim}
for \link[=OM-legacy-class]{OM-legacy} objects.}

\item{nsim}{Integer. Synonym for \code{nSim} for \link[=OM-legacy-class]{OM-legacy} objects. If
\code{NULL} (default), \code{nSim} is used if provided.}

\item{control}{A \code{\link[=SimControl]{SimControl()}} object controlling which optional
calculations are performed. Only used for \link[=om-class]{om} objects. Default
\code{SimControl()}.}

\item{Reduce}{Logical. Reduce object size after simulation for memory
efficiency? Default \code{TRUE}. See \code{\link[=ReduceDims]{ReduceDims()}}.}

\item{refpointsMSY}{A pre-calculated \link[=refpointsMSY-class]{refpointsMSY} object (e.g.
from a previous \code{\link[=Simulate]{Simulate()}} or \code{\link[=CalcMSY]{CalcMSY()}} call), or \code{NULL} (default).
If supplied, it is used directly as \code{Hist@Reference@MSY} and the
(expensive) MSY reference point calculation is skipped -- \code{control}'s
\code{MSYRefs} setting is ignored in this case. Only valid if it was
calculated for an \code{OM} with the same \code{nSim}, stock/complex names, and
reference year as the \code{OM} supplied here; an incompatible object throws
an error rather than being silently ignored. Only used for \link[=om-class]{om}
objects.}

\item{...}{Additional arguments passed to sub-functions. Not currently
used}
}
\value{
\itemize{
\item If \code{OM} is an \link[=om-class]{om} object: a \link[=hist-class]{hist} object.
\item If \code{OM} is an \link[=OM-legacy-class]{OM-legacy} object: a \link[=Hist-legacy-class]{Hist-legacy} object.
}
}
\description{
Simulate historical fishery dynamics and calculate reference points for an
\link[=om-class]{om} or \link[=OM-legacy-class]{OM-legacy} object. Returns a populated \link[=hist-class]{hist}
or \link[=hist-class]{Hist-class} object containing historical population dynamics, fishery
data, and reference points.
}
\details{
\code{Simulate()} is a dispatcher that calls internal functions \code{.SimulateOM()} (for
\link[=om-class]{om} objects) or \code{SimulateOM()} (for legacy \link[=OM-legacy-class]{OM-legacy}
objects) depending on the class of \code{OM}.
}
\examples{
\dontrun{
# Simulate using the default example OM
hist <- Simulate()

# Simulate with specific options
hist <- Simulate(
  OM      = MyOM,
  control = SimControl(RefLandings = TRUE, GenerateData = FALSE),
  silent  = TRUE
)
}

}
\seealso{
\code{\link[=OM]{OM()}}, \code{\link[=runMSE]{runMSE()}}
}
