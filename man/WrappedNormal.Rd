% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-wrapped-normal.R
\name{WrappedNormal}
\alias{WrappedNormal}
\title{Wrapped Normal Distribution}
\usage{
WrappedNormal(n_seasons, mu, sigma, n_wraps = 5)
}
\arguments{
\item{n_seasons}{Integer. Number of seasons per year (e.g. \code{12} for
monthly, \code{4} for quarterly).}

\item{mu}{Numeric. Peak season (need not be an integer; e.g. \code{3.5} for a
peak between seasons 3 and 4).}

\item{sigma}{Numeric. Standard deviation in units of seasons. Smaller
values concentrate recruitment around the peak; larger values spread it
across the year.}

\item{n_wraps}{Integer. Number of periods summed on each side to approximate
wrapping. Default \code{5} is sufficient for any reasonable \code{sigma}.}
}
\value{
A numeric vector of normalised density values of length
\code{n_seasons}.
}
\description{
Creates a wrapped normal distribution to represent a seasonal recruitment
patterns in models with \code{Seasons > 1}.
}
\examples{
# Monthly model: peak recruitment March/April, moderate spread
pi_m <- WrappedNormal(n_seasons = 12, mu = 3.5, sigma = 1.5)
names(pi_m) <- month.abb

# Narrow spread — recruitment concentrated in 1-2 seasons
pi_m <- WrappedNormal(n_seasons = 12, mu = 3.5, sigma = 0.5)

# Wide spread — recruitment across most of the year
pi_m <- WrappedNormal(n_seasons = 12, mu = 3.5, sigma = 3)

# Quarterly model: peak in Q1
pi_m <- WrappedNormal(n_seasons = 4, mu = 1.5, sigma = 0.8)

}
