% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-maturity.R
\name{PopulateMaturity}
\alias{PopulateMaturity}
\title{Populate a Maturity Object}
\usage{
PopulateMaturity(
  Maturity,
  Ages = NULL,
  Length = NULL,
  Weight = NULL,
  Years = NULL,
  nSim = 5,
  seed = NULL,
  silent = FALSE,
  CalcAtLength = FALSE,
  force = FALSE
)
}
\arguments{
\item{Maturity}{A \code{\link[=Maturity]{Maturity()}} object to populate.}

\item{Ages}{An \code{\link[=Ages]{Ages()}} object defining age classes.}

\item{Length}{A \code{\link[=Length]{Length()}} object. Required if the maturity model depends on
length-at-age.}

\item{Weight}{A \code{\link[=Weight]{Weight()}} object. Required if the maturity model depends on
weight-at-age.}

\item{Years}{Numeric vector of model years.}

\item{nSim}{Integer. Number of simulation replicates.}

\item{seed}{Integer. Random seed used for stochastic generation.}

\item{silent}{Logical. If \code{TRUE}, suppress informational messages.}

\item{CalcAtLength}{Logical. If \code{TRUE}, calculate maturity-at-length values
from age-based arrays.}

\item{force}{Logical. If \code{TRUE}, force re-population even if the object
digest is unchanged.}
}
\value{
A populated \code{\link[=Maturity]{Maturity()}} object.
}
\description{
Populate a \code{Maturity} object by generating maturity-at-age or maturity-at-weight
values for a stock, expanding parameters across simulation replicates and years,
and optionally calculating values at length.
}
\details{
\code{PopulateMaturity()} handles population of maturity-at-age or maturity-at-weight
for a stock. Steps include:
\itemize{
\item Structuring model parameters across \code{nSim} and \code{Years}
\item Resolving the maturity model class
\item Generating mean maturity at age, at length, or at weight as appropriate
\item Converting between mean-at-length/weight and mean-at-age
\item Adding stochastic variation via \code{.PopulateRandom()}
\item Setting semelparous array if applicable
}
}
\examples{
\dontrun{
M <- PopulateMaturity(
  Maturity,
  Ages = Ages,
  Length = Length,
  Weight = Weight,
  Years = Years,
  nSim = 100,
  CalcAtLength = TRUE,
  seed = 123
)
}

}
\seealso{
\code{\link[=Populate]{Populate()}}, \code{\link[=PopulateStock]{PopulateStock()}}, \code{\link[=PopulateLength]{PopulateLength()}}, \code{\link[=PopulateWeight]{PopulateWeight()}}
}
