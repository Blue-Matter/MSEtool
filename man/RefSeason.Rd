% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-om.R
\name{RefSeason}
\alias{RefSeason}
\alias{RefSeason<-}
\alias{RefEffortYears}
\alias{RefEffortYears<-}
\title{Reference Point Season Handling for OM Objects}
\usage{
RefSeason(x)

RefSeason(x) <- value

RefEffortYears(x)

RefEffortYears(x) <- value
}
\arguments{
\item{x}{An \link[=om-class]{om}, \link[=hist-class]{hist}, or \link[=mse-class]{mse} object.}

\item{value}{Replacement value; an integer vector of season indices
(\code{RefSeason}, in \verb{1..Seasons}) or a numeric vector of historical
calendar years (\code{RefEffortYears}). \code{NULL} restores the default
behaviour described in \emph{Details}.}
}
\value{
\itemize{
\item \code{RefSeason()}/\code{RefEffortYears()} return the value of the corresponding
slot.
\item \verb{RefSeason<-()}/\verb{RefEffortYears<-()} return \code{x} with the corresponding
slot updated.
}
}
\description{
Access or replace the \code{RefSeason} and \code{RefEffortYears} slots of an
\link[=om-class]{om} object. Only relevant for seasonal OMs (\code{Seasons > 1}): both
control how equilibrium/MSY reference points (e.g. \code{BMSY}, \code{SBMSY}, \code{B0},
\code{SB0}, \code{BLow}) are calculated from a seasonal per-recruit model. Also
accepts \link[=hist-class]{hist} and \link[=mse-class]{mse} objects, extracting the embedded
\code{OM} slot transparently.
}
\details{
\subsection{\code{RefSeason}}{

Reference point biomass quantities are a cross-sectional snapshot within
the annual cycle, so a season (or seasons) must be chosen at which to take
that snapshot. \code{NULL} (default) auto-detects, independently per
simulation, which season(s) have nonzero spawning contribution, and
averages the cross-sectional snapshot across them when more than one is
detected. Set \code{RefSeason} explicitly to override the auto-detection with a
fixed season index/indices.
}

\subsection{\code{RefEffortYears}}{

One or more historical calendar years whose relative seasonal
effort/catchability pattern fixes the seasonal \emph{shape} of fishing
mortality used during per-recruit and MSY reference point optimization -
decoupled from the year used for the biological parameters themselves.
\code{NULL} (default) reuses the same year as the biological parameters. When
more than one year is given, the per-season effort is averaged across
those years before use.
}
}
\examples{
om <- OM(Seasons = 4)
RefSeason(om) <- 1L
RefSeason(om)

}
\seealso{
\itemize{
\item \code{\link[=OM]{OM()}} for the constructor, \link[=om-class]{om} for the class definition.
\item \code{\link[=CalcEquilibrium]{CalcEquilibrium()}}, \code{\link[=CalcMSY]{CalcMSY()}} for the reference point calculations
that use these slots.
\item \link{OM-accessors} for the remaining \code{OM} slot accessors.
}

Other om:
\code{\link[=CatchFrac]{CatchFrac()}},
\code{\link{ComplexOM}},
\code{\link[=DataLag]{DataLag()}},
\code{\link[=EffortAllocation]{EffortAllocation()}},
\code{\link[=Herm]{Herm()}},
\code{\link{HermOM}},
\code{\link[=HistoricalWeight]{HistoricalWeight()}},
\code{\link[=InterimAdvice]{InterimAdvice()}},
\code{\link{MultiStockOM}},
\code{\link[=OM]{OM()}},
\code{\link{OM-accessors}},
\code{\link{SeasonalSpatialOM}},
\code{\link{SingleStockOM}},
\code{\link{TwoFleetOM}},
\code{\link{om-class}},
\code{\link{stocktransition-class}}
}
\concept{om}
