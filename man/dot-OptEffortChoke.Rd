% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt-effort-choke.R
\name{.OptEffortChoke}
\alias{.OptEffortChoke}
\title{Solve fleet effort and targeting under the choke rule}
\usage{
.OptEffortChoke(
  Proj,
  Year,
  TSIndex,
  sim,
  StockNames,
  FleetNames,
  TAC_by_Complex,
  TACType_by_Complex,
  TACUnit_by_Complex,
  Compliance,
  MaxFleetEffort = NULL,
  lambda = NULL,
  n_recent = 5,
  minEffort = 1e-08,
  tol = 1e-06,
  inner_reltol = NULL,
  maxEval = 500
)
}
\arguments{
\item{Proj}{A projection object containing effort, targeting
history, operating model settings, and stock/fleet dimensions.}

\item{Year}{Integer. The simulation year index passed to
\code{.CalcFisheryDynamics()}.}

\item{TSIndex}{Integer. Time-step index into \code{Proj} arrays for the
current year. \code{TSIndex - 1} is used to initialise the warm start.}

\item{sim}{Integer. Simulation replicate index.}

\item{StockNames}{Character vector of stock names.}

\item{FleetNames}{Character vector of fleet names.}

\item{TAC_by_Complex}{Named list of per-fleet TAC vectors, \code{NULL} where a
complex has no TAC.}

\item{TACType_by_Complex, TACUnit_by_Complex}{Per-complex \code{"Removals"}/
\code{"Landings"} and \code{"Biomass"}/\code{"Number"} vectors.}

\item{Compliance}{\verb{[nFleet x nComplex]} matrix from
\code{.ResolveComplianceMatrix()}.}

\item{MaxFleetEffort}{Numeric vector (length \code{nFleet}), or \code{NULL}.}

\item{lambda}{Numeric matrix \verb{[nFleet x nStock]}, or a scalar or
length-\code{nFleet} vector recycled across stocks, or \code{NULL} (default \code{1}).
Ridge penalty weight controlling resistance to year-to-year targeting
changes.}

\item{n_recent}{Integer (default \code{5}). Number of most-recent historical
years examined to determine which stocks each fleet actively targets.}

\item{minEffort}{Numeric (default \code{1e-8}). Floor applied to fleet effort
to avoid degenerate zero-effort fleets.}

\item{tol}{Numeric (default \code{1e-6}). Convergence tolerance passed
to the optimiser.}

\item{inner_reltol}{Numeric, or \code{NULL} (default). Relative convergence
tolerance for the per-complex root finds during optimisation, as a
fraction of the TAC being matched; \code{NULL} keeps \code{.OptEffortSinglestock()}'s
absolute \code{tol}.}

\item{maxEval}{Integer (default \code{500}). Maximum function evaluations
allowed across the BFGS and Nelder-Mead phases combined.}
}
\value{
A named list with \code{Effort}, \code{Delta}, \code{converged} and \code{ActiveStock}.
}
\description{
Solves fleet effort under TAC management in a multi-complex model. Rather
than penalising TAC over/undershoot, effort is derived from the targeting
mix: for each
complex, the effort at which its catch equals its TAC is found by a
bracketed root find (\code{.SolveEffortByComplex()}), and those are combined by
\code{Imp@TAC@Compliance} (\code{.CombineEffortByCompliance()}). Full compliance stops
a fleet at the first TAC reached; zero compliance lets it run until the last
is reached.
}
\details{
Only the targeting mix is optimised, maximising a concave transform of catch
against a penalty for drifting from last year's mix - the same objective
\code{.OptTargetingMultiStock()} uses under effort control, with effort supplied
by the choke rule instead of by the MP.
}
\keyword{internal}
