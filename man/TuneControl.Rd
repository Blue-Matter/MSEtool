% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune-mp.R
\name{TuneControl}
\alias{TuneControl}
\title{Settings for Tuning an MP}
\usage{
TuneControl(
  Interval = c(0.1, 10),
  nGrid = 7,
  Expand = TRUE,
  ExpandFactor = 3,
  MaxExpand = 2,
  nPerRound = NULL,
  MaxIter = 10,
  TolTune = 0.01,
  TolPM = 0.005,
  TolObj = 0.001,
  Direction = c("increasing", "decreasing"),
  nGridConfig = 5,
  nSimConfig = NULL,
  nRefine = 1,
  MaxConfigs = 200,
  ValidateConfigs = TRUE,
  MaxFailRate = 0.05,
  KeepMSE = FALSE,
  DryRun = FALSE
)
}
\arguments{
\item{Interval}{Numeric vector, length 2. Initial range of the tuning
argument. Default \code{c(0.1, 10)}.}

\item{nGrid}{Positive integer. Number of log-spaced values in the initial
grid. Default \code{7}.}

\item{Expand}{Logical. Extend the range when needed? Default \code{TRUE}.}

\item{ExpandFactor}{Number greater than 1. Factor by which the range is
extended. Default \code{3}.}

\item{MaxExpand}{Non-negative integer. Maximum number of extensions.
Default \code{2}.}

\item{nPerRound}{\code{NULL} (default: the number of parallel workers when
\code{parallel = TRUE}, else \code{1}) or the number of values evaluated per
refinement round.}

\item{MaxIter}{Positive integer. Maximum number of refinement rounds.
Default \code{10}.}

\item{TolTune}{Positive number. Relative width of the tuning-argument
bracket at which refinement stops. Default \code{0.01}.}

\item{TolPM}{Non-negative number. Constraint slack tolerance. Default
\code{0.005}.}

\item{TolObj}{Positive number. Relative change in the objective at which
refinement of an interior optimum stops. Default \code{0.001}.}

\item{Direction}{Character. \code{'increasing'} (default) if larger values of
the tuning argument give higher catches (as for \code{tunepar}), otherwise
\code{'decreasing'}. Used when \code{Objective = NULL}.}

\item{nGridConfig}{Positive integer. Number of values of the tuning
argument evaluated for each configuration in the first stage. Default
\code{5}.}

\item{nSimConfig}{\code{NULL} (default, all) or the number of simulations used
in the first stage.}

\item{nRefine}{Positive integer. Number of best configurations tuned fully.
Default \code{1}.}

\item{MaxConfigs}{Positive integer. Maximum number of configurations.
Default \code{200}.}

\item{ValidateConfigs}{Logical. Apply the MP (with each configuration) to
the historical data of the first \code{Hist} before projecting, to catch
invalid arguments? Default \code{TRUE}.}

\item{MaxFailRate}{Number in \verb{[0, 1]}. Candidates whose MP fails in a
larger fraction of simulation-management years are infeasible. Default
\code{0.05}.}

\item{KeepMSE}{Logical. Keep the \link[=mse-class]{mse} objects of every evaluation
(\verb{@MSE} of the result)? Default \code{FALSE}.}

\item{DryRun}{Logical. Only report the number of projections and an
estimate of the run time? Default \code{FALSE}.}
}
\value{
A list of class \code{tunecontrol}.
}
\description{
Controls for \code{\link[=TuneMP]{TuneMP()}}. See the Details of \code{\link[=TuneMP]{TuneMP()}} for how they are
used.
}
\seealso{
\code{\link[=TuneMP]{TuneMP()}}
}
