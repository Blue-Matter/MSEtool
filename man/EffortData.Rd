% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-data.R
\name{EffortData}
\alias{EffortData}
\title{EffortData Constructor and Accessors}
\usage{
EffortData(Name = NULL, Value = NULL, CV = NULL, Units = NULL)
}
\arguments{
\item{Name}{\code{character} or \code{NULL}. Name of the dataset. Default \code{NULL}.}

\item{Value}{\code{array} or \code{NULL}. Observed effort values with dimensions
\verb{[nYear x nFleet]}. Default \code{NULL}.}

\item{CV}{\code{array} or \code{NULL}. Coefficients of variation for the effort
observations, matching the dimensions of \code{Value}. Default \code{NULL}.}

\item{Units}{\code{character} or \code{NULL}. Units of effort measurement, one
element per fleet (e.g., \code{"days"}, \code{"trips"}, \code{"unitless"}).
Default \code{NULL}.}
}
\value{
\itemize{
\item \code{EffortData()} returns an \link[=effortdata-class]{effortdata} object.
\item \code{Effort()} returns the \code{Effort} slot from a \link[=data-class]{data} object \code{x}.
\item \verb{Effort<-} returns \code{x} with the \code{Effort} slot replaced.
}
}
\description{
Construct an \link[=effortdata-class]{effortdata} object storing observed fishing effort and
uncertainty, or access and replace individual slots.
}
\details{
\subsection{Usage}{

\code{EffortData()} is the standard constructor. All arguments are optional;
an empty object is valid and slots are populated later (e.g., by
\code{.GenHistDataEffort()}).
}

\subsection{Attaching to a Data Object}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{Effort(data) <- EffortData(Value = myArray, CV = myCVArray)
}\if{html}{\out{</div>}}
}
}
\examples{
# Empty object
ed <- EffortData()

# With values
yrs <- 2000:2020
nFleet <- 2
val <- array(runif(length(yrs) * nFleet),
             dim = c(length(yrs), nFleet),
             dimnames = list(Year = yrs, Fleet = c("Trawl", "Longline")))
ed <- EffortData(Value = val,
                 CV    = array(0.2, dim = dim(val), dimnames = dimnames(val)),
                 Units = c("hours", "hooks"))

}
\seealso{
\itemize{
\item \link[=effortdata-class]{effortdata} for the class definition.
\item \link[=data-class]{data} and \code{\link[=Data]{Data()}} for the enclosing data object.
\item \code{\link[=EffortObs]{EffortObs()}} for the observation error structure that generates
\link[=effortdata-class]{effortdata} objects during simulation.
\item \code{.GenHistDataEffort()} for how \code{Value}, \code{CV}, and \code{Units} are populated
from the operating model history.
}

Other data:
\code{\link[=CatchData]{CatchData()}},
\code{\link[=CompData]{CompData()}},
\code{\link[=IndicesData]{IndicesData()}},
\code{\link{catchdata-class}},
\code{\link{compdata-class}},
\code{\link{effortdata-class}},
\code{\link{indicesdata-class}}
}
\concept{data}
