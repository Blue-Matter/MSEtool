% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt-effort-multi-stock.R
\name{OptEffort_multi_stock}
\alias{OptEffort_multi_stock}
\title{Estimate fleet effort and stock-specific targeting weights}
\usage{
OptEffort_multi_stock(
  Proj,
  Year,
  TSIndex,
  sim,
  StockNames,
  FleetNames,
  TAC_by_Complex,
  TACType_by_Complex,
  TACUnit_by_Complex,
  Choke,
  UndershootPenalty,
  OvershootPenalty,
  PenaltyMode,
  lambda = NULL,
  n_recent = 5,
  minEffort = 1e-08,
  tol = 1e-06,
  maxEval = 500
)
}
\arguments{
\item{Proj}{A projection object containing effort, targeting history,
operating model settings, and stock/fleet dimensions.}

\item{Year}{Integer. The simulation year index passed to
\code{\link[=CalcFisheryDynamics]{CalcFisheryDynamics()}}.}

\item{TSIndex}{Integer. Time-step index into \code{Proj} arrays for the current
year. \code{TSIndex - 1} is used to initialise the warm start.}

\item{sim}{Integer. Simulation replicate index.}

\item{TAC_by_Complex}{Named list of length \code{nComplex}. Each element is a
length-\code{nFleet} numeric vector of TAC values (one per fleet) for that
complex, or \code{NULL} if no TAC applies to that complex.}

\item{TACType_by_Complex}{Character vector of length \code{nComplex}. Either
\code{"Landings"} or \code{"Removals"} (landings + discards) for each complex.}

\item{Choke}{Integer matrix \verb{[nFleet x nComplex]}. A value of \code{1L}
indicates that complex \code{i} is a choke constraint for fleet \code{f} — its TAC
must not be exceeded.}

\item{UndershootPenalty}{Numeric matrix \verb{[nFleet x nComplex]}. Penalty
weight applied to proportional TAC undershoot for each fleet–complex pair.}

\item{OvershootPenalty}{Numeric matrix \verb{[nFleet x nComplex]}. Base penalty
weight for proportional TAC overshoot. Choke complexes receive a ×1000
multiplier internally.}

\item{PenaltyMode}{Character vector of length \code{nFleet}. Currently accepted
for future extensibility; not used in the current implementation.}

\item{lambda}{Numeric vector of length \code{nFleet} (or scalar, recycled), or
\code{NULL} (default \code{1} for all fleets). Ridge penalty weight controlling
resistance to year-to-year targeting changes. Larger values penalise
deviations from the previous year's targeting mix more heavily.}

\item{n_recent}{Integer (default \code{5}). Number of most-recent historical
years examined to determine which stocks each fleet actively targets.}

\item{minEffort}{Numeric (default \code{1e-8}). Floor applied to all effort
values to avoid log(0) errors.}

\item{tol}{Numeric (default \code{1e-6}). Convergence tolerance passed to
optimisers and used as the feasibility tolerance in bisection.}

\item{maxEval}{Integer (default \code{500}). Maximum function evaluations
allowed across the BFGS and Nelder-Mead phases combined..}
}
\value{
A named list with elements:
\describe{
\item{\code{Effort}}{Numeric vector of length \code{nFleet}. Final effort per fleet,
floored at \code{minEffort}.}
\item{\code{Delta}}{Numeric matrix \verb{[nFleet x nStock]}. Final targeting weights.
Inactive stock entries are set to \code{0}.}
\item{\code{Catch}}{Numeric matrix \verb{[nComplex x nFleet]} of realised catches
at the returned effort and targeting, with informative row/column names.}
\item{\code{converged}}{Logical. \code{TRUE} if the BFGS or Nelder-Mead optimiser
reported convergence in Phase 1.}
\item{\code{ActiveStock}}{Logical matrix \verb{[nFleet x nStock]}. Which stocks are
considered active for each fleet based on recent targeting history.}
}
}
\description{
\code{OptEffort_multi_stock()} solves for fleet effort levels \code{E_f} and
stock-specific targeting weights \verb{delta_\{s,f\}} such that each active fleet
catches its TAC for every stock complex it participates in, subject to choke
constraints and a soft ridge penalty on year-to-year targeting changes.
}
\keyword{internal}
