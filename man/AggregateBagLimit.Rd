% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-aggbaglimit.R
\name{AggregateBagLimit}
\alias{AggregateBagLimit}
\title{Aggregate Bag Limit Constructor}
\usage{
AggregateBagLimit(
  Fleet,
  Stocks,
  BagLimit,
  LimitType = "boat",
  ClosureMode = "discard",
  Misc = list()
)
}
\arguments{
\item{Fleet}{Character or numeric, length 1. The fleet the aggregate
limit applies to, by name or position.}

\item{Stocks}{Character or numeric, length 2 or more. The stocks pooled
under this limit, by name or position. A limit on a single stock does
not need \code{AggregateBagLimit()} - set \code{BagLimit} directly on that
stock's \code{Advice} object instead (see \code{\link[=Advice]{Advice()}}).}

\item{BagLimit}{Numeric, length 1. The aggregate limit, in fish per
trip (per angler if \code{LimitType = "angler"}, per vessel if \code{"boat"}).}

\item{LimitType}{Character. \code{"boat"} (default) or \code{"angler"} - see
\code{\link[=Advice]{Advice()}} for the shared definition. Also governs enforcement of any
per-stock \code{BagLimit} set as a species-specific sub-cap on a stock
included in \code{Stocks} (that stock's own \code{LimitType} is ignored).}

\item{ClosureMode}{Character. \code{"discard"} (default) or \code{"stop"} - see
\code{\link[=Advice]{Advice()}} for the shared definition. Also governs enforcement of any
per-stock \code{BagLimit} sub-cap on a stock included in \code{Stocks} (that
stock's own \code{ClosureMode} is ignored).}

\item{Misc}{Miscellaneous list. Default \code{list()}.}
}
\value{
An \link[=aggbaglimit-class]{aggbaglimit} object.
}
\description{
Construct an \link[=aggbaglimit-class]{aggbaglimit} object declaring an aggregate bag limit
that pools catch across several independently-modelled stocks for a
single fleet - management advice that does not belong to any one
stock's \code{\link[=Advice]{Advice()}} object. Used by \code{mmp}-class management procedures,
which return a list of these objects alongside the ordinary per-stock
\code{Advice} list.
}
\details{
\code{AggregateBagLimit()} declares the one piece of a pooled bag-limit
regulation that cannot be expressed on any single stock's \code{Advice}
object: which stocks are pooled, and the ceiling that applies to their
combined catch. Everything else reuses the ordinary \code{Advice} mechanism:
\itemize{
\item A species-specific sub-cap for one stock within the group is just an
ordinary per-stock \code{BagLimit}, set on that stock's own \code{Advice}
object (see \code{\link[=Advice]{Advice()}}). No separate slot or argument is needed for
this.
\item \code{Theta}, \code{TripsScalar}, and \code{AnglerPerTrip} are read directly from
the fleet's \code{Effort} object for the \emph{first} stock listed in \code{Stocks}
(\code{Fleet(OM)[[Stocks[1]]][[Fleet]]@Effort}) - not set here. This
assumes the fleet's trip behaviour is consistent across the stocks it
is pooled over, since these describe fleet activity rather than any
one species.
}

\code{mmp}-class management procedures return a list of \code{AggregateBagLimit()}
objects (one per pooled regulation) alongside the ordinary per-stock
\code{Advice} list.
}
\seealso{
\link[=aggbaglimit-class]{aggbaglimit} for the class definition. \code{\link[=Advice]{Advice()}} for
the per-stock advice object aggregate bag limits complement.
}
