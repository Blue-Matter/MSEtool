% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-relative.R
\name{relative_ref}
\alias{relative_ref}
\alias{B_B0}
\alias{SB_SB0}
\alias{SP_SP0}
\alias{B_BMSY}
\alias{SB_SBMSY}
\alias{SP_SPMSY}
\alias{F_FMSY}
\title{Biomass and Fishing Mortality Relative to Reference Points}
\usage{
B_B0(
  object,
  type = c("Equilibrium", "Dynamic"),
  df = TRUE,
  Reduce = TRUE,
  IncYear = FALSE
)

SB_SB0(
  object,
  type = c("Equilibrium", "Dynamic"),
  df = TRUE,
  Reduce = TRUE,
  IncYear = FALSE
)

SP_SP0(
  object,
  type = c("Equilibrium", "Dynamic"),
  df = TRUE,
  Reduce = TRUE,
  IncYear = FALSE
)

B_BMSY(object, df = TRUE, Reduce = TRUE, IncYear = FALSE)

SB_SBMSY(object, df = TRUE, Reduce = TRUE, IncYear = FALSE)

SP_SPMSY(object, df = TRUE, Reduce = TRUE, IncYear = FALSE)

F_FMSY(object, df = TRUE, Reduce = TRUE, IncYear = FALSE)
}
\arguments{
\item{object}{A \link[=hist-class]{hist} or \link[=mse-class]{mse} object.}

\item{type}{Character. One of \code{'Equilibrium'} or \code{'Dynamic'}. Controls
which unfished baseline is used. See \code{\link[=B0]{B0()}} for details.
Only applies to the \verb{*_*0} family.}

\item{df}{Logical. If \code{FALSE} returns the raw ratio array.
If \code{TRUE} (default) returns a tidy \code{data.frame}.}

\item{Reduce}{Logical. If \code{TRUE} (default) simulation dimensions are
reduced using \code{\link[=ReduceDims]{ReduceDims()}} before conversion to a data frame.}

\item{IncYear}{Logical. Passed to \code{\link[=ReduceDims]{ReduceDims()}}; controls whether the
year dimension is retained during reduction. Default \code{FALSE}.}
}
\value{
\itemize{
\item \code{df = FALSE}: a numeric array of ratios with dimensions \verb{Sim × Stock × Year}.
\item \code{df = TRUE}: a tidy \code{data.frame} with columns \code{Sim}, \code{Stock}, \code{Year},
\code{Period}, \code{MP} (MSE only), \code{Value}, \code{Variable}.
}
}
\description{
Extract time series of biomass, spawning biomass, spawning production, or
fishing mortality expressed as a fraction of an unfished or MSY reference
level.
}
\details{
\code{B_B0()}, \code{SB_SB0()}, and \code{SP_SP0()} divide the time series by the
corresponding unfished level (\code{type = 'Equilibrium'} or \code{'Dynamic'}).
\code{B_BMSY()}, \code{SB_SBMSY()}, and \code{SP_SPMSY()} divide by the corresponding
MSY reference point. \code{F_FMSY()} divides total apical fishing mortality
(summed over fleets) by \code{FMSY}.

When applied to an \link[=mse-class]{mse} object the historical and projection periods
are row-bound and labelled via the \code{Period} column; historical rows carry
\code{MP = "Historical"}.

For the projection period of an \link[=mse-class]{mse} object the denominator is
extended to cover projection years by repeating the last available
historical reference value (forward fill).

\code{FMSY} is a single "apical fishing mortality" optimised jointly across all
stocks in a complex (see \code{\link[=CalcMSY]{CalcMSY()}}) -- there is no separate \code{FMSY} for an
individual stock within a multi-stock complex. \code{F_FMSY()} therefore reports
its ratio at the \emph{complex} level (\code{OM@Complexes}), not the stock level: the
numerator is the controlling (maximum) realised apical F across the stocks
in each complex -- the same quantity \code{\link[=CalcMSY]{CalcMSY()}} itself optimises against
-- divided by that complex's \code{FMSY}. For a complex containing a single
stock this is identical to the stock's own F/FMSY. The \code{Stock} column of
the returned \code{data.frame} (and dimension of the \code{df = FALSE} array) holds
complex names, matching the convention already used for \code{FMSY} in
\link[=refpointsMSY-class]{refpointsMSY}.
}
\examples{
Hist <- Simulate(SingleStockOM)
B_B0(Hist)
SB_SB0(Hist, type = 'Dynamic')
F_FMSY(Hist)

MSE <- Project(Hist, 'CurrentEffort')
B_BMSY(MSE, df = TRUE)
SB_SBMSY(MSE, df = TRUE)
SP_SPMSY(MSE, df = TRUE)
F_FMSY(MSE, df = TRUE)

}
\seealso{
\code{\link[=Biomass]{Biomass()}}, \code{\link[=B0]{B0()}}, \code{\link[=BMSY]{BMSY()}}, \code{\link[=FDead]{FDead()}}, \code{\link[=FMSY]{FMSY()}}
}
