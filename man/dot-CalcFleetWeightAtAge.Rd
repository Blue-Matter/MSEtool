% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-fleet.R
\name{.CalcFleetWeightAtAge}
\alias{.CalcFleetWeightAtAge}
\title{Calculate Fleet-Specific Weight-at-Age from Selectivity- (and Retention-)
at-Length}
\usage{
.CalcFleetWeightAtAge(Selectivity, Weight, Length, Retention = NULL)
}
\arguments{
\item{Selectivity}{A populated \link[=selectivity-class]{selectivity} object.}

\item{Weight}{A populated \link[=weight-class]{weight} object.}

\item{Length}{A populated \link[=length-class]{length} object supplying the age-length
key (\code{Length@ALK}).}

\item{Retention}{A populated \link[=retention-class]{retention} object, or \code{NULL} (default)
to weight by selectivity only.}
}
\value{
\code{array}. \verb{Sim x Age x Year} fleet weight-at-age.
}
\description{
Derives a fleet's weight-at-age schedule by weighting weight-at-length by
selectivity-at-length (and, if \code{Retention} is supplied, retention-at-length
too) over the age-length distribution, i.e. \verb{W_fleet(a) = sum_L[ P(L|a) sel(L) ret(L) W(L) ] / sum_L[ P(L|a) sel(L) ret(L) ]} (\code{ret(L) = 1}
everywhere if \code{Retention} is not supplied). This mirrors how Stock
Synthesis derives its internal \code{SelWt}/\code{RetWt} weight-at-age-by-fleet
quantities, so that catch biomass matches between the two models -- see
\code{PopulateFleet()} for how the two calls (with and without \code{Retention})
populate \code{WeightFleetSelected} and \code{WeightFleetRetained} respectively.
Falls back to \code{Weight@MeanAtAge} wherever the selectivity- or
weight-at-length schedules, or the age-length key, are unavailable.
}
\keyword{internal}
