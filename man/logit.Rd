% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-transformation.R
\name{logit}
\alias{logit}
\alias{ilogit}
\title{Logit and Inverse Logit (Sigmoid) Functions}
\usage{
logit(p)

ilogit(x)
}
\arguments{
\item{p}{A numeric vector of probabilities. Values must be in the open
interval (0, 1); \code{NaN} is returned for values outside this range.}

\item{x}{A numeric vector of real-valued log-odds.}
}
\value{
\itemize{
\item \code{logit(p)} returns a numeric vector of log-odds in (-Inf, Inf).
\item \code{ilogit(x)} returns a numeric vector of probabilities in (0, 1).
}
}
\description{
\code{logit()} transforms a probability to the log-odds scale. \code{ilogit()} is its
inverse — the sigmoid function — mapping any real number back to [0, 1].
}
\details{
The two functions are mutual inverses:

\deqn{\text{logit}(p) = \log\!\left(\frac{p}{1-p}\right)}
\deqn{\text{ilogit}(x) = \frac{1}{1 + e^{-x}}}

so that \code{ilogit(logit(p)) == p} and \code{logit(ilogit(x)) == x} (up to
floating-point precision).
}
\examples{
logit(0.5)   # 0
logit(0.9)   # ~2.197

ilogit(0)    # 0.5
ilogit(2)    # ~0.880

# Round-trip
p <- c(0.1, 0.5, 0.9)
all.equal(p, ilogit(logit(p)))  # TRUE

}
