% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-spatial.R
\name{PlotSpatial}
\alias{PlotSpatial}
\title{Plot Spatial Structure}
\usage{
PlotSpatial(object, Stocks = NULL, byStock = NULL, Age = NULL, Year = NULL)
}
\arguments{
\item{object}{A \link[=hist-class]{hist} or \link[=mse-class]{mse} object.}

\item{Stocks}{Character or numeric vector restricting the plot to
specific stocks, either by name (matching \code{\link[=StockNames]{StockNames()}}) or by index.
Default \code{NULL} includes every stock in \code{object}.}

\item{byStock}{Logical. Facet the panels by stock? Default \code{NULL} facets
automatically when more than one selected stock has more than one
area.}

\item{Age}{The age class at which to evaluate \code{Movement}/\code{UnfishedDist},
matched against the age dimension of those arrays. Default \code{NULL} uses
each stock's first age class.}

\item{Year}{The calendar year at which to evaluate \code{Movement}/
\code{UnfishedDist}, matched against the year dimension of those arrays.
Default \code{NULL} uses the last historical year.}
}
\value{
A \code{patchwork} object combining the movement-matrix heatmap and
the equilibrium area-distribution bar chart. Invisibly returns \code{NULL}
(with a message) if no selected stock has more than one area.
}
\description{
Visualizes the spatial structure of one or more stocks: the area-to-area
movement probability matrix (as a heatmap) and the equilibrium
(unfished) distribution of biomass across areas (as a bar chart), side
by side.
}
\details{
Both panels are evaluated at a single age class and calendar year, since
the underlying \code{Movement} and \code{UnfishedDist} arrays can vary by age and
year. Simulation replicates are averaged. Stocks with only one area have
no spatial structure to show and are skipped with a message.
}
\examples{
\dontrun{
Hist <- Simulate(SeasonalSpatialOM)
PlotSpatial(Hist)
PlotSpatial(Hist, Stocks = 1, Age = 5)
}

}
\seealso{
\code{\link[=Movement]{Movement()}}, \code{\link[=UnfishedDist]{UnfishedDist()}}, \code{\link[=Populate]{Populate()}}
}
