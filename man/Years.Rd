% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-years.R
\name{Years}
\alias{Years}
\alias{CalcYears}
\title{Calculate or Extract Time Step Values}
\usage{
CalcYears(nYear, pYear, CurrentYear, Seasons = 1, Period = NULL)

Years(x, Period = NULL)
}
\arguments{
\item{nYear}{Integer. Number of historical time steps.}

\item{pYear}{Integer. Number of projection time steps.}

\item{CurrentYear}{Integer. Final historical year (or index if
\code{CurrentYear < 1900}).}

\item{Seasons}{Integer. Number of seasons per year. Default \code{1} (annual).}

\item{Period}{Character or \code{NULL}. Controls which time steps are returned.
See Details.}

\item{x}{An \link[=om-class]{om}, \link[=hist-class]{hist}, or \link[=mse-class]{mse} object, or a list of
such objects. (\code{Years()} only.)}
}
\value{
A numeric vector of time step values, or for \code{Years()} applied to
a list, a list of such vectors.
}
\description{
Two related functions for working with historical and projection time steps:
}
\details{
\itemize{
\item \code{CalcYears()} generates a numeric vector of time step values from
explicit parameters.
\item \code{Years()} extracts time step values from an \link[=om-class]{om}, \link[=hist-class]{hist}, or
\link[=mse-class]{mse} object, or a list of such objects, by delegating to
\code{CalcYears()}.
}

The historical period spans \code{nYear} time steps ending at \code{CurrentYear}.
The projection period spans \code{pYear} time steps beginning after
\code{CurrentYear}. If \code{pYear = 0} or \code{Period = "H"}, only historical values
are returned.

For sub-annual seasons (\code{Seasons > 1}), time steps are expressed as
decimal dates (e.g. \code{2020.5} for mid-2020) using
\code{\link[lubridate:decimal_date]{lubridate::decimal_date()}}. Supported resolutions are half-year, quarter,
month, week, and day, as determined by \code{\link[=CalcTSUnits]{CalcTSUnits()}}.

When \code{CurrentYear < 1900}, a non-calendar index is used and years are
counted backwards from \code{CurrentYear} rather than mapped to real calendar
years.

\code{Period} accepts partial matches to \code{"Historical"} or \code{"Projection"} (e.g.
\code{"H"}, \code{"Hist"}, \code{"Proj"}). Matching is case-insensitive. \code{NULL} (default)
returns all time steps.
}
\examples{
# Annual time steps
CalcYears(nYear=20, pYear=10, CurrentYear=2026)

# Semi-annual time steps
CalcYears(nYear=20, pYear=10, CurrentYear=2026, Seasons=2)

# Historical period only
CalcYears(nYear=20, pYear=10, CurrentYear=2026, Period="H")

}
\seealso{
\code{\link[=CalcTSUnits]{CalcTSUnits()}}, \link[=om-class]{om}
}
