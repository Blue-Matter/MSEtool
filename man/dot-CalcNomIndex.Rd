% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-nominal.R
\name{.CalcNomIndex}
\alias{.CalcNomIndex}
\title{Calculate the nominal (selectivity-weighted) fishery index}
\usage{
.CalcNomIndex(
  Number_List,
  object,
  stocks,
  fleet,
  IndexObs,
  Years,
  SelectivityAtAge = NULL,
  sim = NULL,
  timing = NA_real_,
  TSIndex = NULL,
  apply_timing = TRUE,
  Units = NULL,
  recruitment_years = NULL,
  on_missing_obs_sel = NULL
)
}
\arguments{
\item{Number_List}{Named list of numbers-at-age arrays per stock. Each
element is either \verb{[Sim x Age x Year x Area]}, \verb{[Age x Year x Area]}, or
\verb{[Age x Area]} (single year / simulation slice).}

\item{object}{A \code{hist}-like object (\code{Hist}, \code{Proj}, or \code{mse}).}

\item{stocks}{Integer vector of stock indices in the complex.}

\item{fleet}{Character fleet / index name.}

\item{IndexObs}{An \link[=indicesobs-class]{indicesobs} object.}

\item{Years}{Numeric calendar year(s) to include.}

\item{SelectivityAtAge}{Optional selectivity specification passed to
\code{\link[=.ResolveIndexSelectivity]{.ResolveIndexSelectivity()}}. When \code{NULL}, fleet selectivity is used.}

\item{sim}{Optional simulation replicate index.}

\item{timing}{Numeric fraction of the time step at which the index is
observed, for within-step mortality decay. \code{NA} skips decay.}

\item{TSIndex}{Optional integer time-step index(es) matching \code{Years} in the
population arrays. When \code{NULL}, matched from array dimnames.}

\item{apply_timing}{Logical. Apply timing decay? Default \code{TRUE}.}

\item{Units}{Index units; defaults to \code{IndexObs@Units} or \code{"Biomass"}.}

\item{recruitment_years}{Optional calendar years for seasonal recruitment
indexing (see \code{\link[=.ApplyIndexUnits]{.ApplyIndexUnits()}}).}

\item{on_missing_obs_sel}{Optional callback when \code{"Obs"} selectivity is missing.}
}
\value{
Numeric matrix (\verb{nSim x nYear}) when \code{sim} is \code{NULL}; numeric vector
of length \code{nYear} (or scalar when \code{Years} is length 1) when \code{sim} is set.
}
\description{
Shared core for conditioning (\code{.ConditionObsIndex()}), historical and
projected data generation (\code{.GenHistDataIndices()}, \code{.GenProjDataIndex()}),
and index-fit diagnostics (\code{\link[=PlotIndexFit]{PlotIndexFit()}}).
}
\details{
Computes the selectivity-weighted, area-restricted population index that
\code{Beta} and \code{Efficiency} are calibrated against:
\code{Observed = Efficiency * NomIndex^Beta * Error}.
}
\keyword{internal}
