% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-spatial-movement.R
\name{FitMovement_Multi_Area}
\alias{FitMovement_Multi_Area}
\title{Fit a Multi-Area Movement Matrix}
\usage{
FitMovement_Multi_Area(Spatial, sim = 1, age = 1, year = 1)
}
\arguments{
\item{Spatial}{A \link[=spatial-class]{spatial} object with populated \code{UnfishedDist},
\code{ProbStaying}, and \code{FracOther} slots (named arrays).}

\item{sim}{\code{integer(1)}. Simulation index. Default \code{1}.}

\item{age}{\code{integer(1)}. Age index. Default \code{1}.}

\item{year}{\code{integer(1)}. Year index. Default \code{1}.}
}
\value{
An \verb{nArea × nArea} numeric matrix with rows summing to 1.
}
\description{
Fits an \verb{nArea × nArea} row-stochastic movement matrix for a single
simulation, age, and year by minimising a penalised negative log-likelihood
objective. The objective simultaneously penalises deviations of the implied
asymptotic distribution from \code{UnfishedDist} (log scale, penalty \code{CVDist})
and deviations of the diagonal (staying probabilities) from \code{ProbStaying}
(logit scale, penalty \code{CVStay}). See \code{\link[=SolveMovement_Multi_Area]{SolveMovement_Multi_Area()}} for the
objective details.
}
\details{
Optimisation uses \code{\link[stats:nlminb]{stats::nlminb()}} initialised at \code{rep(0, nArea)} in
logit space (corresponding to staying probabilities of 0.5). Dimension
indices for each slot are clamped to the slot's array size, so singleton
dimensions are treated as constant.

The relative movement structure among areas is fixed by \code{FracOther} and
passed to \code{\link[=.MarkovFrac]{.MarkovFrac()}} to construct the full matrix at each iteration.
}
\seealso{
\code{\link[=FitMovement]{FitMovement()}}, \code{\link[=FitMovement_2_Area]{FitMovement_2_Area()}}, \code{\link[=SolveMovement_Multi_Area]{SolveMovement_Multi_Area()}},
\code{\link[=.MarkovFrac]{.MarkovFrac()}}, \code{\link[=CalcAsymDist]{CalcAsymDist()}}
}
\keyword{internal}
