% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-seasonal-allocation.R
\name{.CheckSeasonalAllocation}
\alias{.CheckSeasonalAllocation}
\title{Check and validate seasonal TAC/Effort allocation for a \code{hist} object}
\usage{
.CheckSeasonalAllocation(Hist)
}
\arguments{
\item{Hist}{A \code{hist} object containing operating model data.}
}
\value{
The input \code{hist} object with \code{hist@OM@HistoricalWeight} and
\code{hist@OM@SeasonalAllocation} populated and validated for all complexes.

For each complex, \code{SeasonalAllocation} is resolved in this order:
\enumerate{
\item Use \code{OM@SeasonalAllocation} if specified.
\item Derive from \code{OM@HistoricalWeight[f]*historical_share[s,f] + (1-OM@HistoricalWeight[f])*abundance_share[s]}, pooled over the last
\code{OM@Control$SeasonalAllocationYears} years (default 5).
}
}
\description{
Validates the \code{SeasonalAllocation} slot in the operating model (OM) of a
\code{hist} object. If unspecified, derives it from \code{HistoricalWeight}, blending
the historical seasonal removals pattern with the population biomass
seasonal pattern. Returns the updated \code{hist} object with fully-specified,
validated \code{HistoricalWeight} and \code{SeasonalAllocation} lists.
}
\keyword{internal}
