% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-calc-ageclasses.R
\name{CalcAgeClasses}
\alias{CalcAgeClasses}
\title{Calculate Age Classes}
\usage{
CalcAgeClasses(Ages)
}
\arguments{
\item{Ages}{An \code{\link[=Ages]{Ages()}} object with slots \code{MinAge}, \code{MaxAge}, and \code{Units}
set.}
}
\value{
A numeric vector of age classes in years, or \code{NULL} if any of
\code{Ages@Units}, \code{Ages@MinAge}, or \code{Ages@MaxAge} are empty.
}
\description{
Computes the numeric vector of age classes (in years) from an \code{\link[=Ages]{Ages()}}
object, respecting sub-annual time steps.
}
\details{
Age classes are always expressed in years, regardless of the time unit
used to define \code{MinAge} and \code{MaxAge}. \code{Units} controls the within-year
resolution via \code{CalcSeasons()}, which maps unit strings to a seasons
divisor:\tabular{lll}{
   \code{Units} \tab Seasons \tab Step size (yr) \cr
   \code{"year"} \tab 1 \tab 1 \cr
   \code{"half-year"} \tab 2 \tab 0.5 \cr
   \code{"quarter"} \tab 4 \tab 0.25 \cr
   \code{"month"} \tab 12 \tab 0.0833 \cr
   \code{"week"} \tab 52 \tab 0.0192 \cr
}


The sequence runs from \code{MinAge / Seasons} to \code{MaxAge / Seasons} in steps
of \code{1 / Seasons}, rounded to three decimal places.

Returns \code{NULL} if \code{Units}, \code{MinAge}, or \code{MaxAge} are not set.
}
\examples{
# Annual age classes 0–5
CalcAgeClasses(Ages(MinAge = 0, MaxAge = 5, Units = "year"))
#> [1] 0 1 2 3 4 5

# Quarterly age classes 0–1
CalcAgeClasses(Ages(MinAge = 0, MaxAge = 4, Units = "quarter"))
#> [1] 0.00 0.25 0.50 0.75 1.00

}
\seealso{
\code{\link[=Ages]{Ages()}}, \link[=ages-class]{ages}, \code{\link[=PopulateStock]{PopulateStock()}}
}
