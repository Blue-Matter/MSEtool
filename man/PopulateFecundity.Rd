% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-fecundity.R
\name{PopulateFecundity}
\alias{PopulateFecundity}
\title{Populate a Fecundity Object}
\usage{
PopulateFecundity(
  Fecundity,
  Ages = NULL,
  Length = NULL,
  Weight = NULL,
  Maturity = NULL,
  Years = NULL,
  nSim = NULL,
  seed = NULL,
  silent = FALSE,
  CalcAtLength = FALSE,
  force = FALSE
)
}
\arguments{
\item{Fecundity}{A \code{\link[=Fecundity]{Fecundity()}} object to populate.}

\item{Ages}{An \code{\link[=Ages]{Ages()}} object defining age classes.}

\item{Length}{A \code{\link[=Length]{Length()}} object. Required if the fecundity model depends on
length-at-age.}

\item{Weight}{A \code{\link[=Weight]{Weight()}} object. Required if the fecundity model depends on
weight-at-age.}

\item{Maturity}{A \code{\link[=Maturity]{Maturity()}} object. Required to calculate fecundity as egg
production of mature individuals.}

\item{Years}{Numeric vector of model years.}

\item{nSim}{Integer. Number of simulation replicates.}

\item{seed}{Integer. Random seed used for stochastic generation.}

\item{silent}{Logical. If \code{TRUE}, suppress informational messages.}

\item{CalcAtLength}{Logical. If \code{TRUE}, calculate fecundity-at-length values
from age-based arrays.}

\item{force}{Logical. If \code{TRUE}, force re-population even if the object
digest is unchanged.}
}
\value{
A populated \code{\link[=Fecundity]{Fecundity()}} object.
}
\description{
Populate a \code{Fecundity} object by generating mean-at-age values, adding
stochastic deviations, and optionally calculating fecundity-at-length. Handles
models that depend on age, length, or weight.
}
\details{
\code{PopulateFecundity()} handles population of mean fecundity-at-age for a stock.
Steps include:
\itemize{
\item Structuring model parameters across \code{nSim} and \code{Years}
\item Resolving the fecundity model class
\item Generating mean fecundity at age, or at length if applicable
\item Converting between mean-at-length and mean-at-age
\item Multiplying by maturity to represent egg production of mature individuals
\item Adding stochastic variation via \code{PopulateRandom()}
}
}
\examples{
\dontrun{
# Assuming `Ages`, `Length`, `Weight`, and `Maturity` objects exist
F <- Fecundity()
F_pop <- PopulateFecundity(
  F,
  Ages = Ages,
  Length = Length,
  Weight = Weight,
  Maturity = Maturity,
  Years = 2000:2025,
  nSim = 5,
  seed = 123,
  CalcAtLength = TRUE
)
}

}
\seealso{
\code{\link[=Populate]{Populate()}}, \code{\link[=PopulateStock]{PopulateStock()}}, \code{\link[=PopulateLength]{PopulateLength()}}, \code{\link[=PopulateWeight]{PopulateWeight()}},
\code{\link[=PopulateMaturity]{PopulateMaturity()}}
}
