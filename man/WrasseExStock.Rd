% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-stock.R
\docType{data}
\name{WrasseExStock}
\alias{WrasseExStock}
\alias{WrasseFemaleExStock}
\alias{WrasseMaleExStock}
\title{Wrasse Example Stocks (Female / Male)}
\format{
Each is a \link[=stock-class]{stock} object with the following slots
populated:
\itemize{
\item \code{Name}: \code{"Example Wrasse Female Stock"} / \code{"Example Wrasse Male Stock"}.
\item \code{CommonName}: \code{"Wrasse"}.
\item \code{Species}: \code{"Thalassoma spp."}.
\item \code{Ages}: An \code{\link[=Ages]{Ages()}} object with \code{MaxAge = 15}, identical for both
stocks - required for the \code{\link[=Herm]{Herm()}} transition, which reclassifies
individuals age-for-age between the two stocks.
\item \code{Length}: A \code{\link[=Length]{Length()}} object with von Bertalanffy parameters. Terminal
males grow larger than females (\code{Linf} 30-35 cm vs 23-27 cm), typical
of this style of protogynous species; \code{K}/\code{t0} otherwise similar.
\item \code{Weight}, \code{Maturity}: \code{\link[=Weight]{Weight()}}/\code{\link[=Maturity]{Maturity()}} objects; males mature
at a larger size than females, consistent with their larger size-at-age.
\item \code{NaturalMortality}: A \code{\link[=NaturalMortality]{NaturalMortality()}} object; males have somewhat
lower \code{M} (0.2-0.3 yr^-1^) than females (0.3-0.4 yr^-1^).
\item \code{SRR}: A \code{\link[=SRR]{SRR()}} object. The female is self-recruiting (\code{SPFrom}
unset) with \code{R0 = 5000}. The male's \code{SPFrom = "Example Wrasse Female Stock"} sources its spawning production (\code{SP}/\code{SP0}) from the female stock. #'
\item \code{Spatial}, \code{Depletion}: matching \code{\link[=Spatial]{Spatial()}}/\code{\link[=Depletion]{Depletion()}} objects for
both stocks.
}

An object of class \code{stock} of length 1.
}
\usage{
WrasseFemaleExStock

WrasseMaleExStock
}
\description{
A pair of example \link[=stock-class]{stock} objects representing the female and
(terminal) male phase of a protogynous hermaphrodite wrasse (\emph{Thalassoma}
spp.), paired via \link{HermOM}'s \code{\link[=Herm]{Herm()}} transition and the male's
\code{SRR@SPFrom}. Not intended to represent any specific stock; illustrates
\code{\link[=SRR]{SRR()}}'s \code{SPFrom} cross-stock spawning-production sourcing and
\code{\link[=Herm]{Herm()}}'s age-dependent stock transition together. See \link{HermOM} for the
full worked example and how the two stocks and the transition are
combined.
}
\examples{
WrasseFemaleExStock
WrasseMaleExStock

}
\seealso{
\link{HermOM} for the full worked example (including the \code{\link[=Herm]{Herm()}}
transition curve linking these two stocks), \code{\link[=SRR]{SRR()}} for \code{SPFrom},
\link[=stocktransition-class]{stocktransition} for the transition mechanism, \link{AlbacoreExStock}
for a single-stock example with the full per-slot \verb{Populate*()} walk-through.
}
\keyword{datasets}
