% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{CalcPerRecruitFScalarSeasonalCpp_}
\alias{CalcPerRecruitFScalarSeasonalCpp_}
\title{Internal C++ per-recruit calculation (seasonal, parallel implementation)}
\usage{
CalcPerRecruitFScalarSeasonalCpp_(
  apicalF,
  StockFleetAllocationList,
  NaturalMortalityList,
  PlusGroupVec,
  MaturityList,
  SemelparousList,
  WeightList,
  SpawnTimeFracVec,
  FecundityList,
  WeightFleetRetainedList,
  WeightFleetSelectedList,
  SelectivityFleetList,
  RetentionFleetList,
  DiscardMortalityFleetList,
  SeasonalWeightsList,
  NPR0_noList,
  NPR0_spList,
  RefSeasonWeights,
  SPFromVec,
  IsSpawnTimeFrac,
  nSeason
)
}
\arguments{
\item{apicalF}{Scalar apical fishing mortality.}

\item{StockFleetAllocationList}{Named list (one per stock) of
nSeason x nFleet \code{NumericMatrix} -- fraction of apicalF allocated to
each fleet in each season, per stock.}

\item{NaturalMortalityList, MaturityList, SemelparousList, WeightList, FecundityList, NPR0_noList, NPR0_spList}{Named lists (one per stock) of nAge x nSeason \code{NumericMatrix}.}

\item{PlusGroupVec, SpawnTimeFracVec}{Per-stock scalars (length nStock).}

\item{WeightFleetRetainedList, WeightFleetSelectedList, SelectivityFleetList, RetentionFleetList, DiscardMortalityFleetList}{Named lists (one per stock) of nAge x (nSeason*nFleet) \code{NumericMatrix}
(column index \code{season + nSeason*fleet}, 0-indexed).}

\item{SeasonalWeightsList}{Named list (one per stock) of length-nSeason
\code{NumericVector} (\code{pi_s}, seasonal recruitment weights).}

\item{RefSeasonWeights}{Length-nSeason \code{NumericVector} (search-invariant,
precomputed once per calendar year).}

\item{SPFromVec}{Integer, 1-based stock index (length nStock) -- which
stock's spawning production each stock's SPR is computed from.}

\item{IsSpawnTimeFrac}{Logical. Whether any stock has a non-zero spawn-time fraction.}

\item{nSeason}{Integer number of seasons per calendar year.}
}
\value{
A named \code{List}: per-stock (length nStock) \code{NumericVector}s NPR0,
NPRF, NPR0_SP, NPRF_SP, SPR0f (unfished SP-per-recruit), SPRFf (fished
SP-per-recruit), Biomass, SBiomass, SProduction, Landings, Discards,
Removals, plus a scalar \code{F_annual_apical}.
}
\description{
Computes the apicalF-dependent seasonal per-recruit quantities for one
sim/calendar-year, mirroring \code{.CalcPerRecruitFScalarSeasonal()}
(\code{R/calc-per-recruit.R}). NPR0/NPR0_SP and RefSeasonWeights (both
F-invariant) are precomputed in R and passed in.
}
