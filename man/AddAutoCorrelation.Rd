% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-add-autocorrelation.R
\name{AddAutoCorrelation}
\alias{AddAutoCorrelation}
\title{Add Auto-Correlation to a Vector of Values}
\usage{
AddAutoCorrelation(values, ac, last_value)
}
\arguments{
\item{values}{\code{numeric} vector. Independent values to be transformed with
auto-correlation (e.g., log recruitment deviations).}

\item{ac}{\code{numeric} scalar. Auto-correlation coefficient, typically in
[-1, 1].}

\item{last_value}{\code{numeric} scalar. The last observed value from the
preceding time period, used to condition the first element of \code{values}.

The AR(1) recursion applied is:

\deqn{x_t = \rho \cdot x_{t-1} + \epsilon_t \cdot \sqrt{1 - \rho^2}}

where \eqn{\rho} is \code{ac} and \eqn{\epsilon_t} are the input \code{values}. The
\eqn{\sqrt{1 - \rho^2}} scaling ensures the marginal variance of \eqn{x_t}
equals the variance of \eqn{\epsilon_t}.}
}
\value{
A \code{numeric} vector of the same length as \code{values} with AR(1)
auto-correlation applied.
}
\description{
Applies an AR(1) auto-correlation structure to a vector of independent
values, conditioning on a provided last observed value. The transformation
scales each value to preserve the marginal variance under the AR(1) process.
}
\seealso{
\code{\link[=GenMultiStockRecDevs]{GenMultiStockRecDevs()}}
}
