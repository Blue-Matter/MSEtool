% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-imp.R
\name{PopulateImpSlot}
\alias{PopulateImpSlot}
\title{Populate an ImpSlot Object}
\usage{
PopulateImpSlot(ImpSlot, nSim, Years)
}
\arguments{
\item{ImpSlot}{An \link[=impslot-class]{impslot} object.}

\item{nSim}{Integer. Number of simulation replicates.}

\item{Years}{Integer vector. Calendar years (historical and projection).}
}
\value{
A populated \link[=impslot-class]{impslot} object.
}
\description{
Expands \code{Mean} and \code{SD} to named \verb{[Sim]} arrays, \code{Compliance} to a named
\verb{[Sim x Year]} array, and generates a stochastic lognormal \code{Error} array
from \code{Mean}/\code{SD} if not supplied directly. Mirrors \code{\link[=PopulateCatchObs]{PopulateCatchObs()}}'s
\code{CV}/\code{Bias}/\code{Error} handling, applied to \code{Mean}/\code{SD} instead. Left
untouched (all slots \code{NULL}) if the input object is empty -- see
\code{\link[=EmptyObject]{EmptyObject()}} -- so that the (common) case of an unconfigured
implementation model incurs no extra computation.
}
\details{
\code{Compliance} is expanded to a \verb{[Sim x Year]} array via
\code{.StructurePars()}, using the same conventions as every other structured
OM parameter: a scalar is a fixed value for every simulation and year; a
length-2 vector is \code{c(lower, upper)} bounds sampled once per simulation
(held constant across years); a full array is respected as-is, with named
\code{Year} dimnames treated as breakpoints and forward-filled to cover all of
\code{Years} (see \code{\link[=Extend]{Extend()}}/\code{\link[=ExtendYears]{ExtendYears()}}).
}
\seealso{
\code{\link[=PopulateImp]{PopulateImp()}}, \link[=impslot-class]{impslot}, \code{\link[=ImpSlot]{ImpSlot()}}
}
