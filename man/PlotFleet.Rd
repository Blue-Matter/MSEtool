% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-fleet.R
\name{PlotFleet}
\alias{PlotFleet}
\title{Plot All Fishery Schedules for a Fleet}
\usage{
PlotFleet(
  Fleet,
  Stock = NULL,
  byStock = NULL,
  byFleet = NULL,
  Stocks = NULL,
  Years = NULL,
  probs = c(0.05, 0.95),
  silent = FALSE
)
}
\arguments{
\item{Fleet}{A \link[=fleet-class]{fleet} object, or an \link[=om-class]{om}, \link[=hist-class]{hist}, or
\link[=mse-class]{mse} object.}

\item{Stock}{A \link[=stock-class]{stock} object supplying the biology needed to
populate a bare \link[=fleet-class]{fleet} \code{Fleet}. Ignored otherwise. Default
\code{NULL} uses an example stock, with a message noting this.}

\item{byStock}{Logical. Facet by stock? Passed to each \verb{Plot*()}
function. Default \code{NULL} facets automatically when \code{Fleet} has more
than one stock.}

\item{byFleet}{Logical. Facet by fleet? Passed to each \verb{Plot*()}
function. Default \code{NULL} facets automatically when \code{Fleet} has more
than one fleet.}

\item{Stocks}{Character or numeric vector. Restrict the plots to
specific stocks, either by name (matching \code{\link[=StockNames]{StockNames()}}) or by
index. Default \code{NULL} (all stocks).}

\item{Years}{Optional numeric vector, or \code{"all"}. Default \code{NULL} plots
every available year.}

\item{probs}{Numeric vector of length 2. Lower/upper quantiles for
simulation envelopes. Default \code{c(0.05, 0.95)}.}

\item{silent}{Logical. If \code{TRUE}, the returned list doesn't announce what
it contains when printed at the console (see \code{\link[=print.PlotFleetList]{print.PlotFleetList()}}).
Default \code{FALSE}.}
}
\value{
A named list of \code{ggplot}/\code{patchwork} objects: \code{Selectivity},
\code{Retention}, \code{DiscardMortality}, \code{Effort}, \code{Catchability}, \code{Closure}
(omitted when no closure is specified).
}
\description{
Runs every fleet-related \verb{Plot*()} function against a \link[=fleet-class]{fleet}
object: \code{\link[=PlotSelectivity]{PlotSelectivity()}}, \code{\link[=PlotRetention]{PlotRetention()}}, \code{\link[=PlotDiscardMortality]{PlotDiscardMortality()}},
\code{\link[=PlotCatchability]{PlotCatchability()}}, and \code{\link[=PlotClosure]{PlotClosure()}} (omitted when no
closure is specified).
}
\seealso{
\code{\link[=Fleet]{Fleet()}}, \code{\link[=PlotSelectivity]{PlotSelectivity()}}, \code{\link[=PlotRetention]{PlotRetention()}},
\code{\link[=PlotDiscardMortality]{PlotDiscardMortality()}}, \code{\link[=PlotEffort]{PlotEffort()}}, \code{\link[=PlotCatchability]{PlotCatchability()}},
\code{\link[=PlotClosure]{PlotClosure()}}
}
