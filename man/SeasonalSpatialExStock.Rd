% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-stock-seasonalspatial.R
\docType{data}
\name{SeasonalSpatialExStock}
\alias{SeasonalSpatialExStock}
\title{Seasonal Spatial Example Stock}
\format{
A \link[=stock-class]{stock} object with the following slots populated:
\itemize{
\item \code{Name}: \code{"Example Seasonal Spatial Stock"}.
\item \code{CommonName}: \code{"Reef Fish"}.
\item \code{Species}: \code{"Example species"}.
\item \code{Ages}: An \code{\link[=Ages]{Ages()}} object with \code{MaxAge = 36} months (\code{Units = "month"}),
giving 37 monthly age classes expressed in years (0, 1/12, 2/12, …, 3).
\item \code{Length}: A \code{\link[=Length]{Length()}} object with fixed von Bertalanffy parameters —
\code{Linf = 30} cm, \code{K = 0.8} yr^-1^, \code{t0 = -0.1} yr — and
\code{CVatAge = 0.1}.
\item \code{Weight}: A \code{\link[=Weight]{Weight()}} object with allometric parameters
\code{alpha = 1e-05} and \code{beta = 3}.
\item \code{NaturalMortality}: A \code{\link[=NaturalMortality]{NaturalMortality()}} object with fixed
\code{M = 0.09} per month (~1.1 yr^-1^). This is a per-time-step rate and must
match \code{Ages@Units = "month"} (see Details).
\item \code{Maturity}: A \code{\link[=Maturity]{Maturity()}} object with fixed logistic maturity-at-length
parameters \code{L50 = 15} cm and \code{L50_95 = 3} cm.
\item \code{Fecundity}: An empty \code{\link[=Fecundity]{Fecundity()}} object; fecundity-at-age is
computed internally during population (see Details).
\item \code{SRR}: A \code{\link[=SRR]{SRR()}} object with Beverton-Holt steepness \code{h = 0.7},
seasonal unfished recruitment from \code{\link[=SetSeasonalR0]{SetSeasonalR0()}} with an annual
pulse peaking in month 6 (\code{Sigma = 1.5} months), recruitment
variability \code{SD = 0.4}, and autocorrelation \code{AC = 0.3}.
\item \code{Spatial}: A \code{\link[=Spatial]{Spatial()}} object specifying a three-area ontogenetic
habitat shift (see Details).
\item \code{Depletion}: A \code{\link[=Depletion]{Depletion()}} object with terminal depletion of
35\% of \code{B0}.
\item \code{nYear}, \code{pYear}, \code{nSim}, \code{CurrentYear}, \code{Years}, \code{Seasons}: Set by
\code{\link[=PopulateStock]{PopulateStock()}}.
\item \code{Misc}: Empty list for user-defined information.
\item \code{Log}: Internal list for tracking object state.
}
}
\usage{
SeasonalSpatialExStock
}
\description{
An example \link[=stock-class]{stock} object representing a short-lived tropical reef fish
with monthly time steps, seasonal recruitment, and an ontogenetic
habitat shift across three spatial areas. All biological parameters are
fixed (no stochastic bounds). This object is intended to demonstrate
seasonal and spatial model features rather than reflect a real species.
}
\details{
\subsection{Monthly Time Steps}{

\code{Ages@Units = "month"} sets the within-year resolution to monthly time
steps. This stock should be paired with an \code{\link[=OM]{OM()}} with \code{Seasons = 12}.
\code{\link[=CalcAgeClasses]{CalcAgeClasses()}} produces 37 age classes from 0 to 3 years in steps of
1/12 year.
}

\subsection{Seasonal Recruitment}{

\code{\link[=SetSeasonalR0]{SetSeasonalR0()}} distributes annual unfished recruitment (\code{AnnualR0 = 1000}) across months using a Gaussian curve centred on month 6 with
standard deviation 1.5 months, creating a sharply seasonal pulse.
}

\subsection{Spatial .Structure}{

The three areas represent a linear inshore-to-offshore gradient:\tabular{clc}{
   Area \tab Habitat \tab Relative size \cr
   1 \tab Inshore lagoon / seagrass \tab 15\% \cr
   2 \tab Mid-shelf reef flat \tab 35\% \cr
   3 \tab Outer reef slope \tab 50\% \cr
}


The unfished biomass distribution shifts progressively from Area 1
(juveniles) to Area 3 (adults) with age, specified via a \verb{Sim × Area × Age} array of Gaussian curves centred at months 1, 18, and 36
respectively. Movement is constrained by adjacency: \code{FracOther}
connects Areas 1↔2 and 2↔3 freely, while Areas 1↔3 are weakly
connected (\code{FracOther = 0.02}). \code{\link[=PopulateSpatial]{PopulateSpatial()}} fits the movement
matrix to reproduce the target distribution at each age.
}

\subsection{Length}{

\code{\link[=PopulateLength]{PopulateLength()}} computes mean length-at-age from the fixed von
Bertalanffy parameters using the growth equation, then applies log-normal
variation scaled by \code{CVatAge = 0.1} to construct an age-length key.

See also \code{\link[=LengthModels]{LengthModels()}}.
}

\subsection{Weight}{

\code{\link[=PopulateWeight]{PopulateWeight()}} applies the allometric relationship
\emph{W} = \code{alpha} × \emph{L}^\code{beta}^ to mean length-at-age to produce
mean weight-at-age.

See also \code{\link[=WeightModels]{WeightModels()}}.
}

\subsection{Natural Mortality}{

\code{\link[=PopulateNaturalMortality]{PopulateNaturalMortality()}} expands the fixed \code{M} into a constant
natural mortality-at-age array. Stochastic annual deviates are added
via \code{.PopulateRandom()} using \code{SD} and \code{AC} from the \code{SRR} slot.

See also \code{\link[=NaturalMortalityModels]{NaturalMortalityModels()}}.
}

\subsection{Maturity}{

\code{\link[=PopulateMaturity]{PopulateMaturity()}} applies the logistic maturity-at-length curve,
parameterised by \code{L50} and \code{L50_95}, to the populated length-at-age
array to produce a maturity-at-age array.

See also \code{\link[=MaturityModels]{MaturityModels()}}.
}

\subsection{Fecundity}{

Because the \code{Fecundity} slot is empty, \code{\link[=PopulateFecundity]{PopulateFecundity()}} computes
fecundity-at-age as the element-wise product of weight-at-age and
maturity-at-age.

See also \code{\link[=FecundityModels]{FecundityModels()}}.
}

\subsection{Stock-Recruitment}{

\code{\link[=PopulateSRR]{PopulateSRR()}} uses the fixed steepness \code{h = 0.7} and generates a
time series of log-normal recruitment deviates with \code{SD = 0.4} and
autocorrelation \code{AC = 0.3}.

See also \code{\link[=SRRModels]{SRRModels()}}.
}

\subsection{Depletion}{

\code{\link[=PopulateDepletion]{PopulateDepletion()}} sets the terminal historical biomass to a fixed
35\% of \code{B0} across all simulations.
}
}
\examples{
SeasonalSpatialExStock

\dontrun{
PopulatedStock <- PopulateStock(
  SeasonalSpatialExStock,
  nYear = 20,
  pYear = 10,
  nSim  = 48,
  seed  = 42
)
}

}
\seealso{
\link{AlbacoreExStock}, \link{ButterfishExStock}, \code{\link[=Stock]{Stock()}},
\code{\link[=PopulateStock]{PopulateStock()}}, \code{\link[=Populate]{Populate()}}, \link[=stock-class]{stock}, \code{\link[=SetSeasonalR0]{SetSeasonalR0()}},
\code{\link[=Spatial]{Spatial()}}
}
\keyword{datasets}
