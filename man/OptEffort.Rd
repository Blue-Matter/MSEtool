% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt-effort-from-tac.R
\name{OptEffort}
\alias{OptEffort}
\title{Optimize Fleet Effort to Match TAC}
\usage{
OptEffort(
  Proj,
  Year,
  TSIndex,
  sim,
  stocks,
  TAC_by_Fleet,
  TACType,
  minEffort = 1e-15,
  tol = 0.01,
  maxIter = 200
)
}
\arguments{
\item{Proj}{\code{Hist} object containing projected effort and fishery dynamics}

\item{Year}{Numeric vector of projection years for this step}

\item{TSIndex}{Integer, time-step index corresponding to \code{Year}}

\item{sim}{Integer, simulation index}

\item{stocks}{Integer vector of stock indices to consider}

\item{TAC_by_Fleet}{Numeric vector of target TAC per fleet}

\item{TACType}{Character. Does the TAC refer to \code{"Removals"} (default) or \code{"Landings"}.}

\item{minEffort}{Small numeric to replace zero starting effort (default 1e-6)}

\item{tol}{Numeric, convergence tolerance for derivative solver (default 1e-2)}

\item{maxIter}{Integer, maximum iterations for derivative solver (default 50)}
}
\value{
Numeric vector of optimized effort per fleet
}
\description{
Calculates fleet-specific effort that achieves target removals (TAC) per
fleet. Fleets with \code{TAC == 0} are set to zero effort. Fleets with positive
TAC are solved using \code{optimize()} for a single fleet or Newton-Raphson with
a diagonal Jacobian approximation for multiple fleets, falling back to the
full Jacobian if the diagonal step fails to reduce the residual, and then to
\code{optim()} with BFGS if Newton-Raphson does not converge.
}
\keyword{internal}
