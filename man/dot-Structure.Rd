% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-structure.R
\name{.Structure}
\alias{.Structure}
\title{Reshape a Value into a Standard Simulation Array}
\usage{
.Structure(value, out = c("nSim", "nage", "nTS"), req = "nage")
}
\arguments{
\item{value}{A scalar, vector, or array.}

\item{out}{Character vector of dimension names in order. Default
\code{c('nSim', 'nage', 'nTS')}.}

\item{req}{Character scalar. The dimension name whose size should equal
\code{length(value)} when \code{value} is not already an array. Default \code{'nage'}.}
}
\value{
An array with \code{length(out)} dimensions.
}
\description{
Coerces \code{value} into an array with dimensions specified by \code{out}, setting
the size of the \code{req} dimension to \code{length(value)} and all others to 1.
If \code{value} is already an array of the correct rank it is returned
unchanged. Aborts if \code{value} has more dimensions than \code{out}.
}
\keyword{internal}
