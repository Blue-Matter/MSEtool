% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-obs-catch.R
\name{PopulateCatchObs}
\alias{PopulateCatchObs}
\title{Populate Catch Observation Error}
\usage{
PopulateCatchObs(Catch, nSim, HistYears, ProjYears)
}
\arguments{
\item{Catch}{A \link[=catchobs-class]{catchobs} object. Used for both landed catch
(\code{Obs@Landings}) and discarded catch (\code{Obs@Discards}).}

\item{nSim}{Integer. Number of simulation replicates.}

\item{HistYears}{Integer vector. Calendar years of the historical period.}

\item{ProjYears}{Integer vector. Calendar years of the projection period.}
}
\value{
A populated \link[=catchobs-class]{catchobs} object.
}
\description{
Populate a \link[=catchobs-class]{catchobs} object by expanding CV, generating stochastic
error arrays, applying multiplicative bias, and generating a stochastic
reference level, all across simulation replicates and years.
}
\details{
If \code{\link[=EmptyObject]{EmptyObject()}} returns \code{TRUE} for \code{Catch}, the object is returned
unchanged and no population is performed.

Otherwise the following slots are populated in order:
\itemize{
\item \code{CV}: expanded to a named \verb{[nSim]} array via \code{\link[=PopulateObsCV]{PopulateObsCV()}}.
\item \code{Error}: generated as a lognormal draw from \code{CV} across all historical
and projection years, producing a named \verb{[nSim x nYear]} array via
\code{\link[=PopulateObsError]{PopulateObsError()}}. If \code{Error} is already a fully-formed array it is
validated and dimension names are applied.
\item \code{Bias}: expanded to a named \verb{[nSim]} multiplicative bias array via
\code{\link[=PopulateObsBias]{PopulateObsBias()}}. Defaults to 1 (no bias) if unspecified.
\item \code{Ref}: generated as a lognormal draw from a CV, producing a named
\verb{[nSim]} reference level error multiplier array via \code{\link[=PopulateObsRef]{PopulateObsRef()}}.
}
}
\seealso{
\code{\link[=PopulateObs]{PopulateObs()}}, \link[=catchobs-class]{catchobs}, \code{\link[=PopulateObsCV]{PopulateObsCV()}},
\code{\link[=PopulateObsError]{PopulateObsError()}}, \code{\link[=PopulateObsBias]{PopulateObsBias()}}, \code{\link[=PopulateObsRef]{PopulateObsRef()}}
}
