% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-pm.R
\name{PM-equations}
\alias{PM-equations}
\title{Performance Metric Equations}
\description{
Mathematical definitions of the performance metrics computed by the
\link{PM} functions. Notation: \code{s} indexes simulation replicates, \code{y} indexes
the projection years in the evaluation window, and each metric is
computed per management procedure (MP).
}
\section{Status}{

\code{PM_FFMSY}: fishing mortality relative to \eqn{F_{MSY}}{FMSY},
\deqn{F_{s,y} / F_{MSY,s}}{F[s,y] / FMSY[s]}
with the probability metric \eqn{P(F/F_{MSY} < Ref)}{P(F/FMSY < Ref)}.

\code{PM_SBSBMSY} / \code{PM_SPSPMSY}: spawning biomass or spawning production
relative to its value at MSY,
\deqn{SB_{s,y} / SB_{MSY,s} \quad\text{or}\quad SP_{s,y} / SP_{MSY,s}}{SB[s,y] / SBMSY[s]  or  SP[s,y] / SPMSY[s]}
with the probability metric \eqn{P(SB/SB_{MSY} > Ref)}{P(SB/SBMSY > Ref)}
(or the SP equivalent).

\code{PM_Status}: the joint probability that a complex is neither overfished
nor experiencing overfishing,
\deqn{P\left(\frac{SB_{y}}{SB_{MSY}} > 1 \ \text{and}\ \frac{F_{y}}{F_{MSY}} < 1\right)}{P( SB/SBMSY > 1  and  F/FMSY < 1 )}
evaluated at the complex level, where \code{SB} is spawning biomass or spawning
production according to \code{Definition}.
}

\section{Safety}{

\code{PM_SBSBlim} / \code{PM_SPSPlim}: spawning biomass or spawning production
relative to MSY, relative in turn to a limit fraction \code{Lim} of \code{SBMSY}/
\code{SPMSY},
\deqn{\frac{SB_{s,y}/SB_{MSY,s}}{Lim_s} \quad\text{or}\quad \frac{SP_{s,y}/SP_{MSY,s}}{Lim_s}}{(SB[s,y]/SBMSY[s]) / Lim[s]  or  (SP[s,y]/SPMSY[s]) / Lim[s]}
with the probability metric \eqn{P(\frac{SB/SB_{MSY}}{Lim} > 1)}{P((SB/SBMSY)/Lim > 1)}
(or the SP equivalent) -- i.e. the probability that \code{SB/SBMSY} exceeds
\code{Lim}.

\code{PM_Safety}: the probability that the stock-status metric, relative to
MSY, never falls below the limit fraction \code{Lim} at any point during the
projection,
\deqn{P\left(\min_{y \in Y} \frac{SB_{s,y}}{SB_{MSY,s}} > Lim_s\right)}{P( min over y in Y of SB[s,y]/SBMSY[s] > Lim[s] )}
where \code{SB} is spawning biomass or spawning production according to
\code{Definition}.
}

\section{Rebuild}{

\code{PM_Rebuild}: for stocks overfished (\eqn{SB/SB_{MSY} < 1}{SB/SBMSY < 1})
at the end of the historical period, the probability that the stock has
rebuilt to a target multiple of \eqn{SB_{MSY}}{SBMSY} by a target year,
\deqn{P\left(\frac{SB_{s,\mathrm{Year}}}{SB_{MSY,s}} > \mathrm{Target}\right)}{P( SB[s,Year] / SBMSY[s] > Target )}
}

\section{Yield}{

\code{PM_Yield} / \code{PM_Removals} / \code{PM_Landings}: mean catch over the evaluation
window,
\deqn{\frac{1}{|Y|}\sum_{y \in Y} C_{s,y}}{(1/|Y|) * sum over y in Y of C[s,y]}
where \code{C} is removals (landings + discards), or landings only.

\code{PM_RelYield}: mean catch over the evaluation window, expressed relative
to MSY yield,
\deqn{\frac{1}{|Y|}\sum_{y \in Y} C_{s,y} \Big/ MSY_s}{[(1/|Y|) * sum over y in Y of C[s,y]] / MSY[s]}
with the probability metric \eqn{P(\mathrm{RelYield} > Ref)}{P(RelYield > Ref)}
when \code{Ref} is supplied.
}

\section{Stability}{

\code{PM_AAVY} / \code{PM_AAVE}: average annual variability in TAC, removals,
landings (per \code{Type}), or effort, across management intervals,
\deqn{\frac{1}{|Y|-1}\sum_{y \in Y \setminus \{y_1\}} \frac{|C_{s,y} - C_{s,y-1}|}{C_{s,y-1}}}{(1/(|Y|-1)) * sum over y in Y (excluding the first year) of |C[s,y] - C[s,y-1]| / C[s,y-1]}

\code{PM_Stability}: the probability that the series selected by \code{Type}
changes by less than a threshold amount between one management interval
and the next,
\deqn{P\left(\frac{|C_{s,y} - C_{s,y-1}|}{C_{s,y-1}} < \mathrm{Threshold}\right)}{P( |C[s,y] - C[s,y-1]| / C[s,y-1] < Threshold )}
evaluated per interval \code{y} and averaged across the evaluation window and
simulations.
}

\section{Stat, Prob, and Mean}{

Every \verb{PM_*} function returns a \link[=pm-class]{pm} object with three related
summaries: \code{Stat} is the raw metric above, computed per simulation (and
averaged over the evaluation window for the Yield-family metrics); \code{Prob}
is the per-simulation probability/indicator of meeting the stated
objective (\code{NA} for metrics with no such objective, e.g. \code{PM_Yield});
\code{Mean} is \code{Prob} averaged across simulations, or — for metrics with no
\code{Prob} — \code{Stat} averaged across simulations instead.
}

\seealso{
\link{PM} for the full list of performance metric functions and their
arguments.
}
