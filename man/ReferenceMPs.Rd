% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mp-reference.R
\name{ReferenceMPs}
\alias{ReferenceMPs}
\alias{refFMSY}
\alias{refFMSY75}
\alias{refFMSY50}
\alias{refMSY}
\alias{refMSY75}
\alias{refMSY50}
\alias{refFCurr}
\alias{NoFishing}
\title{Reference Management Procedures}
\usage{
refFMSY(Data)

refFMSY75(Data)

refFMSY50(Data)

refMSY(Data)

refMSY75(Data)

refMSY50(Data)

refFCurr(Data)

NoFishing(Data)

ReferenceMPs()
}
\arguments{
\item{Data}{A \link[=data-class]{data} object containing historical observations.}
}
\value{
An \link[=advice-class]{advice} object.
}
\description{
Diagnostic management procedures that fish at fixed fractions of \code{FMSY}
(effort-based) or \code{MSY} (TAC-based), using true operating-model reference
points.
}
\details{
\code{refFMSY}, \code{refFMSY75}, \code{refFMSY50} set effort so that realised F
equals 100\%, 75\%, or 50\% of \code{FMSY}, and default to being applied every
year (like any MP, this can be overridden via \code{OM@Interval}).

\code{refMSY}, \code{refMSY75}, \code{refMSY50} set TAC to 100\%, 75\%, or 50\% of
\code{MSY} (landings plus discards).

\code{refFCurr} fixes F at its last historical value. It is currently a wrapper
around \code{\link[=CurrentEffort]{CurrentEffort()}}, since \code{advice-class} has no way to specify
apical F directly. In spatial models, or where catchability (\code{q}) drifts
over time, realised F can diverge from the last historical effort. It will
be updated to set F directly once \code{advice-class} supports it.

\code{NoFishing} sets TAC to 0 every year

\code{FMSY}/\code{MSY} are computed once, during \code{Simulate()}, at the terminal
historical year (or \code{OM@Control$RefYears}) and held fixed for the whole
projection; i.e., they are not recalculated per projection year.

If reference points are not available for an \code{OM}  each reference MP returns
empty advice and a single warning is issued the first time it is called.

In spatial \code{OM}s, fleet targeting means the realised F from \verb{refFMSY*}
may differ from the specified fraction of \code{FMSY}, because the \code{FMSY}
calculation does not account for spatial structure.
}
\seealso{
\code{\link[=Advice]{Advice()}}, \link[=data-class]{data}, \link[=advice-class]{advice}, \code{\link[=CalcMSY]{CalcMSY()}}, \code{\link[=F_FMSY]{F_FMSY()}},
\code{\link[=CurrentEffort]{CurrentEffort()}}
}
