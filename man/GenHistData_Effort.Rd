% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-data-hist-effort.R
\name{GenHistData_Effort}
\alias{GenHistData_Effort}
\title{Generate Historical Fishing Effort Data}
\usage{
GenHistData_Effort(
  x,
  Data,
  Hist,
  HistYears,
  i,
  stocks,
  FleetNames,
  defaultCV = 0.2
)
}
\arguments{
\item{x}{Integer index of the simulation replicate to extract.}

\item{Data}{Existing \code{data} object from \code{OM@Data}}

\item{Hist}{A \code{hist} class object.}

\item{HistYears}{Numeric vector of historical years.}

\item{i}{Integer index of the stock complex.}

\item{stocks}{Integer vector of stock indices in the complex.}

\item{FleetNames}{Character vector of fleet names.}

\item{defaultCV}{Numeric default coefficient of variation applied
when no fleet-specific CV is provided. Default \code{0.2}.}
}
\value{
An \link[=effortdata-class]{effortdata} object with:
\itemize{
\item \verb{@Name}: character vector of fleet names
\item \verb{@Value}: \verb{[nYear x nFleet]} array of observed effort
\item \verb{@CV}:  \verb{[nYear x nFleet]} array initialised to \code{defaultCV}
\item \verb{@Units}: per-fleet units, from \code{\link[=EffortObs]{EffortObs()}} \verb{@Units} where defined,
otherwise \code{"unitless"}
}
}
\description{
Internal function to generate simulated historical fleet effort
when real observations are not available.
}
\details{
\subsection{Early Exit Conditions}{

The function returns \code{Data@Effort} unchanged in two cases:
\itemize{
\item \strong{Real data exist}: \code{Data@Effort} is already populated
(\code{!EmptyObject(Data@Effort)}).
\item \strong{No observation structure defined}: every fleet's \code{\link[=EffortObs]{EffortObs()}} object
is a default (unconditioned) object, as determined by \code{isNewObject()}.
}

Fleets without an \code{\link[=EffortObs]{EffortObs()}} object receive \code{NA} effort values; only
fleets with a configured \code{\link[=EffortObs]{EffortObs()}} have simulated effort generated.
}

\subsection{Observation Error Model}{

For each fleet with a non-empty \code{\link[=EffortObs]{EffortObs()}} object, observed effort
is generated as:

\deqn{\tilde{E}_{t} = E_{t} \cdot b \cdot \varepsilon_{t}}
\itemize{
\item \eqn{E_{t}} — true OM effort in year \eqn{t} (\code{Hist@Effort[x, t, fl]})
\item \eqn{b} — multiplicative bias for replicate \code{x} (\code{effortobs@Bias[x]});
see \code{\link[=EffortObs]{EffortObs()}}
\item \eqn{\varepsilon_{t}} — lognormal error multiplier for replicate \code{x},
year \eqn{t} (\code{effortobs@Error[x, t]}); see \code{\link[=EffortObs]{EffortObs()}}
}

The \code{Error} array is subset to \code{HistYears} only before multiplication,
dropping any years outside the historical period.
}

\subsection{CV Handling}{

CVs are initialised to \code{defaultCV} for all fleets and years. Fleet-specific
CVs from the \code{\link[=EffortObs]{EffortObs()}} object are not currently applied here;
\code{defaultCV} serves as the working assumption for downstream MPs.
}

\subsection{Obs Structure}{

Observation parameters are accessed via:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Hist@OM@Obs[[i]][[fl]]@Effort  # returns an effortobs object
}\if{html}{\out{</div>}}

where \code{i} is the stock complex index and \code{fl} the fleet index. The relevant
\code{\link[=EffortObs]{EffortObs()}} slots are:
\itemize{
\item \verb{@Bias[x]}: per-replicate multiplicative bias
\item \verb{@Error[x, t]}: per-replicate, per-year lognormal error multiplier
\item \verb{@Units}: effort units propagated to the output \link[=effortdata-class]{effortdata}
}

See \link[=obs-class]{obs} and \code{\link[=EffortObs]{EffortObs()}} for full slot documentation.
}
}
\seealso{
\code{\link[=EffortObs]{EffortObs()}}, \code{\link[=EffortData]{EffortData()}}, \link[=effortdata-class]{effortdata} \link[=obs-class]{obs}
}
\keyword{internal}
