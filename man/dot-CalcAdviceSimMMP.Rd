% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-advice.R
\name{.CalcAdviceSimMMP}
\alias{.CalcAdviceSimMMP}
\title{Calculate MP Advice for a Single Simulation, \code{mmp}-Class MPs}
\usage{
.CalcAdviceSimMMP(
  sim,
  MPName,
  MPfunction,
  DataList,
  Year,
  Proj,
  YearsProj,
  mp,
  FleetNames,
  Areas
)
}
\arguments{
\item{sim}{Simulation index (integer) identifying which simulation to run.}

\item{MPName}{Name of the management procedure (MP).}

\item{MPfunction}{Function object corresponding to \code{MPName}.}

\item{DataList}{List of stock/complex data objects for this simulation.}

\item{Year}{Numeric year for which advice is calculated.}

\item{Proj}{Projection object containing OM and control settings.}

\item{YearsProj}{Numeric vector of projected years (default = \code{YearsProj} from parent scope).

If \code{Proj@OM@Control$DataOM} is \code{TRUE} or a named list, population dynamics
information from the \code{Proj} will be included in \code{Data@Misc}
for each simulation.}
}
\value{
A list with elements \code{AdviceList} (a named list of \code{Advice}
objects, one per stock/complex) and \code{AggBagLimit} (a list of
\code{aggbaglimit} objects, or \code{NULL}).
}
\description{
Runs an \code{mmp}-class management procedure for a single simulation. Unlike
\code{.CalcAdviceSimMP()}, the MP function is called once, with \code{Data}
objects for every stock/complex passed together as \code{DataList}, so the MP
can coordinate advice across stocks (e.g. an aggregate bag limit).
}
\details{
The MP function must return either:
\itemize{
\item a bare named list of \code{Advice} objects (one per stock/complex, same
names as \code{DataList}) - i.e. everything an ordinary MP could express,
just computed jointly; or
\item \verb{list(Advice = <the same named list>, AggregateBagLimit = <a list of }AggregateBagLimit()\verb{ objects, or NULL>)} when at least one aggregate
bag limit is being declared.
}
}
\keyword{internal}
