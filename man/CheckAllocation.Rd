% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-allocation.R
\name{CheckAllocation}
\alias{CheckAllocation}
\title{Check and validate fleet allocation for a \code{hist} object}
\usage{
CheckAllocation(Hist)
}
\arguments{
\item{Hist}{A \code{hist} object containing operating model data.}
}
\value{
The input \code{hist} object with \code{hist@OM@Allocation} populated
and validated for all stocks.

For each stock, allocation is resolved in this order:
\enumerate{
\item Use \code{OM@Allocation} if specified.
\item Fall back to \code{OM@CatchFrac} if available.
\item Derive from the mean relative removals (landings + discards)
over the last five historical years.
}

After resolution, each allocation matrix is validated:
\itemize{
\item Dimensions must be \code{nSim} × \code{nFleet}.
\item All values must be finite and non-negative.
\item Each row must sum to 1 (within a tolerance of \code{sqrt(.Machine$double.eps)}).
}
}
\description{
Validates the \code{Allocation} slot in the operating model (OM) of a
\code{hist} object. If allocation is unspecified, falls back to \code{CatchFrac}
or derives allocation from the mean of the last five historical years.
Returns the updated \code{hist} object with a fully-specified, validated
\code{Allocation} list.
}
\keyword{internal}
