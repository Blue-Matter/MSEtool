% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt-effort-helpers.R
\name{.SolveEffortByComplex}
\alias{.SolveEffortByComplex}
\title{Effort at which each complex reaches its own TAC}
\usage{
.SolveEffortByComplex(
  Proj,
  Year,
  TSIndex,
  sim,
  TAC_by_Complex,
  TACType_by_Complex,
  TACUnit_by_Complex,
  MaxFleetEffort = NULL,
  Effort_start = NULL,
  ...
)
}
\arguments{
\item{Proj}{A \code{Proj} object, already sliced to \code{sim}.}

\item{Year}{Integer. Current projection year.}

\item{TSIndex}{Integer. Time-step index of \code{Year}.}

\item{sim}{Integer. Simulation index.}

\item{TAC_by_Complex}{Named list of per-fleet TAC vectors, \code{NULL} where a
complex has no TAC.}

\item{TACType_by_Complex, TACUnit_by_Complex}{Per-complex \code{"Removals"}/
\code{"Landings"} and \code{"Biomass"}/\code{"Number"} vectors.}

\item{MaxFleetEffort}{Numeric vector (length \code{nFleet}), or \code{NULL}.}

\item{Effort_start}{\verb{[nComplex x nFleet]} matrix of warm-start efforts, or
\code{NULL}. Each complex's root find starts from its own row; rows that are not
fully finite fall back to a cold start.}

\item{...}{Passed to \code{.OptEffortSinglestock()}.}
}
\value{
A \verb{[nComplex x nFleet]} matrix of efforts. Rows for complexes
without a TAC are \code{NA}, and are ignored when the efforts are combined.
}
\description{
Solves, for every complex with a TAC, the fleet effort at which that
complex's catch equals it. Catch is monotone in effort, so each solve is a
bracketed root find - the same operation \code{.OptEffortSinglestock()} performs,
applied to one complex at a time.
}
\keyword{internal}
