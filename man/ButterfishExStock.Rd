% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-stock-butterfish.R
\docType{data}
\name{ButterfishExStock}
\alias{ButterfishExStock}
\title{Butterfish Example Stock}
\format{
A \link[=stock-class]{stock} object with the following slots populated:
\itemize{
\item \code{Name}: \code{"ButterfishExStock"}.
\item \code{CommonName}: \code{"Butterfish"}.
\item \code{Species}: \code{"Peprilus triacanthus"}.
\item \code{Ages}: An \code{\link[=Ages]{Ages()}} object with \code{MaxAge = 8}.
\item \code{Length}: A \code{\link[=Length]{Length()}} object with von Bertalanffy parameters —
\code{Linf} (38–42 cm), \code{K} (0.16–0.24 yr^-1^), \code{t0} (-0.032 to -0.028 yr)
— and \code{CVatAge} (0.1–0.15).
\item \code{Weight}: A \code{\link[=Weight]{Weight()}} object with allometric parameters
\code{alpha = 1.59e-05} and \code{beta = 3.1}.
\item \code{NaturalMortality}: A \code{\link[=NaturalMortality]{NaturalMortality()}} object with
\code{M} = 0.7–0.9 yr^-1^.
\item \code{Maturity}: A \code{\link[=Maturity]{Maturity()}} object with logistic maturity-at-length
parameters \code{L50} (4.5–10.2 cm) and \code{L50_95} (1–8 cm). The wide
ranges reflect substantial uncertainty in maturity schedule.
\item \code{Fecundity}: An empty \code{\link[=Fecundity]{Fecundity()}} object; fecundity-at-age is
computed internally during population (see Details).
\item \code{SRR}: A \code{\link[=SRR]{SRR()}} object with Beverton-Holt steepness \code{h} (0.4–0.8),
unfished recruitment \code{R0 = 1000}, recruitment variability \code{SD} (0.7–1.1),
and autocorrelation \code{AC} (0.1–0.9). The high \code{SD} reflects the
characteristically variable recruitment of forage species.
\item \code{Spatial}: A \code{\link[=Spatial]{Spatial()}} object with \code{UnfishedDist} (0.095–0.105),
\code{ProbStaying} (0.4–0.6), and \code{RelativeSize} (0.095–0.105).
\item \code{Depletion}: A \code{\link[=Depletion]{Depletion()}} object with current biomass sampled from
5–60\% of \code{B0}.
\item \code{nYear}, \code{pYear}, \code{nSim}, \code{CurrentYear}, \code{Years}, \code{Seasons}: Set by
\code{\link[=PopulateStock]{PopulateStock()}}.
\item \code{Misc}: Empty list for user-defined information.
\item \code{Log}: Internal list for tracking object state.
}
}
\usage{
ButterfishExStock
}
\description{
An example \link[=stock-class]{stock} object representing a Butterfish
(\emph{Peprilus triacanthus}) stock. This is a short-lived, fast-growing
forage species with high natural mortality and wide uncertainty on
recruitment and maturity. See also \link{AlbacoreExStock} for a contrasting
long-lived, slow-growing example.
}
\details{
Two-element numeric vectors in the slot descriptions above are uniform
bounds. \code{\link[=PopulateStock]{PopulateStock()}} samples from these bounds across simulations and
expands the results into \verb{[nSim x nAge x nYear]} arrays. The sections below
describe what each component-level \verb{Populate*()} function produces.
\subsection{Ages}{

\code{\link[=CalcAgeClasses]{CalcAgeClasses()}} generates the age vector \code{Ages@Classes}
(\code{0:8}), which is used as the age dimension in all subsequent arrays.
}

\subsection{Length}{

\code{\link[=PopulateLength]{PopulateLength()}} samples \code{Linf}, \code{K}, and \code{t0} independently for each
simulation from uniform distributions defined by the bounds, then
computes mean length-at-age using the von Bertalanffy growth equation.
\code{CVatAge} is expanded across simulations and, when \code{ALK = TRUE}, an
age–length key is constructed.
}

\subsection{Weight}{

\code{\link[=PopulateWeight]{PopulateWeight()}} applies the allometric relationship
\emph{W} = \code{alpha} × \emph{L}^\code{beta}^ to the mean length-at-age array to produce
mean weight-at-age. An age–weight key is optionally constructed when
\code{AWK = TRUE}.
}

\subsection{Natural Mortality}{

\code{\link[=PopulateNaturalMortality]{PopulateNaturalMortality()}} samples \code{M} for each simulation from the
uniform prior and expands the result into a natural mortality-at-age array.
Stochastic annual deviates are added via \code{PopulateRandom()}. The high \code{M}
range (0.7–0.9 yr^-1^) is consistent with the short lifespan of this species.
}

\subsection{Maturity}{

\code{\link[=PopulateMaturity]{PopulateMaturity()}} applies the logistic maturity-at-length curve,
parameterised by \code{L50} and \code{L50_95}, to the populated length-at-age array
to produce a maturity-at-age array. The wide priors on both parameters
result in high inter-simulation variability in the maturity schedule.
}

\subsection{Fecundity}{

Because the \code{Fecundity} slot is empty, \code{\link[=PopulateFecundity]{PopulateFecundity()}} computes
fecundity-at-age as the element-wise product of weight-at-age and
maturity-at-age, representing spawning output per individual.
}

\subsection{Stock–Recruitment}{

\code{\link[=PopulateSRR]{PopulateSRR()}} samples \code{h} for each simulation from the uniform prior and
generates a time series of log-normal recruitment deviates with standard
deviation \code{SD} and autocorrelation \code{AC}. The high \code{SD} (0.7–1.1) produces
strongly variable recruitment, typical of forage fish.
}

\subsection{Spatial}{

\code{\link[=PopulateSpatial]{PopulateSpatial()}} samples \code{UnfishedDist}, \code{ProbStaying}, and
\code{RelativeSize} for each simulation and uses them to construct a full
inter-area movement matrix, then expands all spatial arrays across
simulations and years. The moderate \code{ProbStaying} (0.4–0.6) allows
substantial mixing between the two areas.
}

\subsection{Depletion}{

\code{\link[=PopulateDepletion]{PopulateDepletion()}} samples current biomass depletion relative to \code{B0}
for each simulation from the uniform prior, setting the initial conditions
of the operating model.
}
}
\examples{
ButterfishExStock

\dontrun{
PopulatedStock <- PopulateStock(
  ButterfishExStock,
  nYear = 50,
  pYear = 30,
  nSim = 48,
  seed = 42
)
}

}
\seealso{
\link{AlbacoreExStock}, \code{\link[=SeasonalSpatialExStock]{SeasonalSpatialExStock()}}, \code{\link[=Stock]{Stock()}},
\code{\link[=PopulateStock]{PopulateStock()}}, \code{\link[=Populate]{Populate()}}, \link[=stock-class]{stock}
}
\keyword{datasets}
