% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-slot-sizes.R
\name{S4SlotSizes}
\alias{S4SlotSizes}
\title{Summarise S4 Object Memory Usage by Slot}
\usage{
S4SlotSizes(obj, n = 5, verbose = TRUE)
}
\arguments{
\item{obj}{An S4 object.}

\item{n}{Integer. Number of largest slots to display in the printed summary.
Default is \code{5}.}

\item{verbose}{Logical. If \code{TRUE} (default), prints a summary table of the
top \code{n} slots. If \code{FALSE}, returns the full results silently.}
}
\value{
A \code{data.frame} with columns:
\itemize{
\item \code{path}: Full slot path using \code{@} notation for S4 slots and \verb{[["name"]]}
or \verb{[[i]]} notation for list elements.
\item \code{mb}: Size of the slot or element in megabytes.
}

Returned invisibly, sorted descending by size.
}
\description{
Recursively inspects all slots of an S4 object, including nested S4 objects
and lists containing S4 objects, and reports their memory footprint. Useful
for identifying which slots dominate memory usage in complex S4 objects.
}
\details{
Slot paths reflect the access pattern needed to reach each element. For
example, a nested S4 slot would appear as \code{obj@SlotA@SlotB}, and a named
list element inside a slot as \code{obj@SlotA[["item"]]}.

Note that sizes are \strong{not additive} — parent slots include the memory of
their children, so a slot containing a large nested S4 object will itself
appear large.
}
\examples{
\dontrun{
# Inspect top 10 slots of a complex S4 model object
S4SlotSizes(my_model, n = 10)

# Retrieve full results without printing
sizes <- S4SlotSizes(my_model, verbose = FALSE)
sizes[sizes$mb > 1, ]  # slots larger than 1 MB
}

}
