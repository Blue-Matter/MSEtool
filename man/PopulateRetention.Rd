% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-retention.R
\name{PopulateRetention}
\alias{PopulateRetention}
\title{Populate a Retention Object}
\usage{
PopulateRetention(
  Retention,
  Ages = NULL,
  Length = NULL,
  Weight = NULL,
  Maturity = NULL,
  nSim = 5,
  Years = NULL,
  nArea = 1,
  CalcAtLength = TRUE,
  seed = NULL,
  silent = FALSE,
  force = FALSE
)
}
\arguments{
\item{Retention}{A \code{\link[=Retention]{Retention()}} object to populate.}

\item{Ages}{An \code{\link[=Ages]{Ages()}} object defining age classes.}

\item{Length}{A \code{\link[=Length]{Length()}} object. Required if the retention model depends
on length-at-age.}

\item{Weight}{A \code{\link[=Weight]{Weight()}} object. Required if the retention model depends
on weight-at-age.}

\item{Maturity}{A \code{\link[=Maturity]{Maturity()}} object. Required if the retention model uses
relative selectivity.}

\item{nSim}{Integer. Number of simulation replicates.}

\item{Years}{Numeric vector of model years.}

\item{nArea}{Integer. Number of spatial areas.}

\item{CalcAtLength}{Logical; if \code{TRUE}, calculates mean-at-length from
age-based arrays.}

\item{seed}{Integer. Random seed used for stochastic generation.}

\item{silent}{Logical; if \code{TRUE}, suppresses informational messages.}

\item{force}{Logical; if \code{TRUE}, forces re-population even if digest
indicates object is current.}
}
\value{
A populated \code{\link[=Retention]{Retention()}} object.
}
\description{
Populates a \code{Retention} object with mean-at-age, mean-at-length, or
mean-at-weight values.
}
\details{
\code{PopulateRetention()} handles population of retention-at-age, retention-
at-length, or retention-at-weight. Steps include:
\itemize{
\item Resolving the retention model class
\item Generating mean retention at age, length, or weight as appropriate
\item Converting between mean-at-length/weight and mean-at-age
\item Adding stochastic variation via \code{PopulateRandom()}
\item Adding an 'Area' dimension and setting dimension names
}
}
\examples{
\dontrun{
Ret <- Retention()
Ret_pop <- PopulateRetention(
  Ret, Ages = Ages, Length = Length, Weight = Weight, 
  Maturity = Maturity, Years = 2000:2025, nSim = 5
)
}

}
