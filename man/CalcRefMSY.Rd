% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-ref-msy.R
\name{CalcRefMSY}
\alias{CalcRefMSY}
\title{Calculate MSY Reference Points}
\usage{
CalcRefMSY(
  Hist,
  Years = NULL,
  type = c("Removals", "Landings"),
  silent = FALSE
)
}
\arguments{
\item{Hist}{A \link[=hist-class]{hist} or \link[=om-class]{om} object. If an \code{om} is supplied
it is first converted to a \code{hist} object.}

\item{Years}{Integer vector of years for which reference points are
evaluated. Biological and fishery parameters are subset to these years.
If \code{NULL} (default), the final
historical year is used.}

\item{type}{Character. Whether MSY is defined in terms of total removals
(landings plus dead discards) or landings only. One of \code{'Removals'}
(default) or \code{'Landings'}.}

\item{silent}{Logical. If \code{TRUE}, suppresses progress messages. Default
\code{FALSE}.}
}
\value{
The input \link[=hist-class]{hist} object with \code{Hist@Reference@MSY} populated
as a \link[=refpointsMSY-class]{refpointsMSY} object. Slots in the returned object:
\itemize{
\item \code{FMSY} — apical fishing mortality at MSY (\verb{Sim × Complex × Year}).
\item \code{BMSY} — total biomass at MSY (\verb{Sim × Stock × Year}).
\item \code{SBMSY} — spawning biomass at MSY (\verb{Sim × Stock × Year}).
\item \code{SPMSY} — spawning production at MSY (\verb{Sim × Stock × Year}).
\item \code{SPRMSY} — spawning potential ratio at MSY (\verb{Sim × Stock × Year}).
\item \code{MSYLandings} — landed catch at MSY (\verb{Sim × Stock × Year}).
\item \code{MSYDiscards} — dead discards at MSY (\verb{Sim × Stock × Year}).
}
}
\description{
Computes maximum sustainable yield (MSY) biological reference points for
all complexes in a \link[=hist-class]{hist} or \link[=om-class]{om} object by identifying the
apical fishing mortality that maximises total yield, accounting for the
stock-recruitment relationship. Results are stored in
\code{Hist@Reference@MSY} and the updated \link[=hist-class]{hist} object is returned.
}
\details{
\subsection{Complexes}{

Reference points are calculated separately for each complex defined in
\code{Hist@OM@Complexes}. Within each complex:
\itemize{
\item \code{FMSY} is the single apical fishing mortality maximising total yield
across all stocks in the complex.
\item Fleet-specific F is distributed according to effort-weighted
catchability and selectivity.
\item Stock-level quantities (\code{BMSY}, \code{SBMSY}, etc.) are reported for each
stock evaluated at the complex-level \code{FMSY}.
}
}

\subsection{Optimisation}{

For each simulation and year, \code{\link[=optimize]{optimize()}} searches for the apical F
maximising total yield on the log-F scale over \verb{[1e-5, maxF]}
}

\subsection{SPR0}{

If \code{Hist@Reference@SPR0} has not yet been computed it is calculated via
\code{\link[=CalcSPR0]{CalcSPR0()}} before reference point calculations begin.
}
}
\seealso{
\code{\link[=CalcPerRecruit]{CalcPerRecruit()}}, \code{\link[=CalcSPR0]{CalcSPR0()}}, \link[=refpointsMSY-class]{refpointsMSY},
}
