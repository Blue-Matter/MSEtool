% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-fishery-dynamics.R
\name{CalcFisheryDynamics}
\alias{CalcFisheryDynamics}
\title{Calculate Fishery Dynamics}
\usage{
CalcFisheryDynamics(
  Hist,
  Years = NULL,
  Sims = NULL,
  DoCalcCatch = 1,
  DoCalcSpawnProduction = 1,
  DoCalcRecruitment = 1,
  DoCalcNumberNext = 1,
  DoCalcBiomass = 1,
  DoCalcOverallF = 1,
  IdenticalSim = FALSE,
  debug = 0,
  clone = NULL
)
}
\arguments{
\item{Hist}{A \link[=hist-class]{hist} object.}

\item{Years}{Numeric vector of years to calculate dynamics over. Defaults to
historical years from \code{Hist@OM}.}

\item{Sims}{Integer vector of simulation indices to compute. Defaults to all
simulations.}

\item{DoCalcCatch}{Integer flag; \code{1} (default) calculates catch, \code{0} skips.}

\item{DoCalcSpawnProduction}{Integer flag; \code{1} (default) calculates global
spawning biomass and spawning production, \code{0} skips.}

\item{DoCalcRecruitment}{Integer flag; \code{1} (default) calculates recruitment
and distributes recruits over areas, \code{0} skips.}

\item{DoCalcNumberNext}{Integer flag; \code{1} (default) calculates numbers at
the beginning of the next time step, \code{0} skips.}

\item{DoCalcBiomass}{Integer flag; \code{1} (default) calculates biomass for the
current time step, \code{0} skips.}

\item{DoCalcOverallF}{Integer flag; \code{1} (default) calculates overall fishing
mortality, \code{0} skips.}

\item{IdenticalSim}{Logical; if \code{TRUE}, computes only simulation 1 and
broadcasts results to all simulations via \code{CopyFirstSim}.}

\item{debug}{Integer flag passed to C++; \code{1} enables verbose debug output.}

\item{clone}{Integer or \code{NULL}; controls whether the internal \code{Hist} object
is deep-cloned before modification. \code{1} = always clone (safe, slower),
\code{0} = no clone (faster, mutates \code{HistIn} directly). If \code{NULL}, uses
\code{Hist@OM@Control$Clone} if set, otherwise defaults to \code{0}.}
}
\value{
The \code{Hist} object with updated fishery dynamics slots.
}
\description{
Internal wrapper around C++ \code{CalcFisheryDynamics_}
}
\keyword{internal}
