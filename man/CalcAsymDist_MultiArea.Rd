% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-asymptotic-distribution.R
\name{CalcAsymDist_MultiArea}
\alias{CalcAsymDist_MultiArea}
\title{Asymptotic Distribution for a Multi-Area Movement Matrix}
\usage{
CalcAsymDist_MultiArea(
  Movement,
  method = c("la", "mc"),
  tol = 1e-10,
  maxiter = 10000
)
}
\arguments{
\item{Movement}{A square row-stochastic matrix with \code{nArea >= 3} rows.}

\item{method}{Character. One of \code{"la"} (linear algebra; default) or \code{"mc"}
(Markov chain iteration). Partially matched.}

\item{tol}{Numeric. Convergence tolerance and threshold for clamping
near-zero negative values. Default \code{1e-10}.}

\item{maxiter}{Integer. Maximum iterations for \code{method = "mc"}. Default
\code{1e4}.}
}
\value{
A numeric vector of length \code{nArea} summing to 1. Emits a warning
if \code{method = "mc"} does not converge within \code{maxiter}.
}
\description{
Computes the stationary distribution of a row-stochastic movement matrix
with 3 or more areas using either a linear-algebra or iterative Markov
chain approach. Called by \code{\link[=CalcAsymDist]{CalcAsymDist()}} when \code{nrow(Movement) > 2}.
}
\keyword{internal}
