% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-obs.R
\name{PopulateObs}
\alias{PopulateObs}
\title{Populate an Obs Object}
\usage{
PopulateObs(Obs, HistYears, ProjYears, nSim, AgeBins = NULL, SizeBins = NULL)
}
\arguments{
\item{Obs}{An \link[=obs-class]{obs} object to populate.}

\item{HistYears}{Integer vector. Calendar years of the historical period.}

\item{ProjYears}{Integer vector. Calendar years of the projection period.}

\item{nSim}{Integer. Number of simulation replicates.}

\item{AgeBins}{Numeric vector. Age class values used to name
the \code{Bin} dimension of populated age-composition arrays. Required when
\code{Obs@LandingsAtAge} or \code{Obs@DiscardsAtAge} is non-empty.}

\item{SizeBins}{Numeric vector. Length class midpoints, used to name the
\code{Bin} dimension of populated size-composition arrays. Required when
\code{Obs@LandingsAtSize} or \code{Obs@DiscardsAtSize} is non-empty.}
}
\value{
A populated \link[=obs-class]{obs} object.
}
\description{
Populate an \link[=obs-class]{obs} object by expanding observation error parameters
across simulation replicates and years, generating stochastic error arrays,
and applying default values where slots are unspecified.
}
\details{
\code{PopulateObs()} coordinates population of all observation error components
of an \link[=obs-class]{obs} object, calling a dedicated sub-function for each slot:
\itemize{
\item Effort observation error via \code{\link[=PopulateEffortObs]{PopulateEffortObs()}}
\item Landed catch observation error via \code{\link[=PopulateCatchObs]{PopulateCatchObs()}}
\item Discarded catch observation error via \code{\link[=PopulateCatchObs]{PopulateCatchObs()}}
\item CPUE index observation error via \code{\link[=PopulateIndexObs]{PopulateIndexObs()}}
\item Survey index observation error via \code{\link[=PopulateIndexObs]{PopulateIndexObs()}}
\item Landed catch-at-age composition error via \code{\link[=PopulateCompObs]{PopulateCompObs()}}
\item Discarded catch-at-age composition error via \code{\link[=PopulateCompObs]{PopulateCompObs()}}
\item Landed catch-at-size composition error via \code{\link[=PopulateCompObs]{PopulateCompObs()}}
\item Discarded catch-at-size composition error via \code{\link[=PopulateCompObs]{PopulateCompObs()}}
}

Slots for \code{LifeHistory} and \code{Exploitation} observation error are reserved
for future implementation and are not populated.

If a slot contains an empty object (as determined by \code{\link[=EmptyObject]{EmptyObject()}}), it
is returned unchanged. See the individual sub-function documentation for
details on expansion rules, default values, and accepted input forms.
}
\seealso{
\code{\link[=Populate]{Populate()}}, \code{\link[=PopulateEffortObs]{PopulateEffortObs()}}, \code{\link[=PopulateCatchObs]{PopulateCatchObs()}},
\code{\link[=PopulateIndexObs]{PopulateIndexObs()}}, \code{\link[=PopulateCompObs]{PopulateCompObs()}}
}
