% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract-advice.R
\name{TACs}
\alias{TACs}
\title{Extract TAC Recommendations from an MSE Object}
\usage{
TACs(MSE)
}
\arguments{
\item{MSE}{An \link[=mse-class]{mse} object.}
}
\value{
A data frame with columns:
\itemize{
\item \code{Sim}: simulation index.
\item \code{Stock}: stock name.
\item \code{MP}: management procedure name.
\item \code{Year}: calendar year (or decimal date for sub-annual time steps).
\item \code{Period}: always \code{"Projection"} for the reasons above.
\item \code{Fleet}: fleet name.
\item \code{Value}: TAC recommendation in the units of the operating model.
\item \code{Variable}: always \code{"TAC"}.
}
}
\description{
Extracts the total allowable catch (TAC) recommendations issued by each
management procedure (MP) in each simulation, year, stock, and fleet from
the \code{PPD} slot of an \link[=mse-class]{mse} object. Returns a tidy data frame
suitable for plotting and performance indicator calculations.
}
\details{
An MP's \code{Advice(TAC = ...)} only gets recorded in the year it actually
changes (see \code{\link[=Advice]{Advice()}}: a TAC applies from the year it's issued "until a
new Advice object is returned"), so the raw recommendation history is
sparse. \code{TACs()} forward-fills each gap (via \code{\link[=ExtendYears]{ExtendYears()}}) so the
returned series has one row per year the TAC was actually in effect, not
just the years it changed. Years before an MP's first recommendation
(i.e. the historical period, where no MP has run yet) are absent rather
than filled with a placeholder.
}
\seealso{
\link[=mse-class]{mse}, \code{\link[=Advice]{Advice()}}, \code{\link[=PPD]{PPD()}}
}
