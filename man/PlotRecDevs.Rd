% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-recdevs.R
\name{PlotRecDevs}
\alias{PlotRecDevs}
\title{Plot Recruitment Deviations}
\usage{
PlotRecDevs(
  object,
  Sim = NULL,
  byStock = NULL,
  Stocks = NULL,
  probs = c(0.05, 0.95)
)
}
\arguments{
\item{object}{A \link[=stock-class]{stock}/\link[=om-class]{om}/\link[=hist-class]{hist}/\link[=mse-class]{mse} object.}

\item{Sim}{Integer or \code{NULL} (default). Which simulation replicate to
plot. \code{NULL} takes the median across all simulations and adds a \code{probs}
quantile ribbon (unless \code{nSim == 1}).}

\item{byStock}{One of \code{TRUE}, \code{FALSE}, or \code{NULL} (default, facets
automatically when \code{object} has more than one stock).}

\item{Stocks}{Character or numeric vector. Restrict the plot to specific
stocks, either by name (matching \code{\link[=StockNames]{StockNames()}}) or by index. Default
\code{NULL} (all stocks).}

\item{probs}{Numeric vector of length 2. Lower and upper quantiles of the
across-simulation ribbon drawn when \code{Sim = NULL}. Default \code{c(0.05, 0.95)}.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots a stock's recruitment deviations
(\code{RecDevInit}/\code{RecDevHist}/\code{RecDevProj}; see \link[=srr-class]{srr})
}
\details{
\code{PlotRecDevs()} plots the full log-recruitment-deviation timeseries as one
continuous series per stock. When \code{object} has more than one simulation, the
median is drawn with a \code{probs} quantile ribbon. A dashed vertical line marks
the historical/projection boundary (the first projection year); a dotted
vertical line marks the initial-age-structure/historical boundary, when
initial-age deviations are present.
}
\seealso{
\code{\link[=PlotSRR]{PlotSRR()}}, \code{\link[=PlotSRRCurve]{PlotSRRCurve()}}, \code{\link[=GenRecDevs]{GenRecDevs()}}, \link[=srr-class]{srr}
}
