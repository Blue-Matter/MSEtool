% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-obs.R
\docType{class}
\name{compobs-class}
\alias{compobs-class}
\alias{CompObs-class}
\title{The \code{compobs} S4 Class}
\description{
Defines the observation error structure for age or length composition data
(catch-at-age, catch-at-length). Used in the \code{LandingsAtAge},
\code{DiscardsAtAge}, \code{LandingsAtSize}, and \code{DiscardsAtSize} slots of an
\link[=obs-class]{obs} object. Objects are typically created via \code{\link[=CompObs]{CompObs()}}, which
documents all parameters and accepted input forms in detail.
}
\details{
\subsection{Composition generation model}{

During simulation, \code{\link[=PopulateCompObs]{PopulateCompObs()}} applies the following steps:
\enumerate{
\item \strong{\code{Shift} non-\code{NULL}}: user-specified (or conditioned) per-bin
log-concentration offsets are exponentiated and applied to the
OM-predicted concentration vector:
\deqn{\alpha_b' = \mathrm{ESS} \times q_b \times \exp(\mathrm{Shift}_b)}
\item \strong{\code{Shift} \code{NULL}} (default): pure Dirichlet-Multinomial draw from the
OM-predicted composition with concentration scaled by \code{ESS} and
dispersion \code{Theta}:
\deqn{\alpha' = \mathrm{ESS} \times \mathbf{q}}
}
}

\subsection{Note on large compositional shifts}{

When observed compositions differ substantially from OM-predicted
compositions (e.g. a length distribution shifted far left or right, or
with markedly different spread), \code{.ConditionObsComp()} will produce large
values in \code{Shift}. These are treated as observation error rather than model
mis-specification — an assumption that should be evaluated carefully.
See the technical manual for a full discussion of this assumption and its
implications for simulation performance.
}
}
\section{Slots}{

\describe{
\item{\code{SampleSize}}{\code{numeric} or \code{NULL}. Nominal sample size (number of fish
aged or measured) applied when drawing the final multinomial sample.
Accepted input forms are documented in \code{\link[=CompObs]{CompObs()}}. After population by
\code{\link[=PopulateCompObs]{PopulateCompObs()}}, stored as a named \verb{[nSim x nYear]} array.
\code{NULL} (default) suppresses composition data generation for this data
type; \code{\link[=EmptyObject]{EmptyObject()}} returns \code{TRUE} when \code{SampleSize} is \code{NULL}.}

\item{\code{ESS}}{\code{numeric} or \code{NULL}. Effective sample size, which scales the
Dirichlet-Multinomial concentration vector and therefore controls
stochastic variability in the composition draw independently of
\code{SampleSize}. Accepted input forms are the same as for \code{SampleSize}; see
\code{\link[=CompObs]{CompObs()}}. After population, stored as a named \verb{[nSim x nYear]} array.
\code{NULL} (default) uses \code{SampleSize} as the effective sample size.}

\item{\code{Theta}}{\code{numeric} or \code{NULL}. Dirichlet-Multinomial dispersion parameter
in \verb{(0, 1]}. Values less than 1 produce overdispersed compositions
relative to a standard multinomial; \code{Theta = 1} (default) recovers the
standard multinomial. Accepted input forms are the same as for
\code{SampleSize}; see \code{\link[=CompObs]{CompObs()}}. After population, stored as a named
\verb{[nSim x nYear]} array.}

\item{\code{Years}}{\code{numeric} vector or \code{NULL}. Calendar years of observed
composition data to be used during the conditioning step (see
\code{.ConditionObsComp()}). \code{NULL} (default) uses all available historical years during
conditioning.}

\item{\code{Shift}}{\code{numeric} or \code{NULL}. Systematic per-bin offset on the
log-concentration scale applied to the Dirichlet concentration vector
before drawing, capturing directional bias between observed and
OM-predicted compositions. After population by \code{\link[=PopulateCompObs]{PopulateCompObs()}},
stored as a named \verb{[nSim x nYear x nBin]} array. \code{NULL} (default) applies
no shift. In conditioning mode, populated internally by \code{.ConditionObsComp()}
from the mean per-bin log-concentration residual across historical years.}

\item{\code{Misc}}{\code{list}. Reserved for internal use.}
}}

\seealso{
\itemize{
\item \code{\link[=CompObs]{CompObs()}} for the constructor and full parameter documentation.
\item \code{\link[=PopulateCompObs]{PopulateCompObs()}} for the population function.
\item \code{.ConditionObsComp()} for the conditioning function.
\item \link[=obs-class]{obs} for the enclosing observation model object.
}

Other obs:
\code{\link[=CatchObs]{CatchObs()}},
\code{\link[=CompObs]{CompObs()}},
\code{\link[=EffortObs]{EffortObs()}},
\code{\link[=ExploitationObs]{ExploitationObs()}},
\code{\link[=IndicesObs]{IndicesObs()}},
\code{\link[=LifeHistoryObs]{LifeHistoryObs()}},
\code{\link[=Obs]{Obs()}},
\code{\link{catchobs-class}},
\code{\link{effortobs-class}},
\code{\link{exploitationobs-class}},
\code{\link{indicesobs-class}},
\code{\link{lifehistoryobs-class}},
\code{\link{obs-class}}
}
\concept{obs}
