% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-atage-atsize.R
\name{.WeightedAtSize2AtAge}
\alias{.WeightedAtSize2AtAge}
\title{Convert Mean-at-Size to Mean-at-Age, Weighted by Selectivity}
\usage{
.WeightedAtSize2AtAge(object, Weighting, Length)
}
\arguments{
\item{object}{An S4 object with \code{MeanAtLength}/\code{MeanAtWeight} and
\code{MeanAtAge} slots (e.g. a \link[=retention-class]{retention} or \link[=weight-class]{weight} object).}

\item{Weighting}{An S4 object supplying the weighting at-size schedule
(e.g. a populated \link[=selectivity-class]{selectivity} object).}

\item{Length}{A \code{\link[=Length]{Length()}} or \code{\link[=Weight]{Weight()}} object supplying the ALK/AWK.}
}
\value{
\code{object} with \code{object@MeanAtAge} populated.
}
\description{
Like \code{.AtSize2AtAge()}, but weights the age-length key by a second object's
at-size schedule (typically selectivity) before collapsing to age, i.e.
\verb{object(age) = sum_L[ P(L|age) Weighting(L) object(L) ] / sum_L[ P(L|age) Weighting(L) ]}. This avoids biasing the at-age result toward the
unconditional population length distribution at age when \code{object}
(e.g. retention) is only meaningful conditional on capture, and capture is
itself size-selective. Falls back to the unweighted \code{.AtSize2AtAge()} result
wherever the weighted ratio is non-finite (e.g. no weight in that age
class) or \code{Weighting}'s at-size schedule is unavailable.
}
\keyword{internal}
