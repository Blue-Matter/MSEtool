% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-obs-comp.R
\name{PopulateCompObs}
\alias{PopulateCompObs}
\title{Populate Composition Observation Error}
\usage{
PopulateCompObs(
  Comp,
  nSim,
  HistYears,
  ProjYears,
  Bins = NULL,
  BinName = "Class"
)
}
\arguments{
\item{Comp}{A \link[=compobs-class]{compobs} object. Used for landed catch-at-age
(\code{Obs@LandingsAtAge}), discarded catch-at-age (\code{Obs@DiscardsAtAge}),
landed catch-at-size (\code{Obs@LandingsAtSize}), and discarded catch-at-size
(\code{Obs@DiscardsAtSize}).}

\item{nSim}{Integer. Number of simulation replicates.}

\item{HistYears}{Integer vector. Calendar years of the historical period.}

\item{ProjYears}{Integer vector. Calendar years of the projection period.}

\item{Bins}{Numeric vector. Age classes for age composition
slots, or length class midpoints for size
composition slots. Used to set the length of the \code{Bin} dimension and to
name it in the populated \code{Shift} array. \code{NULL} is accepted only when
\code{\link[=EmptyObject]{EmptyObject()}} returns \code{TRUE} for \code{Comp}.}

\item{BinName}{Character scalar. Name used for the bin dimension in
populated arrays, typically \code{"Age"} or \code{"Class"}.}
}
\value{
A populated \link[=compobs-class]{compobs} object.
}
\description{
Populate a \link[=compobs-class]{compobs} object by expanding \code{SampleSize}, \code{ESS},
\code{Theta}, and \code{Shift} across simulation replicates, years, and composition
bins, and applying default values where slots are unspecified.
}
\details{
If \code{\link[=EmptyObject]{EmptyObject()}} returns \code{TRUE} for \code{Comp} (i.e. \code{SampleSize} is
\code{NULL}), the object is returned unchanged and no population is performed.

Otherwise the following slots are populated:

\strong{\code{SampleSize}}: expanded to a named \verb{[nSim x nYear]} array via
\code{\link[=PopulateObsScalar]{PopulateObsScalar()}}, covering all historical and projection years. A
length-2 input \code{c(lower, upper)} is interpreted as bounds of a Uniform
distribution from which \code{nSim} values are drawn.

\strong{\code{ESS}}: expanded in the same way as \code{SampleSize}. If \code{NULL}, defaults
silently to the populated \code{SampleSize} array. Typically \code{ESS <= SampleSize};
values of \code{ESS < SampleSize} produce overdispersion relative to a pure
multinomial with \code{SampleSize} draws.

\strong{\code{Theta}}: expanded to a named \verb{[nSim x nYear]} array of values in
\verb{(0, 1]}. If \code{NULL}, defaults silently to 1 (standard multinomial, no
additional overdispersion). A length-2 input is treated as Uniform bounds.

\strong{\code{Shift}}: expanded to a named \verb{[nSim x nYear x nBin]} array via
\code{\link[=Extend]{Extend()}}, where \code{nBin = length(Bins)}. Accepted input forms:
\itemize{
\item \code{NULL} (default): no shift applied; slot remains \code{NULL}.
\item Scalar: constant offset across all simulations, years, and bins.
\item Vector of length \code{nBin}: bin-specific offset, constant across simulations
and years.
\item Named matrix with any subset of \code{Sim}, \code{Year}, and \code{Bin} dimensions with
change-point years.
\item Full \verb{[nSim x nYear x nBin]} array.
}

In conditioning mode, \code{ESS}, \code{Theta}, and \code{Shift} are populated internally
by \code{.ConditionObsComp()} and should not be set by the user.
\subsection{Composition generation model}{

After population, the fully-expanded slots are used by the simulation
engine to generate observed compositions as follows. Let \strong{q} be the
OM-predicted composition vector for a given simulation, year, and fleet:
\enumerate{
\item Base concentration: \eqn{\alpha = \mathrm{ESS} \times \mathbf{q}}
\item Apply shift (if non-\code{NULL}):
\eqn{\alpha_b' = \alpha_b \times \exp(\mathrm{Shift}[\mathrm{sim}, \mathrm{year}, b])}
\item Dirichlet draw:
\eqn{\mathbf{p}^* \sim \mathrm{Dirichlet}(\alpha' / \mathrm{Theta})}
\item Multinomial draw:
\eqn{\mathrm{obs} \sim \mathrm{Multinomial}(\mathrm{SampleSize}, \mathbf{p}^*)}
}
}
}
\seealso{
\code{\link[=CompObs]{CompObs()}}, \link[=compobs-class]{compobs}, \code{\link[=PopulateObs]{PopulateObs()}}, \code{.ConditionObsComp()}
}
