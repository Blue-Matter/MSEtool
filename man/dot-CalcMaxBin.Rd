% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-classes.R
\name{.CalcMaxBin}
\alias{.CalcMaxBin}
\title{Calculate the Maximum Size Bin for a Length or Weight Distribution}
\usage{
.CalcMaxBin(MeanAtAge, CVatAge, TruncSD = 2, dist = "normal")
}
\arguments{
\item{MeanAtAge}{Numeric array of mean size-at-age (\verb{Sim × Age × Year}).}

\item{CVatAge}{Numeric array of CV-at-age, matching the dimensions of
\code{MeanAtAge}.}

\item{TruncSD}{Numeric. Number of standard deviations above the mean used
to define the upper size bound. Default \code{2}.}

\item{dist}{Character. Distribution assumption. One of \code{"normal"} or
\code{"lognormal"}.}
}
\value{
A single integer: the ceiling of the maximum expected size across
all simulations, ages, and years.
}
\description{
Computes the upper bound of the size class range by finding the maximum
expected size across all simulations and ages, extended to \code{TruncSD}
standard deviations above the mean. Used by \code{\link[=.PopulateClasses]{.PopulateClasses()}} to set
default size class lower bounds.
}
\details{
For the normal distribution the upper bound is:
\code{max(mean + TruncSD * SD)}

For the log-normal distribution the upper bound is:
\code{max(exp(log_mean + TruncSD * CV))}

where \code{log_mean = log(MeanAtAge) - 0.5 * CV^2}.
}
\keyword{internal}
