% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-om.R
\name{CatchFrac}
\alias{CatchFrac}
\alias{CatchFrac<-}
\alias{FleetAllocation}
\alias{FleetAllocation<-}
\title{Fleet Allocation of TAC for OM Objects}
\usage{
CatchFrac(x)

CatchFrac(x) <- value

FleetAllocation(x)

FleetAllocation(x) <- value
}
\arguments{
\item{x}{An \link[=om-class]{om}, \link[=hist-class]{hist}, or \link[=mse-class]{mse} object.}

\item{value}{Replacement value; a named list of length 0 or \code{nStock(OM)}
(\code{CatchFrac}) / the number of stock complexes (\code{FleetAllocation}), each
element an \code{nSim} by \code{nFleet} matrix (or a single row recycled across
sims) with rows summing to 1. \code{NULL} clears it.}
}
\value{
\itemize{
\item \code{CatchFrac()}/\code{FleetAllocation()} return the value of the corresponding
slot.
\item \verb{CatchFrac<-()}/\verb{FleetAllocation<-()} return \code{x} with the corresponding
slot updated.
}
}
\description{
Access or replace the \code{CatchFrac} and \code{FleetAllocation} slots of an
\link[=om-class]{om} object, which together control how catch is split among
fleets: \code{CatchFrac} as a historical calibration target, \code{FleetAllocation}
as the split applied to a stock/complex-level TAC during projection.
Also accepts \link[=hist-class]{hist} and \link[=mse-class]{mse} objects, extracting the
embedded \code{OM} slot transparently.
}
\details{
\subsection{\code{CatchFrac}}{

Only relevant when there is more than one fleet and a historical
\code{Depletion@Final} target is set for at least one stock. In that case,
catchability is calibrated during conditioning so that the modelled catch
split across fleets in the final historical year reproduces \code{CatchFrac}.
If left unspecified for a stock, \code{CatchFrac} is instead derived from
relative Effort times Catchability in the final historical year, and used
as-is (no calibration target).
}

\subsection{\code{FleetAllocation}}{

Controls how a stock/complex-level TAC is split among fleets during
projection - both TAC advice returned by an MP and \code{InterimAdvice}. For
each stock/complex, resolved in this order:
\enumerate{
\item Use \code{FleetAllocation} if specified.
\item Fall back to the deprecated \code{Allocation} slot if specified (retained
only so that objects saved before the \code{FleetAllocation} rename still
carry their data; not user-facing, set \code{FleetAllocation} directly
instead).
\item Fall back to \code{CatchFrac} if specified.
\item Derive from the mean relative removals (landings + discards) over the
last five historical years.
}
}
}
\examples{
om <- OM(nSim = 10)
FleetAllocation(om) <- list(Stock1 = matrix(c(0.7, 0.3), 10, 2, byrow = TRUE))
FleetAllocation(om)

}
\seealso{
\itemize{
\item \code{\link[=OM]{OM()}} for the constructor, \link[=om-class]{om} for the class definition.
\item \code{\link[=HistoricalWeight]{HistoricalWeight()}}/\code{\link[=SeasonalAllocation]{SeasonalAllocation()}} for how a periodically-set
TAC/Effort is further split across seasons.
\item \link{OM-accessors} for the remaining \code{OM} slot accessors.
}

Other om:
\code{\link{ComplexOM}},
\code{\link[=DataLag]{DataLag()}},
\code{\link[=Herm]{Herm()}},
\code{\link{HermOM}},
\code{\link[=HistoricalWeight]{HistoricalWeight()}},
\code{\link[=InterimAdvice]{InterimAdvice()}},
\code{\link{MultiStockOM}},
\code{\link[=OM]{OM()}},
\code{\link{OM-accessors}},
\code{\link[=RefSeason]{RefSeason()}},
\code{\link{SeasonalSpatialOM}},
\code{\link{SingleStockOM}},
\code{\link{TwoFleetOM}},
\code{\link{om-class}},
\code{\link{stocktransition-class}}
}
\concept{om}
