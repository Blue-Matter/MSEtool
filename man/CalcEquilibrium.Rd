% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-equilibrium.R
\name{CalcEquilibrium}
\alias{CalcEquilibrium}
\title{Calculate Absolute Equilibrium Quantities}
\usage{
CalcEquilibrium(OM, apicalF = NULL, Years = NULL, Complex = NULL)
}
\arguments{
\item{OM}{An \link[=om-class]{om} or \link[=hist-class]{hist} object. If a \link[=hist-class]{hist}
object is supplied, \code{Hist@OM} is used.}

\item{apicalF}{Numeric vector of apical fishing mortality values at which
equilibrium quantities are evaluated. If \code{NULL} (default), a
log-spaced sequence of 50 values is generated automatically.}

\item{Years}{Numeric years for which equilibrium quantities are
evaluated. Biological and fishery parameters are subset to this year.
Must match years in \code{Years(OM)}. If \code{NULL} (default), the final
historical year is used.}

\item{Complex}{Character vector of complex names to evaluate. If \code{NULL}
(default), all complexes are evaluated.}
}
\value{
An \link[=equilibrium-class]{equilibrium} object containing absolute equilibrium
numbers, biomass, spawning biomass, spawning production, removals, and
landings evaluated at each value of \code{apicalF}, with results reported per
stock.
}
\description{
For a given vector of apical fishing mortality values, computes absolute
equilibrium abundance and yield by combining per-recruit calculations with
stock-recruitment relationship scaling. Results span all complexes defined
in the operating model.
}
\details{
\subsection{Default apicalF grid}{

When \code{apicalF = NULL}, a log-spaced sequence of 30 values is generated
automatically, spanning from \verb{0.01 × max(M)} to \verb{2 × max(M)} with zero
prepended, where  \code{max(M)} is the maximum natural mortality across all
stocks.

Log-spacing is used because population dynamics are approximately linear in
log(F): yield and biomass change rapidly at low F relative to M but become
increasingly insensitive at high F, so equal spacing on the log scale
concentrates resolution where the equilibrium curve is most informative.

Supply an explicit vector to override, for example
\code{apicalF = seq(0, 2, by = 0.01)}.
}

\subsection{Complexes}{

Per-recruit and equilibrium quantities are calculated separately for each
complex, with \code{apicalF} defined as the maximum fishing mortality across all
stocks within the complex.
}

\subsection{Single year}{

Unlike \code{\link[=CalcPerRecruit]{CalcPerRecruit()}}, this function accepts only a single \code{Year}
value. For multi-year equilibrium curves, call the function separately
for each year.
}
}
\seealso{
\code{\link[=CalcPerRecruit]{CalcPerRecruit()}}, \code{\link[=CalcMSY]{CalcMSY()}}, \link[=equilibrium-class]{equilibrium}
}
