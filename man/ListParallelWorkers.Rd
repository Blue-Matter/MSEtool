% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-parallel.R
\name{ListParallelWorkers}
\alias{ListParallelWorkers}
\alias{CleanupOrphanWorkers}
\title{List and Clean Up Parallel Worker Processes}
\usage{
ListParallelWorkers()

CleanupOrphanWorkers(dry_run = TRUE)
}
\arguments{
\item{dry_run}{Logical. If \code{TRUE} (default), \code{CleanupOrphanWorkers()}
reports what would be terminated without actually terminating anything.}
}
\value{
\code{ListParallelWorkers()} returns a \code{data.frame} with columns \code{pid},
\code{parent_pid} (\code{NA} if it could not be determined), \code{parent_alive} (\code{NA}
if undetermined), and \code{orphan} (\code{TRUE} only when \code{parent_pid} is known
and confirmed dead). \code{CleanupOrphanWorkers()} invisibly returns the same
data.frame restricted to the orphaned rows that were (or, under
\code{dry_run = TRUE}, would be) terminated.
}
\description{
Clean up orphan worker processes
}
\details{
\code{future::multisession} (the backend \code{\link[=SetupParallel]{SetupParallel()}} uses) spawns
separate, persistent R processes for its workers. If the R session that
created them exits abnormally rather than calling \code{\link[=DisableParallel]{DisableParallel()}}
those worker processes are not automatically cleaned up; each keeps
running (holding whatever memory it had at the time) until manually killed
or the machine restarts.
\itemize{
\item \code{ListParallelWorkers()} finds every such worker
process on this machine and reports whether its parent R session is still
alive.
\item \code{CleanupOrphanWorkers()} terminates only the ones whose parent is
confirmed gone.
}
}
\seealso{
\code{\link[=SetupParallel]{SetupParallel()}}, \code{\link[=DisableParallel]{DisableParallel()}}, \code{\link[=CheckParallel]{CheckParallel()}}
}
