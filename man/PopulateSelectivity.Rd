% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-selectivity.R
\name{PopulateSelectivity}
\alias{PopulateSelectivity}
\title{Populate a Selectivity Object}
\usage{
PopulateSelectivity(
  Selectivity,
  Ages = NULL,
  Length = NULL,
  Weight = NULL,
  Maturity = NULL,
  nSim = 5,
  Years = NULL,
  nArea = 1,
  CalcAtLength = TRUE,
  seed = NULL,
  silent = FALSE,
  CheckMaxValue = TRUE,
  replace = FALSE,
  ASKOverride = NULL
)
}
\arguments{
\item{Selectivity}{A \code{\link[=Selectivity]{Selectivity()}} object to populate.}

\item{Ages}{An \code{\link[=Ages]{Ages()}} object defining age classes.}

\item{Length}{A \code{\link[=Length]{Length()}} object. Required if the selectivity model
depends on length-at-age.}

\item{Weight}{A \code{\link[=Weight]{Weight()}} object. Required if the selectivity model
depends on weight-at-age.}

\item{Maturity}{A \code{\link[=Maturity]{Maturity()}} object. Required if \code{isRel = TRUE} to
scale selectivity relative to maturity \code{L50.}}

\item{nSim}{Integer. Number of simulation replicates.}

\item{Years}{Numeric vector of model years.}

\item{nArea}{Integer. Number of spatial areas.}

\item{CalcAtLength}{Logical. If \code{TRUE}, calculate selectivity-at-length
values from age-based arrays.}

\item{seed}{Integer. Random seed used for stochastic generation.}

\item{silent}{Logical. If \code{TRUE}, suppress informational messages.}

\item{CheckMaxValue}{Logical. If \code{TRUE}, ensure selectivity has a maximum
value of 1 across age classes.}

\item{replace}{Used interally.}

\item{ASKOverride}{Used internally.}
}
\value{
A populated \code{\link[=Selectivity]{Selectivity()}} object.
}
\description{
Populate a \code{Selectivity} object by generating selectivity-at-age,
selectivity-at-length, or selectivity-at-weight values for a fleet.
}
\details{
\code{PopulateSelectivity()} performs the following steps:
\itemize{
\item Structures selectivity parameters across simulation replicates, years,
and areas.
\item Resolves the selectivity model class and applies relative scaling to
maturity if \code{isRel = TRUE}.
\item Generates mean selectivity-at-age, selectivity-at-length, or
selectivity-at-weight depending on the model.
\item Converts between mean-at-length/weight and mean-at-age.
\item Ensures maximum selectivity does not exceed 1 if \code{CheckMaxValue = TRUE}.
\item Adds an area dimension to all arrays and sets dimension names.
}
}
\examples{
\dontrun{
S <- Selectivity()
S_pop <- PopulateSelectivity(
  Selectivity = S,
  Ages = Ages,
  Length = Length,
  Weight = Weight,
  Maturity = Maturity,
  Years = 2000:2025,
  nSim = 50,
  nArea = 5
)
}

}
