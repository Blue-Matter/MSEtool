% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-empty-object.R
\name{EmptyObject}
\alias{EmptyObject}
\title{Test Whether an Object is Empty}
\usage{
EmptyObject(object)
}
\arguments{
\item{object}{An S4 object, or any R object.

For S4 objects, \code{EmptyObject()} first checks whether the object is
identical to a freshly constructed default of the same class via
\code{\link[=isNewObject]{isNewObject()}}. If so, it returns \code{TRUE} immediately.

Otherwise it inspects every slot recursively:
\itemize{
\item Slots containing functions are skipped.
\item Slots containing nested S4 objects are tested recursively via
\code{EmptyObject()}.
\item All other slots are considered empty if they are \code{NULL}, zero-length, or
entirely \code{NA}.
}

\code{EmptyObject()} returns \code{TRUE} only when every slot passes the emptiness
check. A single non-empty slot causes the object to be considered
non-empty.

For non-S4 objects, \code{EmptyObject()} returns \code{TRUE} if the object has
length zero or consists entirely of \code{NA} values.

\code{EmptyObject()} is used throughout the \verb{Populate*()} functions as an early
exit guard: if an object is empty, population is skipped and the object is
returned unchanged.}
}
\value{
A length-1 logical.
}
\description{
Returns \code{TRUE} if an object contains no user-supplied information — i.e. it
is either a freshly constructed default object or all of its slots are
\code{NULL}, zero-length, or entirely \code{NA}.
}
\examples{
# A freshly constructed object is empty
EmptyObject(CompObs())

# An object with any slot filled is not empty
EmptyObject(CompObs(SampleSize = 200))

# Non-S4 objects
EmptyObject(NULL)
EmptyObject(numeric(0))
EmptyObject(c(NA, NA))

}
\seealso{
\code{\link[=isNewObject]{isNewObject()}}
}
