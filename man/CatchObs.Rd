% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-obs.R
\name{CatchObs}
\alias{CatchObs}
\title{CatchObs Constructor and Accessors}
\usage{
CatchObs(
  CV = NULL,
  Bias = NULL,
  Error = NULL,
  Years = NULL,
  Units = NULL,
  Ref = NULL,
  Misc = list()
)
}
\arguments{
\item{CV}{\code{numeric} or \code{NULL}. Coefficient of variation of the lognormal
catch observation error. Accepted forms:
\itemize{
\item \code{NULL} (default): no observation error is generated unless \code{Error} is
supplied directly.
\item Length-1 scalar: a single CV applied uniformly across all simulations
and years when generating the \code{Error} array internally.
\item Length-2 vector \code{c(mean, sd)}: \code{CV} is itself drawn stochastically —
a unique CV per simulation is generated via \code{.StructurePars()} before
being used to generate the \code{Error} array.
\item Length-\code{nSim} named array: used directly, one CV per simulation.
}

When conditioning on real data via \code{.ConditionObs()}, \code{CV} is replaced by
the simulation-specific standard deviation of historical log-residuals.}

\item{Bias}{\code{numeric} or \code{NULL}. Multiplicative observation bias. Accepted
forms:
\itemize{
\item \code{NULL} (default): bias defaults to \code{1} (unbiased) internally.
\item Positive scalar: interpreted as the CV of a lognormal distribution; a
unique bias is drawn per simulation as
\code{rlnorm(nSim, mconv(1, Bias), sdconv(1, Bias))}.
\item Length-2 vector \code{c(lower, upper)} (when \code{nSim != 2}): treated as
uniform bounds; a unique bias per simulation is drawn via
\code{runif(nSim, lower, upper)}.
\item Length-\code{nSim} vector or named array: used directly, one bias per
simulation.
}

When conditioning on real data via \code{.ConditionObs()}, \code{Bias} is replaced
by the mean ratio of simulated to observed catch across historical years,
per simulation.}

\item{Error}{\code{numeric} array or \code{NULL}. Pre-specified lognormal observation
error multipliers with dimensions \verb{nSim x nYear} and named dimnames
(\code{Sim}, \code{Year}). When supplied, \code{Error} is used directly and \code{CV} is
ignored for error generation. Values should be positive multipliers
centred near 1. Default \code{NULL}, in which case \code{Error} is generated from
\code{CV} internally. When conditioning on real data via
\code{.ConditionObs()}, any user-supplied \code{Error} is replaced.}

\item{Years}{\code{numeric} vector or \code{NULL}. Calendar years over which to
condition the observation error when real catch data are provided. When
\code{NULL} (default), all historical years in the \code{Data} object are used.
Has no effect when no real data are supplied.}

\item{Units}{\code{character(1)} or \code{NULL}. Units of catch measurement. Must be
\code{"Biomass"} or \code{"Number"}. Determines how simulated catch is aggregated
when generating pseudo-observed data. Default \code{NULL} (treated as
\code{"Biomass"}).}

\item{Ref}{\code{numeric} or \code{NULL}. Reference catch value. Accepted forms:
\itemize{
\item \code{NULL} (default): no reference value.
\item Positive scalar: interpreted as the CV of a lognormal distribution; a
unique reference value is drawn per simulation as
\code{rlnorm(nSim, mconv(1, Ref), sdconv(1, Ref))} internally.
\item Length-\code{nSim} vector or named array: used directly.
}}

\item{Misc}{\code{list}. Miscellaneous additional objects. Default \code{list()}.}
}
\value{
\itemize{
\item \code{CatchObs()} returns a \link[=catchobs-class]{catchobs} object.
\item \code{Landings()} and \code{Discards()}, when called on an \link[=obs-class]{obs} or
\link[=data-class]{data} object, return the corresponding \link[=catchobs-class]{catchobs} or
\link[=catchdata-class]{catchdata} slot directly (no data frame conversion).
\item \verb{Landings<-} and \verb{Discards<-} return \code{x} with the slot replaced.
}
}
\description{
Construct a \link[=catchobs-class]{catchobs} object defining observation error for landed or
discarded catch, or access and replace individual slots.
}
\details{
\subsection{Observation Error Generation}{

The error applied to simulated catch is \code{Bias[sim] * Error[sim, year]}. The
\code{Error} array (\verb{nSim x nYear}) is obtained in one of three ways, in order
of precedence:
\enumerate{
\item \strong{User-supplied \code{Error}}: stored directly; \code{CV} is not used.
\item \strong{Stochastic from \code{CV}}: if \code{Error} is \code{NULL} and \code{CV} is specified,
Internally the model generates a lognormal array where each cell is
\code{rlnorm(1, mconv(1, CV[sim]), sdconv(1, CV[sim]))}.
\item \strong{Conditioned from real data}: \code{.ConditionObs()} estimates \code{CV}, \code{Bias},
and \code{Error} from historical residuals; any user-supplied values are
replaced.
}

\code{Bias} is expanded internally: a scalar input is treated as
the CV of a lognormal from which one bias per simulation is drawn; a
length-\code{nSim} input is used directly.

\code{Ref} is expanded internally using the same convention as
\code{Bias}.
}

\subsection{Conditioning on Real Data}{

When real catch data are provided (via \code{OM@Data}) and \code{.ConditionObs()} is
run, \code{CV} and \code{Bias} are estimated from the historical residuals between
simulated and observed catch, and \code{Error} is populated to cover both
historical and projection years. Any user-supplied \code{CV}, \code{Bias}, and \code{Error}
values are replaced.
}

\subsection{Attaching to an Obs Object}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{Landings(obs) <- CatchObs(CV = 0.2)
Discards(obs) <- CatchObs(CV = 0.3, Bias = 1.1)
}\if{html}{\out{</div>}}
}
}
\examples{
# Uniform 20\% CV — same CV used for all simulations
co <- CatchObs(CV = 0.2)

# Stochastic CV — unique CV per simulation drawn from lognormal(mean=0.2, sd=0.05)
co <- CatchObs(CV = c(0.2, 0.05))

# Bias as a CV — unique bias per simulation drawn from lognormal(mean=1, sd=0.1)
co <- CatchObs(CV = 0.2, Bias = 0.1)

# Pre-specified error array — bypasses CV entirely
err <- array(rlnorm(48 * 20), dim = c(48, 20),
             dimnames = list(Sim = 1:48, Year = 2001:2020))
co <- CatchObs(Error = err)

# Attach to an obs object
obs <- Obs(Landings = CatchObs(CV = 0.2),
           Discards = CatchObs(CV = 0.3, Bias = 0.05))
}
\seealso{
\itemize{
\item \link[=catchobs-class]{catchobs} for the class definition.
\item \code{\link[=Obs]{Obs()}} for the enclosing observation model constructor.
\item \code{.ConditionObs()} for how these slots are estimated from real data.
}

Other obs:
\code{\link[=CompObs]{CompObs()}},
\code{\link[=EffortObs]{EffortObs()}},
\code{\link[=ExploitationObs]{ExploitationObs()}},
\code{\link[=IndicesObs]{IndicesObs()}},
\code{\link[=LifeHistoryObs]{LifeHistoryObs()}},
\code{\link[=Obs]{Obs()}},
\code{\link{catchobs-class}},
\code{\link{compobs-class}},
\code{\link{effortobs-class}},
\code{\link{exploitationobs-class}},
\code{\link{indicesobs-class}},
\code{\link{lifehistoryobs-class}},
\code{\link{obs-class}}
}
\concept{obs}
