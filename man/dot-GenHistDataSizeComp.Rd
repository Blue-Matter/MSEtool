% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-data-hist-sizecomp.R
\name{.GenHistDataSizeComp}
\alias{.GenHistDataSizeComp}
\title{Generate Historical Size Composition Data}
\usage{
.GenHistDataSizeComp(
  x,
  Data,
  Hist,
  HistYears,
  i,
  stocks,
  FleetNames,
  type = c("LandingsAtSize", "DiscardsAtSize")
)
}
\arguments{
\item{x}{Integer index of the simulation replicate.}

\item{Data}{Existing \code{data} object from \code{OM@Data}.}

\item{Hist}{A \code{hist} class object.}

\item{HistYears}{Numeric vector of historical years.}

\item{i}{Integer index of the stock complex.}

\item{stocks}{Integer vector of stock indices in the complex.}

\item{FleetNames}{Character vector of fleet names.}

\item{type}{Character. Either \code{"LandingsAtSize"} or \code{"DiscardsAtSize"}.}
}
\value{
A \link[=compdata-class]{compdata} object with:
\itemize{
\item \verb{@Name}: character vector of fleet names
\item \verb{@Value}: \verb{[nYear x nFleet x nSize]} array of composition counts,
\code{nSize} being the largest class count among the fleets (see
\link[=compdata-class]{compdata} for the fleet-relative position convention)
\item \verb{@Classes}: named list, one numeric vector of size class midpoints per
fleet
}
}
\description{
Internal function to generate simulated historical size composition data
(landings or discards) when real observations are not available.
}
\details{
\subsection{Early Exit Conditions}{

The function returns \code{slot(Data, type)} unchanged in three cases:
\itemize{
\item \strong{Real data exist}: \code{slot(Data, type)} is already populated
(\code{!EmptyObject(slot(Data, type))}).
\item \strong{No observation structure defined}: every fleet's \code{\link[=CompObs]{CompObs()}} object
is a default (unconditioned) object, as determined by \code{isNewObject()}.
\item \strong{True size compositions are all zero}: \code{Hist@LandingsAtSize} or
\code{Hist@DiscardsAtSize} are present but contain no fish, indicating size
composition data were not generated by the operating model.
}
}

\subsection{True Composition}{

True catch-at-size is extracted from \code{Hist@LandingsAtSize} or
\code{Hist@DiscardsAtSize} (selected by \code{type}) for replicate \code{x}, summed over
areas and stocks within the complex, computed independently for each
fleet since fleets are not required to share a size-class grid (see
\link[=compdata-class]{compdata}). The result is normalised to proportions
\eqn{\mathbf{q}} within each year and fleet.
}

\subsection{Composition Simulation Model}{

For each fleet and year where \code{SampleSize > 0}, the Dirichlet concentration
vector is constructed as:

\deqn{\alpha_b = \mathrm{ESS} \cdot \Theta \cdot q_b \cdot \exp(\mathrm{Shift}_b)}

A Dirichlet-Multinomial sample of size \code{SampleSize} is then drawn via
\code{\link[=rDirichletMultinomial]{rDirichletMultinomial()}}. Years where \code{SampleSize} is zero or \code{NA} are
left as \code{NA} in the output.

The slot defaults are:
\itemize{
\item \code{ESS}: falls back to \code{SampleSize} if \code{NULL}
\item \code{Theta}: defaults to \code{1} if \code{NULL}, recovering a near-multinomial draw
\item \code{Shift}: defaults to zero for all bins if \code{NULL}, applying no tilt
}
}

\subsection{Obs .Structure}{

Observation parameters are accessed via:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{Hist@OM@Obs[[i]][[fl]]@LandingsAtSize  # or @DiscardsAtSize
}\if{html}{\out{</div>}}

where \code{i} is the stock complex index and \code{fl} the fleet index. The relevant
\code{\link[=CompObs]{CompObs()}} slots are:
\itemize{
\item \verb{@SampleSize[sim, t]}: nominal sample size; output counts sum to this value
\item \verb{@ESS[sim, t]}: effective sample size scaling the concentration vector
\item \verb{@Theta[sim, t]}: Dirichlet-Multinomial dispersion parameter
\item \verb{@Shift[sim, t, bin]}: per-bin log-concentration offset
}

See \link[=obs-class]{obs} and \code{\link[=CompObs]{CompObs()}} for full slot documentation.
}
}
\seealso{
\code{\link[=CompObs]{CompObs()}}, \code{\link[=CompData]{CompData()}}, \link[=compdata-class]{compdata}, \link[=obs-class]{obs},
\code{\link[=rDirichletMultinomial]{rDirichletMultinomial()}}, \code{.GenHistDataAgeComp()}, \code{.GenHistDataCatch()}
}
\keyword{internal}
