% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpatialStock.R
\name{CalcMovement}
\alias{CalcMovement}
\alias{FitMovement}
\alias{CalcAsymptoticDist}
\alias{MarkovFrac}
\title{Calculate the Movement Matrix}
\usage{
CalcMovement(
  Spatial,
  TimeSteps = NULL,
  nsim = NULL,
  seed = NULL,
  nits = 100,
  plot = FALSE,
  silent = FALSE,
  control = list(iter.max = 5000, eval.max = 10000)
)

FitMovement(
  LogitProbs,
  UnfishedDist,
  ProbStaying,
  FracOther = NULL,
  CVDist = 0.1,
  CVStay = 1,
  nits = 100
)

CalcAsymptoticDist(Movement, ExpectedDist = NULL, nits = 100, plot = F)

MarkovFrac(LogitProbs, FracOther = NULL)
}
\arguments{
\item{nits}{Number of iterations}

\item{plot}{Logical. Produce the plot?}

\item{Movement}{A movement matrix with dimensions \code{c(narea, narea)}  where the
diagonals are the probability of staying in an area, and off-diagonals are the
probability of moving from area in row and column \code{i} to area in row \code{i} and column \code{j}}

\item{ExpectedDist}{Optional. A numeric vector of length \code{narea} describing the expected
unfished distribution.}
}
\value{
A numeric vector of length \code{nareas} with the asymptotic distribution
}
\description{
Calculate the Movement Matrix

Calculate Asymptotic Spatial Distribution
}
