% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-index-fit.R
\name{IndexFitTable}
\alias{IndexFitTable}
\title{Summarize Fitted/Estimated Index Observation Error Parameters}
\usage{
IndexFitTable(
  object,
  type = c("CPUE", "Survey"),
  Fleets = NULL,
  Stocks = NULL,
  print = TRUE
)
}
\arguments{
\item{object}{A \link[=hist-class]{hist} or \link[=mse-class]{mse} object.}

\item{type}{Character. One of \code{"CPUE"} or \code{"Survey"}.}

\item{Fleets}{Character vector. Restrict to these fleet names. Default
\code{NULL} (all fleets with a populated \code{type} index).}

\item{Stocks}{Character vector. Restrict to these stock/complex names.
Default \code{NULL} (all).}

\item{print}{Logical. Print a markdown table to console with \code{cat()}?
Default \code{TRUE}.}
}
\value{
returns the \code{data.frame}.
}
\description{
Tabulates the hyperstability (\code{Beta}), Beta fit diagnostics (\code{R2},
\code{PValue}, \code{Status}), and residual (\code{SD}, \code{AC}, \code{nYears}) parameters
stored on each fleet's \code{CPUE}/\code{Survey} \link[=indicesobs-class]{indicesobs} object.
Companion summary for \code{\link[=PlotIndexFit]{PlotIndexFit()}}.
}
\details{
Each parameter may vary by simulation. When more than one distinct value
is present, the column reports \code{median [min, max]}; a single fixed value
is reported as-is. \code{R2} and \code{PValue} come from the per-simulation \code{Beta}
fit (\code{Misc$BetaFit}, see \code{\link[=EstimateBeta]{EstimateBeta()}}) and are \code{NA} where \code{Beta} was
not freely estimated (fixed by the user or \code{SimControl(EstimateBeta = FALSE)}). \code{Status} lists the distinct fit outcomes across simulations
(\code{"estimated"}, \code{"fixed_user"}, \code{"fixed_insufficient_data"},
\code{"fixed_low_variance"}, \code{"fixed_not_significant"}, \code{"fixed_bounds"}).
\code{nYears} is the number of years used in the fit. \code{Beta}, \code{R2}, \code{PValue},
and \code{Status} are placed last since they're only meaningful when Beta
estimation was attempted; a column that is \code{NA} for every row (e.g. \code{R2}/
\code{PValue} when \code{Beta} was fixed everywhere) is dropped from the result.
}
\seealso{
\code{\link[=PlotIndexFit]{PlotIndexFit()}}, \code{\link[=IndicesObs]{IndicesObs()}}
}
