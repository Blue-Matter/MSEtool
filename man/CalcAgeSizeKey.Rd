% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CalcAgeSizeKey.R
\name{CalcAgeSizeKey}
\alias{CalcAgeSizeKey}
\title{Calculate an Age-Size Key}
\usage{
CalcAgeSizeKey(
  MeanAtAge,
  CVatAge,
  Classes,
  TruncSD = 2,
  Dist = c("normal", "lognormal"),
  AgeClasses = NULL,
  silent = FALSE,
  type = "Length"
)
}
\arguments{
\item{MeanAtAge}{The mean size at age. Either a numeric vector of length \code{nage}, a 2D array with
dimensions \code{c(nSim, nage)}, or a 3D array with dimensions \code{c(nSim, nage, nTS)}.
Alternatively, can be a \code{\link[=Length]{Length()}} object, in which case no other arguments are needed.}

\item{CVatAge}{The coefficient of variation (CV) at age. Same structure as \code{MeanAtAge}.}

\item{Classes}{A numeric vector with the midpoints of the size classes for the age-size key.}

\item{TruncSD}{Numeric value indicating the number of standard deviations
where the distribution is truncated. Use high values to approximate a non-truncated distribution}

\item{Dist}{The distribution of \code{MeanAtAge}. Character, either \code{normal} or \code{lognormal}}

\item{AgeClasses}{Optional. Numeric vector of values for the age classes. Defaults to \code{0:(nage-1)}.}
}
\value{
A 4D array with dimensions \code{nSim}, \code{nage}, \code{nClasses}, and \code{nTS}
}
\description{
Generates an Age-Size key given mean and standard deviation of size-at-age. The size-at-age
can be normally or log-normally distributed. By default the distribution is truncated at 2
standard deviations.
}
