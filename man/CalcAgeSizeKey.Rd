% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-age-size-key.R
\name{CalcAgeSizeKey}
\alias{CalcAgeSizeKey}
\title{Calculate an Age-Size Key}
\usage{
CalcAgeSizeKey(
  MeanAtAge,
  CVatAge,
  Classes,
  TruncSD = 2,
  Dist = c("normal", "lognormal"),
  silent = FALSE
)
}
\arguments{
\item{MeanAtAge}{Numeric vector, matrix, or 3D array of mean size-at-age, or
a \code{\link[=Length]{Length()}} object.}

\item{CVatAge}{Numeric vector, matrix, or 3D array of coefficient of variation (CV)
at age. Same structure as \code{MeanAtAge}.}

\item{Classes}{Numeric vector of lower bounds of size classes. Must be strictly
increasing and non-negative. Bins need not be equal width.}

\item{TruncSD}{Numeric value for the number of standard deviations at which the
distribution is truncated.}

\item{Dist}{Character string, either \code{"normal"} or \code{"lognormal"} indicating
the distribution of size-at-age.}

\item{silent}{Logical; if \code{TRUE}, suppress progress bar.}
}
\value{
A 4D array with dimensions \code{Sim}, \code{Age}, \code{Class}, and \code{Year}.
}
\description{
Generates an Age-Size key given mean and standard deviation of size-at-age. The size-at-age
can be normally or log-normally distributed. By default the distribution is truncated at 2
standard deviations.
}
\details{
Each bin \code{k} spans \verb{[Classes[k], Classes[k+1])}. The final bin captures all
probability above \code{Classes[nClass]}. Bins need not be equal width.
}
\examples{

nSim <- 5
nAge <- 15
nYear <- 2
ages <- 0:(nAge-1)
years <- 2001:2002

TruncSD <- 2
Dist <- 'normal'

set.seed(123)
Linf <- runif(nSim, min = 150, max = 200)
K <- 0.3      
t0 <- 0   

MeanAtAge <- array(0, 
                   dim = c(nSim, nAge, nYear),
                   dimnames = list(Sim = 1:nSim, Age = ages, Year = years))

for (sim in 1:nSim) {
  for (yr in 1:nYear) {
    MeanAtAge[sim, , yr] <- Linf[sim] * (1 - exp(-K * (ages - t0)))
  }
}

# Coefficient of variation (CV)
CVatAge <- array(0.1, dim = dim(MeanAtAge), dimnames = dimnames(MeanAtAge))

# Define size classes
SDatAge <- MeanAtAge * CVatAge
class_min <- floor(min(MeanAtAge - TruncSD * SDatAge))
class_max <- ceiling(max(MeanAtAge + TruncSD * SDatAge))

Classes <- seq(class_min, class_max, by = 10)


# Calculate Age-Size Key
ASK <- CalcAgeSizeKey(
  MeanAtAge = MeanAtAge,
  CVatAge = CVatAge,
  Classes = Classes,
  TruncSD = TruncSD,
  Dist = Dist
)

dim(ASK)


# Inspect first simulation, first year
ASK[1,,,1]

}
