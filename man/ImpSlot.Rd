% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-imp-slot.R
\name{ImpSlot}
\alias{ImpSlot}
\title{ImpSlot Constructor and Accessors}
\usage{
ImpSlot(Mean = NULL, SD = NULL, Compliance = NULL, Error = NULL, Misc = list())
}
\arguments{
\item{Mean}{Numeric array or list of arrays, or \code{NULL} (default). Mean
implemented fraction of the management recommendation (e.g. \code{1} = perfect
compliance with the TAC). When \code{NULL}, the slot is left at its class
default.}

\item{SD}{Numeric array or list of arrays, or \code{NULL} (default). Standard
deviation of implementation error around \code{Mean}. When \code{NULL}, the slot is
left at its class default.}

\item{Compliance}{Scalar, length-2 uniform bounds \code{c(lower, upper)}, a
\verb{[Sim x Year]} array, or \code{NULL} (default). Meaning depends on which
control (\code{TAC}, \code{Effort}, or \code{Size}) this \code{impslot} is attached to (see
Details). Values should be in \eqn{[0, 1]}.}

\item{Error}{Numeric array or list of arrays, or \code{NULL} (default). Realised
implementation error, a per simulation/year multiplicative factor
applied to the advised TAC or effort before effort-solving. When
supplied directly, \code{Mean} and \code{SD} are stored but \code{Error} is not
re-derived from them during simulation. When \code{NULL}, \code{Error} is
stochastically generated from
\code{Mean} and \code{SD} at simulation time (see \code{\link[=PopulateImpSlot]{PopulateImpSlot()}}).}

\item{Misc}{List. Miscellaneous additional objects. Default \code{list()}.}
}
\value{
\itemize{
\item \code{ImpSlot()} returns a new \link[=impslot-class]{impslot} object.
\item \code{Mean()}, \code{SD()}, \code{Compliance()}, \code{Error()} return the corresponding slot
from an \link[=impslot-class]{impslot} object \code{x}.
\item \verb{Mean<-}, \verb{SD<-}, \verb{Compliance<-}, \verb{Error<-} return \code{x} with the named
slot replaced.
}
}
\description{
Construct an \link[=impslot-class]{impslot} object representing implementation uncertainty
for a single management control, or access and replace individual slots.
}
\details{
\subsection{Mean, SD, and Error}{

\code{Mean} and \code{SD} together parameterise a lognormal distribution for
\code{Error}, a general implementation-error multiplier applied to the
advised TAC/effort regardless of how many stocks are in the OM (e.g.
\code{Mean = 0.9} models a fleet that implements 90\% of the recommended TAC
on average). See \code{\link[=PopulateImpSlot]{PopulateImpSlot()}} for exactly how \code{Mean}/\code{SD} expand
to \code{Error}.
}

\subsection{Compliance}{

For \code{TAC}/\code{Effort}, only meaningful for multi-stock/multi-complex OMs,
where it governs how a fleet reconciles competing TAC or effort
recommendations across stocks.

For \code{Size}, it is the fraction of the fleet adopting a newly-advised size
regulation.
}
}
\examples{
# Empty impslot
sl <- ImpSlot()

# With Mean and SD specified
sl <- ImpSlot(Mean = 1, SD = 0.1, Compliance = 0.9)

# Supplying Error directly (bypasses stochastic generation)
sl <- ImpSlot(Error = array(rnorm(100, 1, 0.05), dim = c(10, 10)))

}
\seealso{
\itemize{
\item \link[=impslot-class]{impslot} for the class definition.
\item \code{\link[=Imp]{Imp()}} for the enclosing constructor and accessor.
\item \code{\link[=TACImp]{TACImp()}}, \code{\link[=Effort]{Effort()}}, \code{\link[=Size]{Size()}} for slot-level accessors on
\link[=imp-class]{imp} objects.
}

Other imp:
\code{\link{FullComplianceImp}},
\code{\link[=Imp]{Imp()}},
\code{\link{OverageImp}},
\code{\link{PartialSizeComplianceImp}},
\code{\link{UnderageImp}},
\code{\link{imp-class}},
\code{\link{impslot-class}}
}
\concept{imp}
