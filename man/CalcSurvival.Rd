% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-survival.R
\name{CalcSurvival}
\alias{CalcSurvival}
\title{Calculate Cumulative Survival-at-Age}
\usage{
CalcSurvival(
  NaturalMortality,
  FishingMortality = NULL,
  PlusGroup = TRUE,
  SpawnTimeFrac = 0,
  Semelparous = FALSE
)
}
\arguments{
\item{NaturalMortality}{Numeric array of natural mortality-at-age. Either
a 2D array (\verb{Age × Year}) or 3D array (\verb{Sim × Age × Year}), with named
dimensions.}

\item{FishingMortality}{Optional numeric array of fishing mortality-at-age,
matching the dimensions of \code{NaturalMortality}. If \code{NULL} (default),
only natural mortality is used.}

\item{PlusGroup}{Logical. If \code{TRUE} (default), the plus-group survival is
adjusted for ongoing mortality beyond the maximum age class.}

\item{SpawnTimeFrac}{Numeric. Fractional position of spawning within a
time step, between 0 (start; default) and 1 (end). Either length 1
(applied to all simulations) or length \code{nSim}.}

\item{Semelparous}{Either \code{FALSE} (default) to ignore semelparous
mortality, or a numeric array of semelparous mortality-at-age matching
the dimensions of \code{NaturalMortality}.}
}
\value{
A numeric array of cumulative survival-at-age with the same
dimensions and dimnames as \code{NaturalMortality}. Each value represents
the probability of surviving from recruitment to that age class in that
year.
}
\description{
Computes cumulative survival from recruitment to each age class, accounting
for natural mortality, optional fishing mortality, spawn timing within a
time step, and optional semelparous mortality. The calculation follows:
}
\details{
\itemize{
\item \strong{Age 1}: \code{S[1] = exp(-Z[1] * SpawnTimeFrac)}
\item \strong{Age a}: \code{S[a] = S[a-1] * exp(-(Z[a-1] * (1 - f) + Z[a] * f)) * (1 - Semelparous[a-1])}
}

where \code{Z = M + F} and \code{f = SpawnTimeFrac}.

If \code{PlusGroup = TRUE}, the plus-group age class is adjusted to account for
ongoing mortality: \code{S[nAge] = S[nAge] / (1 - exp(-Z[nAge]))}.
}
