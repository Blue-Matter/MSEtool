% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-data.R
\name{CatchData}
\alias{CatchData}
\title{CatchData Constructor}
\usage{
CatchData(
  Name = NULL,
  Value = NULL,
  CV = NULL,
  Units = NULL,
  Ref = NULL,
  RefCV = NULL
)
}
\arguments{
\item{Name}{\code{character} or \code{NULL}. Fleet names, length \code{nFleet}.
Default \code{NULL}.}

\item{Value}{\code{array} or \code{NULL}. Observed catch values with dimensions
\verb{[nYear x nFleet]}. Default \code{NULL}.}

\item{CV}{\code{array} or \code{NULL}. Coefficients of variation for the catch
observations, matching the dimensions of \code{Value}. Default \code{NULL}.}

\item{Units}{\code{character} or \code{NULL}. Units of catch measurement, one element
per fleet: \code{"Biomass"} or \code{"Number"}. Default \code{NULL}. TAC is always set
in units of \code{"Biomass"}; see \code{\link[=CheckCatch]{CheckCatch()}} for the consequences of
supplying any other value.}

\item{Ref}{\code{array} or \code{NULL}. Reference catch values (e.g., a historical
baseline), matching the dimensions of \code{Value}. Default \code{NULL}.}

\item{RefCV}{\code{array} or \code{NULL}. Coefficients of variation for the reference
values, matching the dimensions of \code{Ref}. Default \code{NULL}.}
}
\value{
A \link[=catchdata-class]{catchdata} object.
}
\description{
Construct a \link[=catchdata-class]{catchdata} object storing observed landed or discarded
catch and associated uncertainty.
}
\details{
\subsection{Usage}{

All arguments are optional; an empty object is valid and slots are
populated later (e.g., by \code{.GenHistDataCatch()}).
}

\subsection{Attaching to a Data Object}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{Landings(data) <- CatchData(Value = myArray, CV = myCVArray)
Discards(data) <- CatchData(Value = myArray, CV = myCVArray)
}\if{html}{\out{</div>}}
}
}
\examples{
# Empty object
cd <- CatchData()

# With values
yrs <- 2000:2020
fleets <- c("Trawl", "Longline")
val <- array(runif(length(yrs) * length(fleets)),
             dim = c(length(yrs), length(fleets)),
             dimnames = list(Year = yrs, Fleet = fleets))
cd <- CatchData(Name  = fleets,
                Value = val,
                CV    = array(0.1, dim = dim(val), dimnames = dimnames(val)),
                Units = c("Biomass", "Biomass"))

}
\seealso{
\itemize{
\item \link[=catchdata-class]{catchdata} for the class definition.
\item \link[=data-class]{data} and \code{\link[=Data]{Data()}} for the enclosing data object.
\item \code{\link[=CatchObs]{CatchObs()}} for the observation error structure that generates
\link[=catchdata-class]{catchdata} objects during simulation.
}

Other data:
\code{\link[=CompData]{CompData()}},
\code{\link[=EffortData]{EffortData()}},
\code{\link[=IndicesData]{IndicesData()}},
\code{\link{catchdata-class}},
\code{\link{compdata-class}},
\code{\link{effortdata-class}},
\code{\link{indicesdata-class}}
}
\concept{data}
