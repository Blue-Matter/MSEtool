% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-atage-atsize.R
\name{.AtAge2AtSize}
\alias{.AtAge2AtSize}
\title{Convert Mean-at-Age to Mean-at-Size}
\usage{
.AtAge2AtSize(
  object,
  Length,
  max1 = TRUE,
  Years = NULL,
  ASKOverride = NULL,
  minAgeRes = 50
)
}
\arguments{
\item{object}{An S4 object with slots \code{MeanAtAge} (array:
\verb{Sim × Age × Year} or \verb{Sim × Age × Year × Area}) and \code{MeanAtLength}
(\code{NULL} if not yet populated).}

\item{Length}{A \code{\link[=Length]{Length()}} or \code{\link[=Weight]{Weight()}} object supplying \code{MeanAtAge},
\code{CVatAge}, \code{Classes}, \code{TruncSD}, \code{Dist}, and either an \code{ALK} or \code{AWK}
slot (array: \verb{Sim × Age × Class × Year}).}

\item{max1}{Logical. If \code{TRUE}, standardises output so the maximum value
across size classes is 1. Default \code{TRUE}.}

\item{Years}{Numeric vector of years. If \code{NULL} (default), the union of
years in \code{object@MeanAtAge} and the Age-Size Key is used.}

\item{ASKOverride}{Optional pre-computed Age-Size Key array, used in place
of calling \code{\link[=CalcAgeSizeKey]{CalcAgeSizeKey()}} when interpolation is triggered. Avoids
redundant computation in repeated calls.}

\item{minAgeRes}{Integer. Minimum number of age classes required before
linear interpolation to a finer age resolution is triggered. Default \code{50}.}
}
\value{
\code{object} with \code{object@MeanAtLength} and \code{object@Classes} populated.
}
\description{
Populates \code{object@MeanAtLength} (or \code{object@MeanAtWeight}) by projecting
\code{object@MeanAtAge} through a column-standardised Age-Size Key (ALK or AWK)
from a \code{\link[=Length]{Length()}} or \code{\link[=Weight]{Weight()}} object. If the number of age classes is
below \code{minAgeRes}, both \code{MeanAtAge} and the Age-Size Key are linearly
interpolated to a finer age resolution before conversion, ensuring the
at-age schedule has sufficient resolution to fill all size classes. Returns
\code{object} unchanged if \code{MeanAtLength} is already populated.
}
\keyword{internal}
