% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor-obs.R
\name{EffortObs}
\alias{EffortObs}
\title{EffortObs Constructor and Accessors}
\usage{
EffortObs(
  CV = NULL,
  Bias = NULL,
  Error = NULL,
  Years = NULL,
  Units = NULL,
  Ref = NULL,
  Misc = list()
)
}
\arguments{
\item{CV}{\code{numeric} or \code{NULL}. Coefficient of variation of the lognormal
effort observation error. Accepted forms:
\itemize{
\item \code{NULL} (default): no observation error is generated unless \code{Error} is
supplied directly.
\item Length-1 scalar: a single CV applied uniformly across all simulations
and years when generating the \code{Error} array internally.
\item Length-2 vector \code{c(mean, sd)}: \code{CV} is itself drawn stochastically —
a unique CV per simulation is generated via \code{.StructurePars()} before
being used to generate the \code{Error} array.
\item Length-\code{nSim} named array: used directly, one CV per simulation.
}}

\item{Bias}{\code{numeric} or \code{NULL}. Multiplicative observation bias. Accepted
forms:
\itemize{
\item \code{NULL} (default): bias defaults to \code{1} (unbiased) internally.
\item Positive scalar: interpreted as the CV of a lognormal distribution; a
unique bias is drawn per simulation as
\code{rlnorm(nSim, mconv(1, Bias), sdconv(1, Bias))}.
\item Length-2 vector \code{c(lower, upper)} (when \code{nSim != 2}): treated as
uniform bounds; a unique bias per simulation is drawn via
\code{runif(nSim, lower, upper)}.
\item Length-\code{nSim} vector or named array: used directly, one bias per
simulation.
}}

\item{Error}{\code{numeric} array or \code{NULL}. Pre-specified lognormal observation
error multipliers with dimensions \verb{nSim x nYear} and named dimnames
(\code{Sim}, \code{Year}). When supplied, \code{Error} is used directly and \code{CV} is
ignored for error generation. Default \code{NULL}, in which case \code{Error} is
generated from \code{CV} internally.}

\item{Years}{\code{numeric} vector or \code{NULL}. Calendar years over which the
observation error applies. Default \code{NULL} (all historical years).}

\item{Units}{\code{character(1)} or \code{NULL}. Units of effort
(e.g., \code{"hours"}, \code{"trips"}, \code{"unitless"}). Default \code{NULL}.}

\item{Ref}{\code{numeric} or \code{NULL}. Reference effort value. Accepted forms:
\itemize{
\item \code{NULL} (default): no reference value.
\item Positive scalar: interpreted as the CV of a lognormal distribution; a
unique reference value is drawn per simulation as
\code{rlnorm(nSim, mconv(1, Ref), sdconv(1, Ref))} internally.
\item Length-\code{nSim} vector or named array: used directly.
}}

\item{Misc}{\code{list}. Miscellaneous additional objects. Default \code{list()}.}
}
\value{
\itemize{
\item \code{EffortObs()} returns an \link[=effortobs-class]{effortobs} object.
\item \code{Effort()} (accessor) returns the \code{Effort} slot from object \code{x}.
\item \verb{Effort<-} returns \code{x} with the \code{Effort} slot replaced.
\item \code{CV()}, \code{Bias()}, \code{Units()}, \code{Years()}, \code{Ref()} return the corresponding
slot from an \link[=effortobs-class]{effortobs} object \code{x}.
\item Their replacement forms return \code{x} with the corresponding slot updated.
}
}
\description{
Construct an \link[=effortobs-class]{effortobs} object defining observation error for fishing
effort data, or access and replace individual slots.
}
\details{
\subsection{Observation Error Generation}{

The error applied to simulated effort is \code{Bias[sim] * Error[sim, year]}. The
\code{Error} array (\verb{nSim x nYear}) is obtained in one of two ways, in order of
precedence:
\enumerate{
\item \strong{User-supplied \code{Error}}: stored directly; \code{CV} is not used.
\item \strong{Stochastic from \code{CV}}: if \code{Error} is \code{NULL} and \code{CV} is specified,
Internally the model generates a lognormal array where each cell is
\code{rlnorm(1, mconv(1, CV[sim]), sdconv(1, CV[sim]))}. \code{CV} is first
expanded to length \code{nSim} internally — a length-2 input
triggers a stochastic draw of a unique CV per simulation.
}

\strong{Note}: Effort observation error is not currently conditioned from observed
data.

\code{Bias} is expanded internally: a scalar input is treated as
the CV of a lognormal from which one bias per simulation is drawn; a
length-\code{nSim} input is used directly.

\code{Ref} is expanded internally using the same convention as
\code{Bias}.

Unlike catch and index observation error, effort observation error is not
conditioned on real data; the user-supplied \code{CV}, \code{Bias}, and \code{Error} are
always used as specified.
}

\subsection{Attaching to an Obs Object}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{Effort(obs) <- EffortObs(CV = 0.15)
}\if{html}{\out{</div>}}
}
}
\examples{
# Uniform 15\% CV
eo <- EffortObs(CV = 0.15)

# Stochastic CV — unique CV per simulation drawn from lognormal(mean=0.15, sd=0.03)
eo <- EffortObs(CV = c(0.15, 0.03))

# Bias as a CV — unique bias per simulation
eo <- EffortObs(CV = 0.15, Bias = 0.05)

# Attach to obs
obs <- Obs(Effort = EffortObs(CV = 0.15))

}
\seealso{
\itemize{
\item \link[=effortobs-class]{effortobs} for the class definition.
\item \code{\link[=Obs]{Obs()}} for the enclosing observation model constructor.
}

Other obs:
\code{\link[=CatchObs]{CatchObs()}},
\code{\link[=CompObs]{CompObs()}},
\code{\link[=ExploitationObs]{ExploitationObs()}},
\code{\link[=IndicesObs]{IndicesObs()}},
\code{\link[=LifeHistoryObs]{LifeHistoryObs()}},
\code{\link[=Obs]{Obs()}},
\code{\link{catchobs-class}},
\code{\link{compobs-class}},
\code{\link{effortobs-class}},
\code{\link{exploitationobs-class}},
\code{\link{indicesobs-class}},
\code{\link{lifehistoryobs-class}},
\code{\link{obs-class}}
}
\concept{obs}
