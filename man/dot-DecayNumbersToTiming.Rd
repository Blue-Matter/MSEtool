% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-data-index-timing.R
\name{.DecayNumbersToTiming}
\alias{.DecayNumbersToTiming}
\title{Decay numbers-at-age to an observation point within the time step}
\usage{
.DecayNumbersToTiming(Number, Proj, stocks, x, TSIndex, timing, areas)
}
\arguments{
\item{Number}{List (per stock) of numbers, \verb{[Age x Area]} for a single time
step or \verb{[Age x Year x Area]} for several.}

\item{Proj}{A \code{hist}/\code{Proj} object.}

\item{stocks}{Integer vector of stock indices matching \code{Number}.}

\item{x}{Integer. Simulation index.}

\item{TSIndex}{Integer vector of time-step indices, matching \code{Number}.}

\item{timing}{Numeric. Fraction of the time step, in \verb{[0, 1)}.}

\item{areas}{Integer vector of area indices to include.}
}
\value{
\code{Number}, decayed. Returned unchanged when \code{timing} is \code{0}, \code{NA}, or
the mortality needed to decay it is unavailable.
}
\description{
An index observed part-way through a time step sees the population after the
mortality accumulated up to that point, so numbers are decayed by
\eqn{\exp(-Z_a \tau)}. \code{Timing} is a fraction of the \emph{time step}, so in a
seasonal model it is within-season - the same convention as
\code{Stock@SpawnTimeFrac}.
}
\details{
Applied identically when generating historical and projected indices, so the
two are on the same footing and catchability is not left absorbing a timing
offset across the projection boundary.
}
\keyword{internal}
