% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set-seasonal-R0.R
\name{SetSeasonalR0}
\alias{SetSeasonalR0}
\title{Set Seasonal R0 Using a Wrapped Normal Distribution}
\usage{
SetSeasonalR0(AnnualR0, Seasons, PeakSeason, Sigma, Years = NULL)
}
\arguments{
\item{AnnualR0}{Numeric. Annual unfished recruitment. Accepted forms:
a scalar (same value for all simulations), a numeric vector of length
\code{nSim}, or an \verb{nSim x 1} matrix. Per-season values sum to \code{AnnualR0}
for each simulation.}

\item{Seasons}{Integer. Number of seasons per year (must match \code{OM@Seasons}).}

\item{PeakSeason}{Numeric. Season of peak recruitment. Doesn't need to be an
integer; e.g. \code{3.5} places the peak midway between seasons 3 and 4.
Passed as \code{mu} to \code{\link[=WrappedNormal]{WrappedNormal()}}.}

\item{Sigma}{Numeric. Spread of recruitment around the peak, in units of
seasons. Smaller values concentrate recruitment; larger values spread it
across the year. Passed as \code{sigma} to \code{\link[=WrappedNormal]{WrappedNormal()}}.}

\item{Years}{Optional numeric vector of year labels, e.g. from
\code{Years(OM, 'Historical')}. If provided, the \code{Year} dimension of the
returned matrix is named using the first \code{Seasons} elements
(\code{Years[1:Seasons]}). If \code{NULL} (default), dimension names are omitted.}
}
\value{
An \verb{nSim x nSeason} numeric matrix of per-season R0 values.
If \code{Years} is supplied, \code{dimnames = list(Sim = 1:nSim, Year = Years[1:Seasons])}.
}
\description{
Distributes annual unfished recruitment (\code{AnnualR0}) across seasons using a
wrapped normal distribution, producing an \verb{nSim x nSeason} matrix of
per-season R0 values that sum to \code{AnnualR0} for each simulation.
}
\details{
Seasonal recruitment is controlled entirely by \code{SRR@R0}: at unfished
equilibrium the stock-recruit relationship returns exactly \code{R0[m]} recruits
in season \code{m}. Setting \code{R0[m] = pi_m * AnnualR0} via this function is
therefore both necessary and sufficient to produce the target seasonal
recruitment pattern. Optionally call \code{\link[=AdjustSeasonalFecundity]{AdjustSeasonalFecundity()}} afterwards
to also concentrate reported seasonal spawning production in the same
seasons, reflecting the biological reality that fish only spawn during
certain months.
}
\seealso{
\code{\link[=WrappedNormal]{WrappedNormal()}}, \code{\link[=AdjustSeasonalFecundity]{AdjustSeasonalFecundity()}}
}
