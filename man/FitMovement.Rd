% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-spatial-movement.R
\name{FitMovement}
\alias{FitMovement}
\title{Fit a Movement Matrix to Target Spatial Parameters}
\usage{
FitMovement(Spatial, sim = 1, age = 1, year = 1)
}
\arguments{
\item{Spatial}{A populated \link[=spatial-class]{spatial} object whose \code{UnfishedDist},
\code{ProbStaying}, and (for multi-area models) \code{FracOther} slots have been
expanded to named arrays.}

\item{sim}{\code{integer(1)}. Simulation index. Clamped to the \code{Sim} dimension
of each slot if that dimension has length 1. Default \code{1}.}

\item{age}{\code{integer(1)}. Age index (position in the \code{Age} dimension).
Clamped to the \code{Age} dimension if length 1. Default \code{1}.}

\item{year}{\code{integer(1)}. Year index (position in the \code{Year} dimension).
Clamped to the \code{Year} dimension if length 1. Default \code{1}.}
}
\value{
A numeric matrix of dimensions \verb{nArea × nArea}. Each row gives the
movement probabilities from that area to all areas (including itself),
and rows sum to exactly 1.
}
\description{
Fits a row-stochastic movement matrix for a single simulation, age class,
and year by numerical optimisation, so that the asymptotic (equilibrium)
unfished distribution and the diagonal (staying probability) simultaneously
reproduce the targets in \code{Spatial@UnfishedDist} and \code{Spatial@ProbStaying}.
Dispatches to \code{\link[=FitMovement_2_Area]{FitMovement_2_Area()}} for two-area models and
\code{\link[=FitMovement_Multi_Area]{FitMovement_Multi_Area()}} for three or more areas.
}
\details{
This function is called internally by \code{\link[=Populate]{Populate()}} for every combination of
simulation, age, and year. It can also be called directly to inspect or
debug the fitted movement matrix for a specific slice of a \link[=spatial-class]{spatial}
object.
}
\seealso{
\itemize{
\item \code{\link[=Spatial]{Spatial()}} for the constructor and full spatial specification.
\item \code{\link[=FitMovement_2_Area]{FitMovement_2_Area()}} for the two-area optimisation.
\item \code{\link[=FitMovement_Multi_Area]{FitMovement_Multi_Area()}} for the multi-area optimisation.
\item \code{\link[=Populate]{Populate()}} which calls this function internally.
}
}
