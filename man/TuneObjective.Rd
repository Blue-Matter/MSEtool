% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune-mp.R
\name{TuneObjective}
\alias{TuneObjective}
\alias{TuneConstraint}
\title{Specify the Objective and Constraints for Tuning an MP}
\usage{
TuneObjective(
  PM,
  ...,
  Value = "Mean",
  Stocks = NULL,
  StockSummary = c("sum", "mean", "min", "max"),
  HistSummary = c("weighted", "pooled", "min", "max"),
  Hists = NULL,
  Name = NULL
)

TuneConstraint(
  PM,
  ...,
  Min = NULL,
  Max = NULL,
  Value = "Mean",
  Stocks = NULL,
  StockSummary = c("worst", "mean", "sum", "min", "max"),
  HistSummary = c("weighted", "pooled", "worst", "min", "max"),
  Hists = NULL,
  Name = NULL
)
}
\arguments{
\item{PM}{A performance metric function (class \code{pm}, see \link{PM} and
\code{\link[=NewPM]{NewPM()}}), or its name.}

\item{...}{Arguments passed to \code{PM}, e.g. \code{Ref}, \code{Years}, \code{Lim}.}

\item{Value}{\code{'Mean'} (default; \code{pm@Mean}), \code{'Stat'} (mean over
simulations of \code{pm@Stat}), or a function of the \code{pm} object returning a
Stock x MP matrix.}

\item{Stocks}{\code{NULL} (default, all) or the names of the stocks (complexes)
to use.}

\item{StockSummary}{Character. How the metric is combined across stocks.}

\item{HistSummary}{Character. How the metric is combined across \code{Hist}
objects.}

\item{Hists}{\code{NULL} (default, all) or the names or positions of the \code{Hist}
objects the metric applies to.}

\item{Name}{Character. Label of the metric. Default is the name of \code{PM}.}

\item{Min, Max}{\code{NULL} or a number: minimum and maximum allowed values.
At least one is needed.}
}
\value{
A list of class \code{tunemetric}.
}
\description{
\code{TuneObjective()} specifies the performance metric maximised by
\code{\link[=TuneMP]{TuneMP()}}; \code{TuneConstraint()} a performance metric that must be at least
\code{Min} and/or at most \code{Max}.
}
\details{
A metric is calculated for each \code{Hist} object and stock (or complex, see
\link{PM}) as \code{Value} of the \link[=pm-class]{pm} object returned by \code{PM}, then combined
across stocks (\code{StockSummary}) and across \code{Hist} objects (\code{HistSummary}).
\code{'worst'} requires the constraint to hold for every stock (or \code{Hist})
separately; \code{'weighted'} averages with \code{HistWeights}; \code{'pooled'} averages
with weights proportional to the number of simulations (equivalent to
pooling the simulations of all \code{Hist} objects).
}
\examples{
TuneObjective(PM_Landings, Years = 2031:2050)
TuneConstraint(PM_SBSBMSY, Min = 0.6)
TuneConstraint(PM_AAVY, Value = 'Mean', Max = 0.2)

}
\seealso{
\code{\link[=TuneMP]{TuneMP()}}, \link{PM}
}
