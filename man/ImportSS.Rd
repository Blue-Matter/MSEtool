% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-ss3.R
\name{ImportSS}
\alias{ImportSS}
\title{Import a Stock Synthesis Model into an Operating Model}
\usage{
ImportSS(
  SSDir,
  Name = "Imported SS3 Model",
  nSim = 48,
  pYear = 30,
  Agency = "",
  Author = "",
  Email = "",
  Region = "",
  Latitude = numeric(),
  Longitude = numeric(),
  Sponsor = "",
  StockName = NULL,
  CommonName = NULL,
  Species = NULL,
  FleetNames = NULL,
  Interval = 1,
  DataLag = 0,
  MPStartYear = NULL,
  InterimAdvice = NULL,
  LengthUnits = "cm",
  WeightUnits = "kg",
  R0Units = 1,
  silent = FALSE,
  Populate = TRUE,
  ...
)
}
\arguments{
\item{SSDir}{Character string giving the path to a directory containing SS3
output files, a vector of such paths, or a list of SS3 report objects
generated by \code{\link[=ImportSSReport]{ImportSSReport()}}}

\item{Name}{Character string giving the name of the imported operating model.}

\item{nSim}{Integer specifying the number of simulations. Ignored if
multiple SS3 runs are detected; in that case \code{nSim} is set to the number
of SS3 runs imported.}

\item{pYear}{Integer specifying the number of projection years.}

\item{Agency}{Character string giving the management or scientific agency.}

\item{Author}{Character string giving the model author(s).}

\item{Email}{Character string giving the author(s) email address.}

\item{Region}{Character string describing the geographic region.}

\item{Latitude}{Numeric vector giving stock latitude(s).}

\item{Longitude}{Numeric vector giving stock longitude(s).}

\item{Sponsor}{Character string giving the project sponsor.}

\item{StockName}{Optional character vector of stock names. Length must match
the number of stocks inferred from the SS3 model.}

\item{CommonName}{Optional character vector of common names for each stock.}

\item{Species}{Optional character vector of species names for each stock.}

\item{FleetNames}{Optional character vector of fleet names. If \code{NULL},
fleet names are inferred from SS3 output.}

\item{Interval}{Integer specifying the number of years between management actions.}

\item{DataLag}{Integer specifying the observation lag in years.}

\item{MPStartYear}{Numeric or \code{NULL}. First calendar year in which MPs are
applied; see \code{\link[=OM]{OM()}}/\link[=om-class]{om}. Default \code{NULL}.}

\item{InterimAdvice}{A \code{data.frame} or \code{NULL}. Fixed or stochastic
TAC/Effort values for years before \code{MPStartYear}; see \code{\link[=OM]{OM()}}/\link[=om-class]{om}.
Default \code{NULL}.}

\item{LengthUnits}{Character string giving the physical unit of length
measurements in the SS3 model (e.g., \code{"cm"}, \code{"mm"}, \code{"inch"}; see
\code{\link[=ValidUnits]{ValidUnits()}}). SS3/\code{r4ss} output does not report this, so it cannot be
inferred; \code{"cm"} is the near-universal SS3 convention. Override if the
source model used different units. Default \code{"cm"}.}

\item{WeightUnits}{Character string giving the physical unit of weight
measurements in the SS3 model (e.g., \code{"kg"}, \code{"g"}, \code{"lb"}; see
\code{\link[=ValidUnits]{ValidUnits()}}). Not reported by SS3/\code{r4ss}; \code{"kg"} is the conventional
pairing with \code{LengthUnits = "cm"} (SS3's \code{WtLen1}/\code{WtLen2} growth
parameters are calibrated for cm/kg allometry). Override if the source
model used different units. Default \code{"kg"}.}

\item{R0Units}{Numeric scaling factor for unfished recruitment (\code{R0}) and
related numbers-at-age quantities; see \code{Units} in \link[=srr-class]{srr}. SS3 does
not export the numeric scale of its population numbers (e.g. individuals
vs. thousands) as metadata, so this cannot be inferred and must be
supplied by the user if not \code{1} (absolute numbers of fish). Default \code{1}.}

\item{silent}{Logical; if \code{TRUE}, suppress informational output during import.}

\item{Populate}{Logical; if \code{TRUE} (default), populate the OM using
\code{PopulateOM()}. If \code{FALSE}, return the partially constructed OM.}

\item{...}{Additional arguments passed to SS3 import functions.}
}
\value{
\itemize{
\item \code{ImportSS()} returns an \link[=om-class]{om} object containing stock, fleet, data,
observation, and allocation components derived from the SS3 model.
\item \code{ImportSSReport()} returns a list of SS3 report objects.
}
}
\description{
Import a Stock Synthesis 3 (SS3) model and convert it
into an \code{\link[=OM]{OM()}} object.
}
\details{
\code{ImportSS()} reads SS3 output files from \code{SSDir} and constructs an
\code{\link[=OM]{OM()}} object. Biological processes, fleet structure, and historical time series
are inferred directly from the SS3 report output and mapped into the
corresponding OM slots.
\itemize{
\item Multiple simulation replicates are supported when multiple SS3 runs are detected.
\item Optional metadata describing authorship, region, and stock identity are stored in the resulting OM.
\item By default, the OM is fully populated using \code{PopulateOM()}.
\item Set \code{Populate = FALSE} to return the partially constructed OM.
}

\code{ImportSSReport()} reads SS3 output generated by Stock Synthesis 3,
either from one or more directories or from existing \code{r4ss} report objects,
and returns a standardized list of SS3 report lists.
}
\section{Functions}{

\itemize{
\item \code{ImportSS()} Convert SS3 output into a fully specified operating
model suitable for management strategy evaluation.
\item \code{ImportSSReport()} Read SS3 output from directories or existing
\code{r4ss} report objects and return a standardized list of SS3 reports.
}
}

\examples{
\dontrun{
## Path to a directory containing SS3 output
ss_dir <- "path/to/SS3/output"

## Read SS3 output only
reps <- ImportSSReport(ss_dir)

## Build an operating model from SS3 output
OM <- ImportSS(
  SSDir = reps,
  Name = "Example SS3 Import",
  Author = "A. Scientist",
  Region = "Example Region",
  StockName = "ExampleStock",
  FleetNames = c("Commercial", "Recreational")
)

## Inspect imported stocks and fleets
StockNames(OM)
FleetNames(OM)
}
}
\seealso{
\itemize{
\item \code{\link[=OM]{OM()}}
\item \code{\link[=PopulateOM]{PopulateOM()}}
\item \code{\link[=CompareSS]{CompareSS()}}
\item \url{https://nmfs-ost.github.io/ss3-website/} (Stock Synthesis 3)
}
}
