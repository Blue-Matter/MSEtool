% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert-mse-to-slick.R
\name{.MSE2Kobe}
\alias{.MSE2Kobe}
\title{Build a Kobe Plot Object from an \code{mse} Object}
\usage{
.MSE2Kobe(MSE, Complex = NULL)
}
\arguments{
\item{MSE}{An \link[=mse-class]{mse} object or a named list of \link[=mse-class]{mse} objects.}

\item{Complex}{Character, or \code{NULL} (default: the first complex).}
}
\value{
A \code{\link[Slick:Kobe]{Slick::Kobe()}} object with \code{Value} array of dimensions
\verb{Sim × OM × MP × PI × Year}, where \code{PI} contains SB/SBMSY (index 1)
and F/FMSY (index 2).
}
\description{
Constructs a \code{\link[Slick:Kobe]{Slick::Kobe()}} object containing SB/SBMSY and F/FMSY
time series for the projection period, for use in the Slick Kobe plot.
The Kobe plot compares MP performance with respect to biomass (x-axis)
and fishing mortality (y-axis) relative to MSY-based reference points.
}
\details{
Kobe's \code{Value} array has no stock/complex dimension (\code{PI} is fixed at
exactly SB/SBMSY and F/FMSY) -- a Kobe plot is inherently single-stock, so
\code{Complex} selects which one to show when \code{MSE} has more than one.
\code{SB_SBMSY} is resolved via the same complex- and spawning-stock-level
aggregation \code{PM_Status()} uses (not the raw per-stock \code{SB_SBMSY()}
accessor), so a sex-linked pair (e.g. \link{HermOM}) correctly uses the female
sub-stock; \code{F_FMSY()} is already complex-level.
}
\keyword{internal}
