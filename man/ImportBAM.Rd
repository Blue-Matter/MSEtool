% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-bam.R
\name{ImportBAM}
\alias{ImportBAM}
\alias{GetBAMOutput}
\alias{ListBAMStocks}
\title{Import BAM Output into an Operating Model}
\usage{
ImportBAM(
  Stock = "Red Snapper",
  nSim = 48,
  pYear = 30,
  StockName = NULL,
  Source = NULL,
  DiscMortDF = NULL,
  DiscFleets = NULL,
  DiscSelFleets = NULL,
  RetSelFleets = NULL,
  incData = TRUE,
  SurveyNames = NULL,
  UnitsLandings = NULL,
  UnitsDiscards = NULL,
  populate = TRUE,
  silent = FALSE
)

GetBAMOutput(Stock = "Red Snapper", type = c("rdat", "dat"))

ListBAMStocks(type = c("rdat", "dat"))
}
\arguments{
\item{Stock}{Character string matching a stock name available in \code{bamExtras}
(e.g., \code{'Red Snapper'}), or a list of BAM output objects containing
elements \code{rdat} and \code{dat}.}

\item{nSim}{Integer. Number of simulations. Default \code{48}.}

\item{pYear}{Integer. Number of projection years. Default \code{30}.}

\item{StockName}{Optional character string to override the species name taken
from the BAM output.}

\item{Source}{Optional. Character string specifying the source (usually
a link) for the OM.}

\item{DiscMortDF}{Optional data.frame with columns \code{Fleet}, \code{Value}, and
\code{Year} specifying discard mortality rates by fleet and time block. If
\code{NULL}, values are extracted directly from BAM parameters.}

\item{DiscFleets}{Optional named character vector mapping retain fleet names
to their corresponding discard fleet names in the BAM output, for cases
where naming conventions differ.}

\item{DiscSelFleets}{Optional named character vector mapping retain fleet
names to the selectivity series to use for discards.}

\item{RetSelFleets}{Optional named character vector mapping retain fleet
names to alternative retention selectivity series.}

\item{incData}{Logical. Include \code{Data} in the input? Default = \code{TRUE}}

\item{SurveyNames}{A character vector of user-facing names for survey
indices detected in \code{BAMdata}. Must have the same length as the number of
survey indices found (i.e. indices not matched to any fleet name). Use
this to override the default names derived from column names.
Used by \code{\link[=ImportBAMData]{ImportBAMData()}}.}

\item{UnitsLandings}{A character vector of length equal to the number of
landings fleets detected. Each element must be one of:
\itemize{
\item \code{"1000 lb"} — values will be converted from thousands of pounds to kg.
\item \code{"1000 n"} — values will be multiplied by 1,000 (number in thousands).
Used by \code{\link[=ImportBAMData]{ImportBAMData()}}.
}}

\item{UnitsDiscards}{A character vector of length equal to the number of
discard fleets detected. Accepts the same values as \code{UnitsLandings}.
Used by \code{\link[=ImportBAMData]{ImportBAMData()}}.}

\item{populate}{Logical. If \code{TRUE} (default), calls \code{\link[=PopulateOM]{PopulateOM()}} to
populate the OM after construction.}

\item{silent}{Logical. If \code{TRUE}, suppresses console messages. Default
\code{FALSE}.}

\item{type}{Character. Format of BAM output to retrieve: \code{'rdat'}
(default) returns the R data object; \code{'dat'} returns the raw input data.}
}
\value{
\code{ImportBAM()} returns a populated OM object. \code{GetBAMOutput()} returns
a BAM output list of class \code{BAMdata} (for \code{type = 'rdat'}).
\code{ListBAMStocks()} returns a character vector of available stock names.

\code{ImportBAM()} requires the \code{bamExtras} package, which can be installed with
\code{pak::pkg_install('nikolaifish/bamExtras')}.

The \code{DiscMortDF} argument accepts a data.frame with columns:
\describe{
\item{Fleet}{Fleet name matching the retain fleet names in the OM.}
\item{Value}{Discard mortality rate (0--1).}
\item{Year}{The year which the value takes effect (i.e., the
value applies to all years after this year).}
}

When \code{DiscFleets}, \code{DiscSelFleets}, or \code{RetSelFleets} are provided, they
should be named character vectors where names are the retain fleet names and
values are the corresponding BAM fleet/selectivity names to use instead of
the defaults.
}
\description{
Imports output from the Beaufort Assessment Model
(\href{https://repository.library.noaa.gov/view/noaa/4847}{BAM}) and converts it
into an operating model (\code{\link[=OM]{OM()}}) object. Stock and fleet objects are populated
from BAM parameters and time series, including selectivity, retention,
discard mortality, recruitment deviations, and stock-recruitment
relationships.
}
\seealso{
\code{\link[=ImportBAMData]{ImportBAMData()}}
}
