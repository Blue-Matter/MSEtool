% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-om-complex.R
\docType{data}
\name{ComplexOM}
\alias{ComplexOM}
\title{Stock Complex Example Operating Model}
\format{
An \link[=om-class]{om} object with the following slots populated:
\itemize{
\item \strong{\code{Name}}: \code{"Stock Complex - Single Fleet"}.
\item \strong{\code{nSim}}: \code{8}. Number of stochastic simulations.
\item \strong{\code{nYear}}: \code{20}. Number of historical years.
\item \strong{\code{pYear}}: \code{30}. Number of projection years.
\item \strong{\code{Stock}}: a length-2 list of \verb{[[AlbacoreExStock, ButterfishExStock]]}.
\item \strong{\code{Fleet}}: a length-2 list, each containing a length-1 list of
\link{AsympExFleet}. Structure is \verb{[[stock]][[fleet]]}. Both stocks are
fished by the same fleet type.
\item \strong{\code{Obs}}: a length-1 list containing a length-1 list of
\link{AgeStructuredObs}. Structure is \verb{[[complex]][[fleet]]}. A single
observation model applies to the whole complex, since data are
aggregated across both stocks before being observed.
\item \strong{\code{Complexes}}: \code{list(Stock_Complex = 1:2)}. Both stocks are assigned
to a single complex named \code{"Stock_Complex"}, so data and management
advice are aggregated across stocks 1 and 2.
\item \strong{\code{Imp}}: \code{NULL}. Implementation error is not specified; the \code{Imp}
slot retains its class default. See \code{\link[=Imp]{Imp()}} for details.
}
}
\usage{
ComplexOM
}
\description{
An example \link[=om-class]{om} object representing two stocks managed jointly as a
single complex, each exploited by one fleet. The \code{Complexes} slot groups
both stocks so that data and management advice are aggregated across the
complex. Intended to illustrate stock complex construction.
Pairings of stocks, fleets, and observation models are for illustration
only and should not be taken as ecologically meaningful. See also
\link{SingleStockOM}, \link{TwoFleetOM}, and \link{MultiStockOM} for structural variants.
}
\details{
\subsection{Stocks}{

Two biologically contrasting stocks are included:
\itemize{
\item \link{AlbacoreExStock}: long-lived, slow-growing, low natural mortality.
\item \link{ButterfishExStock}: short-lived, fast-growing, high natural mortality.
}

See \link{AlbacoreExStock} and \link{ButterfishExStock} for full parameter details.
}

\subsection{Stock Complex}{

Setting \code{Complexes = list(Stock_Complex = 1:2)} instructs the operating
model to treat both stocks as a single management unit. Catch and index
data are aggregated across the complex when generating management advice,
while population dynamics are simulated independently for each stock.
}

\subsection{Fleet}{

\link{AsympExFleet} is applied to both stocks, representing a single fleet type
with asymptotic length-based selectivity. See \link{AsympExFleet} for full
parameter details.
}

\subsection{Observation Model}{

Because both stocks are aggregated into a single complex, the \code{Obs} slot
is indexed by complex and fleet rather than by individual stock. A single
\link{AgeStructuredObs} object applies to the complex as a whole: landings, a
spawning-biomass survey, and annual age-composition samples from landed
and discarded fish. See \link{AgeStructuredObs} for full parameter details.
}

\subsection{Implementation Error}{

The \code{Imp} slot is \code{NULL}. The \link[=imp-class]{imp} is currently a placeholder and
implementation error is not applied during simulation. See \code{\link[=Imp]{Imp()}} for
details.
}
}
\examples{
ComplexOM
ComplexOM@Complexes
nStock(ComplexOM)

\dontrun{
Hist <- runMSE(ComplexOM, MPs = ExampleMPs())
}

}
\seealso{
\link{SingleStockOM}, \link{TwoFleetOM}, \link{MultiStockOM} for structural variants.
\link{AlbacoreExStock}, \link{ButterfishExStock}, \link{AsympExFleet}, \link{AgeStructuredObs}
for the component objects.
\code{\link[=OM]{OM()}}, \link[=om-class]{om}, \code{\link[=runMSE]{runMSE()}}, \code{\link[=PopulateOM]{PopulateOM()}}, \code{\link[=ExampleMPs]{ExampleMPs()}}

Other om:
\code{\link{MultiStockOM}},
\code{\link[=OM]{OM()}},
\code{\link{OM-accessors}},
\code{\link{SingleStockOM}},
\code{\link{TwoFleetOM}},
\code{\link{om-class}}
}
\concept{om}
\keyword{datasets}
