% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-structure.R
\name{StructurePars_}
\alias{StructurePars_}
\title{Convert a Single Parameter Value to a Structured Array}
\usage{
StructurePars_(Par, nSim = NULL, Years = NULL, nArea = NULL)
}
\arguments{
\item{Par}{A scalar, length-2 bounds vector, numeric vector of length
\code{nSim}, or an existing array.}

\item{nSim}{Integer. Number of simulations.}

\item{Years}{Numeric vector of year labels for the \code{Year} dimension.}

\item{nArea}{Integer or \code{NULL}. If non-\code{NULL}, output gains an \code{Area}
dimension.}
}
\value{
A named array with dimensions \verb{Sim × Year} or
\verb{Sim × Year × Area}.
}
\description{
Converts one element of a \code{Pars} list to a named \verb{Sim × Year} (or
\verb{Sim × Year × Area}) array according to the following rules:
}
\details{
\itemize{
\item If already an array, dimension names are assigned via
\code{\link[=NameParDimensions]{NameParDimensions()}} if missing.
\item If length 2 (and \code{nSim != 2}), treated as uniform bounds and sampled
via \code{\link[stats:Uniform]{stats::runif()}}. When \code{nSim == 1} the midpoint is used.
\item Otherwise, values are recycled or truncated to \code{nSim} and wrapped in a
1-year array.
}
}
\keyword{internal}
