% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-bam.R
\name{BAMFleetNames}
\alias{BAMFleetNames}
\alias{GetBAMFleetNames}
\alias{FixFleetNames}
\title{Get and Fix BAM Fleet Names}
\usage{
GetBAMFleetNames(Stock)

FixFleetNames(FleetNames)
}
\arguments{
\item{Stock}{A character string matching a stock name available in
\code{bamExtras} (e.g., \code{'Red Snapper'}), or a list of BAM output objects
containing elements \code{rdat} and \code{dat}.}

\item{FleetNames}{A character vector of raw fleet name strings as extracted
from BAM output.}
}
\value{
\code{\link[=GetBAMFleetNames]{GetBAMFleetNames()}} returns a named list with four elements:
\itemize{
\item \verb{$FleetNames}: character vector of all fleet names, cleaned via
\code{\link[=FixFleetNames]{FixFleetNames()}}.
\item \verb{$RetainFleets}: character vector of landings (retained catch) fleets,
i.e. all fleets not classified as discard fleets.
\item \verb{$DiscardFleets}: character vector of discard fleets, identified as
those ending in \code{".D"} or, if none found, starting with \code{"D."}.
\item \verb{$FleetNamesOrig}: character vector of original fleet names as they
appear in \code{BAMdata$parms}, before cleaning.
}

\code{\link[=FixFleetNames]{FixFleetNames()}} returns a character vector the same length as
\code{FleetNames} with standardised names.
}
\description{
\code{\link[=GetBAMFleetNames]{GetBAMFleetNames()}} extracts and classifies fleet names from a BAM stock
assessment, separating retained (landings) fleets from discard fleets.
\code{\link[=FixFleetNames]{FixFleetNames()}} is a helper that standardises raw BAM fleet name strings
by stripping leading \code{"L."} prefixes and converting leading \code{"D."} prefixes
to trailing \code{".D"} suffixes.
}
\examples{
\dontrun{
GetBAMFleetNames('Red Snapper')
}

FixFleetNames(c('L.cHL', 'D.rHB', 'cPT'))

}
\seealso{
\code{\link[=GetBAMOutput]{GetBAMOutput()}}
}
