% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/populate-mean-at-weight.R
\name{PopulateMeanAtWeight}
\alias{PopulateMeanAtWeight}
\title{Populate the \code{MeanAtWeight} Slot from a Model and Weight Object}
\usage{
PopulateMeanAtWeight(
  object,
  Weight = NULL,
  Years = NULL,
  Ages = NULL,
  seed = NULL,
  silent = FALSE
)
}
\arguments{
\item{object}{An S4 object with slots \code{MeanAtWeight}, \code{Model}, \code{Pars}, and
\code{Classes}.}

\item{Weight}{A \code{\link[=Weight]{Weight()}} object supplying \code{Classes} used as the size
class labels and inputs to the model.}

\item{Years}{Numeric vector of year labels used to assign the \code{Year}
dimension of the output array.}

\item{Ages}{An \link[=ages-class]{ages} object. Not used directly but checked to
determine whether the model is age-based, in which case the function
returns early.}

\item{seed}{Integer or \code{NULL}. Random seed for stochastic models. Reserved
for future use.}

\item{silent}{Logical. If \code{TRUE}, suppresses messages. Reserved for future
use. Default \code{FALSE}.}
}
\value{
\code{object} with \code{object@MeanAtWeight} and \code{object@Classes} populated,
or \code{object} unchanged if the early-exit conditions are met.
}
\description{
Generates \code{object@MeanAtWeight} by calling\code{GenMeanAtWeight} with
\code{object@Model}, \code{object@Pars}, and the size classes from \code{Weight}. Assigns
named \verb{Sim × Class × Year} (or \verb{Sim × Class × Year × Area}) dimnames to
the result.
}
\details{
Returns \code{object} unchanged if \code{object@Model} is \code{NULL}, \code{object@Pars} is
empty, or if the model function expects \code{Ages} rather than weight-based
inputs (indicating this is an age-based rather than size-based object).
}
\keyword{internal}
