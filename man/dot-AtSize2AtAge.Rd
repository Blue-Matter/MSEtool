% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-atsize-to-atage.R
\name{.AtSize2AtAge}
\alias{.AtSize2AtAge}
\title{Convert Mean-at-Size to Mean-at-Age}
\usage{
.AtSize2AtAge(object, Length, max1 = FALSE)
}
\arguments{
\item{object}{An S4 object with slots \code{MeanAtLength} or \code{MeanAtWeight}
(array: \verb{Sim × Class × Year} or \verb{Sim × Class × Year × Area}) and
\code{MeanAtAge} (\code{NULL} if not yet populated).}

\item{Length}{A \code{\link[=Length]{Length()}} or \code{\link[=Weight]{Weight()}} object supplying an \code{ALK} or \code{AWK}
slot (array: \verb{Sim × Age × Class × Year}).}

\item{max1}{Logical. If \code{TRUE}, forces \code{max(MeanAtAge) == 1} via
\code{.CheckSelectivityMaximum}, correcting cases where selectivity-at-length
produces an apical selectivity-at-age below 1. Default \code{FALSE}.}
}
\value{
\code{object} with \code{object@MeanAtAge} populated.
}
\description{
Populates \code{object@MeanAtAge} by passing \code{object@MeanAtLength} (or
\code{object@MeanAtWeight}) through the Age-Size Key stored in \code{Length}.
Handles optional simulation and area dimensions. Early-exits when all
values are effectively 1 or 0. Returns \code{object} unchanged if \code{MeanAtAge}
is already populated.
}
\details{
When \code{max1 = TRUE}, selectivity-at-age values are rescaled so the maximum
across age classes equals 1. This is sometimes necessary because a
selectivity-at-length schedule can produce a maximum selectivity-at-age
below 1, which is technically correct but can cause unexpected behaviour in
downstream calculations.
}
\keyword{internal}
