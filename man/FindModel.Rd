% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find-model.R
\name{FindModel}
\alias{FindModel}
\title{Identify the Model Function for an Object}
\usage{
FindModel(
  object,
  ignore = c("Ages", "MeanLength", "Length", "Weight", "nage", "AtAge", "MaxLen", "S0",
    "S", "R0"),
  doCheck = TRUE
)
}
\arguments{
\item{object}{An S4 object with slots \code{Model} and \code{Pars}.}

\item{ignore}{Character vector of formal argument names to exclude from
matching. Defaults to common auxiliary arguments such as \code{"Ages"},
\code{"Length"}, \code{"Weight"}, etc.}

\item{doCheck}{Logical. If \code{TRUE} (default), calls \code{\link[=CheckModel]{CheckModel()}} and
throws an informative error when no matching model is found.}
}
\value{
A character string naming the matched model function, a function
object if \code{object@Model} is already a function, or \code{NULL} if \code{Pars} is
empty or \code{NA}.
}
\description{
Attempts to identify the model function associated with \code{object} by matching
the names of \code{object@Pars} against the formal arguments of candidate model
functions returned by the relevant \verb{*Models()} function. If \code{object@Model}
is already a function it is returned directly. Returns \code{NULL} if \code{Pars} is
empty or contains \code{NA}.
}
\keyword{internal}
