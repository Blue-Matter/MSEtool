% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllNewClasses.R
\docType{class}
\name{Ages}
\alias{Ages}
\alias{Ages<-}
\title{Ages Object}
\usage{
Ages(MaxAge = NA, Units = "year", PlusGroup = TRUE)

Ages(x) <- value
}
\arguments{
\item{MaxAge}{An integer value (length 1) specifying the maximum age to include in the model. If \code{PlusGroup==TRUE}, \code{MaxAge} will be a plusgroup. \strong{Required}. Note that the age classes always start at 0, so there will be \code{MaxAge+1} age classes.}

\item{Units}{A character string describing the units corresponding to \code{MaxAge}. Valid values are found in \code{ValidUnits(Ages())}. Defaults to \code{year} if not specified.}

\item{PlusGroup}{Include a plus-group? Defaults to TRUE.}

\item{x}{A \code{\link[=Stock]{Stock()}} class object}

\item{value}{An \code{ages} class object to assign to \code{x}}

\item{Classes}{Numeric vector length \code{MaxAge+1}. The age classes for the stock. Always starts at 0. Populated internally from \code{MaxAge}.}
}
\description{
The \code{Ages} function is used to create S4 class \code{ages} objects or to access or
assign \code{ages} objects to \code{\link[=Stock]{Stock()}} class objects.
}
\details{
\subsection{About the \code{ages} Class}{

\code{ages} is an S4 class used in \code{\link[=Stock]{Stock()}} class objects. It contains information
relating to the age classes of the stock.
}

\subsection{Creating New Objects}{

New \code{ages} objects can be created with \code{Ages()}, see \code{Usage} and \code{Examples} for details.
}

\subsection{Accessing and Assigning Slots}{

Like all of the new style objects in \code{MSEtool}, the slots in \code{ages} objects can be accessed and assigned with functions matching the slot names. See \verb{See Also} section for functions.
}
}
\section{Functions}{
\itemize{
\item \code{Ages()}: Create a new \code{ages} class object

\item \code{Ages(x) <- value}: Assign an \code{ages} class object to a \code{\link[=Stock]{Stock()}} object

}}
\section{Slots}{

\describe{
\item{\code{MaxAge}}{An integer value (length 1) specifying the maximum age to include in the model. If \code{PlusGroup==TRUE}, \code{MaxAge} will be a plusgroup. \strong{Required}. Note that the age classes always start at 0, so there will be \code{MaxAge+1} age classes.}

\item{\code{Units}}{A character string describing the units corresponding to \code{MaxAge}. Valid values are found in \code{ValidUnits(Ages())}. Defaults to \code{year} if not specified.}

\item{\code{PlusGroup}}{Include a plus-group? Defaults to TRUE.}

\item{\code{Classes}}{Numeric vector length \code{MaxAge+1}. The age classes for the stock. Always starts at 0. Populated internally from \code{MaxAge}.}
}}

\examples{

# Populate an `Ages` object
Ages <- Ages(MaxAge=20)

# Change units
Units(Ages)
Units(Ages) <- 'month'

# Check valid units
ValidUnits(Ages)

# Check `Ages` object is complete
Check(Ages)



}
\seealso{
\code{\link[=MaxAge]{MaxAge()}}, \code{\link[=Units]{Units()}}, \code{\link[=PlusGroup]{PlusGroup()}}, \code{\link[=Classes]{Classes()}}, \code{\link[=ValidUnits]{ValidUnits()}}, \code{\link[=Check]{Check()}}
}
