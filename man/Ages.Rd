% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_Class_ages.R, R/01_Generic_ages.R,
%   R/02_Methods_ages.R
\name{Ages}
\alias{Ages}
\alias{Ages<-}
\alias{Ages,missing-method}
\alias{Ages,numeric-method}
\alias{Ages,stock-method}
\alias{Ages<-,stock,ages-method}
\title{Ages Class and Constructor}
\usage{
Ages(MaxAge, ...)

Ages(x) <- value

\S4method{Ages}{missing}(MaxAge, ...)

\S4method{Ages}{numeric}(MaxAge, MinAge = 0, Units = "year", PlusGroup = TRUE, ...)

\S4method{Ages}{stock}(MaxAge, ...)

\S4method{Ages}{stock,ages}(x) <- value
}
\arguments{
\item{MaxAge}{Either a numeric value specifying the maximum age (in units
corresponding to \code{Units}, or a \code{\link[=Stock]{Stock()}} object, in which case
the existing \code{Ages} slot is returned.}

\item{x}{A \code{\link[=Stock]{Stock()}} object.}

\item{value}{An \code{\link[=Ages]{Ages()}} object to assign.}

\item{MinAge}{Numeric value specifying the minimum age. Default is \code{0}.}

\item{Units}{Character string specifying the units used to define age
classes. Must be one of \code{\link[=ValidUnits]{ValidUnits()}}.}

\item{PlusGroup}{Logical; should the maximum age be treated as a plus group?}
}
\value{
\itemize{
\item \code{Ages()}: returns an \link{Ages} class object
\item \code{Ages(x)}: returns an \code{Ages} object from [Stock() object \code{x}
\item \verb{Ages<-}: returns the modified \code{\link[=Stock]{Stock()}} object
}
}
\description{
The \code{Ages} class defines the age structure associated with a
\code{\link[=Stock]{Stock()}} object. It specifies the minimum and maximum ages,
whether a plus group is used, and the resulting vector of age classes.
}
\details{
Age classes are internally represented as numeric values in units of \strong{years}.
Fractional ages (e.g. seasonal or sub-annual ages) are supported and are
rounded to three decimal places.

The \code{Ages} generic is used to:
\itemize{
\item construct new \code{Ages} objects;
\item access \code{Ages} when supplied with a \code{\link[=Stock]{Stock()}} object;
\item assign an \code{Ages} object to to a \code{\link[=Stock]{Stock()}} object.
}
\subsection{Slots}{

Objects of class \code{"ages"} contain the following slots:
\itemize{
\item \code{MaxAge}: Numeric scalar giving the maximum age. If \code{PlusGroup == TRUE},
this represents the plus group age.
\item \code{MinAge}: Numeric scalar giving the minimum age.
\item \code{Units}: Character string describing the time units used to define
age classes (e.g. \code{"year"}).
\item \code{PlusGroup}: Logical; indicates whether the maximum age is treated
as a plus group.
\item \code{Classes}: Numeric vector of age classes expressed in \strong{years}, or
\code{NULL} if undefined.
}
}
}
\examples{
Ages(MaxAge=20)


}
\seealso{
\code{\link[=Classes]{Classes()}}, \code{\link[=MaxAge]{MaxAge()}}, \code{\link[=MinAge]{MinAge()}}, \code{\link[=Stock]{Stock()}}
}
