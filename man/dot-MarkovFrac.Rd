% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-spatial-movement.R
\name{.MarkovFrac}
\alias{.MarkovFrac}
\title{Construct a Row-Stochastic Movement Matrix from Logit Probabilities}
\usage{
.MarkovFrac(LogitProbs, FracOther = NULL, tol = 1e-10)
}
\arguments{
\item{LogitProbs}{\code{numeric}. Logit-transformed staying probabilities, one
per area. Back-transformed via \code{\link[=ilogit]{ilogit()}} to give the diagonal entries.}

\item{FracOther}{\code{matrix} or \code{NULL}. Square matrix of relative movement
probabilities among areas. Diagonal elements should be \code{NA} and are
ignored. When \code{NULL} (default), a two-area matrix is constructed.}

\item{tol}{\code{numeric(1)}. Tolerance for the row-sum check. Default \code{1e-10}.}
}
\value{
A square numeric matrix with \code{length(LogitProbs)} rows and columns.
Rows sum to 1 (verified to within \code{tol}).
}
\description{
Converts a vector of logit-transformed staying probabilities into a
row-stochastic movement matrix. When \code{FracOther} is supplied (multi-area
case), the off-diagonal mass for each row is distributed proportionally
across destination areas according to \code{FracOther}. When \code{FracOther} is
\code{NULL} (two-area case), a \verb{2 × 2} matrix is constructed with the remaining
probability evenly split.
}
\seealso{
\code{.FitMovement2Area()}, \code{.FitMovementMultiArea()}
}
\keyword{internal}
