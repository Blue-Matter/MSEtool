% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers-mp.R
\name{.ReconcileAdviceShapes}
\alias{.ReconcileAdviceShapes}
\title{Reconcile Mismatched \code{Data@Advice} Record Shapes Across Years}
\usage{
.ReconcileAdviceShapes(Current, New)
}
\arguments{
\item{Current}{Existing accumulated array (with a \code{Year} dimension).}

\item{New}{This year's array (with a \code{Year} dimension already added).}
}
\value{
A list with elements \code{Current} and \code{New}, each with identical
dimension names (and order), ready for \code{\link[=ArrayFill<-]{ArrayFill<-()}}.
}
\description{
\code{Data@Advice@TAC}/\verb{@Effort} accumulate across years via \code{\link[=ArrayFill<-]{ArrayFill<-()}},
which requires matching dimension names throughout. In practice, a plain
per-fleet \code{Effort} value (\code{Fleet} only) and the
freeze-last-effort default (\code{Fleet} x \code{Area}, always built regardless of
whether the MP's own advice carried area detail) can legitimately occur
in different years for the same MP, which would otherwise abort the
whole projection.
}
\details{
This is a record-keeping reconciliation only (\code{Data@Advice} is read by
MPs for introspection of past decisions; it does not feed population
dynamics) - the smaller array is broadcast across the extra dimension's
levels rather than an attempt to reconstruct any real spatial split.
}
\keyword{internal}
