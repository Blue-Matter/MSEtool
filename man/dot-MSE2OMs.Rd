% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert-mse-to-slick.R
\name{.MSE2OMs}
\alias{.MSE2OMs}
\title{Build an OMs Object Describing the Operating Model Design}
\usage{
.MSE2OMs(MSE, Design = NULL, AutoPreset = TRUE)
}
\arguments{
\item{MSE}{An \link[=mse-class]{mse} object or a named list of \link[=mse-class]{mse} objects.}

\item{Design}{A \code{data.frame} (one row per OM, one column per factor) or
an equivalent named list of length-\code{nOM} vectors, or \code{NULL}.}

\item{AutoPreset}{Logical. If \code{TRUE} (default) and \code{Design} has one or
more columns, \code{OMs@Preset} is populated with one button per unique
level of each column (unrestricted on every other column) -- e.g. a
\code{Design} with columns \code{OM_Type}/\code{Growth}/\code{M} gets a preset button for
each \code{OM_Type} level, each \code{Growth} level, and each \code{M} level. Ignored
when \code{Design} is \code{NULL} (the single-factor \code{Scenario} default).}
}
\value{
A \code{\link[Slick:OMs]{Slick::OMs()}} object.
}
\description{
Constructs a \code{\link[Slick:OMs]{Slick::OMs()}} object labeling each element of a list of
\link[=mse-class]{mse} objects. \code{Design} gives each OM's level along one or more
factorial-design factors (e.g. \code{M}, steepness) directly, in the same
shape \code{Slick::OMs()@Design} itself expects; the per-factor level catalog
Slick separately needs (\code{OMs@Factors}) is derived automatically from the
unique values in each column. Without \code{Design}, OMs are labeled by a
single \code{Scenario} factor built from the list names.
}
\details{
\code{Design}'s rows must be unambiguously matched to the right element of
\code{MSE} -- when \code{Design} is supplied and \code{MSE} has more than one element,
\code{MSE} must be a \emph{named} list (aborts otherwise, rather than silently
assuming row/list order line up). If \code{Design} is itself a \code{data.frame}
with its own row names, those are validated against \code{names(MSE)} (as
sets, order-independent) and \code{Design} is reordered to match \code{MSE}'s list
order by name -- never by position.
}
\keyword{internal}
