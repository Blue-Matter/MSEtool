% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-asymptotic-distribution.R
\name{CalcAsymDist}
\alias{CalcAsymDist}
\title{Calculate the Asymptotic Spatial Distribution of a Movement Matrix}
\usage{
CalcAsymDist(Movement, method = c("la", "mc"), tol = 1e-10, maxiter = 10000)
}
\arguments{
\item{Movement}{A square row-stochastic matrix of dimensions \verb{nArea × nArea}, where \code{Movement[i, j]} is the probability of moving from area
\code{i} to area \code{j}. All rows must sum to 1.}

\item{method}{Character. Solution method for matrices larger than 2 × 2.
One of \code{"la"} (linear algebra; default) or \code{"mc"} (Markov chain
iteration). Partially matched.}

\item{tol}{Numeric. Convergence tolerance. Used to check row sums, detect
reducible 2-area chains, clamp near-zero negative probabilities, and
assess convergence of the \code{"mc"} method. Default \code{1e-10}.}

\item{maxiter}{Integer. Maximum number of iterations for the \code{"mc"} method.
A warning is emitted if convergence is not reached. Default \code{1e4}.}
}
\value{
A numeric vector of length \code{nArea} giving the stationary
probability of occupying each area. Values sum to 1.
}
\description{
Computes the stationary (asymptotic) distribution of a Markov chain
movement matrix — the long-run proportion of the population expected to
occupy each area. For a 2-area matrix a closed-form solution is used. For
larger matrices, either a linear-algebra or iterative Markov chain approach
is available.
}
\details{
The movement matrix is treated as a row-stochastic transition matrix, where
\code{Movement[i, j]} is the probability of moving from area \code{i} to area \code{j}.
Diagonal entries represent the probability of remaining in an area. All
rows must sum to 1.

Two solution methods are available for matrices larger than 2 × 2 via the
\code{method} argument:
\itemize{
\item \code{"la"} (default): solves the system \code{pi * P = pi} subject to
\code{sum(pi) = 1} using least-squares linear algebra (\code{solve(t(A) \%*\% A, t(A) \%*\% B)}). Exact and fast for well-conditioned matrices.
\item \code{"mc"}: iteratively multiplies the current distribution by \code{Movement}
until convergence. More robust for nearly-reducible chains but slower.
}

For 2-area matrices the stationary distribution is computed analytically
regardless of \code{method}:

\code{pi[1] = P[2,1] / (P[1,2] + P[2,1])}

\code{pi[2] = P[1,2] / (P[1,2] + P[2,1])}
}
\examples{
# 2-area movement matrix
M <- matrix(c(0.8, 0.2,
              0.3, 0.7), nrow=2, byrow=TRUE)
CalcAsymDist(M)

# 3-area movement matrix using linear algebra (default)
M3 <- matrix(c(0.7, 0.2, 0.1,
               0.1, 0.8, 0.1,
               0.2, 0.2, 0.6), nrow=3, byrow=TRUE)
CalcAsymDist(M3)

# Using Markov chain iteration
CalcAsymDist(M3, method="mc")

}
\seealso{
\code{\link[=Spatial]{Spatial()}}, \code{\link[=CalcUnfishedDist]{CalcUnfishedDist()}}
}
