% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join-year.R
\name{JoinYear}
\alias{JoinYear}
\title{Join Two Objects Along the Year Dimension}
\usage{
JoinYear(object1, object2)
}
\arguments{
\item{object1}{The earlier object. Must have years that precede those in
\code{object2}. Can be an S4 object, a \code{list}, a named \code{array} with a \code{"Year"}
dimension, or a \code{numeric} vector.}

\item{object2}{The later object with the same structure as \code{object1}.
Its years must be strictly greater than those in \code{object1}.}
}
\value{
An object of the same class and structure as \code{object1}, with
\code{object2} appended along the year dimension. Non-array, non-numeric,
and non-list slots or elements are returned unchanged from \code{object1}.
}
\description{
Recursively combines two objects of the same structure by appending
\code{object2} after \code{object1} along the year dimension. Handles S4 objects,
lists, named arrays, and numeric vectors. Intended for joining early and
late year segments of MSEtool data objects (e.g. historical and projection
periods).
}
\details{
\code{JoinYear} recurses through the structure of \code{object1} and \code{object2}
in parallel, dispatching on the type of each element:
\itemize{
\item \strong{S4 objects} — each slot is processed recursively via \code{Recall()}.
\item \strong{Lists} — each element is processed recursively via \code{Recall()}.
\item \strong{Named arrays with a \code{"Year"} dimension} — joined along the year
axis using \code{\link[abind:abind]{abind::abind()}}. An error is thrown if years are not
strictly increasing across \code{object1} and \code{object2}.
\item \strong{Numeric vectors} — concatenated with \code{c()}. If all values exceed
1000 (i.e. are likely calendar years), strict monotonicity is checked
and an error is thrown if the combined sequence is not increasing.
\item \strong{All other types} — returned unchanged from \code{object1}.
}
}
\section{Error handling}{

If years are not strictly increasing across the two objects,
\code{\link[cli:cli_abort]{cli::cli_abort()}} is called with a diagnostic message showing the
year values from both objects and a hint that the arguments may be
in the wrong order.
}

\seealso{
\code{\link[abind:abind]{abind::abind()}}
}
