% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc-blow.R
\name{CalcBLow}
\alias{CalcBLow}
\title{Calculate the BLow Reference Point}
\usage{
CalcBLow(Hist, HZN = 2, Bfrac = 0.5, silent = FALSE)
}
\arguments{
\item{Hist}{A \link[=hist-class]{hist} or \link[=om-class]{om} object. If an \code{om} is supplied
it is first converted to a \code{hist} object.}

\item{HZN}{Numeric. Number of mean generation times (see \code{\link[=CalcMGT]{CalcMGT()}})
defining the rebuilding horizon. Default \code{2}.}

\item{Bfrac}{Numeric. Target spawning biomass as a fraction of \code{SBMSY}
(see \code{\link[=SBMSY]{SBMSY()}}). Default \code{0.5}.}

\item{silent}{Logical. If \code{TRUE}, suppresses progress messages. Default
\code{FALSE}.}
}
\value{
The input \link[=hist-class]{hist} object with \code{Hist@Reference@BLow}
populated.
}
\description{
Computes \code{BLow}: the current spawning biomass at which it would take
\code{HZN} mean generation times to rebuild to \verb{Bfrac x SBMSY} under zero
future catch. Stored in \code{Hist@Reference@BLow}.
}
\details{
For each simulation and stock, the current (last historical year)
numbers-at-age are scaled by a factor \code{k} and projected forward under
zero fishing effort for \verb{round(HZN x MGT)} years. \code{k} is found via
\code{\link[stats:optimize]{stats::optimize()}} such that the projected spawning biomass at the end
of that horizon equals \verb{Bfrac x SBMSY}. \code{BLow} is \code{k} times the current
spawning biomass.

Requires \code{Hist@Reference@MSY} (see \code{\link[=CalcMSY]{CalcMSY()}}) and enough projection
years in the operating model to cover the rebuilding horizon for every
simulation/stock; horizons exceeding the available projection years are
capped with a warning.

This is substantially more expensive than the other reference points in
\code{\link[=CalcRefPoints]{CalcRefPoints()}} (a full population projection plus a numerical search,
repeated per simulation and stock) and is not included by
\code{CalcRefPoints()}'s default reference-point set.
}
\seealso{
\code{\link[=CalcMGT]{CalcMGT()}}, \code{\link[=CalcMSY]{CalcMSY()}}, \code{\link[=CalcRefPoints]{CalcRefPoints()}}, \link[=reference-class]{reference}
}
