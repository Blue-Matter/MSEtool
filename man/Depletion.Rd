% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllNewClasses.R
\docType{class}
\name{Depletion}
\alias{Depletion}
\alias{Depletion<-}
\title{Depletion Object}
\usage{
Depletion(Initial = numeric(), Final = numeric(), Reference = "B0")

Depletion(x) <- value
}
\arguments{
\item{Initial}{A numeric of length \code{nSim} or length 1 specifying the biomass
relative to \code{Reference} in the first historical time step. See \code{Details}.}

\item{Final}{A numeric of length \code{nSim} or length 1 specifying the biomass
relative to \code{Reference} in the last historical time step.  See \code{Details}.}

\item{Reference}{The reference point used to calculate Depletion.  See \code{Details}.}

\item{x}{A \code{\link[=Stock]{Stock()}} class object}

\item{value}{An \code{depletion} class object to assign to \code{x}}
}
\description{
The \code{Depletion} function is used to create S4 class \code{depletion} objects or to access or
assign \code{depletion} objects to \code{\link[=Stock]{Stock()}} class objects
}
\details{
\subsection{About the \code{depletion} Class}{

\code{depletion} is an S4 class used in \code{\link[=Stock]{Stock()}} class objects. It describes the
\code{Initial} depletion (at beginning of historical period) and the \code{Final} depletion
(at the end of the historical period).

Depletion is defined as stock biomass divided by the unfished biomass; i.e.,
the lower the depletion value the lower the stock's biomass is related to it's
unfished level.
\subsection{\code{Initial}}{

The stock at the beginning of the historical period is assumed to be in an
unfished state unless \code{Initial} is populated.

When \code{Initial} is populated, an optimization routine adjusts the mean
recruitment deviations for the initial age classes such that the biomass relative
to \code{Reference} in the first time step is equal to \code{Initial}.
}

\subsection{\code{Final}}{

\code{Final} is used to specify the depletion level in the last historical time step.
It is only required if the \code{Catchability} slot (\code{q}) in \code{\link[=FishingMortality]{FishingMortality()}} is
not populated. In this case, an optimization routine will calculate the \code{q}
value for each simulation that results in the biomass relative to \code{Reference}
in the last historical time step to be equal to \code{Final}.
}

\subsection{\code{Reference}}{

\code{Reference} describes the reference point used to calculate depletion.

It can be a character string of either \code{B0} (default) or \code{BMSY},
where \code{B0} is the equilibrium unfished biomass and \code{BMSY} the equilibrium
biomass corresponding the maximum sustainable yield.

How are \code{B0} and \code{BMSY} calculated? Good question. That hasn't been documented
yet. Bug us to update the documentation!

It can also be a numeric matrix with dimensions \verb{(nSim, 2)} where the first
column is the absolute value for \code{Initial} and the second column the absolute
value for \code{Final}. Like all objects, the number of rows can be \code{nSim} if \code{Initial}
or \code{Final} vary over simulations, or otherwise a value of \code{1}.
}

}

\subsection{Creating New Objects}{

New \code{depletion} objects can be created with \code{Depletion()}, see \code{Usage} and \code{Examples} for details.
}

\subsection{Accessing and Assigning Slots}{

Like all of the new style objects in \code{MSEtool}, the slots in \code{depletion} objects can be accessed and assigned with functions matching the slot names. See \verb{See Also} section for functions.
}
}
\section{Functions}{
\itemize{
\item \code{Depletion()}: Create a new \code{Depletion} object

\item \code{Depletion(x) <- value}: Assign an \code{Depletion} object to a \code{\link[=Stock]{Stock()}} object

}}
\section{Slots}{

\describe{
\item{\code{Initial}}{A numeric of length \code{nSim} or length 1 specifying the biomass
relative to \code{Reference} in the first historical time step. See \code{Details}.}

\item{\code{Final}}{A numeric of length \code{nSim} or length 1 specifying the biomass
relative to \code{Reference} in the last historical time step.  See \code{Details}.}

\item{\code{Reference}}{The reference point used to calculate Depletion.  See \code{Details}.}

\item{\code{Misc}}{A list for additional miscellaneous objects. Not currently used.}

\item{\code{Created}}{Time the object was created.}

\item{\code{Modified}}{Time the object was last modified.}
}}

\examples{
Depletion <- Depletion(Initial=c(0.1,0.2),
                       Final=c(0.5, 0.6))

Populate(Depletion, nsim=5)


}
\seealso{
\code{\link[=Initial]{Initial()}}, \code{\link[=Final]{Final()}}, \code{\link[=Reference]{Reference()}}, \code{\link[=Misc]{Misc()}}, \code{\link[=Created]{Created()}}, \code{\link[=Modified]{Modified()}}, \code{\link[=Check]{Check()}}
}
