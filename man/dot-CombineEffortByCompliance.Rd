% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt-effort-helpers.R
\name{.CombineEffortByCompliance}
\alias{.CombineEffortByCompliance}
\title{Combine per-complex efforts into one effort per fleet}
\usage{
.CombineEffortByCompliance(EffortByComplex, Compliance, default = 1)
}
\arguments{
\item{EffortByComplex}{\verb{[nComplex x nFleet]} matrix from
\code{.SolveEffortByComplex()}. \code{NA} rows are ignored.}

\item{Compliance}{\verb{[nFleet x nComplex]} matrix from
\code{.ResolveComplianceMatrix()}. \code{NA} entries take \code{default}.}

\item{default}{Numeric. Compliance used where unset. Default \code{1}, i.e. a
TAC is a hard cap.}
}
\value{
Numeric vector of length \code{nFleet}. \code{NA} for fleets with no TAC in
any complex, which the caller leaves unchanged.
}
\description{
Compliance sets how far a fleet respects each complex's TAC as a cap.
Full compliance stops the fleet at the first TAC reached; zero compliance
lets it run until the last TAC is reached, overshooting the tighter ones.
}
\details{
\deqn{E^*_f = \min_c \left[ \kappa_{fc} E_{fc} +
  (1 - \kappa_{fc}) \max_c E_{fc} \right]}

Each complex contributes its own effort in proportion to how far the fleet
complies with it, and the effort that fills every quota in proportion to how
far it does not.
}
\keyword{internal}
